# CLAUDE.md — Nexus by MSI

> Lean entry point untuk Claude Code. Detail lengkap ada di `docs/` (lihat tabel di bawah). `AGENTS.md` = identitas produk + safety rules — struktur/kebijakan/prinsip JANGAN diubah (kontrak Codex, lihat `.codex/agents/*.toml`); koreksi angka/fakta yang TERVERIFIKASI basi (mis. line-count, nama domain) BOLEH, syarat diff sekecil mungkin + commit message eksplisit sebut "koreksi fakta" (pola commit `c5476ae`). `PROGRESS.md` = dev log per-tanggal.

## Quick Reference

- **Stack:** React 19 + Vite 8 · TailwindCSS 3 · Supabase (PostgreSQL + Auth + RLS + Edge Functions + Storage) · Vercel auto-deploy dari `main` (= production).
- **Supabase ref:** `untmpqceexwxzuhlmyrg` (org "Milenial Solusi Internusa", project `nexus-msi`; ref TIDAK berubah pasca-migrasi) · pooler/`pg_dump` host **`aws-1-ap-northeast-2.pooler.supabase.com:5432`** (region Seoul — ⚠️ BUKAN `ap-southeast-1`; catatan lama yang salah menyebabkan gagal koneksi backup).
- **Live URL:** `nexus.msigroup.co.id` (production, CNAME via Domainesia) — satu-satunya domain resmi · **lama `nexus.dli.my.id` sempat dibiarkan hidup sbg safety net, kini SUDAH DINONAKTIFKAN (20 Jul 2026):** DNS record CNAME `nexus` di zona `dli.my.id` (Domainesia) dihapus, tanpa redirect.
- **Repo:** GitHub `Milenial-Solusi-Internusa/storbit-manifest`, branch `main` (solo dev; `fix/*` hotfix). ⚠️ **repo masih PUBLIC** (backlog keamanan — kode ERP 3 entitas + schema terbuka).
- **Deploy:** Vercel team "MSI Group" (Hobby), project `nexus` · project Vercel lama (akun pribadi Hobby, `storbit-manifest`) dibiarkan hidup sementara (safety net).
- **Migrasi kepemilikan (9-10 Jul 2026):** Supabase/GitHub/Vercel/domain dipindah dari akun pribadi ke org MSI. Backup penuh (schema+data, 3.5 MB, 96 tabel) diambil sebelum migrasi. **Backlog keamanan (belum dikerjakan):** repo PUBLIC → private; 2FA 2 Owner org GitHub (mhmmdjaelaniii, msigroup-sys); Vercel Hobby → paid (pemakaian komersial); Supabase Auth Site URL masih `localhost:3000` + Redirect URLs kosong (rapikan); rename repo→`nexus` (opsional).
- **Entity UUID:** MSI `0e1840d8-e6fb-4190-bd09-88338e68b492` · JCI `42569e7c-531b-4d2b-832a-d5a7268c455b` · SOA `d2e5e565-5f67-4954-b8d9-5979a2a0c697`.
- **Sumber kebenaran DB:** `supabase/schema_snapshot.sql` (**132 tabel = 122 bisnis + 10 tabel backup** — BUKAN `migrations/`, yang berhenti 3 Jun 2026). Status freshness lengkap (riwayat stale/segar 10 Jul–27 Jul + aturan baca snapshot): `03_DATA_MODEL.md` **gotcha #10**.
- **Brand:** navy `#144682` · orange `#E85A1E` · coral `#F08C7D` · app shell putih `#ffffff` · Montserrat (heading) + Inter (body) · Lucide icons · **no emoji, no dark green**.

## Aturan Wajib (ringkasan — detail: `docs/Governance/02_RULES_GOVERNANCE.md`)

- **Fetch:** selalu `.limit(1000)` (default PostgREST 10) · `.is('deleted_at', null)` · scope `company_id` + role-aware (`isAllEntities=['super_admin']`, `isSalesOnly=['sales','operations']`).
- **`profiles`** pakai kolom **`active`** (TIDAK ada `deleted_at`). · **`showToast?.(message, type)`** (urutan message dulu).
- **Soft delete** (`deleted_at`/`is_active=false`), jangan hard-delete data business.
- **DB:** GRANT setelah CREATE (tabel CLI tak auto-grant) · trigger ordering pakai prefix `trg_z_` · `auth.uid()` NULL di SQL Editor (test RLS di browser) · super-admin bypass = top-level `OR is_super_admin()`.
- **Embed alias** saat constraint FK belum di-rename (`customers:accounts!sp_items_customer_id_fkey(name)`).
- **Deploy code yang berhenti baca kolom DULU, baru drop kolom.** · Refresh `schema_snapshot.sql` via `pg_dump` setelah perubahan SQL Editor.
- **Workflow:** Inspect → Plan → Edit (scope kecil, hindari big-bang & rewrite App.jsx sekaligus) → Verify (`npm run build` clean, lint net-zero) → Summarize. Push HANYA bila diinstruksikan. Audit-before-fix untuk bug non-trivial.
- **Status commit/merge/branch: JANGAN ditulis ke `CLAUDE.md`/`PROGRESS.md`** (berlaku juga untuk doc-keeper). Git satu-satunya sumber kebenaran (`git log`, `git branch --merged`) — menyalinnya ke dokumen = dua sumber yang pasti melenceng (terbukti 19 Jul 2026: 9 entri harus dikoreksi). Yang TETAP dicatat karena git tak tahu: keputusan desain + alasannya, langkah manual DB belum jalan (mis. refresh `pg_dump`, RPC dibuat manual), pertanyaan terbuka, status tes runtime, tech debt. Nama branch boleh disebut sebagai KONTEKS ("dikerjakan di branch X"), tapi JANGAN nyatakan sudah/belum merge.
- **Safety rules lengkap:** lihat `AGENTS.md` (15 non-negotiable).

## Dokumentasi Lengkap

| File | Isi |
|------|-----|
| `AGENTS.md` | Identitas produk, prinsip engineering, 15 safety rules, workflow per tipe task |
| `docs/Governance/02_RULES_GOVERNANCE.md` | Konvensi kode, pola wajib frontend/DB, brand, Do/Don't, workflow |
| `docs/Governance/03_DATA_MODEL.md` | Referensi DB per modul, entity UUID, RLS patterns, RPC, gotchas (incl. mesin status SP 12-tahap) |
| `docs/Governance/08_TECH_DEBT.md` | Daftar tech debt (TD-01…TD-44) + prioritas fix |
| `docs/Governance/09_ROADMAP.md` | Status modul, milestone, next up |
| `PROGRESS.md` | Dev log kronologis per-tanggal (jangan dihapus) |
| `docs/architecture/`, `docs/security/`, `docs/database/`, dll | Blueprint, baseline keamanan/performa, dll (lihat AGENTS.md "Required Reading") |

## Current Phase & Recent Changes

**Current phase: MVP Storbit end-to-end — chain SP → Picking → Surat Jalan LENGKAP + master-data tooling (dikerjakan di branch `restruktur-nexus`)** ✅ Selesai di branch: Slice 0.1 (status SP) · Fase 2 (Picking List + Cancel) · Import Data Produksi (720 baris/435 SP, terverifikasi SQL) · Fase 0.2 (product_id backfill) · Fase 0.3 (SP external_url) · Fase 3 (Packing & Surat Jalan/delivery notes) · Fase 1 (cek stok & reservasi otomatis) · Material Packing + PickingListPDF (Fase 3.x) · Lokasi Rak + Riwayat Harga produk (kontrak/PKS) · BulkEditPricePage (update harga massal, picker role-aware lintas entitas) · ProductPicker dropdown-only di InputSPPage/FormModal/EditItemModal + prefill harga snapshot · fix mapping `is_active` (dropdown customer). **Smoke test UI belum penuh** (banyak fitur "build clean, belum tes runtime" — perlu login). **PERLU KONFIRMASI:** selisih SP 431 vs 435 (Gigih) · mapping 30 item kontrak PKS Indomarco. Detail granular: `PROGRESS.md` + `STATUS.md`. **➕ FASE 0 skema SP baru** (`dc_master`/`sp_orders`/`sp_order_items`/`sp_btb` + 3 harga kategori + repoint FK gudang + constraint) **SELESAI & terverifikasi di branch `feat/sp-schema`** — **DB-only, non-destruktif** (tabel lama utuh), **belum frontend, belum refresh snapshot**; **sudah merge ke `main`**. Lihat entri Recent teratas + `PROGRESS.md` 2026-07-06 + `DESIGN_SP_SCHEMA.md`.

Recent (terbaru → lama; detail granular di git history & `PROGRESS.md`):
- **Migrasi kit desain resmi (TD-14) — 9 halaman Foundation admin-settings lepas dari `PASTEL` lokal + `AdminPageHeader`, 3 batch hari yang sama: Batch 1 (5 halaman read-only) + Batch 2 (Branches/Departments/DC Master, CRUD penuh) + Batch 3 (Positions, kasus khusus multi-entitas).** FE-only lintas ketiga batch, NOL perubahan DB/RLS. Pindah ke kit resmi `admin-settings/kit.jsx`+`tokens.js` (dipakai `ApprovalWorkflowsPage.jsx`+9 halaman admin-settings lain). **Batch 1** (Companies/Document Types/Status Catalog/Taxes/Payment Terms, read-only list-only, disengaja utk validasi proses migrasi dulu) — breadcrumb kini klik-tembus 3-level (dulu label statis), heading Montserrat/`FONT_HEAD` (dulu Fraunces off-brand), zebra table baru, konsolidasi badge 5+ hue pastel arbitrer→4 hue semantik kit (navy=identifier/orange=tipe-kategori/danger=flag/hijau=status aktif). **Batch 2** (Branches/Departments/DC Master, CRUD via `AdminFormModal` — komponen itu sendiri SENGAJA TAK disentuh, 6 konsumen sistem-wide: 4 halaman batch 2+3 sesi ini + `UserAccessPage.jsx`/`HrgaRequestForm.jsx` di luar sesi; cuma isinya yang dimigrasi): field→`FloatingInput`/`FloatingSelect` kit, toggle is_active→`Toggle` kit (SELALU oranye saat aktif — kit `Toggle` hardcode `ORANGE`, tak expose prop warna sama sekali, `ApprovalWorkflowsPage.jsx` mewarisi warna yang sama); badge identifier (company code+kode record)→navy, Wilayah (DC Master)→orange, Status→hijau/cream. `ConfirmModal` (23 konsumen lain) & `AccountPicker` (DC Master, field Customer Terkait+filter) TAK disentuh — DC Master cuma `inputStyle` lokalnya yang direcolor, dropdown picker sendiri tetap palet lama (drift TD-93 pre-existing). Kompromi API kit: `maxLength` via `.slice(0,N)` di `onChange` (kit `FloatingInput` tak expose prop itu; code:20/name:100/city:100 Branches&Departments, kode:50/nama:200 DC Master); `OutlineBtn` kit tak punya prop `disabled`→guard "jangan klik dobel saat saving" pindah ke `onClick`; `FloatingSelect` jg tak punya `disabled`→field dibungkus div `opacity`+`pointerEvents:'none'`. Toast tetap state lokal (`useState`+`setTimeout`, bukan `useToast()` kit) — direstyle jadi pill gelap (`INK`) dgn ikon aksen hijau/merah-muda; **koreksi thd draft awal:** hex ikon (`#7FD6A0`/`#FF9B9B`) adalah literal baru utk kontras di latar gelap, BUKAN referensi ke token `GREEN`/`DANGER` yang diimpor (token itu justru dipakai di banner "Save failed" dalam modal). **Batch 3** (Positions, kasus khusus) — array lokal `ENTITIES` (id UUID company/code/name, dipakai loop multi-entitas `handleSave`) DIPERTAHANKAN, hanya field `color` (hex hardcode, 100% presentasional) dihapus. Kit py `EntitySwitcher` tapi DIEVALUASI & SENGAJA TAK dipakai — dikonfirmasi baca `kit.jsx`: single-select sliding pill (`id` kit = kode singkat "MSI"/"JCI"/"SOA"), pill aktifnya SELALU navy (hardcoded, nol pewarnaan per-entitas) — dua alasan struktural PositionsPage butuh multi-select independen ber-`id` UUID (dipakai langsung `.in('company_id', ENTITY_IDS)`). `EntityPill`/`EntityCheckbox` custom tetap, direcolor uniform navy (present)/cream+muted (absent). `LevelBadge` (5 level) jadi 1 keluarga ORANGE bergradasi intensitas per seniority (Staff transparan→Director solid+teks putih — keputusan desain diusulkan+disetujui Den saat approve plan, klaim relay TAK diverifikasi independen). `GroupStatusBadge` Active/Partial→hijau/orange. **Verifikasi PALING KRITIS (doc-keeper, via `git diff -U0`, bukan relay):** logic `handleSave` (loop 3-entitas, `ENTITY_IDS`, query pre-check) dikonfirmasi **BYTE-IDENTICAL** — rentang hunk diff berhenti tepat sebelum & lanjut tepat setelah baris fungsi itu (baris 274-336 di commit sebelum migrasi), nol baris di antaranya tersentuh. **Verifikasi lain (independen, ketiga batch):** `npm run build` clean 2613 modules; `npm run lint` whole-repo 173/151/22 = net-zero; `npx eslint` terisolasi ke 5 file Batch 2+3 (Branches/Departments/DcMaster/Positions/AdminHub) → 2 warning, KEDUANYA dikonfirmasi pre-existing (`Unused eslint-disable directive` di `handleSave`'s `useCallback`, Branches+Departments — dicek ulang thd isi commit sebelum migrasi via salinan sementara, warning sudah ada di situ jg cuma nomor baris bergeser; bukan regresi) — DcMaster/Positions/AdminHub isolated = 0 finding; `git diff --stat` pada 4 hooks (`useBranches.js`/`useDepartments.js`/`useDcMaster.js`/`usePositions.js`) + 7 komponen shared (`AdminFormModal.jsx`/`ConfirmModal.jsx`/`AccountPicker.jsx`/`AdminPageHeader.jsx`/`LoadingState.jsx`/`EmptyState.jsx`/`ErrorState.jsx`) = 0 baris di semuanya. `AdminHub.jsx` — total 9 `case` kini ber-`onHome` (5 batch1+3 batch2[branches/departments/dc-master]+1 batch3[positions]), `+9/−9` persis. **NOL tes visual/browser** ketiga batch (halaman di balik gate admin, tanpa kredensial sesi ini — sama keterbatasan sesi Foundation lain minggu ini). `08_TECH_DEBT.md` **TD-14** OPEN→**PARTIAL**, **9/22 file migrasi selesai (13 tersisa)** — baseline 22 dikonfirmasi independen via `git grep -l "const PASTEL" HEAD` (bukan cuma re-derivasi 17+5); 13 sisa: 6 file di luar Foundation (`App.jsx`/`AuthGate.jsx`/`CompanySwitcher.jsx`/`ErrorBoundary.jsx`/`UserManagement.legacy.jsx`/`Dashboard.jsx`), 6 komponen shared admin (`AdminShell.jsx`+5 lain, sengaja belum disentuh), 1 file token bersama `userAccessTokens.js` (dipakai `RolesPage.jsx`/`RoleDefaultsPage.jsx`/`UserAccessPage.jsx`, ketiganya jg masih `AdminPageHeader` lama meski tak punya `const PASTEL` lokal sendiri). Doc-keeper tak menemukan entri Governance/PROGRESS terdahulu yang mendokumentasikan audit design-system ini sbg thread tersendiri sblm Batch 1 — dicatat murni dari kondisi kode saat itu. Lihat `PROGRESS.md` 2026-08-13.
- **Struktur Organisasi (Org Chart) — fix garis penghubung anak tunggal + Ctrl+scroll zoom + drag-to-pan (klik-tahan-geser).** 1 file (`OrgStructurePage.jsx`, **+96/−6** kumulatif 3 potong kerja hari ini, nol commit di antaranya utk memisahkan per-sesi), FE-only, nol perubahan DB/RLS — halaman sendiri sudah **LIVE sejak Fase 2.9S–V** (`00_DEV_JOURNEY.md` BAGIAN 3), satu utas kerja berkelanjutan ("bikin org chart interaktif"), **terpisah dari entri BNF di bawah** (sesi kerja lain, hari yang sama). **Bug:** rule CSS `:only-child` mematikan KEDUA pseudo-element garis konektor (`::before` DAN `::after`) untuk node ber-anak-tunggal — padahal `::after`-nya jugalah yang memasok drop-line vertikal 34px ke card anak itu sendiri (bukan cuma garis "bahu" horizontal ke saudara), jadi mematikan keduanya menyisakan celah 34px mengambang persis keluhan "garis menggantung/offset" yang dilaporkan; kasus 2+ anak (termasuk nested/campuran) sudah benar sejak awal, ditelusuri manual, nol celah lain ditemukan. **Fix:** `::after` anak-tunggal dikembalikan `border-left`-nya (`border-top` dihapus, `border-radius` di-reset `0`), rule dipindah ke SETELAH rule `:first-child`/`:last-child` (spesifisitas CSS sama → urutan sumber yang menentukan pemenang cascade, kalau tetap di depan `:last-child::after{border:0 none}` akan menang lagi dan menge-nol-kan border yang baru dipulihkan). **Zoom:** native `wheel` listener `addEventListener(...,{passive:false})` langsung ke DOM (bukan prop `onWheel`, supaya `preventDefault` konsisten blokir native page-zoom browser); tanpa Ctrl = scroll normal 100% tak disentuh (`return` langsung, syarat keras agar search tak terganggu); dgn Ctrl = `preventDefault()` + skala `-deltaY*0.001` clamp `[0.4,2]`, diterapkan via `useRef`+mutasi `style.transform` langsung — **sengaja BUKAN `useState`**, krn wheel gesture menembak puluhan event/detik dan tree org di-render rekursif tanpa memoization (re-render lewat setState tiap tick = zoom patah-patah). **Drag-to-pan [tambahan, potong kerja ketiga]:** panning digabung ke transform+effect YANG SAMA dgn zoom (bukan mekanisme terpisah) lewat closure bersama `applyTransform()` yang set `translate(...) scale(...)` — translate DISENGAJA di LUAR scale, drag N px layar harus tetap menggeser N px layar terlepas level zoom (urutan terbalik bikin jarak pan ikut terkali faktor zoom). `mousedown` di container scroll dikecualikan kalau target `.closest('.node')` (card person) — klik card tetap buka modal "Atur Atasan" seperti biasa, tak kebajak jadi drag. `mousemove`/`mouseup` sengaja di `window`, bukan container — drag cepat bisa membawa kursor keluar bounding box chart di tengah gesture. Pan disimpan `panRef` (`useRef`, alasan sama dgn `zoomRef` — gesture puluhan event/detik, tree org rekursif tanpa memoization); `dragStartRef` snapshot posisi+pan awal, delta dihitung dari snapshot (bukan akumulasi per-tick, tahan drift). Cursor `grab`↔`grabbing`; card `Node` sudah punya `cursor:"pointer"` sendiri yang menang saat hover card (spesifisitas CSS inline, nol kode tambahan). **Verifikasi doc-keeper independen (diulang tiap sesi, termasuk sesi ketiga ini):** `git diff` dibaca penuh, cocok 1:1 dgn seluruh klaim ketiganya (termasuk `panRef`/`draggingRef`/`dragStartRef`/`applyTransform()`/`.closest('.node')`); `npm run build` clean (2613 modules); `npm run lint` whole-repo 173/151/22 = identik baseline (net-zero, kumulatif 3 sesi); lint terisolasi file ini → 1 finding, persis `useEffect fetchTree` pre-existing (nol diff, kini baris 290, cuma bergeser lagi); `.node` dikonfirmasi nyata di komponen `Node`; `git status` konfirmasi nol jejak file test sementara yang diklaim sudah dihapus. **Verifikasi visual 5 skenario connector + jalur zoom Ctrl-tahan = klaim Den, direlay apa adanya, TAK diverifikasi independen** (di luar kapasitas sesi dok, nol akses browser) — **zoom-Ctrl tetap PARTIAL** (jalur non-Ctrl terbukti jalan, jalur Ctrl-tahan belum visual-confirm, keterbatasan tooling automation). **Drag-to-pan diverifikasi via metode lain, lebih kuat secara numerik tapi tetap laporan yang di-relay** — automation drag OS-level gagal propagasi `mousemove` nyata (spt kasus `ctrlKey` di atas), diakali via harness sintetis (`MouseEvent`/`WheelEvent` di-dispatch langsung ke DOM asli lewat dev server proyek sendiri, file sementara dihapus lagi) yang membuktikan dgn angka konkret: pan tracking 1:1 piksel, retensi pan setelah mouseup, exclusion `.node` benar-benar memblokir drag (bukan cuma teori), dan urutan transform terbukti benar numerik (drag 40px di zoom 2x → pan +40px persis, bukan +80px) — tetap belum dicoba gesture mouse fisik sungguhan di browser oleh siapa pun. Detail verifikasi lengkap tiap klaim: `PROGRESS.md` 2026-08-12 (entri terpisah dari BNF).
- **Layer 0-4 BNF extension: Briefing Harian (modul baru) + BNF diperluas (gate `is_bnf_authorized()`, tab "Tarik dari Insiden", Action Items) + Meeting Mingguan (modul baru) + "Akses BNF Tambahan" (grant/revoke). Dikerjakan 10 & 12 Agu 2026 (jeda 1 hari tanpa commit, 11 Agu).** 7 tabel baru: `bnf_authorized_users` (grant/revoke akses BNF di luar jalur struktural head/direktur/CEO), `bnf_division_scopes`/`bnf_department_scopes` (model home+scope, gantikan tombol "Salin ke entity lain" yang **dibatalkan** — doc-keeper TAK menemukan jejak commit tombol itu di `git log`, klaim Den belum terverifikasi independen), `daily_report_items` (backing Briefing Harian, kategori task/aktivitas/insiden + carry-forward otomatis), `bnf_report_action_items` (checklist tindak lanjut, `assigned_to` wajib), `weekly_meetings`+`weekly_meeting_items` (backing Meeting Mingguan). Fungsi gate baru **`is_bnf_authorized()`** (SQL SECURITY DEFINER) — true utk `super_admin`/head departemen/direktur divisi/role aktif `ceo` (**diperluas 12 Agu 2026** ke `gm`/`gm_bd`/`manager`/`finance_controller` — seluruh staff akan dilatih SOP BNF, role FUNGSIONAL `hrga`/`it`/`operations`/`procurement`/`sales`/`finance` sengaja dikecualikan)/grant `bnf_authorized_users` non-revoked. Dipakai RLS `bnf_reports_select`/`_insert` (**diperketat** — staff biasa tak lagi baca/tulis `bnf_reports` langsung) + gate menu `bnf` (dibalik dari `public:true` tanpa syarat — pembalikan keputusan produk disengaja; `briefing-harian` TETAP public genuinely) + `meeting-mingguan` (baru, gate sama). RPC baru **`get_linked_bnf_status()`** menutup bug produksi nyata: badge "Jadi BNF" tak tampilkan nomor (akar: RLS blokir embed `bnf_reports` utk reporter non-`is_bnf_authorized()`, PostgREST resolve NULL senyap — BUKAN join rusak) — badge kini "Jadi BNF · Status" by design. Bug fix kedua: default filter tab Overview BNF `'active'`→`'all'` (laporan Closed sempat tampak hilang — gap discoverability filter dropdown, BUKAN bug logika; KPI strip sudah benar sejak awal). **Seluruh SQL dikonfirmasi live** (6 migrasi `20260810000001`-`20260811000002`, `schema_snapshot.sql` fresh, GRANT lengkap 7/7 tabel baru — TAK ada insiden GRANT-hilang spt pola TD-166). **NOL tes runtime independen doc-keeper** — Den melaporkan smoke test penuh Passed, di luar kapasitas verifikasi sesi dokumentasi. +11 tech debt baru **TD-184…TD-194** (`08_TECH_DEBT.md` — MEDIUM tertinggi: **TD-184** `bnf_divisions`/`bnf_departments` belum direkonsiliasi ke SK org chart resmi; **TD-193** profil "Deny Triawan" berrole `super_admin` tapi fungsi Manager Logistik, butuh audit dampak SEBELUM diubah; sisanya LOW — gap notifikasi eskalasi/cron/reassign PIC/Irisan departemen, auto-pull Aktivitas, 1 flag data-quality email domain). **Governance docs 03/04/05 sebelumnya nol dokumentasi modul BNF sama sekali** (termasuk fondasi 3-5 Agu 2026) — kini net-new (`03_DATA_MODEL.md` §BNF, `05_WORKFLOW_MAP.md` §BNF Flow). Lihat `PROGRESS.md` 2026-08-10 & 2026-08-12 (2 entri).
- **9 perubahan dalam satu sesi: Access Control fixes + multi-entity plumbing Fase 1 (`activeCompanyId`) + AdminHub (unifikasi Master Data+Admin Settings) + CompanySwitcher + PPN consolidation.** Redirect legacy `'users'` kini landing tepat di User Access; tab "Permissions" `UserEditPage` digate `isSuperAdmin` (dulu trap-UX utk admin biasa); Picking List/Surat Jalan masuk sistem `hasMenuPermission`/`role_menu_permissions` granular (migrasi `20260809000001`, lepas dari `role[]` hardcode). `AuthContext.jsx` +`activeCompanyId`/`setActiveCompanyId`/`myCompanyIds` + fungsi SQL baru `get_user_company_ids()`; `pickPrimaryErpRole`/tier-3 `hasMenuPermission` kini company-scoped. RLS `roles_insert`/`roles_update`/`user_roles_insert`/`user_roles_update` diperbaiki jadi bypass `is_super_admin()` TOP-LEVEL yang benar (`user_roles` versi TD-170 sebelumnya menaruhnya nested di dalam `AND` — bahkan super_admin terkunci home company sendiri). `AdminShell.jsx`+`AdminSettingsHub.jsx`(+9 sub-halaman) melebur jadi `AdminHub.jsx` (25 destinasi, 6 grup, gating 2-lapis replikasi persis kondisi asli) — file lama jadi orphan (`08_TECH_DEBT.md` **TD-181**, baru). `CompanySwitcher.jsx` ganti tombol topbar dekoratif mati (**TD-92 RESOLVED**) — **diuji dgn user multi-company asli** (Elvira Nurhuda, Finance MSI+SOA), menyingkap+menutup RLS gap `companies_read_own`/`roles_read` (pola `is_super_admin() OR get_user_company_id()` TIDAK CUKUP utk user genuinely multi-company, ditemukan berulang 3× satu hari — `payment_terms_read` **masih OPEN**, `08_TECH_DEBT.md` **TD-180**, baru). `entity_finance_settings.default_payment_term_id` FK baru (Payment Terms dropdown, ganti input angka manual). `src/lib/taxConstants.js` satukan literal PPN (0.11 vs 0.011 freight-forwarding, BUKAN bug). Fix bug produksi nyata: dropdown VAT Quotation sempat menampilkan PPh21/PPh23 sbg opsi PPN (200-500× lipat kalau kepilih) — ditutup. **Ditemukan folding-in audit read-only sesi ini** (lalu dihapus dari root): sistem permission KETIGA `can()`/`PERMISSIONS` (`App.jsx:273-313`) masih hidup, 10 titik Finance/Master Customer nyata, independen dari `hasMenuPermission`/deny admin (`08_TECH_DEBT.md` **TD-182**, baru, HIGH — TD-06 diperbarui). **NOL tes runtime penuh** kecuali CompanySwitcher (di atas). Migrasi tercatat hanya `20260809000001` — perubahan RLS/kolom lain belum direkam migrasi. Lihat `PROGRESS.md` 2026-08-09 (entri terpanjang, 9 subbagian).
- **6 area saling terkait — Storbit SP Fase 4 Invoice (INVOICED/SUBMITTED) + DC Master jadi CRUD penuh + DcPicker (searchable combobox) + RLS GRANT hardening (`sp_orders.status`/kolom sistem `sp_invoices`) + dual-write trilogy `sp_items`↔`sp_order_items` + InvoicePDF baru.** ✅ **SQL SEMUA LIVE** — dikonfirmasi via `schema_snapshot.sql` refresh commit `8117127` (23:33 WIB, sama hari; koreksi atas catatan lebih awal hari yang sama yang sempat bilang "SQL belum dijalankan" saat snapshot masih basi — itu bacaan jujur thd bukti saat itu, ground truth-nya yang berubah). Tabel baru `sp_invoices`/`sp_invoice_lines`/`sp_payments` (`sp_payments` dibuat tapi FASE 5/payment belum dibangun) + `ar_ttfs`+2 kolom FK (kolom+FK live, TAPI data historis belum di-backfill, itu step M7 terpisah) + RPC baru `create_invoice`/`submit_invoice`/`update_sp_item_dual` + `sp_recompute_status`/`dispatch_delivery`/`cancel_delivery` (`CREATE OR REPLACE`) — semuanya dibaca penuh doc-keeper langsung dari body fungsi live, bukan cuma grep. **Bug guard nyata ditemukan+diperbaiki (Den, bukan doc-keeper), dikonfirmasi live:** freeze `sp_recompute_status` dulu memuat `INVOICED`/`SUBMITTED` di daftarnya sendiri → status macet permanen di INVOICED; guard sekarang persis `CANCELLED,LUNAS`. RLS/GRANT: `authenticated` bisa dulu `UPDATE sp_orders.status` langsung lewat PostgREST, bypass total mesin status — **ditambal & LIVE** (24/25 kolom `sp_orders` + 10/16 kolom `sp_invoices` ber-GRANT individual, dihitung persis); kolom sejenis lain (`sp_order_items.shipped_qty`/`sp_items.sp_status`/`sp_invoice_lines.*`/`sp_payments`, semua `GRANT ALL`) SENGAJA belum ditambal. TD-175 & TD-40 kini **RESOLVED**; TD-176/177/178 tetap OPEN (TD-178 diperluas — hardcode SOA juga ada di `dispatch_delivery`). Kode entitas invoice dikonfirmasi diresolusi dinamis dari `companies.code` (bukan hardcode "SBI"). `update_sp_item_dual`: kelima kolom (`qty`/`sla_days`/`estimated_delivery_date`/`shipping_price`/`notes`) dikonfirmasi lewat baca kode; eksekusi data-uji nyata baru terbukti utk `notes` (ZZZTEST), `sla_days`/`estimated_delivery_date` belum. **NOL tes runtime/UI tetap NOL** — SQL live ≠ sudah dicoba di browser. Lihat `PROGRESS.md` 2026-08-08 (2 entri: draft awal + koreksi susulan).
- **Sesi RBAC hardening besar: fix privilege escalation `user_roles` (CRITICAL, RESOLVED) + pensiun sistem `role_permissions` legacy (tab Roles/`AppLauncher.jsx`/dead branch `hasPermission` dihapus dari kode — objek DB dibiarkan ada) + sistem `role_menu_permissions` baru Fase B (resolusi `hasMenuPermission` 3-tingkat: user override > role default > deny; halaman `RoleDefaultsPage` super_admin-only) + fix double-write `accounts.pipeline_stage` (in-flight lock 3 jalur tulis) + audit RLS/RBAC menyeluruh (19 tabel CRITICAL, seluruhnya belum dieksekusi).** RLS `user_roles` + fungsi `is_admin_tier_role()` sudah live DB (migrasi `20260807000001`, snapshot fresh). Tabel `role_menu_permissions` + kolom `user_menu_permissions.effect` juga sudah live DB (migrasi `20260807000002`) **tapi snapshot BELUM di-refresh untuk keduanya**. **Fase B3** (isi data default per role) & **Fase C** (satukan 4 struktur menu hardcode `App.jsx` dgn katalog DB `modules`/`module_menus`/`menu_actions`/`module_actions`) **belum dimulai**. Fix double-write: **kode selesai, Test A/B lolos, Test D/E belum dijalankan** — trigger `set_customer_on_won()` (nol validasi legitimasi) sengaja belum disentuh. Audit RLS/RBAC (skor RLS Coverage 7/10, Company Isolation 3/10, RBAC Consistency 4/10) sempat HANYA ada di chat sesi, kini direkam permanen. +TD-170 (RESOLVED)/TD-171 (PARTIAL)/TD-172/TD-173/TD-174 (semua baru) + TD-02/TD-06 diperbarui. ⚠️ Belum tes manual runtime untuk fitur baru. Lihat `PROGRESS.md` 2026-08-07.
- **3 fix kecil FE turunan audit `OVERDUE_STATUS_AUDIT.md`: badge "Overdue" Kanban Pipeline disinkronkan ke Edge Function `aging-pipeline`** — `AGING_LIMITS` (`PipelineKanbanPage.jsx`) disamakan ulang dgn `AGING_RULES` backend (drift sejak `proposal`/`negotiation` dicabut 24 Jul; stage `new` sebelumnya tanpa badge sama sekali) + badge digate ke `companies.aging_enabled` (keterbatasan disengaja: satu nilai per papan, bukan per-kartu — lihat TD-168) + notifikasi "Prospect Masuk Lead Pool" kini bisa diklik-tembus ke Lead Pool (`App.jsx`). **Terpisah, sesi sama:** koreksi klaim salah trigger `track_stage_change()` di `03_DATA_MODEL.md`/`05_WORKFLOW_MAP.md` — trigger itu HANYA menstempel `stage_changed_at`, tak pernah menulis ke `activity_logs`. +TD-168/TD-169 (baru, RESOLVED kode). Lihat `PROGRESS.md` 2026-08-06.
- **Insiden produksi pasca-BNF: GRANT tabel baru kelewat (403 berulang, 517 error identik) memicu loop render/refetch dari `showToast` non-`useCallback` di `App.jsx` + `CRMDashboardPage.jsx`** — semua sudah diperbaiki; kelas kerapuhan `showToast` dicatat 1 entri gabungan **TD-165**, pola GRANT-hilang **TD-166** (kemunculan kedua setelah TD-163), gap notifikasi Edit BNF **TD-167** (baru, ditemukan doc-keeper). Lihat `PROGRESS.md` 2026-08-05.
- **Modul BNF (Bad News First) baru — 7 fase (A-G): fondasi DB (org roles + trigger 4-tier), submit+admin UI, edit/hapus laporan, notifikasi eskalasi Direktur Divisi/CEO, reminder overdue otomatis (cron), Divisi/Dept Irisan multi-select** (branch `feat/bnf-module`; migrasi dari app Google Apps Script lama, data lama TAK dipindah; Governance docs 03/04/05 belum diperbarui utk modul ini — di luar scope sesi dok. 6 Agu). Lihat `PROGRESS.md` 2026-08-04 & 2026-08-05.
- **Notifikasi email saat di-tag di komentar Chatter — Edge Function baru `send-email` (relay generik ke Resend API)**, jadi fondasi yang lalu dipakai ulang modul BNF di atas. Lihat `PROGRESS.md` 2026-08-03.
- **Redesign `DealDetailPage` — 4-tab restructure + field link-able (nama sales/akun, `ProfileMiniView.jsx`) + Chatter (komentar+@mention+notifikasi, `InquiryChatter.jsx`)** (3 commit berurutan; 2 tabel baru `inquiry_comments`/`inquiry_comment_mentions` belum direkam migrasi, snapshot kini memuatnya — TD-163 PARTIAL; Chatter tanpa `showToast` — TD-164). Lihat `PROGRESS.md` 2026-07-31.
- **Fix PRF prefill dari inquiry (kehilangan `inquiry_no`) + `InquiryPicker` tampilkan nama+tipe akun + nomor Quotation/PRF jadi link**. Lihat `PROGRESS.md` 2026-07-31.
- **Tab "BANT & Pipeline" (Detail Account) — edit inline 4 dimensi BANT + fix silent-fail RLS di `saveDealUpdate`** (berdampak retroaktif ke Pindah Stage/Edit Deal juga; TD-161). Lihat `PROGRESS.md` 2026-07-30.
- **PRF: draft bisa diedit (TD-76) + redesign Detail PRF jadi 9 section bernomor + rename "Cetak PRF"→"Buat PRF"** (TD-93/148/149). Lihat `PROGRESS.md` 2026-07-29.
- **Gerbang "Buat Quotation" terima jalur penawaran vendor + fix gate "Cetak PRF" baca seluruh role aktif** (dua batch independen; TD-90/105/157/158). Lihat `PROGRESS.md` 2026-07-28.
- **Modul "Penawaran Vendor" PRF — fondasi s/d Batch 3C** (klaim/lepas PRF, tambah/edit/hapus penawaran vendor, RPC `prf_mark_quoted`/`prf_select_offer`) **+ 2 fix kecil independen** (dropdown Inquiry di Quotation, nama sales di Detail PRF). Lihat `PROGRESS.md` 2026-07-27 (2 entri tanggal ini).

> ⚠️ Banyak fase "build clean, **belum tes manual runtime**" — selalu cek sebelum anggap fitur jalan. Update bagian ini + `PROGRESS.md` tiap akhir task. Histori lengkap sebelum 26 Jul 2026 (143 entri, dipangkas 31 Jul 2026 setelah diverifikasi satu-per-satu tersalin ke `PROGRESS.md` — 19 entri kecil yang ternyata belum tercatat dipindah ke section "Catatan Susulan" di puncak `PROGRESS.md` sebelum dihapus dari sini) ada di git history file ini + `PROGRESS.md`.

## Known Issues (Quick Ref — detail: `docs/Governance/08_TECH_DEBT.md`)

- **CRITICAL** — Migrasi RLS RBAC-driven belum jalan; ~51 policy pakai `is_admin_or_above()` (tak kenal manager/ceo) → bug akses (TD-01, **PARTIAL**: 3 oversight-read sudah +`is_manager_or_above`+STABLE). `has_permission()` flagged broken/unseeded (TD-02, **7 Agu 2026: dikonfirmasi 0 pemakaian RLS+FE**, kandidat DONE). **Audit RLS/RBAC menyeluruh 7 Agu 2026 menemukan 19 tabel LAIN ber-RLS `USING(true)`/setara** (seluruh modul Storbit SP/Fulfillment + `warehouses`/`rate_sheets`/`profiles`/`app_settings`/`audit_logs`/`contacts`/`inquiry_comments`/`inquiry_comment_mentions`) — **SELURUHNYA BELUM DIEKSEKUSI** (TD-173, daftar lengkap). Privilege escalation `user_roles` (admin biasa dulu bisa assign role `super_admin`) sudah **RESOLVED** hari yang sama (TD-170).
- **HIGH** — Audit CRUD/DELETE policy semua tabel (silent 0-row; TD-03, **PARTIAL**: 4 tabel transaksional sudah dpt DELETE policy). Write non-atomik ar_btbs/permission-diff (TD-33). `profiles_read = USING(true)` stopgap CEO (perketat saat HRIS; TD-04). Edge Functions delete-user/reset-password belum di-deploy (TD-21). Belum ada audit logging / test / Sentry (TD-05/07/08). Double-write `accounts.pipeline_stage` — 3 jalur tulis, fix in-flight-lock **kode selesai tapi test D/E belum dijalankan** (TD-171); gap terpisah `applyStageMove` Kanban masih tanpa `.select()` (TD-172). **[8 Agu 2026]** PostgREST bisa tulis `sp_orders.status`/kolom sistem `sp_invoices` langsung, bypass mesin status SP — fix **DIKONFIRMASI LIVE** (TD-175 RESOLVED); kolom sejenis lain (`sp_order_items.shipped_qty`/`sp_items.sp_status`/`sp_invoice_lines.*`/`sp_payments`, semua `GRANT ALL`) sengaja belum ditambal (TD-176, tetap OPEN). **[9 Agu 2026]** Sistem permission KETIGA `can()`/`PERMISSIONS` (`App.jsx:273-313`, di luar `hasPermission()`/`hasMenuPermission()` yang sudah dikenal) masih hidup, menggerbang 10 titik aksi Finance/Master Customer nyata (Add Customer, Add/Edit TTF) — deny eksplisit admin lewat `user_menu_permissions` **tidak mengikat** di titik-titik ini (TD-182, baru, OPEN). Pola RLS `is_super_admin() OR company_id=get_user_company_id()` (tanpa varian jamak `get_user_company_ids()`) TIDAK CUKUP utk user genuinely multi-company — ditemukan berulang 3× satu hari (`companies`/`roles` sudah ditambal, `payment_terms_read` **masih OPEN**; TD-180, baru).
- **MEDIUM/LOW** — Dua sistem permission belum sinkron (TD-06, **PARTIAL 7 Agu 2026**: axis FE kini satu — `role_permissions`/`hasPermission()` pensiun dari kode, `hasMenuPermission` dapat resolusi 3-tingkat baru `role_menu_permissions`+`user_menu_permissions.effect` — axis RLS tetap terpisah, TD-01 utuh; **[koreksi 9 Agu 2026]** klaim "satu axis" cuma benar utk menu-gating — sistem KETIGA `can()`/`PERMISSIONS` yang independen dari keduanya masih hidup, lihat TD-182 di atas). Tabel dormant `sales_calls`/`sales_visits`/`customers` + kolom `profiles.role` belum di-drop (TD-18/19/20); App.jsx god-file 5.274 baris (pecah setelah ada test; TD-12).
- **FLAG (RBAC — JANGAN diperbaiki sekarang; perlu bahas terpisah)** — Menu `input` (Input SP) di-gate `module:'logistics'` → `hasPermission('logistics','view')`, dan role-def-nya masih meng-include `sales`. Konsekuensi: kalau RBAC grant `logistics.view` ke role **sales**, sales bisa lihat "Input SP" (sidebar) + "Buat SP" (home) → mungkin **tak diinginkan** (sales harusnya tak bikin SP). **Ini isu konfig data-permission/RLS (di luar scope FE)**, bukan bug home-gating (home 3.0C sudah konsisten dgn sidebar). Perbaikan = ubah role-def/permission `logistics.view` utk sales, berisiko → tunda sampai dibahas. Selain itu halaman `InputPage`/`InputSPPage` **tak punya page-level guard** (hanya `canAccessActiveMenu` yg kasar) → idealnya tambah guard halaman saat RBAC dirapikan. **⚠️ [VERIFIKASI 7 Agu 2026, doc-keeper — kemungkinan STALE, PERLU KONFIRMASI]** Diverifikasi grep `src/App.jsx` hari ini (turunan kerja retirement `hasPermission`): kedua definisi menu `id:'input'` (baris ~620, ~1001) **tidak punya properti `module:` sama sekali**, dan `MENU_KEY_MAP['input'] = 'logistics_input'` — jalur gate AKTUAL saat ini tampak `hasMenuPermission('logistics_input','view')`, BUKAN `hasPermission('logistics','view')` seperti tertulis di atas. Belum ditelusuri KAPAN mekanismenya berubah (kemungkinan besar sudah stale SEBELUM sesi 7 Agu, bukan akibat kerja hari ini — dead branch `hasPermission` fallback di `canSeeMenuItem` yang dihapus hari ini sudah dikonfirmasi 0 menu items yang masih memicunya). **Paragraf di atas TIDAK diubah/dihapus** (menghormati "JANGAN diperbaiki sekarang") — hanya ditandai perlu verifikasi ulang sebelum dipakai sbg dasar keputusan.
