# CLAUDE.md — Nexus by MSI

> Lean entry point untuk Claude Code. Detail lengkap ada di `docs/` (daftar 18 dokumen acuan: `docs/Governance/00_INDEX_README.md`, lihat §Dokumentasi Lengkap di bawah). `AGENTS.md` = identitas produk + safety rules — struktur/kebijakan/prinsip JANGAN diubah (kontrak Codex, lihat `.codex/agents/*.toml`); koreksi angka/fakta yang TERVERIFIKASI basi (mis. line-count, nama domain) BOLEH, syarat diff sekecil mungkin + commit message eksplisit sebut "koreksi fakta" (pola commit `c5476ae`). `PROGRESS.md` = dev log per-tanggal.

## Quick Reference

- **Stack:** React 19 + Vite 8 · TailwindCSS 3 · Supabase (PostgreSQL + Auth + RLS + Edge Functions + Storage) · Vercel auto-deploy dari `main` (= production).
- **Supabase ref:** `untmpqceexwxzuhlmyrg` (org "Milenial Solusi Internusa", project `nexus-msi`; ref TIDAK berubah pasca-migrasi) · pooler/`pg_dump` host **`aws-1-ap-northeast-2.pooler.supabase.com:5432`** (region Seoul — ⚠️ BUKAN `ap-southeast-1`; catatan lama yang salah menyebabkan gagal koneksi backup).
- **Live URL:** `nexus.msigroup.co.id` (production, CNAME via Domainesia) — satu-satunya domain resmi · **lama `nexus.dli.my.id` sempat dibiarkan hidup sbg safety net, kini SUDAH DINONAKTIFKAN (20 Jul 2026):** DNS record CNAME `nexus` di zona `dli.my.id` (Domainesia) dihapus, tanpa redirect.
- **Repo:** GitHub `Milenial-Solusi-Internusa/storbit-manifest`, branch `main` (solo dev; `fix/*` hotfix). ✅ **repo sudah PRIVATE** (26 Agu 2026, dikonfirmasi dari GitHub Settings; sebelumnya PUBLIC — kode ERP 3 entitas + schema sempat terbuka).
- **Deploy:** Vercel team "MSI Group" (Hobby), project `nexus` · project Vercel lama (akun pribadi Hobby, `storbit-manifest`) dibiarkan hidup sementara (safety net).
- **Migrasi kepemilikan (9-10 Jul 2026):** Supabase/GitHub/Vercel/domain dipindah dari akun pribadi ke org MSI. Backup penuh (schema+data, 3.5 MB, 96 tabel) diambil sebelum migrasi. **Backlog keamanan — ✅ SELESAI:** repo PUBLIC → **private** (26 Agu 2026). **Sisa (belum dikerjakan):** 2FA 2 Owner org GitHub (mhmmdjaelaniii, msigroup-sys); Vercel Hobby → paid (pemakaian komersial); Supabase Auth Site URL masih `localhost:3000` + Redirect URLs kosong (rapikan); rename repo→`nexus` (opsional).
- **Entity UUID:** MSI `0e1840d8-e6fb-4190-bd09-88338e68b492` · JCI `42569e7c-531b-4d2b-832a-d5a7268c455b` · SOA `d2e5e565-5f67-4954-b8d9-5979a2a0c697`.
- **Sumber kebenaran DB:** `supabase/schema_snapshot.sql` (~~132 tabel = 122 bisnis + 10 tabel backup~~ ~~**[dihitung ulang doc-keeper 5 Sep 2026: 133 tabel `public`]**~~ ~~**[dihitung ulang doc-keeper 7 Sep 2026: **138** tabel `public`**~~ ~~**[dihitung ulang 9 Sep 2026 saat refresh pasca-5-migrasi quotation: **139** tabel `public`** — angka pembanding `CREATE TABLE public.` **139 → 139**, nol tabel baru dari kelima migrasi itu; ⚠️ **pola grep GRANT WAJIB ber-anchor `^`** — angka 247 yang sempat beredar berasal dari pola tanpa anchor, yang benar **243** — `grep -c "^CREATE TABLE public\."`; kenaikan 133→138 berasal dari migrasi CRM v3 (6-7 Sep), bukan dari sesi Storbit]~~ **[dihitung ulang doc-keeper 18 Sep 2026, snapshot `9f6e232`: **141** tabel `public`** (`grep -c "^CREATE TABLE public\."`; vs 138 di `8f13c5e`): −`customers` (TD-19) +`customers_backup_20260614` +`goods_receipts` +`goods_receipt_items` — dua yang terakhir = objek `20260914000002` yang 15 Sep dicatat staging-only → **perlu konfirmasi Den apakah sudah jalan di produksi**] — BUKAN `migrations/`, yang berhenti 3 Jun 2026). ⚠️ **Cara `pg_dump`-nya BERUBAH 5 Sep 2026** jadi `--schema-only --schema=public`: jumlah tabel `public` **tidak berubah oleh flag itu** (dump lama ikut membawa 35 tabel skema sistem, kini tidak — **nol tabel bisnis hilang**) **dan seluruh 133 blok `COPY` hilang**. ✅ **DIPUTUSKAN 6 Sep 2026 — snapshot TETAP schema-only**; `02_RULES_GOVERNANCE.md` §4 sudah diselaraskan (verifikasi `COPY ~133` dicabut; larangan `--no-owner`/`--no-privileges` tetap, ACL harus ikut). Alasan + jejak audit: §4 · `09_ROADMAP.md` **#32** · `03_DATA_MODEL.md` **gotcha #23**. Status freshness lengkap (riwayat stale/segar 10 Jul–27 Jul + aturan baca snapshot): `03_DATA_MODEL.md` **gotcha #10**.
- **Brand — tampilan APLIKASI internal Nexus (KEPUTUSAN Den 19 Sep 2026; ⛔ BARU 22 HALAMAN ADMIN/FOUNDATION YANG MEMAKAINYA *[21 sejak 21 Sep 2026 — `AdminSettingsHub.jsx` dihapus Batch FS Fase 1]* — kit tunggalnya ADA di `src/kit/` sejak Batch DS 1, 20 Sep 2026 malam; ~~nol halaman memakainya~~ **[20 Sep malam — Batch DS 2 ✅ SELESAI, review DITUTUP Den 20 Sep 2026 malam (sesi kesembilan; dikerjakan di branch `feat/kit-ds2-adminkit`): 22 halaman admin/Foundation (11 `admin-settings/*` + `AdminHub` + 10 master data) + `main.jsx` (`kit.css`) memakai kit tunggal di `main` *[→ 21 Sep 2026: tinggal **21** (10 `admin-settings/*Page` + `AdminHub` + 10 master data) — `AdminSettingsHub.jsx`, hub lama yatim yang ikut dimigrasi, dihapus Batch FS Fase 1; 22 tetap benar sebagai cakupan Batch DS 2 saat itu]*; kit lama `admin-settings/kit.jsx`+`tokens.js` masih ada tapi nol pemakai (dihapus Batch DS 7); tampilan produksi 22 halaman itu berubah begitu deploy `main` berikutnya — halaman LAIN belum ber-sage; ⚠️ catatan terbuka BELUM LOLOS: alur simpan HRGA "Simpan Semua" belum pernah dites (→ daftar cek Batch DS 6)]**):** palet resmi aplikasi = **sage** — latar halaman `#F6F8F3` · latar kartu **`#FFFFFF`** (direvisi Den 20 Sep 2026 malam, semula `#EEF3EA` — kartu putih polos, pemisah dari halaman = border `LINE`, sage tampil lewat teks/ikon/aksen; turunan `HEAD_BG`/`LINE_SOFT`/`DISABLED_BG` dihitung ulang di `tokens.js`) · aksen utama `#C3D9B8` (hover `#AFC9A0`) · aksen kedua `#D7C9B0` (hover `#C9B896`) · border `#D8E0D2` · teks utama `#33422B` · teks sekunder `#5C6B52`. Palet ini **menggantikan seluruh navy dan oranye lama sebagai warna aplikasi** (tidak ada lagi navy/oranye "resmi" untuk UI). Font resmi **TIGA saja**: **Montserrat** (heading/display) · **Inter** (body) · **IBM Plex Mono** (angka & kode) — font lain apa pun tidak resmi. Lucide icons · **no emoji, no dark green**. ⚠️ **Palet sage KHUSUS tampilan aplikasi internal.** Dokumen yang dikirim ke customer — **Print kit** (`src/modules/logistics/printKit.jsx` + `printTokens.js`), **PDF Storbit** (Picking List, Surat Jalan, Invoice, Laporan), Quotation PDF — **tetap memakai warna brand entitas MSI/JCI/SOA yang asli, TIDAK ikut berubah, dan TIDAK disentuh keputusan ini.** ⛔ **STATUS KODE HARI INI (`main`):** kit CRM v3, ~~AdminKit~~, `index.css`, dan puluhan file token lokal **masih memakai navy/oranye lama** *[AdminKit: 22 pemakainya sudah pindah ke `src/kit/` (Batch DS 2 selesai) — `admin-settings/kit.jsx`+`tokens.js` sendiri = kode mati s/d Batch DS 7]*, dua tumpukan font legacy masih dimuat, Oswald `.kpi-value` + pengecualian palet 4 tile KPI hero CRM Dashboard masih ada, dan dua halaman **web** Storbit (`SalesOrderDetailPage.jsx`, `StorbitDashboardPage.jsx`) memakai keluarga ungu `#5b3fa0` + Cormorant/Lora (`@font-face` `salesOrderDetail.module.css`, sengaja sewarna PDF Storbit) — **implementasi (penyatuan kit tunggal + ganti warna/font + TD-269/TD-270) sengaja DITUNDA menunggu instruksi Den.** **[20 Sep 2026 malam — Batch DS 1 SELESAI, review DITUTUP Den (sesi keenam, kode + screenshot diterima): `src/kit/` 12 file + `src/lib/entities.js` (13 file baru, 2.374 baris) lahir sebagai KODE MATI — `tokens.js` sage (9 hex + turunan; palet STATUS/DATA `SEMANTIC`/`TONE`/`DEAL_STATUS` kelas terpisah) + 31 komponen/hook + etalase `__demo/KitGallery.jsx` (tidak di-route) ADA, tapi belum diimpor satu halaman pun dan `kit.css` belum masuk `main.jsx`; nol file lama disentuh; build 2.636 / lint 160 (139/21) = baseline. ~~Batch DS 2–7 BELUM mulai~~ *[→ Batch DS 2 selesai (sesi ketujuh–kesembilan) — lihat awal bullet; Batch DS 3–7 belum mulai]*; TD-269/TD-270 TETAP OPEN; #61 tetap berlaku — **tafsiran FINAL dikonfirmasi Den 20 Sep 2026 (sesi keenam):** kit ada ≠ boleh diimpor; `src/kit` hanya boleh diimpor sebuah halaman pada giliran Batch DS-nya, halaman baru tetap ikut kit tetangganya (`09_ROADMAP.md` #61, DSR A.3). **#62 DIJAWAB (sesi keenam): 4 fg STATUS digeser minimal ke AA (`ok #2D794D` · `warn #8E620D` · `slate #676D7B` · `orange #BE4A19`), kontras diukur ulang = 0 gagal teks/status.** Detail: `docs/DESIGN_SYSTEM_REFERENCE.md` A.3/A.4.3 + `PROGRESS.md` 2026-09-20 (entri kelima).]** Jangan baca dokumen mana pun sebagai "kode sudah sesuai palet sage" — **satu-satunya halaman ber-sage = 22 halaman admin/Foundation (Batch DS 2, selesai 20 Sep 2026 malam; 21 sejak 21 Sep 2026)**; sisanya (CRM, Storbit, HRGA, BNF, User Access, shell, komponen bersama) masih navy/oranye (`PROGRESS.md` 2026-09-20 entri ketujuh–kesembilan · DSR A.4.3). Hex navy/oranye yang masih muncul di entri Recent di bawah = jejak historis/keadaan kode saat itu, bukan kebijakan. **Keputusan final 20 Sep 2026 (#60/#61 DIJAWAB, `09_ROADMAP.md`):** (1) **halaman/fitur baru selama masa tunggu ikut kit/token yang ADA, bukan sage duluan** (DISETUJUI, #61) — supaya tidak lahir palet KELIMA di kode (empat yang hidup: v3 `#144682`, AdminKit/shell `#1B4D8A`, warm-beige lokal, ungu/serif Storbit web); (2) **dua halaman web Storbit ber-ungu/serif IKUT penyatuan ke sage + tiga font** — bukan pengecualian dokumen customer, pembukanya staff internal (BELUM DIKERJAKAN, ikut batch penyatuan); (3) **5 warna KPI hero CRM Dashboard TETAP warna-warni, TIDAK diseragamkan ke sage** — warna status/data ≠ warna identitas aplikasi, harus tetap saling kontras (merah ≠ hijau) supaya sinyalnya tidak kabur; bukan pengecualian yang lupa diseragamkan; (4) **Oswald di angka KPI hero DIPERTAHANKAN untuk saat ini** — sengaja belum diganti ke tiga font resmi, keputusan sadar ditunda, **bukan pelanggaran** kebijakan tiga font. Arah penyatuan kit + seluruh angka kit/token yang ada: `docs/DESIGN_SYSTEM_REFERENCE.md`.

## Aturan Wajib (ringkasan — detail: `docs/Governance/02_RULES_GOVERNANCE.md`)

- **Fetch:** selalu `.limit(1000)` (default PostgREST 10) · `.is('deleted_at', null)` · scope `company_id` + role-aware (`isAllEntities=['super_admin']`, `isSalesOnly=['sales','operations']`).
- **`profiles`** pakai kolom **`active`** (TIDAK ada `deleted_at`). · **`showToast?.(message, type)`** (urutan message dulu).
- **Soft delete** (`deleted_at`/`is_active=false`), jangan hard-delete data business.
- **DB:** GRANT setelah CREATE (tabel CLI tak auto-grant) · trigger ordering pakai prefix `trg_z_` · `auth.uid()` NULL di SQL Editor (test RLS di browser) · super-admin bypass = top-level `OR is_super_admin()` · **tambah parameter RPC = fungsi BARU** (overload lama tetap hidup; salin badan dari LIVE, `REVOKE PUBLIC` signature baru, putuskan nasib overload lama — gotcha #37) · kolom sumbu RLS **baru** wajib lahir dengan pengisi lapis DB, bukan cuma payload FE (gotcha #38; **tidak retroaktif** — `inquiries.owner_id` yang sudah ada = Keputusan Terbuka #58).
- **Embed alias** saat constraint FK belum di-rename (`customers:accounts!sp_items_customer_id_fkey(name)`).
- **Deploy code yang berhenti baca kolom DULU, baru drop kolom.** · Refresh `schema_snapshot.sql` via `pg_dump` setelah perubahan SQL Editor.
- **Workflow:** Inspect → Plan → Edit (scope kecil, hindari big-bang & rewrite App.jsx sekaligus) → Verify (`npm run build` clean, lint net-zero) → Summarize. Push HANYA bila diinstruksikan. Audit-before-fix untuk bug non-trivial.
- **Status commit/merge/branch: JANGAN ditulis ke `CLAUDE.md`/`PROGRESS.md`** (berlaku juga untuk doc-keeper). Git satu-satunya sumber kebenaran (`git log`, `git branch --merged`) — menyalinnya ke dokumen = dua sumber yang pasti melenceng (terbukti 19 Jul 2026: 9 entri harus dikoreksi). Yang TETAP dicatat karena git tak tahu: keputusan desain + alasannya, langkah manual DB belum jalan (mis. refresh `pg_dump`, RPC dibuat manual), pertanyaan terbuka, status tes runtime, tech debt. Nama branch boleh disebut sebagai KONTEKS ("dikerjakan di branch X"), tapi JANGAN nyatakan sudah/belum merge.
- **Safety rules lengkap:** lihat `AGENTS.md` (15 non-negotiable).

## Dokumentasi Lengkap

Daftar lengkap **18 dokumen acuan tetap** (13 file `docs/Governance/`, `CLAUDE.md`, `AGENTS.md`, `PROGRESS.md`, `supabase/schema_snapshot.sql`, `docs/DESIGN_SYSTEM_REFERENCE.md`) — dengan kolom *isinya apa · buat siapa · kapan dibuka* — hidup di **satu tempat**: `docs/Governance/00_INDEX_README.md` §Daftar Dokumen (rumah resmi sejak 20 Sep 2026; tabel ini sengaja tidak disalin ke sini supaya tidak ada dua salinan yang melenceng). Urutan baca wajib tiap sesi: `CLAUDE.md` → `AGENTS.md` → `docs/Governance/02_RULES_GOVERNANCE.md`, lalu dokumen per tipe task menurut tabel "Cara Pakai" di index itu. Subfolder `docs/architecture/`, `docs/security/`, `docs/database/`, dll. = blueprint/baseline pre-existing (lihat "Required Reading" `AGENTS.md`).

## Current Phase & Recent Changes

**Current phase — DUA jalur berjalan BERSAMAAN:**

**(A) MVP Storbit end-to-end — chain SP → Picking → Surat Jalan LENGKAP + master-data tooling (dikerjakan di branch `restruktur-nexus`)** ✅ Selesai di branch: Slice 0.1 (status SP) · Fase 2 (Picking List + Cancel) · Import Data Produksi (720 baris/435 SP, terverifikasi SQL) · Fase 0.2 (product_id backfill) · Fase 0.3 (SP external_url) · Fase 3 (Packing & Surat Jalan/delivery notes) · Fase 1 (cek stok & reservasi otomatis) · Material Packing + PickingListPDF (Fase 3.x) · Lokasi Rak + Riwayat Harga produk (kontrak/PKS) · BulkEditPricePage (update harga massal, picker role-aware lintas entitas) · ProductPicker dropdown-only di InputSPPage/FormModal/EditItemModal + prefill harga snapshot · fix mapping `is_active` (dropdown customer). **Smoke test UI belum penuh** (banyak fitur "build clean, belum tes runtime" — perlu login). **PERLU KONFIRMASI:** selisih SP 431 vs 435 (Gigih) · mapping 30 item kontrak PKS Indomarco. Detail granular: `PROGRESS.md` + `STATUS.md`. **➕ FASE 0 skema SP baru** (`dc_master`/`sp_orders`/`sp_order_items`/`sp_btb` + 3 harga kategori + repoint FK gudang + constraint) **SELESAI & terverifikasi di branch `feat/sp-schema`** — **DB-only, non-destruktif** (tabel lama utuh), **belum frontend, belum refresh snapshot**; **sudah merge ke `main`**. Lihat entri Recent teratas + `PROGRESS.md` 2026-07-06 + `DESIGN_SP_SCHEMA.md`.

**(B) Redesign Commercial & CRM v3 — kode yang ada kini LIVE DI PRODUKSI (18 Sep 2026); cakupan redesign ⚠️ MASIH JAUH DARI SELESAI** (dikerjakan di branch `feature/crm-v3-batch-persiapan` 27 Agu–17 Sep; deploy produksi 18 Sep 00:33 WIB terverifikasi Den). **✅ [18 Sep 2026] Semua gerbang rilis di paragraf ini SUDAH TERTUTUP — TD-225, TD-249, #35 (jalur B), migrasi hari-merge `20260830000003` + `20260917000002` + `20260916000001` LIVE staging & produksi; gate role CRM lewat `src/lib/roles.js`; `pipeline_stage` legacy beku (Batch 3C belum). Kalimat-kalimat "belum di produksi"/"gerbang rilis" di bawah = keadaan s/d 10 Sep, dipertahankan sebagai jejak. Yang masih berlaku: daftar halaman BELUM disentuh redesign + ⚠️ tes staging akun `bd_*` belum ada laporan.** Cakupan rencana = Batch B1–B6 di blueprint CRM v3 (dokumen di luar repo). **Yang BENAR-BENAR sudah migrasi penuh ke design kit v3 kini DUA halaman:** `InquiryListPage` (2 Sep 2026 — `ListView mode="table"`, kolom/filter Owner, filter Deadline Quote, toggle My/All, kolom Stage dicabut, badge status tinted rounded-square) dan **`DealDetailPage`/Detail Deal** (4 Sep 2026 — `FormSheet`+`Notebook`+`ListView`+`StatusBar` chevron, kartu badan 18 field, kolom Readiness 4-keadaan, Chatter ke slot `aside`). Papan **Pipeline** menyusul sebagai satu-satunya permukaan v3 lain yang genuine; halaman lain yang "kelihatan v3" sebenarnya cuma meminjam konstanta. **BELUM disentuh redesign sama sekali: Account · Approval Lead Pool · Quotation · Customer · Aktivitas** (Detail Deal sudah KELUAR dari daftar ini per 4 Sep 2026; sisanya tak berubah). ⚠️ **Belum berjalan di produksi** — fondasi DB-nya (9 migrasi CRM v3, 27–30 Agu, termasuk `inquiries.owner_id`) hidup di **staging**, belum di produksi; FE-nya akan rusak kalau naik mendahului migrasi itu (**TD-225**, gerbang rilis). ⛔ **[4 Sep 2026] Gerbangnya kini ADA DUA, dan yang kedua LEBIH KERAS:** migrasi `20260827000002_crm_v3_lifecycle` **me-RENAME** `accounts.account_status` → `lifecycle_stage`, sementara `main` yang melayani produksi masih membacanya di **14 file / 29 baris hidup** → **produksi patah SEBELUM branch di-merge**. Migrasi itu **DITAHAN** (blok ⛔ ditempel di kepala filenya); empat migrasi WAJIB lainnya boleh jalan duluan. Dua jalan keluar **belum diputuskan** → `09_ROADMAP.md` **Keputusan Terbuka #35**; migrasi yang sama juga menyelipkan perubahan perilaku `set_prospect_on_inquiry` yang **tetap pertanyaan terbuka** → **#36**. ⚠️ **Tes runtime: ADA untuk Batch A+B (staging, 4 Sep), NOL untuk sisanya** — jangan digeneralisasi ke dua arah. Dua sumbu ganda masih terbuka: `accounts.pipeline_stage` (4 penulis tersisa → Batch B3) dan trigger `set_customer_on_won()` (→ Batch B2). Detail: `PROGRESS.md` 2026-09-03 & 2026-09-04 + `09_ROADMAP.md`. **➕ [9-10 Sep 2026] Batch B4 (Quotation versioning & approval) BERJALAN — dan sengaja MENDAHULUI penutupan B3** (deviasi urutan batch = **keputusan sadar Den, BUKAN kelupaan**; B3 memang belum tutup, `accounts.pipeline_stage` masih punya 4 penulis FE). Lima migrasinya ber-header **LIVE staging + produksi 9 Sep**, tapi **itu objek DB, BUKAN halamannya** — daftar "BELUM disentuh redesign" di atas **tidak berubah**, `QuotationListPage`/`QuotationDetailPage` masih non-v3 (token v3 sengaja TIDAK diimpor ke sana supaya tidak memulai migrasi halaman itu diam-diam). ⛔ **GERBANG RILIS TETAP DUA:** **TD-225** (`owner_id`) · **TD-249** (`p_scope_own` `created_by`↔`owner_id` harus serempak dengan `20260830000003`). ✅ **TD-243 RESOLVED 10 Sep 2026** — migrasi `20260909000006` ternyata **SUDAH LIVE di staging DAN produksi sejak 9 Sep** (dibuktikan query `pg_proc` langsung ke kedua environment, bukan lewat snapshot); yang basi cuma header filenya, dan itu sudah dikoreksi. Ia sempat dihitung sebagai gerbang ketiga selama beberapa jam pada 10 Sep — **jangan dihidupkan lagi**. ⚠️ **Tes runtime: ADA untuk quotation versioning (staging), NOL untuk restore "Mark as Won" 10 Sep.** Detail: `PROGRESS.md` 2026-09-09 & 2026-09-10.

Recent (terbaru → lama; detail granular di git history & `PROGRESS.md`):
- **[21 Sep 2026] HOTFIX TD-271 — race `permissionsLoading` vs `erpRoles`: izin ROLE-DEFAULT (tier 3 `hasMenuPermission`, tabel `role_menu_permissions`) tidak terbaca dua gate navigasi saat aplikasi dibuka → deep-link `?menu=<id>` ditolak & DIKUNCI (`urlMenuReady`), restore menu terakhir dipental ke Command Center — dikerjakan di branch `fix/td-271-permissions-loading-race` (dari `main` `46f249f`; BUKAN bagian Batch FS) — ⛔ SATU file `src/contexts/AuthContext.jsx` (+37/−3); NOL `App.jsx`; NOL migrasi/SQL; produksi mengikuti deploy `main` berikutnya (JANGAN dibaca LIVE); ⏳ gerbang = uji manual Den di Vercel Preview branch (env staging).** **Akar (diverifikasi ke kode `main` sebelum fix):** `fetchMenuPermissions` (`:242-276`) pertama kali jalan saat `erpRoles` masih `[]` (closure) → role perms `[]` + `setPermissionsLoading(false)` `:274`, selesai lebih dulu (1 request) daripada `fetchProfileById` (`:27-50`, 2 request); `App` mount di commit yang sama dengan `setErpRoles`+`setLoading(false)` (`:105-111`), effect anak `App.jsx` (adopsi `?menu=` `:2325-2339` mengunci `setUrlMenuReady(true)` `:2338`; FIX B `:2223-2276` → `setActiveMenu(visFlat[0])` = `dashboard` `:2275`) berjalan sebelum effect induk men-set loading lagi → memutus dengan izin tier-3 kosong. **Terdampak = persis populasi sistem role default** (`bd_*` sejak 12 Sep, `proc_staff`, `hcga_*`, `finance`/`operations` tanpa grant per-user); `super_admin` (tier 1) & tier-2 (grant per-user ikut fetch pertama) selamat → **tak pernah terlihat Den**. Ketahuan 21 Sep lewat sweep QA G0 Batch FS Fase 2.5 (branch `feat/fs-fase2-5-router`; tooling tak ada di branch ini). **Fix:** state `rolesReady` (`:67`; `false` sampai `fetchProfileById` user INI selesai — juga `true` saat fetch gagal/safety timeout supaya tidak loading selamanya; di-reset saat sign-out & user berganti) · `fetchMenuPermissions` menolak jalan + menegaskan `permissionsLoading=true` selama `!rolesReady` (`:276-279`), `rolesReady` jadi dependency → fetch nyata tepat sekali dengan daftar role lengkap · `value.permissionsLoading = permissionsLoading || (!!session?.user && !rolesReady)` (`:397`). **Verifikasi (laporan sesi, sweep staging build hotfix vs build `main`):** tujuan BARU tercapai = persis himpunan yang diizinkan role — `?menu=`: sales 6→**20**/162 (+14 = 14 key default `bd_*`) · `hcga_personel` 6→9 · `proc_staff` 6→10 · warehouse 19→20 (+`reporting-mom`) · `viewer` tetap 6; restore: sales 84→100 · hcga 84→87 · proc 84→88 · warehouse 97→98 · viewer tetap 84; **nol tujuan hilang**. Doc-keeper mengukur ulang: himpunan +14/+3/+4 cocok key default migrasi 12 Sep; baseline `main` 6/19 & 77/64 terpental cocok output sweep untracked; build **2.646** · lint **159 (138/21)** = baseline (1 error AuthContext sudah ada di `main`); **angka pasca-hotfix tidak bisa dihitung ulang di sini**. ⚠️ smoke `super_admin` BELUM (bypass tier 1 tak berubah secara kode; deep-link menunggu izin ±0,5 s). **Efek samping (bukan regresi keamanan):** `App.jsx:3144` masih merender blok halaman selama `permissionsLoading` → halaman tak diizinkan sempat mount lebih lama sebelum dipental (console-error `inventory-*`/`storbit-dashboard` untuk tier-3 mode restore; RLS melindungi) — kandidat perbaikan terpisah, belum bernomor. **Register:** +TD-271 (✅ RESOLVED kode) · +TD-272 (LOW, OPEN — restore menembus gate id di luar pohon + anak `assets-*`) · TD-130 → DONE · **nol Keputusan Terbuka baru**. ⚠️ **Rekonsiliasi dokumen dua branch:** branch routing (`ee68d92`) memuat TD-271 sebagai OPEN + **#63** ("hotfix `main` vs tunggu G1") + `04 §5 #13/#14` — di sini #63 **sengaja tidak dibuat** (pilihan = hotfix `main` dulu → #63 kelak DIJAWAB); **baseline sweep Batch FS** (`scripts/qa/baseline/*`, dari build `main` `46f249f`) merekam perilaku buggy → **wajib dibuat ulang** setelah hotfix masuk `main` (perbedaan = perbaikan, bukan regresi); G1 `RequireMenu` tak perlu menambal race ini sendiri. Detail: `PROGRESS.md` 2026-09-21 (entri HOTFIX TD-271) · `08_TECH_DEBT.md` TD-271/TD-272.
- **[21 Sep 2026] Batch FS (Folder Structure) LAHIR — batch BARU berdampingan dengan Batch DS: Fase 0 (aturan struktur folder/penamaan/routing) + Peta Struktur Target ditulis Den (dua dokumen baru di `docs/`, belum terdaftar di `00_INDEX_README.md`), lalu Fase 1 DIKERJAKAN: 3 file dead code dihapus (1.045 baris; dikerjakan di branch `feat/fs-fase1-dead-code`) — ⛔ NOL migrasi/SQL; NOL perubahan perilaku (ketiga file nol pengimpor); NOL file lain tersentuh; snapshot tidak terdampak; Batch DS 3 TETAP belum mulai.** Aturannya: `docs/Batch_FS_Fase_0_Aturan_Struktur.md` (14 bagian — 9 modul Bagian 1 Grand Design + `modules/system/`; flat hanya ≤3 file; sub-fitur per Level 2 + `pages/`/`documents/`; tiga tingkat shared; **routing tunggal `src/routes/`**, `App.jsx` tinggal shell — "menutup TD-12"; alias `@/`; penamaan Inggris; `docs/` → hanya `Governance/` + `archive/`; root → `scripts/seed|backup/`) + `docs/Batch_FS_Peta_Struktur_Target.md` (target akhir `src/`/`docs/`/`supabase/`/`scripts/`/root). Urutan fase: 1 dead code → routing → rename bentrok makna → gabung BNF (3–4) → `docs/` → root → struktur modul (7) → penamaan (8, opsional). **Fase 1 (diverifikasi doc-keeper):** `git rm` `src/modules/hrga/components/HrgaRequestDetail.jsx` (542) + `HrgaRequestForm.jsx` (398; folder `hrga/components/` ikut hilang; keduanya digantikan `hrga/pages/HrgaDetailPage.jsx`/`BuatRequestPage.jsx`, terakhir disentuh `e8f8cfd` 2 Jun 2026) + `src/pages/foundation/admin-settings/AdminSettingsHub.jsx` (105; hub LAMA yatim — ⚠️ BUKAN `AdminHub.jsx` yang hidup) — nol `import` tersisa (rujukan = komentar `App.jsx:41/:3384/:4062/:4065`, `AdminHub.jsx:4/:16/:43`; `useHrgaRequestDetail` = hook lain), build **2.646** · lint **159 (138/21)** = baseline. **Konsekuensi:** pengimpor `src/kit` di Foundation **22 → 21** (22 tetap benar untuk Batch DS 2); **TD-181 (a) tertutup** (`AdminShell.jsx` sudah `f3172c7` 11 Sep) — **(b) tetap OPEN** (3 blok render `App.jsx:3421/3434/3447` + redirect `:3393` tanpa 3 id itu). ⚠️ Komentar `App.jsx:41-43` "filenya dibiarkan di disk (orphan)" kini basi (kode, bukan wewenang doc-keeper). ⚠️ `RolesPage.jsx` (TD-204) ternyata juga sudah dihapus `f3172c7` tapi TD-204 masih OPEN — perlu konfirmasi (sesi 11–13 Sep belum terdokumentasi). **SENGAJA TIDAK DISENTUH (instruksi Den):** `crm/v3/Chatter.jsx` + `CrmV3DemoPage.jsx` → Batch DS 3 · `admin-settings/kit.jsx`+`tokens.js` → Batch DS 7 · `LightHandoverModal.jsx`+`StrategicHandoverModal.jsx` → Fase 3/7 Batch FS (jejak Order Handover, bukan dead code biasa). ⚠️ Peta target menyerap `DESIGN_SYSTEM_REFERENCE.md` → `06` dan `11_FINANCE…` → `03/05/09` (daftar 18 acuan akan berubah) — perlu konfirmasi Den. Nol TD baru, nol Keputusan Terbuka baru. Detail: `PROGRESS.md` 2026-09-21 · Fase 0 §Catatan Terbuka "Status Fase 1".
- **[20 Sep 2026 · sesi kesembilan, malam] Review Batch DS 2 DITUTUP Den — Batch DS 2 (AdminKit → kit tunggal `src/kit/`) ✅ SELESAI: 22 halaman admin/Foundation + `main.jsx` memakai kit sage di `main` (dikerjakan di branch `feat/kit-ds2-adminkit`) — ⛔ sesi ini DOKUMENTASI SAJA (nol kode berubah, nol migrasi/SQL); Batch DS 3 BELUM mulai.** Hasil review = **revisi `CARD` → `#FFFFFF`** (sudah diterapkan sesi kedelapan), **nol permintaan perubahan lain**. Gate akhir Batch DS 2 (diukur ulang doc-keeper, identik laporan sesi): build **2.646** modul · lint **159 (138/21)** = baseline −1 · kode = 23 file Batch DS 2 + 2 file kit revisi `CARD` · smoke staging 5 alur simpan lolos ("Tersimpan!" 15–30 ms SETELAH respons PostgREST) + offline → "Gagal menyimpan" · sweep 20 destinasi AdminHub + 3 sub-tab nol error · 375 px nol overflow · screenshot sebelum/sesudah + kartu putih di luar repo (18 PNG + `_errors.json` kosong, dilihat doc-keeper). Verifikasi kode doc-keeper: **22 file** mengimpor `…/kit` + `…/kit/tokens`, **nol** pemakai `admin-settings/kit|tokens` di luar komentar, `main.jsx:4` `kit.css`, `CARD = '#FFFFFF'` `tokens.js:32`. ⚠️ **Catatan terbuka — BELUM LOLOS, bukan lolos:** alur simpan HRGA **"Simpan Semua"** (`ApprovalWorkflowsPage.jsx:617` `SaveButton` → `saveCategory` `:675` → `hrga_approval_configs.upsert` `:686`) **belum pernah dites** — staging MSI nol baris `hrga_request_types` sehingga tombolnya tak dirender (`cats.length === 0` → empty state `:701`); pola handler sama dengan 5 alur yang lolos (`return { error }` / `return false` `:685-687`) tapi itu bukan bukti → wajib dicoba manual begitu ada datanya di staging (JCI/SOA?) atau setelah produksi; **masuk daftar cek Batch DS 6, TD-270 tetap OPEN**. Konsekuensi #61: larangan impor `src/kit` sudah berhenti untuk 22 halaman itu, halaman lain tetap dilarang. Tampilan produksi 22 halaman berubah begitu deploy `main` berikutnya; halaman lain belum ber-sage. Yang belum: `PositionsPage.jsx` `ENTITIES` lokal (perlu konfirmasi giliran) · TD-269/TD-270 OPEN (Batch DS 6) · kit lama + 5 alias (Batch DS 7). **Nol TD baru, nol Keputusan Terbuka baru.** Berikutnya = **Batch DS 3** (kit CRM v3 → kit; **wajib staging**; `crm/dealStatus.js` belum ada), hanya atas instruksi Den. Detail: `PROGRESS.md` 2026-09-20 (entri kesembilan) · DSR A.4.3 baris Batch DS 2.
- **[20 Sep 2026 · sesi kedelapan, malam] Revisi kebijakan A.1 saat review Batch DS 2 — token `CARD` (latar kartu) `#EEF3EA` → `#FFFFFF`, keputusan Den — ⛔ 2 file kit berubah (`src/kit/tokens.js` + `kit.css`) di branch `feat/kit-ds2-adminkit`; NOL file halaman; NOL migrasi/SQL; status review Batch DS 2 TETAP ⏳; produksi belum berubah; nol TD/Keputusan Terbuka baru.** *[→ review DITUTUP sesi kesembilan, Batch DS 2 SELESAI — lihat entri di atas]* Kartu **putih polos**; pemisah dari halaman = border `LINE`; sage tampil lewat teks/ikon/aksen; **8 token lain tetap**. Turunan dari `CARD` dihitung ulang rumus sama (diverifikasi doc-keeper): `LINE_SOFT` → `#ECF0E9` · `HEAD_BG` → `#EFF3ED` (= `SEMANTIC.neutral.bg`) · `DISABLED_BG` → `#E8ECE4`; `INPUT_BG` tetap `= BG` tapi kini "bidang isian sage samar di atas kartu" (komentar diganti); `ROW_HOVER` tetap; `kit.css` `--nk-line-soft` cermin. Kontras diukur ulang (doc-keeper, identik laporan sesi): INK di CARD **10,74** (9,54) · INK_SOFT 5,71 (5,07) · INK_SOFT di HEAD_BG 5,09 (4,70) — **0 gagal teks/status**; batas non-teks: kartu vs halaman 1,07:1 · LINE vs kartu 1,35:1 — 4 FAIL "ui" yang sama, tidak bertambah. Gate: `eslint src/kit` 0/0 · build **2.646** (diukur ulang) · screenshot ulang 6 halaman × 2 lebar nol error (di luar repo). Menutup baris risiko A.4.5 "kartu ≈ halaman 1,05:1" (dicoret). ⚠️ Efek ke grep gate: klaim "7 hex sage hanya di `src/kit/`" kini berlaku utk **6 hex khas** — `#FFFFFF` ada di 35 file lain sebagai putih biasa. ~~⚠️ Komentar kode `tokens.js:90-91` masih "4,70:1" (basi; angka `:48` benar) — bukan dokumen, dicatat untuk Den.~~ *[koreksi sesi kesembilan: sudah dibetulkan dalam revisi `CARD` yang difinalkan — `tokens.js:91` kini "5,09:1", nol "4,70" tersisa]* 9 hex A.1 kini `tokens.js:31-39`. Detail: `PROGRESS.md` 2026-09-20 (entri kedelapan) · DSR A.1 catatan revisi.
- **[20 Sep 2026 · sesi ketujuh, malam] Batch DS 2 — AdminKit → kit tunggal `src/kit/`: 22 file + `main.jsx` (persis 23 file) dikerjakan di branch `feat/kit-ds2-adminkit` — ⏳ MENUNGGU REVIEW Den (dicoba lewat Preview deployment branch + Supabase Staging); ⛔ nol migrasi/SQL; tampilan PRODUKSI belum berubah; kit lama TIDAK dihapus (Batch DS 7); TD-269/TD-270 TETAP OPEN (Batch DS 6); Batch DS 3+ belum mulai.** *[→ review DITUTUP sesi kesembilan, Batch DS 2 SELESAI — lihat entri teratas]* Cakupan (diverifikasi doc-keeper `git diff --name-only`): 11 `admin-settings/*` (termasuk `AdminSettingsHub` yang yatim — nol pengimpor, tetap dimigrasi karena masuk hitungan 22) + `AdminHub.jsx` + 10 `modules/admin/pages/*` + `main.jsx` `+1` `import './kit/kit.css'`; +774/−927 baris kode. **Gate A.4.4 diukur ulang doc-keeper:** build **2.646** modul (+10 = file kit masuk graph karena diimpor) · lint **159 (138/21)** = baseline −1, nol error baru per file (laporan sesi) · grep gate 22 file: **nol** `#1B4D8A`/`#E85A1E`/`#E8703D`/`#F6EFE3`/`#FFFDF8`/`#F2F5F9`/`#144682`, nol `variant="navy"`, nol kelas `ak-*` · `<KitStyles/>` dicabut 17× · 7 hex sage tetap hanya di `src/kit/` (halaman lewat token) *[→ sesi kedelapan: `CARD` kini `#FFFFFF`; klaim ini berlaku utk 6 hex khas]* · print kit/PDF/`.kpi` nol diff · `src/kit/`, `entities.js`, `admin-settings/kit.jsx`+`tokens.js` (kini **nol pemakai** di luar folder-nya), `modules/admin/components/*` (PASTEL, Batch DS 4b — chrome modal Branches masih putih/serif) nol diff. Sebaran hex lama seluruh `src/`: `#1B4D8A` 54 → **52** file · `#E85A1E` 67 → **66** (halaman AdminKit memakai nama token, bukan literal). **Peta token:** `NAVY` teks → `INK`, `NAVY` solid+putih → `ACCENT`+`INK` (#2), `ORANGE` chip → `ACCENT_2`, `CREAM` → `HEAD_BG`, `SURFACE` → `CARD`/`INPUT_BG`, `DANGER`/`GREEN` → `SEMANTIC`, warna jenis dokumen/kanal/aksi log/integrasi → `SEMANTIC`/`TONE`/`DEAL_STATUS` (kelas STATUS/DATA); hex yang sengaja tersisa = katalog DATA (`StatusCatalogPage.jsx:26-38` peta `color_class` seed DB; `GeneralPreferencesPage.jsx:65/:67` swatch `teal`/`violet`); `ENTITY_IDS` lokal 5 halaman → `src/lib/entities.js` (pemakai pertama di luar kit); alias `Tabs`/`PrimaryBtn`/`OutlineBtn`/`KitSelect`/`Skel` dipakai 13 file (dicabut Batch DS 7); `Card` padding 16 (dulu 24) diterima sebagai restyle. **`SaveButton` (arah TD-270):** 6 pemanggil TD-270 kini `SaveButton` kit + handler `return { error }` / `return { error: e }` / `return false` (11 titik di 7 file) + 4 alur simpan lain dipindah (Simpan Workflow · Simpan Rule · Simpan Rekening · Simpan penanda tangan · baris penomoran) = 11 `<SaveButton` di 7 file; alur simpan 5 halaman master + Integrations sengaja tetap `PrimaryBtn` (sudah `await`/tanpa DB). **Smoke staging (laporan sesi, akun ZZZTEST, Playwright — doc-keeper tak bisa verifikasi):** 5 simpan sukses, "Tersimpan!" **15–30 ms SETELAH** respons PostgREST (bukti menunggu server); offline → "Gagal menyimpan" + toast, idle 2,6 s; ⚠️ HRGA "Simpan Semua" **tidak teruji** (staging MSI nol `hrga_request_types`); sweep 20 destinasi AdminHub + 3 sub-tab nol error, 15 halaman 375 px nol overflow. Screenshot sebelum/sesudah 6 halaman × 2 lebar + 4 keadaan `SaveButton` di luar repo (#12). ⚠️ **Deviasi kecil dari rencana tertulis:** `PositionsPage.jsx` `ENTITIES` lokal (DSR menyebut "→ Batch DS 2") **tetap lokal, belum disentuh** (beda bentuk; `handleSave` byte-identik per TD-14) — perlu konfirmasi Den Batch DS mana. **#61 tetap berlaku untuk halaman di luar 22 file ini.** Nol TD baru, nol Keputusan Terbuka baru. Detail: `PROGRESS.md` 2026-09-20 (entri ketujuh) · DSR A.4.3 (baris Batch DS 2 ⏳).
- **[20 Sep 2026 · sesi keenam] Review Batch DS 1 DITUTUP Den — #62 DIJAWAB: empat fg kelas STATUS di `src/kit/tokens.js` DIGESER minimal ke AA + #61 DIKONFIRMASI FINAL — ⛔ satu file kode berubah (`tokens.js`, 4 nilai + komentar); nol migrasi/SQL; nol file lama; kit TETAP kode mati; Batch DS 2 BELUM mulai.** Hasil review (kode kit + screenshot diterima) = dua keputusan, nol permintaan lain. **#62 DIGESER (`tokens.js:100-101/:117-118`):** `SEMANTIC.ok` fg → `#2D794D` (4,53) · `SEMANTIC.warn` → `#8E620D` (4,58) · `TONE.slate` → `#676D7B` (4,54) · `TONE.orange` → `#BE4A19` (4,57); bg/bd tetap; hex lama sengaja tidak ditulis di komentar (grep gate) — rekam jejak di `09_ROADMAP.md` #62. Konsekuensi diterima: nilai kit ≠ `C.ok/C.warn` warm-beige lama & ≠ `crm/v3/tokens.js` TONE (3–18 % lebih gelap, bukan hue baru; file lama ikut kit pada gilirannya); **nol `#E85A1E` di `src/kit/`** (hex brand lama tersisa di kit hanya `#144682` sebagai STATUS). Kontras dihitung ulang doc-keeper (skrip sendiri meng-`import` `tokens.js`, 40 pasangan): **seluruh pasangan teks/status yang dipakai kit lolos AA — 0 gagal**; yang tetap FAIL = 4 batas non-teks yang sudah diprediksi A.4.1 (LINE/ACCENT vs latar 1,20–1,60) + pasangan yang memang dilarang header `tokens.js` (putih/INK_SOFT di aksen). **#61 FINAL:** kit ada ≠ boleh diimpor — `src/kit` hanya pada giliran Batch DS halaman itu; halaman baru ikut kit tetangganya; berhenti per halaman saat Batch DS-nya selesai, sepenuhnya setelah Batch DS 7 (rujukan `02 §5`/`06`/`00_INDEX`/DSR §16 diselaraskan, nol kata "tafsiran" tersisa). Gate diukur ulang: eslint kit 0/0 · lint **160 (139/21)** · build **2.636** = baseline; `tokens.js` 166 → 175 baris (13 file kini 2.384; koreksi kecil: `index.js` 32, bukan 31). Nol TD baru, nol Keputusan Terbuka baru — **nol keputusan design system tersisa terbuka**. Detail: `PROGRESS.md` 2026-09-20 (entri keenam) · DSR A.3/A.4.3.
- **[20 Sep 2026 · sesi kelima, malam] Batch DS 1 SELESAI — kit tunggal ber-palet sage LAHIR sebagai KODE MATI: `src/kit/` 12 file + `src/lib/entities.js` (13 file baru, 2.374 baris; nol file lama disentuh; nol halaman dimigrasi; nol migrasi/SQL; tampilan aplikasi BELUM berubah; Batch DS 2–7 belum mulai; menunggu review Den *[→ review DITUTUP sesi keenam, lihat entri di atas]*).** Gate A.4.4 diukur ulang doc-keeper: build **2.636** modul = baseline (kit belum diimpor siapa pun — satu-satunya `from '../kit'` di luar `src/kit/` = `CrmV3DemoPage.jsx:24` yang menunjuk `crm/v3/kit.jsx`), lint **160 (139/21)** = baseline, `eslint src/kit src/lib/entities.js` 0/0; nol hex di kit selain `tokens.js` (`kit.css` = cermin terdokumentasi); `#144682`/`#E85A1E` hanya sebagai kelas STATUS (`TONE`/`DEAL_STATUS.WON`, keputusan #3); 7 hex sage ada **hanya** di `src/kit/`; print kit/PDF/`.kpi` nol diff. **`tokens.js`** = 9 hex A.1 + turunan berumus `mix()` (`INK_FAINT #838E7A` · `LINE_SOFT` · `HEAD_BG` · `ROW_HOVER` · `INPUT_BG=BG` · `DISABLED_*` · `FOCUS_RING` · `BACKDROP` · `SHADOW_1/2/DROP` · `WHITE` hanya di atas STATUS solid) + tiga font + `SP`/`RADIUS`/`Z`/`MOTION`/**`SIZE`** + palet STATUS/DATA kelas terpisah (`SEMANTIC` · `TONE` v3 apa adanya · `DEAL_STATUS` 7 trio = `STATUS_META` persis, WON navy) + `STAGE_TONE`/`toneOf`/`closedFrom` + `fmtRp`/`fmtNum`; `STATUS_LABEL`/urutan sumbu sengaja **tidak** di kit (→ `crm/dealStatus.js` Batch DS 3, belum ada). **Komponen:** `SaveButton` **dibangun benar** (`await onSave()`; throw / `{ error }` truthy → GAGAL + `onError`; `false` → idle; terverifikasi headless di etalase; satu bug ref `alive` vs StrictMode ditemukan & ditutup saat uji) — **TD-270 TETAP OPEN** (AdminKit `kit.jsx:332-354` + 6 pemakai tak disentuh); `FormSheet`/`Notebook`/`StatusBar`/`ListView` = **salinan v3** berwarna sage (dilewati ACCENT+INK, aktif ACCENT_2+INK, WON navy solid + putih 9,42:1; `'✓'` → `<Check/>` Lucide di kode BARU saja — **TD-269 TETAP OPEN**); `DocNo` tinggal di `Layout.jsx` (tabel A.4.2 disusul); `Icon` registri 66 nama identik AdminKit; `kit.css` prefiks `nk-`, **belum diimpor `main.jsx`**; `KitGallery` tidak di-route; `entities.js` `{code, id UUID, name}` — temuan doc-keeper: versi lokal **keempat** `CRMReportPage.jsx:61` (array kode) belum disebut rencana. **Kontras (dihitung ulang doc-keeper, identik laporan sesi):** INK di ACCENT 7,13 · putih di ACCENT 1,51 (bukti #2) · `DEAL_STATUS` 7/7 lolos; ⚠️ **empat hex STATUS di bawah AA 4,5:1 untuk teks badge, dibawa apa adanya (keputusan #3): `SEMANTIC.ok` 4,30 · `warn` 3,99 · `TONE.slate` 4,23 · `TONE.orange` 3,23 → +Keputusan Terbuka #62** (usulan geser minimal ke 4,53–4,58 tercatat, belum diterapkan) *[→ DIJAWAB sesi keenam: DIGESER, sudah di `tokens.js` — lihat entri di atas]*. Screenshot etalase + "SEBELUM" 16 halaman × 2 lebar tersimpan **di luar repo** (keputusan #12; DB staging; doc-keeper tak bisa memverifikasi). ⚠️ Tes runtime = etalase saja; nol dampak ke aplikasi. ⚠️ **#61 tetap berlaku** — kit ada ≠ boleh diimpor halaman baru di luar giliran Batch DS *[→ FINAL, dikonfirmasi Den sesi keenam]*. Nol TD baru. Detail: `PROGRESS.md` 2026-09-20 (entri kelima) · `docs/DESIGN_SYSTEM_REFERENCE.md` A.3/A.4.3/§1.
- **[20 Sep 2026 · sesi ketiga] Konsolidasi dokumentasi — rencana penyatuan kit Batch DS 1–7 DISETUJUI Den (`docs/DESIGN_SYSTEM_REFERENCE.md` A.4) + `00_INDEX_README.md` jadi rumah TUNGGAL tabel 18 dokumen acuan — ⛔ DOKUMENTASI SAJA; NOL kode/migrasi/SQL; NOL Batch DS dimulai.** **A.4 (ditulis Den):** cakupan terukur (AdminKit **22** file · kit v3 3 halaman + `DealPanels` · token lokal **52** + `PASTEL` **11** = 63 file/±41.600 baris · +50 file hex · **194** kelas Tailwind palet/6 file → **±110 file**; angka 22/11/52/194 diverifikasi ulang doc-keeper ke kode) · struktur `src/kit/` 12 file + `src/lib/entities.js` + `crm/dealStatus.js` (**belum ada** — `ls` kosong) · tabel Batch DS 1–7 (4 dipecah 4a–4g; semua `⛔ belum mulai`; Batch DS 1 = kit lahir sebagai **kode mati** + screenshot "SEBELUM"; 3/4c/4e/4g/5 wajib staging) · protokol 6 gate (baseline build **2.636** modul · lint **160 = 139/21** = angka 18 Sep, nol kode berubah sejak itu, tidak diukur ulang) · risiko (TD-265 → smoke Storbit di staging hindari "Tandai Terkirim") · **14 keputusan Den** (teks di atas aksen `#33422B`, putih gagal AA · palet STATUS/DATA kelas terpisah, WON navy tetap sebagai warna status · TD-270 dibangun benar sejak Batch DS 1 · TD-70 + `index.css`/`tailwind.config.js` di Batch DS 4e, Oswald tetap · alias nama lama sampai Batch DS 7 · branch per Batch DS + satu commit + review Den · screenshot di luar repo). **Istilah "Batch DS", sengaja BUKAN "Wave"** (Wave = Grand Design roadmap terpisah, di luar repo) — jangan tertukar dengan "Batch B1–B6"/"3C" CRM v3; sejak 20 Sep 2026 (sesi keempat) namanya resmi "Batch DS" — rename dikerjakan Den di 7 file dokumen (git status hari itu 8 file = 7 + config agent; **39** kemunculan di A.4 DSR + **49** di luarnya, terukur doc-keeper; Batch CRM v3 B1–B6/3C dan Batch 1–3 TD-14 Agustus **sengaja tetap "Batch" polos**; nol kode/migrasi/SQL, nol TD/Keputusan Terbuka berubah, nol Batch DS dimulai), plus **PETA JAGA `.claude/agents/doc-keeper.md` +1 baris design system** (desain/kit UI/token/komponen bersama/rencana Batch DS → `DESIGN_SYSTEM_REFERENCE.md` Bagian A + status A.4.3, `02 §5`, `06`) — menutup gap cek silang (c) sesi ketiga; detail: `PROGRESS.md` 2026-09-20 (entri ketiga). **Rumah dokumen:** `00_INDEX_README.md` §Daftar Dokumen = satu-satunya tabel 18 acuan (3 kolom *isinya apa / buat siapa / kapan dibuka*; kolom "Update terakhir" **dicabut**, git = sumber tanggal; angka basi 73 tabel/13 role/TD-31 dikoreksi — 141/penanda basi/TD-270; path `docs/Governance/`) — `CLAUDE.md` §Dokumentasi Lengkap + `PROJECT_CONTEXT.md` §5 kini **hanya merujuk**; `PROJECT_CONTEXT.md` §1 repo → org MSI (private), §6 → 13 doc. Doc-keeper: **10 path `docs/0X_` tanpa `Governance/` di 6 file Governance dibetulkan**; penunjuk Batch DS di TD-14/70/269/270 (nol status berubah); `09` Next Up + `10` penanda disusul. **Env lokal (`.env.local`/`.env.staging`, gitignored, nilai key tidak dicatat) kini menunjuk STAGING `nexus-staging`** — prasyarat Batch DS berlabel staging (#13); kode membaca `VITE_SUPABASE_URL`+`VITE_SUPABASE_KEY` saja (`src/lib/supabase.js:6-7`); ⚠️ konsekuensi: dev lokal membaca DB staging yang tertinggal `signed_date`/overload (TD-265) = **environment, bukan bug kode**. Nol Keputusan Terbuka baru, nol TD baru. Detail: `PROGRESS.md` 2026-09-20 (entri kedua).
- **[20 Sep 2026] Design system — Keputusan Terbuka #60 & #61 DIJAWAB Den + TD-17 digabung ke TD-269 (bagian emoji diukur) — ⛔ DOKUMENTASI SAJA; NOL kode/migrasi/SQL; penyatuan kit TETAP BELUM DIKERJAKAN.** **#60 (final):** (a) dua halaman **web** Storbit ber-ungu/serif (`SalesOrderDetailPage.jsx:67`, `StorbitDashboardPage.jsx:64`, `@font-face` `salesOrderDetail.module.css`) **IKUT penyatuan ke sage + tiga font** — bukan pengecualian dokumen customer, pembukanya staff internal (BELUM DIKERJAKAN, ikut batch D4; PDF-nya tetap D2) · (b) **5 warna KPI hero CRM Dashboard TETAP, tidak diseragamkan ke sage** — warna status/data ≠ warna identitas aplikasi, harus tetap saling kontras (merah ≠ hijau) supaya sinyal tak kabur; final, bukan pengecualian yang lupa · (c) **Oswald `.kpi-value` DIPERTAHANKAN untuk saat ini** — sadar ditunda, **bukan pelanggaran** tiga font. Catatan turunan (bukan keputusan eksplisit Den): hijau-status lama `#1F8B4D`/`#0F766E` ikut prinsip (b). **#61 DISETUJUI:** halaman/fitur baru selama masa tunggu ikut kit/token yang ADA, bukan sage duluan — supaya tidak lahir palet **kelima** ("ke-4" di entri 19 Sep dihitung sebelum keluarga ungu/serif Storbit web diakui). **TD-17 → DIGABUNG ke TD-269** (nomor tunggal: hijau `#2F6B3F` 5 file/9× + emoji): emoji piktografik tinggal `App.jsx` (`✨` ×8, `🎉` ×1) + `AssetDetailITPage.jsx:1499/:1501`; `✓`/`✗` string UI ±10 file termasuk `crm/v3/StatusBar.jsx:140`. ⚠️ **Koreksi doc-keeper saat verifikasi ulang:** `QuotationFormPage.jsx:45-58` = **7×** (bukan 8); `SchemaManagerPage.jsx:209` `✨` + `App.jsx:4338/:4637/:5030` `✓`/`○` semula tak tercantum. ⚠️ **Temuan cek silang:** hex peach KPI hero di dokumen (`#EE9A7E`) **basi sejak `a709bd5` 31 Agu 2026** — kode `#EEAA8D` (`CRMDashboardPage.jsx:3548`), `#EE9A7E` nol hit di `src/`; dikoreksi di `02 §5`/`06 §1`/`09 #60`, **`DESIGN_SYSTEM_REFERENCE.md:63` belum** (flag Den) *[→ sudah dikoreksi Den di `f4a8124`, kini `:166` `#EEAA8D` — tertutup]*. Tidak ada Keputusan Terbuka tersisa dari audit design system. Detail: `PROGRESS.md` 2026-09-20.
- **[19 Sep 2026] Design system — audit read-only (`docs/DESIGN_SYSTEM_REFERENCE.md`, baru) + KEPUTUSAN Den: palet APLIKASI "sage" + tiga font resmi + arah SATU kit tunggal — ⛔ DOKUMENTASI SAJA; NOL kode/migrasi/SQL; implementasi SENGAJA DITUNDA menunggu instruksi Den.** Audit memotret `main` @ `660168f`: **tiga keluarga kit/token hidup bersamaan** — AdminKit (`admin-settings/{kit.jsx,tokens.js}`, 24 export, 22 pemakai, navy soft `#1B4D8A`) · kit CRM v3 (`crm/v3/`, 3 halaman produksi, navy `#144682`; `Chatter` v3 **nol pemakai**) · print kit (`logistics/{printKit.jsx,printTokens.js}`, 4 PDF, ungu/krem) — plus `hrga-tokens.js`/`userAccessTokens.js`, `index.css :root`, **52 file** token lokal (11 masih `PASTEL`). Terukur (diverifikasi ulang doc-keeper): `#144682` 23 file / `#1B4D8A` 54 / `#E85A1E` 67 / `#E8703D` 9; font legacy masih di-`@import` **dua tempat** (`index.css:1`, `App.jsx:2782`), kelas `.font-display/.font-numeric/.font-mono` 18/39/49×, `tailwind.config.js` masih menunjuk keempatnya; **dark green `#2F6B3F` 5 file / 9×** (token pusat `hrga-tokens.js` menular ke seluruh HRGA + 4 file 1×) → **TD-269**; **`SaveButton` AdminKit tidak menunggu Promise** (`kit.jsx:332-354`, 6 pemakai) → **TD-270**; `06_UI_UX_FLOW.md` basi 2 titik (sidebar putih sejak `368a10d` 1 Jul, shell `#F2F5F9`) — dikoreksi. **Keputusan:** palet sage (7 token, lihat bullet Brand) menggantikan seluruh navy/oranye **untuk aplikasi**; dokumen customer (print kit/PDF Storbit/Quotation PDF) **tetap brand entitas asli**; font resmi **tiga**; target **satu kit tunggal** (print kit dikecualikan) — **BELUM DIKERJAKAN, nol `tokens.js` sage, nol halaman dimigrasi**. **+#60** (nasib KPI hero/Oswald/hijau-status di bawah sage) · **+#61** (halaman baru selama masa tunggu → ikut kit yang ada, asumsi kerja) *[→ DISETUJUI 20 Sep 2026, lihat entri di atas]*. ⚠️ **Temuan doc-keeper saat cek silang:** pengecualian *heading Space Grotesk Detail SP* (TD-70/#60 b) **sudah tidak ada di kode sejak 17 Agu 2026** (`8c5b4f2`); yang ada = **keluarga ungu Storbit `#5b3fa0` + Cormorant Garamond/Lora** (`salesOrderDetail.module.css` `@font-face`, TTF lokal) di **dua halaman WEB** — `SalesOrderDetailPage.jsx` & `StorbitDashboardPage.jsx` (18 Agu) — sengaja sewarna print kit; statusnya di bawah sage/tiga font **belum diputuskan** (masuk #60, dikoreksi di TD-70 & `DESIGN_SYSTEM_REFERENCE` §4.1/§17) *[→ dijawab 20 Sep 2026: IKUT sage + tiga font, BELUM DIKERJAKAN — lihat entri di atas]*. TD-17 (Jun) tumpang tindih TD-269 — rujukan silang, penggabungan = keputusan Den *[→ DIGABUNG ke TD-269, 20 Sep 2026]*. Detail: `PROGRESS.md` 2026-09-19 · `docs/DESIGN_SYSTEM_REFERENCE.md` · `09_ROADMAP.md` §Design System.
- **[18 Sep 2026] CRM v3 LIVE PRODUKSI (kode branch `feature/crm-v3-batch-persiapan`; deploy produksi 00:33 WIB terverifikasi Den) + 3 migrasi hari-merge LIVE staging & produksi + INSIDEN 121 inquiry ber-`owner_id` NULL ditutup + `QuotationDetailPage` lepas dari daftar role lokal + refresh snapshot.** **13 file konflik** (bukan 12 — +`05_WORKFLOW_MAP.md`, sepele) **diresolusi manual**: 6 dokumen dilebur kedua sisi, snapshot ambil `main` lalu di-refresh (`9f6e232`), `App.jsx` ambil `main`. **8 titik role-hardcode di 4 file CRM → helper `src/lib/roles.js`** (`DealDetailPage` 4 — termasuk `canCreatePRF → canCreatePrf(erpRoles)` `:748` yang **lolos auto-merge senyap**; `canReassignOwner`/`canSelect` → `isManagerOrAbove(erpRoles)` **global**, cermin RLS `inquiries_update` · `CRMDashboardPage` 2 — destructure `useAuth()` digabung `{ profile, erpRole, erpRoles, activeCompanyId }`, `isAllEntities` **tanpa `companyId`** = keputusan Den: `super_admin` selalu semua entitas · `InquiryListPage` 2 — `isSalesOnly` + filter `created_by` **sengaja tak diambil** (TD-223) · `PipelineKanbanPage` 2 — signature branch + 4 import mati dibuang) + **`QuotationDetailPage` (C.6, `3a64a2a`) → `isManagerOrAboveRole(erpRoles, { companyId: quot?.company_id })` — satu-satunya yang ber-scope company**, cermin `quotations_update`. `20260830000001` **SUPERSEDED, tidak dijalankan** (header dikoreksi). Sisa sengaja: `QuotationFormPage:46` `'sales_spv'` (#54) · `LeadPoolPage:151` `['manager','supervisor']` (#55). **Migrasi DIJALANKAN (staging → produksi, satu sesi):** `20260830000003` (RLS `inquiries_*` → `owner_id`) **+ `20260917000002`** (TD-249: `p_scope_own` 2 RPC → `owner_id`, badan disalin dari LIVE; ⛔ **jangan jalankan ulang `20260909000006`** — mengembalikan `created_by`) **+ `20260916000001`** (`mark_inquiry_won` **berhenti** menulis `accounts.pipeline_stage='WON'`; `trg_set_customer_on_won` + `set_customer_on_won()` **DICABUT**; syarat "dashboard baca `inquiries.status`" diverifikasi dulu — `CRMDashboardPage` nol `pipeline_stage` di luar komentar) → **TD-249 RESOLVED · TD-225 ditutup · TD-222 RESOLVED (trigger dicabut total) · `pipeline_stage` legacy beku = prasyarat Batch 3C (belum dikerjakan)**. EF `aging-pipeline` v8 **terkonfirmasi deploy 18 Sep** (dry-run 95→90 akun). **INSIDEN:** `InquiryFormPage` pra-merge tak mengisi `owner_id` (satu-satunya jalur INSERT; nol trigger/DEFAULT) → **121 inquiry NULL bocor tiap hari 7-17 Sep** sampai deploy 18 Sep; **backfill manual `owner_id = created_by` 121 baris** (24 WON/LOST → `trg_z_lock_inquiry_owner` dinonaktifkan sementara dalam transaksi yang sama), **nol data hilang**; pertahanan lapis DB = **Keputusan Terbuka #58**. **TD-19 RESOLVED** (`20260914000001` jalan staging + produksi; snapshot tanpa `customers`, +`customers_backup_20260614`). Build 2636 modules · lint **160 (139/21)** = baseline −8, nol file memburuk. ⚠️ **Snapshot `9f6e232` memuat `goods_receipts`/`create_goods_receipt`/`trg_stock_ledger_recompute_sp` (`20260914000002`) — indikasi kuat sudah jalan di PRODUKSI, tapi TANPA laporan eksekusi → PERLU KONFIRMASI DEN** (jumlah tabel `public` kini **141**). ⚠️ **Tes staging akun `bd_*` belum ada laporan.** ⚠️ 4 header migrasi kini basi (`BELUM DIJALANKAN`/`DITAHAN` padahal LIVE: `20260830000003`, `20260914000001`, `20260916000001`, `20260917000002`) — kelas TD-256, koreksi per file. Detail: `PROGRESS.md` 2026-09-18.
- **[17 Sep 2026] Tanggal SJ ditandatangani (`delivery_notes.signed_date`) WAJIB sebelum "Tandai Terkirim" + `BTB_TERBIT` masuk whitelist Generate Picking List.** **(A)** RPC baru **`mark_delivery_delivered(uuid, date)`** dijalankan **MANUAL di produksi 17 Sep**, direkam retroaktif `20260917000001_delivery_signed_date.sql` (guard `roles.level <= 6`/`operations` · `signed_date` wajib · tidak masa depan · tidak sebelum `dispatched_at` WIB · `REVOKE PUBLIC`); FE `DeliveryNoteDetailPage` date-picker WIB inline + tombol disabled sampai terisi + validasi 3 lapis; `db.js` `setDeliveryStatus(id, status, signedDate)` selalu mengirim kedua argumen. **INSIDEN KEAMANAN (ditutup hari yang sama):** versi awal RPC 2-argumen **tanpa guard role + PUBLIC EXECUTE** — hardening `20260826000002`/`20260911000004` **tak terbawa** karena beda jumlah argumen = **fungsi TERPISAH bagi Postgres, bukan pengganti** (gotcha **#37**). **Tiga hal baru tercatat sebagai TD:** overload lama `mark_delivery_delivered(uuid)` **masih hidup, tanpa `REVOKE PUBLIC`, tanpa syarat `signed_date`** — jalur pintas + ambigu untuk panggilan 1-argumen (**TD-263**, DROP = #59) · cek masa depan pakai `current_date` **UTC** (00:00-06:59 WIB ditolak bila RPC dipanggil langsung; FE aman) (**TD-264**) · ⛔ **STAGING BELUM punya kolom & overload → FE `main` di staging gagal keras di "Tandai Terkirim" — pekerjaan tertunda PRIORITAS (TD-265)**. **(B)** `SalesOrderDetailPage.jsx:1535` whitelist status `canGeneratePicking` ketinggalan `BTB_TERBIT` → SP dengan BTB pertama tapi sisa kirim (kasus **SP 2290815**) kehilangan tombol **permanen** (`BTB_TERBIT` tak pernah turun lagi); backend `generate_picking_from_sp` **tak mengecek `sp_orders.status`** → gap FE murni. `INVOICED`/`SUBMITTED` **sengaja belum** (menunggu Finance, **#57**); ganti whitelist → cek langsung ala backend + headline Detail SP tetap "BTB Terbit" saat picking susulan (**TD-262**). **(C) [SQL manual, nol jejak git] Akses CEO ke entitas SOA/Storbit:** akun CEO (Pak Adam) sebelumnya hanya terdaftar di MSI (`ceo` aktif, `super_admin` nonaktif) → dashboard Storbit kosong (RLS company-scoped). Ditambah baris `user_roles` (`ceo` @ SOA, aktif, `valid_from` 17 Sep 2026) — pola `user_roles` jamak yang sudah dipakai 3 orang lain (Elvira MSI→SOA; Camelia & Dery SOA→3 entitas), **bukan kasus baru**; ⚠️ prosedurnya **belum pernah ditulis sebagai SOP** — gap dokumentasi, dicatat `04_ROLE_PERMISSION_MATRIX.md` §5 #12. **(D) [SQL manual, nol jejak git] Dua gap fitur Storbit/Inventory dikonfirmasi, SENGAJA belum dibereskan proper:** picking list = snapshot statis (`qty_picked`/`qty_short`/status dibekukan saat dibuat, nol RPC penyesuai; badge "Kurang" pun tak ikut update) → satu kasus dikoreksi SQL: `PICK/SOA/WH/2026/0146` (SP 2280683, Shelf Strip Flat 90 CM, `qty_picked` 500→550) — **TD-267**; belum ada fitur transfer/edit stok gudang → 1.165 unit Shelf Strip Flat 90 CM dipindah SQL dari Gudang Lainnya ke Gudang Semper (koreksi input penerimaan salah gudang) — **TD-268**. Detail: `PROGRESS.md` 2026-09-17.
- **[16 Sep 2026] Persiapan Batch B3 — sumbu "menang" lepas dari `accounts.pipeline_stage` (nol SQL dijalankan hari itu; migrasinya LIVE 18 Sep).** EF `aging-pipeline` v8: prefilter **`lifecycle_stage IN ('lead','mql','sql','prospect')`** sebagai **tambahan** (filter `pipeline_stage` tetap) — customer selama ini lolos aging **kebetulan** karena `WON` tak ada di `AGING_RULES`; deploy terkonfirmasi 18 Sep. `CustomerDetailPage`: Health Score / chevron deal / badge kartu Pipeline Stage memakai **`lifecycle_stage === 'customer'`** (badge "WON" → "CUSTOMER"), bukan `pipeline_stage==='WON'`. Migrasi **`20260916000001_mark_inquiry_won_stop_pipeline_stage`** ditulis (DITAHAN sampai CRM v3 LIVE + dashboard `main` terverifikasi baca `inquiries.status`) — isi: cabut blok `UPDATE accounts SET pipeline_stage='WON'` + DROP `trg_set_customer_on_won`/`set_customer_on_won()` (pelaksanaan F3-8/3C lama). `20260908000002_accounts_lifecycle_drop_legacy` §2b disesuaikan jadi DROP IF EXISTS (idempoten kedua urutan). Detail: `PROGRESS.md` 2026-09-16.
- **[15 Sep 2026] Vendor List — `super_admin` melihat vendor SEMUA entitas + kolom "Entitas"** (FE-only `VendorListPage.jsx`; dicatat susulan 18 Sep). Daftar dulu selalu `.eq('company_id', home)` padahal RLS `vendors_select` sudah bypass `is_super_admin()` — pembatasnya FE; kini `isAllEntitiesRole(erpRoles)` (pola 14 halaman lain) + embed `companies!vendors_company_id_fkey(code, name)`. ⚠️ **Keterbatasan diterima (Den 15 Sep): tambah vendor tetap distempel `company_id` HOME** — `super_admin` belum bisa membuat vendor untuk entitas lain dari halaman ini (**TD-266**, LOW). Tes manual Den: super_admin 7 vendor semua entitas; Camelia (procurement) tetap 4 vendor SOA. Detail: `PROGRESS.md` 2026-09-15 (sub-bagian Vendor List).
- **[15 Sep 2026] Penerimaan Barang (Inventory) — jalur simpan pindah dari INSERT langsung `stock_ledger` ke RPC `create_goods_receipt` + validasi baris ketat; migrasi `20260914000002` direkam bersama FE (`882f629`).** ⛔ **`Status: LIVE DI STAGING (14 Sep 2026) — BELUM DIJALANKAN DI PRODUCTION`** — di produksi tombol Konfirmasi **gagal keras** (`function create_goods_receipt does not exist`) sampai migrasi dijalankan di sana; jalankan **SEBELUM atau segera sesudah** deploy (gotcha #36). Snapshot 0 hit `goods_receipt` = benar untuk produksi, bukan basi — bedakan *di repo* / *di snapshot* / *LIVE per lingkungan*. **Migrasi (389 baris, dibaca doc-keeper):** tabel `goods_receipts` (`reference_no varchar(50) NOT NULL`, tak UNIQUE · `receipt_type` CHECK 4 nilai · `vendor_id` nullable · status `posted`/`void` · `created_by NOT NULL`) + `goods_receipt_items` (`qty int > 0`, `unit_cost numeric(15,2)` nullable, CASCADE) · RLS pola `sp_btb` (SELECT+INSERT `authenticated`; insert super_admin/manager+/`operations`; DELETE super_admin; **nol UPDATE policy, sengaja**) · `REVOKE TRUNCATE/REFERENCES/TRIGGER` eksplisit (default privileges DB menempelkannya ke tiap tabel baru, `schema_snapshot.sql:22028/:22038` — **temuan:** 99 tabel `GRANT ALL` + 29 `TRUNCATE` eksplisit ke `authenticated`, laten, audit = keputusan terpisah) · RPC `create_goods_receipt` (`SECURITY DEFINER`; company dari `warehouses.company_id`; **vendor NULL ditolak utk `purchase_order`/`restock_produksi` → TD-259 RESOLVED**; `movement_type` `transfer_in`/`adjustment`/`inbound`; ledger `reference_type 'goods_receipt'` + `reference_id` + `created_by`) · RPC `void_goods_receipt` (manager+, baris pembalik `-abs(qty)`, `'goods_receipt_void'`; **nol UI → TD-260**) · **trigger BARU `trg_stock_ledger_recompute_sp` AFTER INSERT ON `stock_ledger`** (status SP Storbit menyesuaikan otomatis begitu stok masuk dari sumber mana pun — pertama kali `stock_ledger` punya trigger; staging saja). ⚠️ 4 policy + 2 guard RPC `get_user_company_id()` **singular** — instance baru TD-180 (dicatat di sana). Ekor file: `REVOKE INSERT ON stock_ledger FROM authenticated` sengaja komentar = langkah terpisah berikutnya (prasyarat FE+grep sudah terpenuhi; SELECT `InventoryDashboardPage.jsx:497` wajib tetap terbuka). **FE:** `RECEIPT_TYPES` `{value,label}` · vendor wajib hanya 2 tipe (Den 14 Sep; FE = cermin RPC) · `validateRow` `empty`/`complete`/`partial` (partial = submit DITOLAK) · **tanpa kolom harga beli** (Den 15 Sep: `unit_cost` diisi Accounting lewat Master Product — `products.unit_cost` hari ini seluruhnya NULL per komentar dashboard) · `maxLength 50` · `company_id` tak dikirim. Jalur lama (diverifikasi): vendor/PO/tanggal tak pernah tersimpan, 3 tipe runtuh jadi `'ADJ'`, `reference_id`/`created_by` NULL (83 baris lama → **Keputusan Terbuka #56**). ✅ **6 checklist tes manual LOLOS di staging 15 Sep** (laporan Den); produksi belum. Build clean 2626 modules, lint 168 (146/22) = baseline −1. **TD-261 RESOLVED** (jejak; sisa eksekusi produksi + snapshot dicatat di barisnya). Detail: `PROGRESS.md` 2026-09-15.
- **[14 Sep 2026] Pra-merge CRM v3: audit role sales di branch + koreksi `20260830000003` + checklist hari merge ditulis ulang.** Nol kode di `main`; satu commit di branch (`1cb2edd`, file migrasi saja, BELUM dijalankan). ⚠️ **Konteks yang belum tercatat di dokumen mana pun:** sesi 11 (sore)–13 Sep (`src/lib/roles.js`, satu rezim gate menu, `roles.level` `20260911000004`…`000006`, pilot role HCGA/Procurement/BD `20260912000001`…`000006` = **11 role baru**, 9 sales → `bd_*`, role `sales` nol pemegang) — entri ini merujuknya lewat commit/migrasi, bukan mencatatnya. Branch CRM v3 (merge-base `9391e6e`, 8 Sep) **tidak punya `roles.js`** → 5 file CRM bentrok + `App.jsx` (logo saja) + snapshot + 5 dokumen (simulasi `git merge-tree`, terverifikasi). Yang mengikat: **`DealDetailPage.jsx:752` (`['sales',…]`) LOLOS auto-merge tanpa konflik** → tombol Buat PRF hilang untuk semua sales; **3 file white-screen** kalau blok konflik diselesaikan "ambil main" mentah (`DealDetailPage` `MANAGER_OR_ABOVE` · `CRMDashboardPage` destructure `useAuth()` — gabungkan `erpRoles`+`activeCompanyId` · `InquiryListPage` `canScopeToggle`); 3 titik yang `main` pun belum bereskan (`QuotationDetailPage:17/:379` branch-only; `QuotationFormPage:45` role hantu `sales_spv` → **#54**; `LeadPoolPage:151` roster `['manager','supervisor']` → **#55** — dua terakhir ada di `main` hari ini). **`20260830000001` SUPERSEDED, jangan dijalankan** (role `sales` kosong; 6 key CRM sudah diseed `bd_*` oleh `20260912000005`). **`20260830000003` sempat basi 15 hari** (klausa `has_role('procurement')` akan menimpa `is_procurement_functional()` LIVE 12 Sep — Camelia/Dery kehilangan inquiry ber-PRF, senyap) → dikoreksi `1cb2edd`; **jalankan serempak `p_scope_own` 2 RPC (TD-249, branch)** → gotcha **#35**. **TD-225 & bullet GERBANG RILIS hidup di BRANCH dan BASI** (`owner_id` LIVE 6 Sep, 4 CHECK BANT ada — terverifikasi snapshot) — rujukan "§4.6.1 handoff" tidak ada; dilebur saat sinkron. Lampiran per-file + checklist: `09_ROADMAP.md` §Pekerjaan Sinkron Branch. **TD-209 → PARTIAL** (3 titik ditutup 11 & 13 Sep; `StrategicHandoverModal` tetap yatim). Detail: `PROGRESS.md` 2026-09-14.
- **[14 Sep 2026] `customers` → DROP disiapkan (TD-19: PERSIAPAN SELESAI, EKSEKUSI PENDING) + 2 temuan baru.** Migrasi datanya ternyata **sudah selesai 14 Jun 2026** (`e31fa86`, Phase 2.5A, SQL manual tanpa file migrasi; INDOMARCO disalin ke `accounts` **dengan id sama** `a18fad3c-…` — terverifikasi snapshot berdata `0c736fb`). Hari ini: **nol** `REFERENCES public.customers`, **nol** fungsi/trigger/view pembaca, **nol** `.from('customers')` di kedua branch, 9 FK `*_customer_id_fkey` → `accounts`; sisa rujukan `useCustomFields('customers')` (`App.jsx`) → 0 baris tanpa error sesudah DROP. Dikerjakan: `SchemaManagerPage` cabut `'customers'` (`58643a3`) + file **`20260914000001_drop_customers_table.sql`** (`cdb8c91`, guard backup/baris/FK, tanpa CASCADE, **BELUM dijalankan**) + backup `customers_backup_20260614` di **staging** (0 baris). ⚠️ **Urutan sisa:** konfirmasi deploy `58643a3` LIVE (belum) → backup di PRODUKSI (2 baris) → staging + LAPIS 1/2 → produksi → refresh snapshot → baru tutup TD-19 + koreksi `AGENTS.md:731`; drop backup ke-9 itu belakangan bareng backup lain (gotcha #10). **Temuan:** `accounts` **tanpa relasi induk-anak** (nol `parent_*`; anak perusahaan = akun lepas) → **#53**; `inquiries.contact_id` **nol penulis di `main`, nol pembaca di kedua sisi** (branch punya jalur tulis sejak 3 Sep `106cc43`, tapi Detail Deal/PDF tak menampilkannya) → **TD-258**. Detail: `PROGRESS.md` 2026-09-14.
- **[11 Sep 2026] Fix `saveUserAccess` — pencabutan role di-scope ke company yang diedit (dikerjakan di branch `fix/save-user-access-scope-company`).** Satu file `src/hooks/useUserAccess.js`, murni query, UI form tak disentuh. **Bug:** Step 2 mencabut SEMUA role aktif user tanpa filter `company_id` dan menyerahkan scope ke RLS — benar untuk editor `admin`, **salah untuk `super_admin`** (`user_roles_update` dimulai `is_super_admin() OR …`), jadi satu Save di User Edit pada user multi-entitas mencabut seluruh role-nya di semua entitas (kasus: Elvira, 4 role MSI & SOA → 3 tercabut). **Fix:** `.eq('company_id', draft.company_id)` + `.neq('role_id', …)` + cek triple `user_roles_unique` (sudah aktif = no-op) + `companyId` kosong DITOLAK + reaktivasi membersihkan `revoked_*`. Semantik single-role-per-company **dipertahankan** (keputusan Den). ⚠️ **NOL tes runtime — menunggu tes manual Den di preview** (user 2+ company: ubah role di satu company → role company lain tetap; user role tunggal: ganti role tetap jalan). **+TD-257** (MEDIUM, rencana terpisah "beres-beres role utama ganda") — `pickPrimaryErpRole` (AuthContext, gate) vs `getPrimaryErpRole` (User Access/User Edit, baris aktif pertama company mana pun) → multi-role tak terlihat di halaman; select bisa tampak "salah" saat tes tapi yang ditulis selalu ter-scope, **bukan bug tulis**. Pelajaran umum → `03_DATA_MODEL.md` gotcha **#34** (*RLS bukan pengganti filter di query untuk editor super_admin*); catatan silang di TD-170 (status tetap); Known Gaps #11. Detail: `PROGRESS.md` 2026-09-11 (blok `saveUserAccess`).
- **[11 Sep 2026 · TINDAK LANJUT, dicatat doc-keeper] Invoice: kop TERVERIFIKASI, dua migrasi akses Finance LIVE, `legal_name` SOA dikoreksi, +Keputusan Terbuka #49-#52.** ✅ **`KOP_HEADER_CM = 3` / `KOP_FOOTER_CM = 3,5` TERVERIFIKASI cetak percobaan di kertas kop sungguhan 10 Sep 2026** — tanda "SEMENTARA" di entri 10 Sep & komentar `InvoicePDF.jsx` sudah dikoreksi (angka lama 4/4,5 dari penggaris 9 Sep terbukti kejauhan). Paginasi **`n≤6` kedua varian**, seragam 4 panjang alamat; ⚠️ **yang boleh dijanjikan tetap `n≤5`** (slack `n=6` cetak cuma **6,45 pt**; `n≤5` = **509 dari 522 SP**). ✅ **`20260910000001_finance_read_access` + `20260911000001_entity_bank_accounts_read` LIVE produksi**, dieksekusi manual (10-11 Sep) & **terverifikasi akun Elvira 11 Sep**: nama customer terisi · panel Invoice benar (bukan lagi "Belum Diterbitkan") · blok Payment terisi · kolom UOM terisi (embed bersarang `products(unit, uom)` berjalan). → **TD-217 & TD-218 RESOLVED** · TD-253 gejala tertutup/pola OPEN · TD-254 1/11 ditutup · **TD-180 tetap PARTIAL** · **+TD-256** (LOW, dicatat sesudahnya: **17** file migrasi ber-header `BELUM DIJALANKAN` padahal dokumen bilang LIVE — sengaja TIDAK dikoreksi massal, verifikasi per-file ke produksi dulu; koreksi massal = klaim baru yang belum tentu benar). ~~⚠️ **Refresh `schema_snapshot.sql` MASIH TERUTANG** untuk kelima policy.~~ **[KOREKSI 14 Sep 2026: sudah dilunasi `3334e06` (11 Sep) — kelima policy ada di snapshot, diverifikasi grep; yang basi di snapshot kini = seluruh migrasi 12 Sep.]** ✅ **DATA: `companies.legal_name` SOA "PT Storbit Indonesia" → "PT Stuja Orbit Abadi"** (tanpa titik; UPDATE ber-guard Den 11 Sep) — 5 permukaan cetak ikut benar tanpa deploy; **`legal_name` = nama legal dokumen cetak, `name` = nama internal** (CompanySwitcher); kalau akta ternyata bertitik, ubah `legal_name` SAJA. ⚠️ Belum direkam sebagai migrasi retroaktif (snapshot schema-only tak memuat data). ⚠️ **Label VAT 12% vs tarif 0,11 SENGAJA berbeda** (`PPN_LABEL_PCT` ≠ `PPN_RATE`; 12% × DPP Nilai Lain 11/12 = 11% efektif, keduanya benar) — gotcha #32 + Don't §6, **jangan "diperbaiki" ke satu arah**. **Blok tanda tangan** (`5106eac`): rata tengah + placeholder "Stamp / Signature" samar + nama PT ungu, tinggi tetap. ⚠️ **Aturan proses baru (`02_RULES_GOVERNANCE.md` §7):** perintah perbaikan yang menyebut PENYEBAB diperiksa dulu ke kode — dua kali 11 Sep diagnosis dari gambar meleset (placeholder yang tak pernah ada · "Account Dept bergeser" padahal rata kiri). **+Keputusan Terbuka #49-#52** (`09_ROADMAP.md`): Controller vs Jr. Manager utk terbit invoice/catat pembayaran — Elvira **66/71 invoice, 65/66 jurnal**, satu-satunya `finance_controller` · bagan satu-entitas vs role per-entitas · nol role `accounting` · bagan tanpa posisi gudang + `supervisor`/TD-106; ⚠️ **#48 sengaja dilewati** (dipakai branch CRM v3). Gotcha **#33**: `finance_controller` superset `finance` di **domain Finance** (5 RPC + 12 policy menolak `finance` polos, nol sebaliknya) — ⚠️ **BUKAN superset global**: 2 policy HRGA + 2 menu HRGA meloloskan `finance` tanpa `finance_controller` (temuan doc-keeper, di luar audit). ⚠️ Gotcha #27-#31 milik branch CRM v3 — lompatan 26→32 disengaja. Detail: `PROGRESS.md` 2026-09-11 (tindak lanjut kedua).
- **[11 Sep 2026] Invoice — enam penataan: empat dipasang, dua diselidiki dulu.** FE-only, nol migrasi baru *[→ tindak lanjut: `20260911000001` lahir & LIVE, lihat entri di atas]*. ⚠️ **Baseline paginasi yang benar: cetak `n≤6` & download `n≤6`** (angka "n≤4" di brief sudah basi sejak 10 Sep). **Dipasang:** (1) baris literal "Storbit" di Billed By dicabut, nama PT naik ke `billName` (style sama dengan nama customer) — **membebaskan slack `n=6` 7,47 → 22,95 pt cetak / 6,84 → 22,27 pt download**, itulah yang menampung tanda tangan; (3) **SKU → UOM**, urutan DESC · QTY · UNIT PRICE · UOM · SUBTOTAL — ⚠️ **satuan BUKAN data baris invoice**: `sp_order_items`/`sp_items`/`sp_invoice_lines` tak punya kolom unit/uom; sumbernya `products.unit`/`uom` via embed bersarang, jadi **satuan di invoice = satuan produk SAAT INI, bukan saat terbit** (beda dari `product_name`/`unit_price` yang di-snapshot; membekukannya = kolom baru + migrasi + `create_sp_order_dual`, belum diputuskan); (4) label **"VAT (12%)"** lewat konstanta baru **`PPN_LABEL_PCT = 12`** di `taxConstants.js` tepat di bawah `PPN_RATE = 0.11` — **nominal nol perubahan** (`total_ppn` dari `create_invoice`, `InvoicePDF` tak menghitung PPN); 12% statutori × DPP Nilai Lain 11/12 = 11% efektif, keduanya benar, **jangan disamakan ke salah satu arah**; (6) **blok tanda tangan kanan bawah** sejajar Terms & Instructions, ruang materai **80 pt = 2,82 cm** (`SIGN_SPACE_PT`; cukup materai melintang 2,2 cm, tidak untuk tegak 3,2 cm) — batas terbesar yang tidak menambah tinggi halaman, dihitung dari kolom kiri 100,55/116,55 pt; nama PT-nya `legal_name` (sumber sama dgn Billed By). **Paginasi TIDAK turun: cetak `n≤6` tetap (slack `n=6` → 6,45 pt), download `n≤6` tetap (→ 20,80 pt)**; ⚠️ yang boleh dijanjikan tetap `n≤5`. **Diselidiki, BELUM diperbaiki** *[→ keduanya SUDAH ditutup 11 Sep — `legal_name` dikoreksi, `20260911000001` LIVE; lihat entri tindak lanjut di atas]***:** (2) **nama PT salah = DATA** `companies.legal_name` SOA ("PT Storbit Indonesia" → seharusnya "PT. Stuja Orbit Abadi"), dibaca **5 permukaan** (Invoice 2 varian · Picking List · Surat Jalan · preview Surat Pesanan — yang terakhir ber-*fallback* literal nama yang benar — · blok tanda tangan); nama benar sudah ada di `entity_bank_accounts.account_holder` dan 5 literal kode (**tanpa titik**) → ⚠️ **putuskan satu ejaan "PT." vs "PT"**; perbaikan data via `EntitySettingsPage` atau UPDATE (query di `PROGRESS.md`). (5) **rekening "belum diatur" = POLICY, bukan data**: `entity_bank_accounts_access` `FOR ALL` ber-`(company_id = get_user_company_id() AND is_admin_or_above()) OR is_super_admin()` — **membaca pun harus admin/super di home company; Finance tak pernah lolos**, bahkan di entitasnya sendiri; rekening memang keyed `sp_orders.company_id` (entitas SP, bukan login); `20260910000001` tak menyentuhnya; pesan "belum diatur" **menyesatkan** (kelas TD-253). Usulan: policy `FOR SELECT` aditif terpisah, tulis tetap admin-only. ⚠️ **NOL tes runtime browser**; embed bersarang `products(unit, uom)` belum pernah dieksekusi PostgREST — gejala gagalnya kolom UOM `—` di semua baris, bukan error. Detail: `PROGRESS.md` 2026-09-11.
- **[10 Sep 2026] Invoice cetak — delapan penataan dari cetak percobaan di kertas kop sungguhan.** FE-only, **nol migrasi**; satu perubahan `db.js` yang MENCABUT query (`dc_master` di `getInvoicePdfData`), bukan menambah. ⭐ **Batas halaman varian cetak NAIK `n≤4` → `n≤6`**, seragam empat panjang alamat — rinciannya supaya tak salah dikreditkan: blok totals **+22,41 pt** (`n≤4`→`n≤5`) dan batas bawah kop **+28,35 pt** (`n≤5`→`n≤6`); **batas atas + blok meta menyumbang 0 pt** (memang rancangannya), alamat customer 0 pt. ⚠️ **Yang boleh dijanjikan tetap `n≤5`** — slack di `n=6` cuma 7,47 pt vs 30,47 pt di `n=5`; `n≤5` sudah mencakup **509 dari 522 SP**. **Yang berubah:** (1) **Billed To pakai `accounts.address`, NOL informasi DC** — nama maupun alamat, di KEDUA varian; ⚠️ **alamat kosong dicetak `—`, NOL cadangan ke `dc_master.alamat`** (keputusan sadar: cadangan membuat invoice tampak konsisten padahal isinya campur dua sumber). Perlakuan kosong **sengaja DIBALIK** dari 9 Sep — alamat DC boleh tak ada, alamat customer seharusnya ada dan kosongnya = master data belum diisi. **Cuma 1 dari 4 customer Storbit yang `address`-nya terisi** (Indomarco). ⚠️ **TD-217 bertambah gejala:** untuk akun Finance invoice kini menampilkan nama customer kosong **DAN** alamat `—`. (2) nilai meta **rata kanan** ke tepi 566 (segaris kolom SUBTOTAL + Grand Total), ⚠️ **titik dua tetap `x=392`** diverifikasi dari koordinat PDF; fork print-only **wajib** — tanpa itu bocor ke download (terukur: nilai ke `x=478`, titik dua tinggal di `x=114`), kelas kebocoran yang sudah diperingatkan untuk `hr`. (3) **blok totals dikecilkan** (cetak saja) — penekanan Grand Total dijaga oleh **rasio** 13/9 = 1,44 (dulu 1,46), bukan kehati-hatian; **sengaja tidak diterapkan ke download** (varian setia desain sumber, tak tertekan paginasi — kalau dibalik: download `n≤6`→`n≤7`). (4) **`KOP_HEADER_CM` 4→3 dan `KOP_FOOTER_CM` 4,5→3,5.** ⚠️⚠️ **KEDUANYA SEMENTARA dan status epistemiknya LEBIH LEMAH dari angka lama:** 4/4,5 dari **pengukuran penggaris langsung**, 3/3,5 dari **pembacaan foto** cetak percobaan. Angka lama tetap tertulis di komentar supaya terlacak. **[→ Koreksi 11 Sep 2026: keduanya TERVERIFIKASI cetak percobaan di kertas kop sungguhan 10 Sep — bukan lagi SEMENTARA.]** (5) **blok meta naik 1 cm** — ⚠️ bukan digeser sendiri: batas atas dikecilkan lalu ruangnya **diserap kembali** `marginBottom` (`3 + META_LIFT_PT`), jadi **Billed By/To tetap di koordinat semula** (diverifikasi `BILLED BY` y=172,63 · `DESCRIPTION` y=283,58). **Kenaikan meta = persis pengurangan `KOP_HEADER_CM`** — batas 3,5 cm otomatis bikin meta cuma naik 0,5 cm; itu definisi, bukan bug. **JANGAN pakai `marginTop` negatif** (menaruh meta di luar padding = di luar batas kop). (6) **Terms & Payment turun 1 cm, dan itu HANYA bisa lewat `KOP_FOOTER_CM`** — `footBlock` ber-`marginTop:'auto'` **selalu menempel batas bawah** (baseline disclaimer 655,86 identik di `n=1` & `n=4`, sisa **2,58 pt**); jangan ditambal `marginBottom` negatif/`paddingTop`. **Verifikasi:** build clean, lint tetap 170 (148+22) dicek per-commit, batas kop terjaga di SEMUA halaman (4 panjang alamat × `n=1..14`, sampai 3 halaman). ⚠️ **NOL tes runtime browser, dan cetak percobaan untuk angka BARU (3/3,5) BELUM dilakukan** — lembar kalibrasi 0,25 cm sudah dikirim ke Den, sengaja **tidak** masuk repo. Detail: `PROGRESS.md` 2026-09-10.
- **[10 Sep 2026] Tombol "Mark as Won" DIKEMBALIKAN — audit produksi membantah asumsi yang mendasari pencabutannya.** FE-only 2 commit, **NOL perubahan DB, nol migrasi, nol SQL** (dikerjakan di branch `feature/crm-v3-batch-persiapan`). Restore **BYTE-IDENTIK** dengan versi sebelum dicabut (state, gate, handler, tombol, ConfirmModal), **nol syarat baru** — kemurnian itu disengaja supaya diff-nya tetap bisa diverifikasi sebagai restore. ⭐ **ALASANNYA, dan ini yang paling layak dibaca:** pencabutan 8 Sep berdiri di atas **satu asumsi yang tak pernah diukur** ("trigger `set_inquiry_won_on_so` sudah menutup jalur itu"). Audit produksi 10 Sep: dari **38 inquiry WON** (37 hidup + 1 soft-deleted), **37 dari 37 WON hidup** lahir dari RPC `mark_inquiry_won` (jejak `audit_logs`, **seluruhnya MSI**) dan **NOL** dari trigger SO; seluruh tabel `sales_orders` = **1 baris DRAFT** dibuat 23 Jul yang **tak pernah SENT**, sehingga trigger itu **belum pernah menyala sekali pun sejak dibuat 22 Jul**; jalur manualnya dipakai **25× di September oleh 7 user, terakhir 10 Sep**. ⭐ **Pelajaran: *trigger yang ADA di skema tidak sama dengan trigger yang PERNAH MENYALA*** — kelas yang sama dengan pelajaran 8 Sep, kali ini menimpa objek DB alih-alih kolom. **Akarnya kenyataan bisnis:** SO yang sebenarnya masih diproses **di Odoo**; `sales_orders.external_ref` terisi **0 dari 1** baris. ⛔ **TD-239 DIKOREKSI KERAS** — dari "nol pemanggil, kandidat pencabutan" jadi **"JALUR PRODUKSI AKTIF, JANGAN dihapus sampai ada pengganti yang TERBUKTI JALAN"**; ia **DIKELUARKAN** dari batch pencabutan bersama TD-237/TD-238. **+Keputusan Terbuka #48** (makna "SO SENT" & nasib modul Sales Order). **+TD-248 (LOW: gate tombolnya memakai `erpRole` primer, bukan `erpRoles` — dicatat, sengaja tidak diperbaiki supaya restore tetap murni).** Trigger SO, tabel `sales_orders`, dan modul Sales Order **SENGAJA TIDAK DISENTUH**. ⚠️ **TES RUNTIME BELUM DIJALANKAN.** Detail: `PROGRESS.md` 2026-09-10.
- **[9 Sep 2026] Cetak biru Finance & Accounting masuk repo + keputusan struktur modul.** Dokumentasi, **nol perubahan kode, nol migrasi**. `11_FINANCE_ACCOUNTING_BLUEPRINT.md` (12 bagian + 2 lampiran) menyusun ulang alur invoice Storbit di atas empat prinsip: **dokumen lahir dari dokumen** · **invoice adalah anak BTB, bukan anak SP** (1 SP dengan 2 BTB → 2 invoice, penagihan parsial jadi mungkin) · **tanggal dokumen ≠ tanggal input** · **jurnal lahir dari BTB, bukan dari invoice**. 12 temuan struktural diukur di produksi 8-9 Sep (**D-01 `sp_btb.btb_date` nol terisi dari 456 baris** · D-04 satu SP hanya boleh satu invoice, padahal **30 SP punya >1 BTB** · D-06 **6 dari 10 invoice terbit tanpa BTB** · D-08 nomor invoice diturunkan dari `now()` bukan `invoice_date`). ⚠️ **Angka-angka itu TIDAK bisa diverifikasi ulang dari repo** — `schema_snapshot.sql` schema-only sejak 5 Sep (nol blok `COPY`), jadi hanya query langsung ke produksi yang bisa mengujinya. **Keputusan struktur (detail + alasannya: `09_ROADMAP.md` §Struktur Modul Finance & Accounting, M1-M5):** Finance & Accounting **SATU modul** (org chart menempatkan kedua Jr. Manager di bawah satu Finance Controller; Odoo mengelompokkan menu per **lawan transaksi**, bukan finance-vs-accounting; invoice yang terbit **langsung jadi jurnal AR**) · **Job Costing turun ke kelompok Laporan** (ia menyandingkan biaya dari Bank Disbursement dengan pendapatan dari Invoice — laporan, bukan tempat entri) · **4 menu TERBLOKIR modul Job Order yang belum ada** (Vendor Invoice, Bank Disbursement, Settlement of Advance, Job Costing — **ketergantungan data, bukan pilihan urutan**) · **seluruh panel Invoice/Pembayaran/TTF pindah ke modul Finance**, menyisakan **satu baris baca-saja tanpa tombol** di halaman SP (TTF ikut karena pengisinya Elvira, bukan orang lapangan). ⛔ **TD-217 dinaikkan jadi prasyarat pemblokir**, bukan lagi sekadar bug akses: tanpa penutupannya, halaman Piutang di modul Finance akan menampilkan **nama customer kosong** *[→ DITUTUP 10-11 Sep 2026, `20260910000001` LIVE]*. **+Keputusan Terbuka #43-#47** (Coretax diurus di mana · sumber `btb_date` 456 BTB lama · apakah BTB 4 SP memang tak pernah ada · sumber nilai HPP · apakah Storbit punya tim finance sendiri). ⚠️ **#42 SENGAJA dilewati** — sudah dipakai branch `feature/crm-v3-batch-persiapan`; lompatan 41→43 bukan kekeliruan. **6 koreksi** atas isi blueprint saat disisir terhadap repo — daftarnya di `PROGRESS.md` 2026-09-09.
- **[9 Sep 2026] Quotation versioning & approval (5 migrasi) + dua gelombang mencabut plafon 1000 baris CRM.** Dikerjakan di branch `feature/crm-v3-batch-persiapan`. **Kelima migrasi quotation ber-header `LIVE — staging 9 Sep 2026, produksi 9 Sep 2026`** (`20260909000001` kolom `accepted_*`/`rejection_reason`+index · `…000002` TD-180 jamak · `…000003` dua RPC revisi · `…000004` trigger NEGOTIATION · `…000005` cabut trigger deal-value); kelimanya sempat **hidup di produksi tanpa jejak apa pun di repo** sebelum direkam (isi SQL tak disentuh, hanya baris header `Status:`). ⚠️⚠️ **MIGRASI KEENAM `…000006_crm_dashboard_aggregate_rpc` JUGA LIVE** (staging + produksi 9 Sep) — headernya sempat tertinggal `BELUM DIJALANKAN` sampai dikoreksi 10 Sep, dan itu sempat terbaca sebagai gerbang rilis (TD-243, kini **RESOLVED**). **Isi:** status baru **`SUPERSEDED`** + jalur tulis `ACCEPTED`/`REJECTED` yang selama ini absen · `create_quotation_revision` (satu transaksi, tiga akibat) · **penomoran quotation diturunkan dari nomor inquiry** (`INQ/.../007` → `QUO/.../007`; deret `QUO` jadi **dorman, jangan dikira rusak**; konsekuensi struktural **satu inquiry = satu `quotation_no`**) · helper bersama `quotationVersion.js` (5 permukaan) · trigger `trg_z_inquiry_negotiation_on_revision` LIVE sehingga **tombol `Start Negotiation` dicabut seluruhnya** · **"Best Quote" ikut versi AKTIF, bukan nilai TERTINGGI** (sejak SUPERSEDED ada, "tertinggi" hampir selalu berarti versi PERTAMA — negosiasi menurunkan harga). ⛔ **Sumbu tampilan BUKAN `updated_at`** (revisi menyentuh `updated_at` baris LAMA) dan **`revision` WAJIB ikut di-`select()`** — lupa = gagal SENYAP (gotcha #29). **Dua gelombang anti-plafon:** Gelombang 1 membuat kegagalan **KELIHATAN** (guard `degraded` turun ke kartunya di 10 widget; laporan CRM yang tadinya **nol guard** dapat banner + penanda KPI **dan** delta), Gelombang 2 **mencabut akarnya** (6 RPC agregat, 100% BACA, `SECURITY INVOKER`; dua rantai `.in()` ~37 KB & ~74 KB hilang; total akun jadi `SUM(cnt)` funnel). ⚠️ **Bug white-screen TDZ `winRateDegraded` lahir & ditutup di sesi ini — dan ia MENGOREKSI laporan Gelombang 1: lint turun 163→161 itu GEJALA bug (compiler bail-out), BUKAN tanda sehat.** ⚠️ **DEVIASI URUTAN BATCH — keputusan sadar Den, BUKAN kelupaan:** sebagian **Batch B4** dieksekusi sebelum **B3** tertutup penuh. **+TD-243…TD-250.** **TD-180 instance KELIMA ditutup, KEENAM langsung lahir (TD-245) → tetap PARTIAL · TD-233 naik 4→5 tempat · TD-238 langkah 1 dari 2 selesai · TD-208 berubah sifat.** Detail: `PROGRESS.md` 2026-09-09.
- **[8 Sep 2026] Dua sprint UI CRM — Detail Deal lepas dari sumbu `accounts`, Pipeline Trend lepas dari selektor periode.** FE-only, **NOL perubahan DB, nol migrasi**, terverifikasi staging (dikerjakan di branch `feature/crm-v3-batch-persiapan`). **Sprint 1:** `DealHeaderControls` berhenti dipanggil (angka **DEAL VALUE** + **Edit Deal** + **Move Stage** hilang, slot `actions` kosong) · panel **Edit Value** dan tombol **Mark as Won** dicabut · toolbar **7→5** · `accounts.assigned_profile` → `assigned_to` · `accounts.estimated_value` + `bant_*` berhenti dibaca · banner **"Next step"** baru di bawah StatusBar. **Sprint 2:** Pipeline Trend jadi **12 bulan tetap**, LEPAS dari selektor (enam widget lain TETAP mengikutinya, default TETAP This Month) · garis pembanding dibuang · query pembanding slot `[1]` dicabut → **34 rujukan `res[]` direnomori** · penjagaan ketiga di MQL→SQL. ⭐ **Yang membuat sprint ini layak dibaca ulang: ketiga kolom `accounts` punya komentar desain yang sama meyakinkannya, dan hasilnya berbeda-beda setelah DIHITUNG di produksi** — `assigned_profile` **duplikat** (dari 1.211 akun hidup: **nol** yang hanya punya kolom itu, **nol** yang isinya beda), `estimated_value` **mati** (trigger pengisinya nol pernah jalan — 384 quotation, **nol** ACCEPTED; kolomnya 0 di **1.178/1.211** akun), `estimated_closing_date` **HIDUP** dan **sengaja dipertahankan** (118 akun, 8 orang, proporsi **naik** 6,5%→26,3%). ⛔ **Start Negotiation SENGAJA DITAHAN** — blueprint menyebut NEGOTIATION dipicu "versi v2 terbit" tapi **triggernya belum ada di DB**; mencabutnya sekarang membuat lajur itu tak terjangkau. ⚠️ **MQL→SQL akan menampilkan 0% di produksi** (kohort 166, converted 0) dan itu MENYESATKAN — penjagaan lama `mqlBase=0 → null` tak menyala karena **penyebutnya 166, yang nol pembilangnya**; cabang baru menampilkan pesan, bukan persentase. **+TD-237 (MEDIUM: DROP `assigned_profile`) / TD-238 (MEDIUM: DROP `estimated_value` — ⛔ CABUT TRIGGER `trg_z_sync_deal_value_on_quotation_accept` DULU, kalau tidak setiap UPDATE quotation ke ACCEPTED gagal KERAS) / TD-239 (LOW: RPC `mark_inquiry_won` nol pemanggil, JANGAN dihapus sekarang) / TD-240 (LOW: "New SQL This Month" bisa menghitung baris backfill — nol hari ini, tapi benar karena KEBETULAN).** **+Keputusan Terbuka #42** (`estimated_closing_date` salah sumbu?). ⚠️ **Satu butir checklist belum terbukti:** cabang MQL→SQL tak bisa diuji di staging (kohort NOL) — verifikasi di produksi setelah merge. ⚠️ **Tanggal sesi ini dikoreksi dari brief** ("9 Sep" → 8 Sep, 7 kemunculan di kode) — harness + `git author date` mengalahkan asumsi. Detail: `PROGRESS.md` 2026-09-08 · pekerjaan setelah merge: `09_ROADMAP.md` §PEKERJAAN SETELAH MERGE.
- **[7 Sep 2026] PRF multi-company guard + Storbit (nilai per kartu status, rekap SP per customer, panel export) + REKONSILIASI Nexus × Finance yang MEMBANTAH dua catatan 5 Sep.** **3 migrasi, semuanya LIVE produksi.** **(1)** `20260907000001_prf_multi_company_guard` — 4 `CREATE OR REPLACE` (`prf_claim`/`prf_release`/`prf_mark_quoted`/`prf_select_offer`), **satu-satunya yang mengubah otorisasi**. Camelia & Dery (Procurement Manager SOA, role `procurement` aktif di 3 entitas, home SOA) **ditolak KERAS** (`RAISE EXCEPTION`) di **seluruh 285 PRF** milik MSI; **78 PRF SUBMITTED** menunggu. **Instance KEEMPAT TD-180, pertama yang memblokir keras.** ✅ **terverifikasi runtime.** ⚠️⚠️ **PELAJARAN:** `20260821000004_crm_prf_jamak` (21 Agu, LIVE) **sudah memperbaiki hal yang sama untuk dua orang yang sama** tapi hanya menyentuh **18 policy / 6 tabel, NOL RPC** — keempat fungsi `SECURITY DEFINER` **tak ikut terbantu**, **bug bertahan 17 hari** → *memperbaiki policy TIDAK otomatis memperbaiki RPC `SECURITY DEFINER` yang menjaga hal yang sama* (gotcha #26). Perbaikannya **BUKAN** tukar singular→jamak (itu memutus keterkaitan dua syarat) melainkan **uji role DI ENTITAS PRF ITU**; blast radius diukur dulu = **NOL orang terdampak**. **(2)** `…000002_storbit_nilai_per_status` (+7 kunci `_value`; **4 kunci SENGAJA tanpa `_value`** — `cancelled`/`dispatch_eligible`/`dispatch_data_tersedia`/`total_sp`). ⚠️ **`btb_terbit_value` Rp 6,20 M ≠ kartu Outstanding Tagih Rp 6,28 M** — selisih Rp 76 jt = `shipping_price`, **by design** (gotcha #25). **(3)** `…000003_storbit_rekap_per_customer` (RPC BARU, permintaan CEO; satu baris per SP). ⚠️ **TIGA kategori sengaja `NULL` BUKAN 0** (`terkirim_penuh`/`pernah_risiko_pinalti`/`cancelled`) — **jangan dikoersi jadi 0**, `SUM()` SQL & `reduce()` JS dua-duanya diam-diam menganggapnya nol (gotcha #24). **FE tanpa migrasi:** label konteks produk (lahir dari CEO salah membaca Rp 43,58 jt DPP-1-produk vs Rp 104,99 jt BRUTO-2-produk — **keduanya benar, konteksnya tak terbaca**) · panel pilih isi export **8 bagian** + pilihan cakupan (**ketiga dropdown sengaja TANPA opsi "semua"** — kategori SP & stok saling tumpang tindih) · **fix dropdown customer yang LAHIR MATI** (`customerFromDb()` membuang `company_id` karena ia ADA di `CUSTOMER_STANDARD_DB_COLS`; komentar penjelasnya sempat **persis terbalik**, sudah dikoreksi) · tata letak PDF `paddingTop` px(20)→px(104) / `paddingBottom` px(26)→px(92) dihitung dari koordinat ornamen (`ReportChrome` **tetap `fixed`**). ⚠️ **2 perbaikan data MANUAL di SQL Editor, bukan migrasi:** 5 SP harga nol → Rp 5.148 (harga dari 5 invoice Finance yang **sudah LUNAS**; `sp_items` **dan** `sp_order_items` satu transaksi; **Outstanding Tagih +Rp 25.740.000**) · 2 invoice uji di-**VOID** + jurnal AR **dihapus permanen** (`journal_entries` tak punya `deleted_at`) → **nomor 0012 & 0013 terpakai permanen, berikutnya mulai 0014**. **+TD-231 (MEDIUM: `is_bnf_authorized()` guard singular sejati **dan** satu-satunya guard tanpa `SET search_path`) / TD-232 (LOW: 4 RPC PRF tanpa `REVOKE ALL FROM PUBLIC`) / TD-233 (MEDIUM: daftar role manajerial kini hidup di **4 tempat**, harus bergerak bersama) / TD-234 (MEDIUM: laporan lintas-produk butuh RPC agregat, bukan ulang RPC per-produk 38×) / TD-235 (LOW: baris SP & produknya di PDF bisa terpisah di pergantian halaman).** ⚠️ **NOL tes runtime FE** kecuali konfirmasi PRF. Detail: `PROGRESS.md` 2026-09-07.
- **[7 Sep 2026 · REKONSILIASI FINANCE — dua koreksi keras + temuan terbesar]** **(A1)** ~~"bottleneck di penerbitan invoice"~~ **SALAH** — 416/421 SP `BTB_TERBIT` **sudah ditagih di luar Nexus** (`JKT-xxxxxx`); sheet `outstanding Indomaret` saja: **635 invoice, Rp 20,4 M ditagih, Rp 19,2 M dibayar**, Nexus cuma punya **7 invoice**. Rp 6,27 M = **selisih PENCATATAN**, bukan uang belum ditagih. **#33 diganti isinya** → *di mana invoice diterbitkan ke depan?* (a) Nexus berhenti menampilkan angka penagihan · (b) impor berkala AR Finance · (c) Nexus ambil alih — ⚠️ **(c) menyentuh rencana Odoo s/d Maret 2027**. **(A2)** ~~akun Azhar Malik `is_active=false`~~ **SALAH** — akun **AKTIF** (`profiles.active`=true; kolomnya **`active`**, bukan `is_active`) dengan role **`super_admin`@MSI**; yang nonaktif cuma baris role `ceo` di `user_roles`, dan `super_admin` bypass semua gate. Role `ceo` juga sudah tercakup `is_manager_or_above()` → **TD-217 hanya mengenai Finance**, bukan eksekutif. **#34 difokuskan ke gate menu** (`role_menu_permissions` utk `logistics_sp` = **0 baris**; 7 user `view`, 2 user `export`/`print`). **⭐ TEMUAN TERBESAR: Nexus & Finance MENGUKUR OBJEK BERBEDA.** Dua file Finance **saling bertentangan** utk customer sama (selisih piutang **Rp 2,58 M**) → **#38**. **61% piutang Finance TIDAK lewat SP sama sekali** — dari OS Rp 9,59 M, **Rp 5,81 M** pakai nomor PO customer & ordernya **tak ada di Nexus** (Mobilindo Rp 2,26 M · Greenfields Rp 1,01 M · Adi Sarana Rp 640 jt · Alfamart Rp 570 jt · dll) → **#39**. ⛔ **Nexus TIDAK AKAN PERNAH align penuh dgn laporan Finance selama order non-SP tak masuk sistem — ini BUKAN sinkronisasi gagal, objeknya memang berbeda.** AR Finance 3 thn: ditagih Rp 19,10 M / dibayar Rp 9,51 M / OS Rp 9,59 M vs Nexus **Rp 73,1 jt** (**±130×**); **seluruh 42 invoice 2024 nol pembayaran** → **#40**. Anomali: SP `2117203` **dua invoice bernilai identik** (tagihan ganda?) · `JKT-260552` dobel · SP `2086402`/`2120505` tak ada di 15 sheet mana pun. **Elvira SUDAH menerbitkan 4 dari 7 invoice hidup lewat Nexus** → pertanyaan meeting bergeser jadi **"apa yang menahan supaya tidak semuanya"**. **Temuan data (jangan diselidiki ulang):** **33 SP `BTB_TERBIT` ber-`shipped_qty`=0** (BTB terbit tanpa dispatch SJ — **belum dipastikan by design, pertanyaan utk Koh Deny** → **#41**; bukti: SP `2254302` ditagih Rp 1,27 jt padahal Nexus mencatat nol → **Finance menagih dari qty PESANAN**) · **SP `SOA-0001` BUKAN data uji** (ada di sheet Gudang, DC Jombang, Closed — **jangan dihapus**); yang data uji cuma **`ZZZTEST-DUEDATE-001`** · **Rekap Gudang 53 SP vs Nexus 44 SP** = **cara memotongnya berbeda** (rekap mencampur per-customer dgn per-produk; **Trolly & Loyang adalah nama BARANG, bukan customer**). Detail: `PROGRESS.md` 2026-09-07.
- **[5 Sep 2026] Dashboard Storbit — Laporan Per Barang + strip Nilai/Outstanding + export Excel/PDF + pecah jadi 3 tab.** 2 migrasi, keduanya **LIVE produksi**, **100% BACA** (4 RPC baru + 1 index; nol DDL tabel, nol perubahan RLS, nol backfill). Tab: Shipping Manifest / Gudang / Laporan Per Barang, dgn strip 4 kartu nilai **di atas** tab bar + lazy-fetch tab 3 yang di-cache (pindah tab tak memicu fetch ulang). Export Excel lewat dependency BARU `exceljs` (dynamic import — chunk 929,92 kB terpisah, chunk halaman tetap ~41 kB) + `StorbitReportPDF.jsx` (ungu/krem, landscape). ~~**⭐ Temuan bisnis terbesar: Outstanding Tagih 422 SP / Rp 6,27 M — barang sudah dikirim & BTB terbit tapi BELUM SATU PUN diinvoice** → ~65% nilai kontrak nyangkut di penerbitan invoice.~~ ⛔ **TERBANTAHKAN 7 Sep 2026 — JANGAN DIPAKAI.** 416 dari 421 SP `BTB_TERBIT` **sudah ditagih**; invoicenya terbit **di luar Nexus** (`JKT-xxxxxx`). Rp 6,27 M = **selisih pencatatan antar-sistem**, bukan uang yang belum ditagih. `09_ROADMAP.md` **#33 sudah diganti isinya** jadi "di mana invoice diterbitkan ke depan?". **+TD-217 (HIGH: Finance tak bisa baca nama customer sama sekali — kolom CUSTOMER kosong di SELURUH tabel Storbit) / TD-218 (HIGH: `sp_invoices_read` singular → Outstanding Piutang Rp 0 utk user multi-entitas; instance ketiga TD-180) / TD-219 (LOW, ligature PDF, sudah dimitigasi) / TD-220 (LOW, advisory `uuid` lewat `exceljs`).** **TD-24 dikoreksi** (RPC ber-`anon` 3 → 2). **Koreksi dokumen:** klaim palet PDF di `03_DATA_MODEL.md` ternyata TERBALIK — Picking List/Surat Jalan/Invoice **satu keluarga** ungu/krem lewat `printTokens.js`; navy/orange itu brand cetak **CRM**. ✅ Checklist manual tab/lazy-fetch/PDF LOLOS; ⚠️ **3 perbaikan kosmetik terakhir belum ditest runtime**. Detail: `PROGRESS.md` 2026-09-05.
- **[4 Sep 2026] CRM v3 — Detail Deal jadi halaman KEDUA yang migrasi penuh ke design kit v3 (Batch A + B) + dua audit kesiapan produksi** (branch `feature/crm-v3-batch-persiapan`; FE-only, nol migrasi baru, **nol SQL dijalankan**). **Batch A:** `StatusBar` jadi chevron + token **`ORANGE_AA`** (WCAG AA 4.90:1 vs 4.36:1 — jangan dikembalikan), sumbu tampilan pindah ke `inquiries.status`; **kontrak `closed` berubah semantik** (bukan lagi early-return yang mengganti seluruh bar) dan **asimetri WON vs LOST/CANCELLED disengaja**. **Batch B (4 sub-batch):** halaman dibungkus `FormSheet`+`Notebook`+`ListView`, 18 field dua kolom berkelompok, kolom **Readiness** 4-keadaan, Chatter ke slot `aside`, konversi label ke Inggris, file baru `inquiryOptions.js` (satu sumber kebenaran, bukan cermin). **Tiga perubahan kontrak kit v3, semuanya aditif** (`FormSheet` +slot `toolbar`; `ListView` search bersyarat `onSearch`; `ListView` `render` terima indeks) — dicatat sebagai design-system di `06_UI_UX_FLOW.md` §3. **✅ Tes runtime ADA** (staging, manual Den): 6 skenario chevron, Set Value/Change Owner bertahan setelah refresh, `prf_select_offer` bekerja, nol regresi lintas halaman. ⚠️ **JANGAN dibaca sebagai LIVE:** (a) **CRM v3 tetap BELUM SELESAI** — 2 halaman dari rencana Batch B1–B6; (b) **belum di produksi**, dan sejak audit hari ini gerbangnya **ADA DUA** — selain `owner_id` (**TD-225**), migrasi `crm_v3_lifecycle` **me-RENAME `accounts.account_status`** sehingga **produksi patah sebelum merge** (**DITAHAN**, → Keputusan Terbuka **#35**/**#36**); (c) **`Move Stage` kini tersimpan tapi tak mengubah chevron — disengaja, bukan bug.** Detail: `PROGRESS.md` 2026-09-04 · kontrak kit: `06_UI_UX_FLOW.md` §3 · gerbang rilis: `08_TECH_DEBT.md` TD-225 + `03_DATA_MODEL.md` gotcha #28.
- **[3 Sep 2026] Rantai dokumen Account → Inquiry → PRF → Quotation — dua lompatan disambung + dua audit** (FE-only 3 commit, nol perubahan DB/RLS/migrasi). **Account → Inquiry:** tombol "+ New Inquiry" di Detail Account + prefill akun/kontak, dan ⭐ **`inquiries.contact_id` akhirnya punya jalur tulis** (sebelumnya **nol penulis**). **PRF → Quotation:** item quotation **dipecah N-baris** dari `prf_cost_items` (grouping `item_group`, currency dikonversi IDR per baris, baris `amount=0` dibuang) + prefill alamat; **jalur lama `suggested_rate>0` tetap 1 baris**; **lolos tes end-to-end di staging**. ⚠️ Gerbang eksekusi "cost item PRF lama selalu kosong" **digugurkan data produksi** — PRF lama ternyata punya cost item. **Dua audit:** `InquiryFormPage` **JANGAN** dimigrasi ke `FormSheet` (beda spesies; subjek benar = `DealDetailPage` — terbukti sehari kemudian), dan peta 9 titik kesasar rantai dokumen (rencana "Rantai Berkonteks" **DITUNDA**; L1/L2 masih terbuka). ⚠️ **Sales dibutakan selama PRF `ACKNOWLEDGED` = BY DESIGN** — yang rusak hanya pesan blokirnya; **addendum TD-158** (dua tombol "Buat Quotation" berlabel identik + pesan menyebut menu "Inquiry" yang kini bernama **"Deal"**). Detail: `PROGRESS.md` 2026-09-03.
- **[2 Sep 2026] CRM v3 — `InquiryListPage` jadi halaman PERTAMA yang migrasi PENUH ke design kit v3** (dikerjakan di branch `feature/crm-v3-batch-persiapan`; FE-only, nol migrasi baru). `ListView mode="table"` + kolom/filter **Owner** (pola dua-langkah, bukan embed FK) + filter **Deadline Quote** + toggle **My/All** + kolom **Stage** dihapus + badge status **tinted rounded-square** dengan **WON = navy `#144682`**. Dua bug ditutup: filter `created_by` yang ber-AND dgn RLS `owner_id` (**TD-223**) & `NEGOTIATION` tanpa entri `STATUS_META` (badge berlabel **"Open"**, **TD-224**). `WinLossModal.jsx` dihapus (kode mati). Trigger `set_customer_on_won()` **diinvestigasi, sengaja TIDAK ditambal** (**TD-222** → dicabut total di Batch B2). **+TD-222…TD-228.** ⚠️ **JANGAN dibaca sebagai LIVE:** (a) **CRM v3 BELUM SELESAI** — 1 halaman dari rencana Batch B1–B6; **Account/Approval Lead Pool/Quotation/Customer/Aktivitas belum tersentuh**; (b) **belum di produksi** — 9 migrasi CRM v3 (`inquiries.owner_id` dkk) **hidup di staging saja**, `schema_snapshot.sql` tak memuatnya, jadi FE ini **akan error di produksi** kalau naik mendahului migrasinya (**TD-225**, urutan wajib: migrasi → `pg_dump` → FE); (c) **NOL tes runtime.** Detail: `PROGRESS.md` 2026-09-02 · status & gerbang rilis: `09_ROADMAP.md` "Next Up".
- **[2 Sep 2026] Item SP Security (Fase 0) + status dokumen Finance naik ke level SP (Fase 1) + TD-180 batch 2 & 3.** 7 migrasi, semua LIVE produksi. **3 bug nyata ditutup:** (1) Finance tak pernah bisa menyimpan status dokumen SP **sejak 25 Agu** — `FinanceModal` memanggil `update_sp_item_dual` yang guard-nya menolak `finance`, Save gagal server-side tanpa penjelasan di UI; (2) **tombol Hapus item SP TANPA GATE SAMA SEKALI** + hard-delete lewat RLS `USING(true)`, dan hapusnya meninggalkan baris hantu `sp_order_items` yang bikin SP **tak bisa di-invoice selamanya**; (3) user multi-company ter-blokir senyap dari `sp_btb`/`dc_master` milik entitas lain tempat dia aktif. Fungsi baru **`is_sp_item_writer()`** (ceo/gm/gm_bd jadi VIEW-ONLY khusus edit item SP — `is_manager_or_above()` global SENGAJA tak diubah) + RPC **`delete_sp_item_dual`** + **`set_sp_finance_docs`** (satu-satunya penulis 6 kolom finance yang kini di header `sp_orders`). **TD-49 & TD-201 & TD-20 → RESOLVED · TD-180 tetap PARTIAL · +TD-215/TD-216.** ✅ Fase 1 smoke test produksi LOLOS; ⚠️ **Fase 0 belum ada laporan tes runtime.** Detail: `PROGRESS.md` 2026-09-02.
- **[31 Agu 2026] Format cetak baru Surat Jalan & Picking List + alamat tujuan pindah ke `dc_master.alamat`.** ⚠️ **Dicatat SUSULAN 2 Sep 2026** — sesi ini sempat nol jejak dokumentasi walau 2 migrasinya sudah LIVE. Dua PDF gudang mengikuti Claude Design (ungu/krem/serif, satu keluarga dgn `InvoicePDF`), duplikasi palet/style dipindah ke `printTokens.js`+`printKit.jsx` (dipisah karena lint `react-refresh/only-export-components`); logika bisnis nol perubahan. Migrasi `20260831000001` menyeed `delivery_notes.destination_address` dari **`dc_master.alamat`** lewat `sp_orders.dc_id`, bukan lagi `accounts.address` yang ternyata alamat HQ — audit 85 SJ: **67 NULL, 15 alamat HQ, 0 beralamat DC**. `20260831000002` mem-backfill 5 SJ aktif saja (79 `delivered` sengaja tak disentuh: kertasnya sudah beredar). Textarea Alamat Tujuan dihapus dari FE → read-only. Refresh snapshot sesi ini juga yang **pertama kali membawa ACL** masuk ke `schema_snapshot.sql`. Detail: `PROGRESS.md` 2026-08-31.
- **[26 Agu 2026] Staging + 3 migrasi Storbit + tab Shipment/Dokumen.** Environment `nexus-staging` dipakai pertama kali sebagai gerbang sebelum produksi. Tiga migrasi LIVE: fix `update_sp_item_dual` (merujuk kolom `sp_items.company_id` yang tak pernah ada — Edit Item SP/Shipment/Finance modal rusak total sejak 21 Agu, **TD-212**), RPC `set_sp_expired_date` (`expired_date` jadi murni atribut header, **TD-201 RESOLVED**), backfill kolom mati `sp_order_id` di `picking_lists`/`delivery_notes`. FE: bug timezone UTC→WIB dikonsolidasi ke `src/lib/dateUtils.js`, UI edit tenggat SP, tab Shipment akhirnya berisi timeline Surat Jalan nyata + card "Dokumen Terkait". **+TD-210…TD-214.** Detail: `PROGRESS.md` 2026-08-26.
- **[24 Agu 2026] CRM lepas dari filter `roles.company_id` yang mati sejak globalisasi roles.** Tiga query roster/notifikasi gagal senyap (nol baris, nol error) sejak 21 Agu; 8 permukaan UI pulih. FE-only 3 file. **TD-207 RESOLVED · +TD-208/TD-209.** Detail: `PROGRESS.md` 2026-08-24.
- **[21 Agu 2026] Storbit — partial picking dibuka.** Toggle biner diganti input qty per item, status diturunkan dari angka; guard idempotensi picking direvisi jadi 2 blok (`dispatched_at IS NOT NULL`, bukan status — lihat gotcha #17). **+TD-206.** Detail: `PROGRESS.md` 2026-08-21.
- **[20 Agu 2026] Sistem permission mati (`hasPermission`/`role_permissions`) dihapus FISIK + fix bug sinyal `permsLoaded`.** Tabel `role_permissions` kini nol pembaca; bug `permsLoaded` mematikan redirect guard + deep-link `?menu=` untuk user tier-3 (prasyarat Fase B3). **+TD-204/TD-205.** Detail: `PROGRESS.md` 2026-08-20.
- **[18 Agu 2026] Dashboard Storbit (Shipping Manifest + Warehouse).** 2 kolom + 3 RPC agregasi/drill-down, semua LIVE. `expired_date` diredefinisi sebagai tenggat WAJIB KIRIM (risiko pinalti), bukan validitas harga. **+TD-200/TD-201/TD-202.** Detail: `PROGRESS.md` 2026-08-18.
- **[16-17 Agu 2026] Storbit SP FASE 5 — pembayaran (LUNAS) + jurnal AR minimal + TTF.** Rantai mesin status 12 tahap akhirnya lengkap sampai LUNAS; redesign visual Detail SP; celah double-entry `ar_btbs` ditutup 2 lapis. **TD-176/TD-180 → PARTIAL · +TD-197/TD-198.** Detail: `PROGRESS.md` 2026-08-17.
- **[13 Agu 2026] Migrasi kit desain resmi (TD-14) — 9 halaman Foundation admin-settings, 3 batch.** Lepas dari `PASTEL` lokal + `AdminPageHeader`. **TD-14 → PARTIAL (9/22 file).** Detail: `PROGRESS.md` 2026-08-13.
- **[12 Agu 2026] Org Chart — fix garis konektor anak tunggal + Ctrl+scroll zoom + drag-to-pan.** FE-only 1 file, tiga potong kerja hari yang sama. Detail: `PROGRESS.md` 2026-08-12.
- **[10 & 12 Agu 2026] BNF Layer 0-4: Briefing Harian + Meeting Mingguan (dua modul baru) + gate `is_bnf_authorized()`.** 7 tabel baru; menu `bnf` dibalik dari public jadi ber-gate. **+TD-184…TD-194.** Detail: `PROGRESS.md` 2026-08-10 & 2026-08-12.
- **[9 Agu 2026] 9 perubahan satu sesi: Access Control + multi-entity plumbing Fase 1 (`activeCompanyId`) + AdminHub + CompanySwitcher.** `AdminShell`+`AdminSettingsHub` melebur jadi `AdminHub` (25 destinasi); **TD-92 RESOLVED**; ditemukan sistem permission KETIGA `can()`/`PERMISSIONS` (**TD-182**, HIGH). **+TD-180/TD-181.** Detail: `PROGRESS.md` 2026-08-09.
- **[8 Agu 2026] Storbit SP Fase 4 Invoice (INVOICED/SUBMITTED) + DC Master CRUD + RLS GRANT hardening.** `authenticated` sempat bisa menulis `sp_orders.status` langsung lewat PostgREST — ditambal. **TD-175 & TD-40 RESOLVED.** Detail: `PROGRESS.md` 2026-08-08.
- **[7 Agu 2026] RBAC hardening besar: fix privilege escalation `user_roles` + sistem `role_menu_permissions` baru (resolusi 3-tingkat).** Audit RLS/RBAC menyeluruh menemukan **19 tabel ber-RLS `USING(true)`** (belum dieksekusi). **TD-170 RESOLVED · +TD-171…TD-174.** Detail: `PROGRESS.md` 2026-08-07.
- **[6 Agu 2026] 3 fix kecil FE turunan audit overdue** — badge Overdue Kanban disinkronkan ke Edge Function `aging-pipeline` + notifikasi Lead Pool bisa diklik. **+TD-168/TD-169.** Detail: `PROGRESS.md` 2026-08-06.
- **[5 Agu 2026] Insiden produksi pasca-BNF: GRANT tabel baru kelewat** (403 berulang) memicu loop render/refetch dari `showToast` non-`useCallback`. **+TD-165/TD-166/TD-167.** Detail: `PROGRESS.md` 2026-08-05.
- **[4 Agu 2026] Modul BNF (Bad News First) baru — 7 fase A-G.** Migrasi dari app Google Apps Script lama; data lama tak dipindah. Detail: `PROGRESS.md` 2026-08-04.
- **[3 Agu 2026] Notifikasi email saat di-tag di komentar Chatter — Edge Function baru `send-email`** (relay generik ke Resend), jadi fondasi yang dipakai ulang modul BNF. Detail: `PROGRESS.md` 2026-08-03.
- **Redesign `DealDetailPage` — 4-tab restructure + field link-able (nama sales/akun, `ProfileMiniView.jsx`) + Chatter (komentar+@mention+notifikasi, `InquiryChatter.jsx`)** (3 commit berurutan; 2 tabel baru `inquiry_comments`/`inquiry_comment_mentions` belum direkam migrasi, snapshot kini memuatnya — TD-163 PARTIAL; Chatter tanpa `showToast` — TD-164). Lihat `PROGRESS.md` 2026-07-31.
- **Fix PRF prefill dari inquiry (kehilangan `inquiry_no`) + `InquiryPicker` tampilkan nama+tipe akun + nomor Quotation/PRF jadi link**. Lihat `PROGRESS.md` 2026-07-31.
- **Tab "BANT & Pipeline" (Detail Account) — edit inline 4 dimensi BANT + fix silent-fail RLS di `saveDealUpdate`** (berdampak retroaktif ke Pindah Stage/Edit Deal juga; TD-161). Lihat `PROGRESS.md` 2026-07-30.
- **PRF: draft bisa diedit (TD-76) + redesign Detail PRF jadi 9 section bernomor + rename "Cetak PRF"→"Buat PRF"** (TD-93/148/149). Lihat `PROGRESS.md` 2026-07-29.
- **Gerbang "Buat Quotation" terima jalur penawaran vendor + fix gate "Cetak PRF" baca seluruh role aktif** (dua batch independen; TD-90/105/157/158). Lihat `PROGRESS.md` 2026-07-28.
- **Modul "Penawaran Vendor" PRF — fondasi s/d Batch 3C** (klaim/lepas PRF, tambah/edit/hapus penawaran vendor, RPC `prf_mark_quoted`/`prf_select_offer`) **+ 2 fix kecil independen** (dropdown Inquiry di Quotation, nama sales di Detail PRF). Lihat `PROGRESS.md` 2026-07-27 (2 entri tanggal ini).

> ⚠️ Banyak fase "build clean, **belum tes manual runtime**" — selalu cek sebelum anggap fitur jalan. Update bagian ini + `PROGRESS.md` tiap akhir task. Histori lengkap sebelum 26 Jul 2026 (143 entri, dipangkas 31 Jul 2026 setelah diverifikasi satu-per-satu tersalin ke `PROGRESS.md` — 19 entri kecil yang ternyata belum tercatat dipindah ke section "Catatan Susulan" di puncak `PROGRESS.md` sebelum dihapus dari sini) ada di git history file ini + `PROGRESS.md`. **Pemangkasan KEDUA 26 Agu 2026:** 15 entri padat 3-24 Agu diringkas jadi 1-2 kalimat + penunjuk tanggal `PROGRESS.md`. Diverifikasi lebih dulu: **16 entri `PROGRESS.md` menutup ke-15 tanggalnya**, dan 11 klaim khas (mis. "8 permukaan UI", "390/463 SP", `pernah_risiko_pinalti`, `panRef`, `ZZZTEST`, "517 error") dicek satu-per-satu ada di `PROGRESS.md`/governance — **nol detail yang cuma hidup di sini**, jadi tak ada yang perlu dipindah lebih dulu (beda dari 31 Jul yang menemukan 19 entri yatim).

## Known Issues (Quick Ref — detail: `docs/Governance/08_TECH_DEBT.md`)

- **[9-10 Sep 2026, Quotation versioning + dua gelombang anti-plafon + restore Mark as Won]** ⛔⛔ **KOREKSI KERAS TD-239 — baca ini SEBELUM merencanakan batch pencabutan apa pun.** TD-239 sebelumnya berbunyi *"nol pemanggil FE, kandidat pencabutan batch berikutnya bersama TD-237/TD-238"*. **ITU SALAH.** Audit produksi 10 Sep 2026: **37 dari 37 inquiry WON hidup** lahir dari RPC `mark_inquiry_won` (jejak `audit_logs` `action='MARK_INQUIRY_WON'`, seluruhnya MSI), **NOL** dari trigger `set_inquiry_won_on_so`; seluruh tabel `sales_orders` = **1 baris DRAFT** yang **tak pernah SENT** sehingga trigger itu **belum pernah menyala sejak dibuat 22 Jul**; jalur manualnya dipakai **25x di September, 7 user, terakhir 10 Sep**. **`mark_inquiry_won` = JALUR PRODUKSI AKTIF — JANGAN dihapus/dicabut sampai ada pengganti yang TERBUKTI JALAN**, dan **KELUARKAN dari batch pencabutan bersama TD-237/TD-238**. Rujukan di `09_ROADMAP.md` §PEKERJAAN SETELAH MERGE butir 3 **sudah ikut dikoreksi**; kalau kamu menemukan rujukan lain yang masih menyebut "nol pemanggil"/"kandidat pencabutan", **itu sisa yang belum tersisir — koreksi, jangan diikuti**. Akar bisnisnya → **Keputusan Terbuka #48** (SO sebenarnya masih di Odoo; "SO SENT" berbagi satu nilai untuk **dua makna** — niat migrasi *"dikirim ke CUSTOMER"* vs implementasi FE *"Kirim ke Procurement"*/`sendToProcurement()`).
  ✅ **TD-243 — RESOLVED 10 Sep 2026, hari yang sama ia lahir.** Migrasi `20260909000006_crm_dashboard_aggregate_rpc` sempat terbaca sebagai gerbang rilis ketiga karena headernya tertulis `BELUM DIJALANKAN` sementara FE sudah memanggil keenam RPC-nya. **Query `pg_proc` langsung ke staging DAN produksi membuktikan keenam fungsi SUDAH ADA sejak 9 Sep** (`prosecdef=false`, `search_path=public`, nol akses `anon`); headernya sudah dikoreksi jadi LIVE. **Jangan hidupkan lagi sebagai gerbang.** ⭐ Pelajaran yang tetap berlaku: header migrasi & `schema_snapshot.sql` **bukan bukti status staging** — snapshot cuma memotret produksi saat di-dump. ⛔ **TD-249 (HIGH, koordinasi mengikat):** `p_scope_own` di `crm_stage_conversion`/`crm_stage_age` memakai **`created_by`**, dan **WAJIB diganti ke `owner_id` BERSAMAAN** saat `20260830000003` dijalankan — salah satu duluan = **IRISAN dua sumbu**, sales kehilangan deal yang dioper kepadanya **SENYAP**.
  **TD-180 instance KELIMA ditutup** (`quotations`/`quotation_items`, migrasi `20260909000002` + fungsi baru **`is_manager_or_above_in(uuid)`**) **tapi instance KEENAM langsung lahir** — `activities_select` masih singular, dibaca `CRMReportPage` tanpa filter FE apa pun (**TD-245**) → **TD-180 tetap PARTIAL**. **TD-233 naik 4 → 5 tempat** (tambahannya `is_manager_or_above_in()`); ⚠️ **duplikasinya tetap DISENGAJA — JANGAN "perbaiki" dengan mengembalikan panggilan ke `is_manager_or_above()`**, dan ⭐ fungsi baru itu **lahir sebagai tempat KELIMA, bukan penyatu keempat yang lama**. **TD-238 langkah 1 dari 2 SELESAI** (trigger `trg_z_sync_deal_value_on_quotation_accept` dicabut) — ⛔ **tapi `DROP COLUMN` MASIH TERBLOKIR, penghalangnya BERGESER ke FE:** `CustomerDetailPage.jsx` membaca **dan menulis** `accounts.estimated_value` di **EMPAT titik** (`:1029` select · **`:1039` TULIS** · `:1297` · `:1867`), jadi **jangan baca "trigger sudah dicabut" sebagai "aman untuk DROP"**. **TD-208 BERUBAH SIFAT** — klaim "NOL file CRM pakai `activeCompanyId`" tak berlaku lagi: `CRMDashboardPage` & `CRMReportPage` sudah pindah, **18 halaman CRM lain belum**, jadi **CompanySwitcher kini PARSIAL** (kandidat batch tersendiri, bukan tambalan per-file). **+TD-244 (MEDIUM: lubang PostgREST `quotations.status` — RPC ber-`SECURITY INVOKER` + `GRANT ALL ON TABLE quotations` utuh; kerabat TD-176, SENGAJA belum ditutup) / TD-246 (MEDIUM: `prospects_read` cabang `operations` masih kolom LAMA `account_status` sementara funnel sudah `lifecycle_stage` — prasyarat `20260908000002` drop-legacy) / TD-247 (perlu konfirmasi: `roles.code` punya DUPLIKAT, akar BELUM diinvestigasi, dicatat mentah tanpa spekulasi) / TD-248 (LOW: gate "Mark as Won" pakai `erpRole` primer bukan `erpRoles` — perilaku ASLI sebelum pencabutan, sengaja dipertahankan supaya restore murni) / TD-250 (LOW: "Pipeline by Stage" berplafon bisa tak cocok dengan "Stage Conversion" tanpa plafon — konsekuensi DITERIMA, dan berbunyi lewat `degraded.pipelineByStage`, tidak diam-diam).**
  ⚠️ **Keputusan desain yang JANGAN dibalik tanpa sengaja:** sufiks revisi `-A`/`-B` **murni tampilan**, mulai di revisi **KE-2** (nomor yang sudah beredar di tangan customer tak boleh berubah) · sumbu versi = **`pickActiveQuotation`, BUKAN `updated_at`** (revisi menyentuh `updated_at` baris LAMA) · **`revision` WAJIB ikut di-`select()`**, lupa = gagal SENYAP · `STATUS_META` quotation hidup di **TIGA file**, sengaja tak disatukan **tapi wajib bergerak bersama** (fallback jatuh ke `DRAFT` → status tak terdaftar **berbohong tertulis "Draft"**) · penomoran quotation dipecah **per `/`**, bukan `.replace('INQ','QUO')` · format tak sah → **throw**, nol silent fallback · **6 RPC agregat `SECURITY INVOKER`** — ⛔ kalau diubah ke DEFINER, **SELURUH** isi lima policy wajib disalin ke badan fungsi · sentinel `'__none__'` **sengaja bukan `'other'`** (`other` nilai `source` yang sah) · **nama trigger `trg_z_...` jangan diubah** (urutan alfabetis yang menjamin QUOTED mendarat sebelum NEGOTIATION). ⚠️ **Pelajaran proses:** lint turun **163 → 161** di Gelombang 1 ternyata **GEJALA bug TDZ** (React Compiler bail-out), **bukan tanda sehat** — angka lint yang membaik tanpa sebab layak dicurigai. Detail: `PROGRESS.md` 2026-09-09 & 2026-09-10.

- **[8 Sep 2026, dua sprint UI CRM]** ⛔ **TD-238 punya URUTAN MENGIKAT yang gagalnya KERAS:** `DROP COLUMN accounts.estimated_value` **wajib** didahului pencabutan trigger `trg_z_sync_deal_value_on_quotation_accept` + fungsinya — tanpa itu **setiap UPDATE quotation ke `ACCEPTED` melempar exception**. Jebakannya belum pernah terpicu karena produksi punya **nol** quotation ACCEPTED, dan justru itu yang membuatnya mudah terlewat. **TD-237** (`DROP assigned_profile`, nol trigger, aman kapan pun) dan **TD-239** (RPC `mark_inquiry_won` nol pemanggil — **JANGAN dihapus sekarang**) menyusul di batch yang sama. ⛔ **`Start Negotiation` HARUS TETAP HIDUP** sampai trigger "quotation v2 → NEGOTIATION" dibuat — disisir di snapshot, hanya ada 3 trigger penulis status dan **nol** yang menghasilkan NEGOTIATION. **TD-240** (LOW): widget "New SQL This Month" bisa menghitung baris backfill sebagai transisi baru; **nol hari ini, tapi benar karena kebetulan** — perbaikannya satu klausa `from_stage IS NOT NULL`. ⭐ **Pelajaran yang berlaku di luar sprint ini:** *komentar desain menjelaskan KENAPA sesuatu dibuat, bukan APAKAH ia masih dipakai* — tiga kolom `accounts` semuanya punya komentar meyakinkan, dua ternyata mati, dan itu baru ketahuan setelah **dihitung di produksi**.

- **✅ [KOREKSI 18 Sep 2026: bullet 4 Sep di bawah SELURUHNYA TERTUTUP — RENAME tak pernah dijalankan (digantikan jalur B `20260908000001`, LIVE 7 Sep), `rls_owner_based` LIVE 18 Sep sesudah backfill tuntas, #35 terjawab; #36 tetap terbuka/tergantung #37. Dipertahankan sebagai jejak.]** **⛔ [4 Sep 2026, BLOKIR RILIS — baca sebelum menjalankan SQL CRM v3 apa pun]** Migrasi `20260827000002_crm_v3_lifecycle.sql:60` **me-RENAME** `accounts.account_status` → `lifecycle_stage`. Begitu jalan, kolom lama **lenyap seketika**, sementara `main` yang **sedang melayani produksi** masih membacanya di **14 file / 29 baris hidup** (8 di `.select()`, 15 filter, 3 menulis — diukur `git grep` ke `main`; **angka "10 file" yang sempat beredar UNDER-COUNT**). **Produksi patah SEBELUM branch CRM v3 di-merge** → ini **blokir**, bukan urutan deploy. **Migrasi itu DITAHAN** (blok ⛔ ditempel di kepala filenya). **Yang boleh jalan sekarang (nol dampak ke `main`), berurutan:** `crm_v3_master_data` → `inquiry_status_history` → `inquiries_closure_fields` → `inquiry_owner_backfill_and_lock`, lalu `pg_dump`, baru FE. Penahanan dicabut hanya setelah **Keputusan Terbuka #35** (rename langsung **vs** tambah-kolom + sinkron dua arah) dijawab. ⚠️ Migrasi yang sama juga **menyelipkan perubahan perilaku bisnis** — `set_prospect_on_inquiry` dipersempit `('lead','mql','sql')` → `('lead','mql')`, **membatalkan keputusan tertulis 18 Jul 2026**; header migrasinya mengklaim sudah disetujui, **tapi statusnya sengaja tetap PERTANYAAN TERBUKA** (**#36**) — jangan dijalankan sambil menganggap poin ini beres. ⚠️ **`rls_owner_based` kalau naik sebelum backfill tuntas: setiap sales kehilangan SELURUH inquiry-nya, SENYAP.** Detail: **TD-225** · `03_DATA_MODEL.md` **gotcha #28** · `09_ROADMAP.md` **Keputusan Terbuka #35/#36**.
- **CRITICAL** — Migrasi RLS RBAC-driven belum jalan; ~51 policy pakai `is_admin_or_above()` (tak kenal manager/ceo) → bug akses (TD-01, **PARTIAL**: 3 oversight-read sudah +`is_manager_or_above`+STABLE). `has_permission()` flagged broken/unseeded (TD-02, **7 Agu 2026: dikonfirmasi 0 pemakaian RLS+FE**, kandidat DONE). **Audit RLS/RBAC menyeluruh 7 Agu 2026 menemukan 19 tabel LAIN ber-RLS `USING(true)`/setara** (seluruh modul Storbit SP/Fulfillment + `warehouses`/`rate_sheets`/`profiles`/`app_settings`/`audit_logs`/`contacts`/`inquiry_comments`/`inquiry_comment_mentions`) — **SELURUHNYA BELUM DIEKSEKUSI** (TD-173, daftar lengkap). Privilege escalation `user_roles` (admin biasa dulu bisa assign role `super_admin`) sudah **RESOLVED** hari yang sama (TD-170).
- **HIGH** — Audit CRUD/DELETE policy semua tabel (silent 0-row; TD-03, **PARTIAL**: 4 tabel transaksional sudah dpt DELETE policy). Write non-atomik ar_btbs/permission-diff (TD-33). `profiles_read = USING(true)` stopgap CEO (perketat saat HRIS; TD-04). Edge Functions delete-user/reset-password belum di-deploy (TD-21). Belum ada audit logging / test / Sentry (TD-05/07/08). Double-write `accounts.pipeline_stage` — 3 jalur tulis, fix in-flight-lock **kode selesai tapi test D/E belum dijalankan** (TD-171); gap terpisah `applyStageMove` Kanban masih tanpa `.select()` (TD-172). **[8 Agu 2026]** PostgREST bisa tulis `sp_orders.status`/kolom sistem `sp_invoices` langsung, bypass mesin status SP — fix **DIKONFIRMASI LIVE** (TD-175 RESOLVED); kolom sejenis lain (`sp_order_items.shipped_qty`/`sp_items.sp_status`/`sp_invoice_lines.*`, `GRANT ALL`) sengaja belum ditambal — **[17 Agu 2026] `sp_payments` sudah KELUAR dari daftar itu** (GRANT ALL → SELECT + UPDATE 3 kolom, policy INSERT dihapus → RPC-only), **TD-176 kini PARTIAL**, tiga tabel sisanya tetap OPEN. **[9 Agu 2026]** Sistem permission KETIGA `can()`/`PERMISSIONS` (`App.jsx:273-313`, di luar `hasPermission()`/`hasMenuPermission()` yang sudah dikenal) masih hidup, menggerbang 10 titik aksi Finance/Master Customer nyata (Add Customer, Add/Edit TTF) — deny eksplisit admin lewat `user_menu_permissions` **tidak mengikat** di titik-titik ini (TD-182, baru, OPEN). Pola RLS `is_super_admin() OR company_id=get_user_company_id()` (tanpa varian jamak `get_user_company_ids()`) TIDAK CUKUP utk user genuinely multi-company — ditemukan berulang 3× satu hari (`companies`/`roles` sudah ditambal, `payment_terms_read` **masih OPEN**; TD-180, **kini PARTIAL** — policy baru 17 Agu 2026 [`journal_entries_read`/`journal_entry_lines_read`/`sp_payments`] lahir langsung dgn varian jamak, jadi polanya punya preseden hidup untuk disalin; sisir sistematis policy lama TETAP belum dilakukan). **[17 Agu 2026, baru]** Casing `dppPPN` (UI `App.jsx`) vs `dppPpn` (`db.js`) memutus kolom DPP+PPN AR Tracker **di kedua arah** — belum berdampak (`ar_btbs` kosong), sengaja belum ditambal, jangan diperbaiki setengah (TD-197, MEDIUM). `sp_orders.confirmed_at/by` kolom mati, sumber benar `sp_items.confirmed_*` (TD-198, LOW).
- **[18 Agu 2026, Dashboard Storbit]** **TD-178 kini 3 titik** (2 RPC + 1 FE `StorbitDashboardPage.jsx`) — pin SOA di FE adalah **perbaikan sadar atas bug senyap**, bukan kelalaian; ⚠️ kalau kelak dibalik ke `activeCompanyId`, **WAJIB** disertai empty-state utk entitas non-SOA. **TD-200** (MEDIUM, baru — `ProductsPage` bisa menampilkan `MOCK_PRODUCTS` seolah data asli saat fetch gagal ATAU hasil sah 0 baris, tanpa indikator apa pun). **TD-201** (LOW, baru — `sp_orders.expired_date` divergen dari `sp_items.expired_date`, kerabat TD-198; sumber kebenaran = `sp_items`). **TD-202** (LOW, baru — dropdown Customer Dashboard Storbit disaring `accounts.company_id`, bukan "punya SP di SOA"; **gejala: laporan "customer X hilang dari filter"** → periksa `accounts.company_id` dulu). **TD-199 dikoreksi** — sisa langkah manualnya sudah lunas.
- **[20 Agu 2026, FE-only]** `hasPermission`/`role_permissions` **dihapus fisik** dari kode (bukan lagi vestigial) → tabel `role_permissions` **nol pembaca**; penulis tersisa cuma halaman ORPHAN `RolesPage.jsx` (**TD-204**, MEDIUM, baru — kerabat TD-181/TD-41). **TD-205** (LOW, baru — `roleMenuPermissions` **dan** `menuPermissions` di-export dari `value` AuthContext tanpa konsumen di luar AuthContext; housekeeping, nol dampak). ⚠️ **Bug `permsLoaded` (SUDAH DIPERBAIKI, tapi NOL tes runtime):** redirect guard `activeMenu` + adopsi deep-link `?menu=` + penulis URL **mati total** untuk user yang aksesnya bersumber murni dari `role_menu_permissions` — **prasyarat Fase B3**, seeding tanpa fix ini justru mematahkan URL-sync. **[KOREKSI 21 Sep 2026: penggantinya (`permissionsLoading`) ternyata punya RACE sendiri — jadi `false` sebelum `erpRoles` diketahui, sehingga untuk user tier-3 yang sama kedua gate tetap memutus dengan izin kosong (deep-link ditolak & dikunci, restore dipental ke Command Center). Tes runtime pertama = sweep QA 21 Sep 2026, GAGAL. Sudah di-hotfix di branch `fix/td-271-permissions-loading-race` (`rolesReady`, 1 file `AuthContext.jsx`) — TD-271 RESOLVED di kode, produksi mengikuti deploy `main` berikutnya. Kalimat "SUDAH DIPERBAIKI" di atas benar untuk heuristik `length>0` (TD-130 DONE), bukan untuk perilaku end-to-end.]** **TD-182 TIDAK terpengaruh** — `can()`/`PERMISSIONS` masih menggerbang 10 titik Finance/Master Customer.
- **[21 Agu 2026, partial picking]** **TD-206** (MEDIUM, baru) — picking berstatus `done` yang Surat Jalannya tak pernah dibuat **memblokir picking berikutnya** sampai SJ dibuat DAN diberangkatkan, sementara `cancel_picking` menolak status `done` → **jalan buntu tanpa escape hatch di UI**. ⚠️ **Ini konsekuensi DISENGAJA (Opsi B, keputusan sadar Den), BUKAN bug — JANGAN "diperbaiki" dengan melonggarkan guard**, itu justru membuka dobel-reservasi stok yang jadi alasan guard ini ada. Escape hatch = keputusan terbuka (`09_ROADMAP.md` #31). Sub-kasus terkeras (reachable): seluruh item `qty_picked=0` → SJ-nya bahkan tak bisa dibuat. **[koreksi, hari yang sama]** Jalur lahirnya sub-kasus itu **sudah ditutup di UI** (FE-only, gate `allItemsZero` menolak "Selesaikan Picking" + mengarahkan ke tombol Batalkan yang memang jalan keluar sah) — **opsi "blokir di UI" DIPILIH**, opsi "longgarkan `cancel_picking`" **DI-SKIP tapi BELUM GUGUR** karena hanya ia yang bisa menolong baris `done` nol-pick yang **sudah terlanjur ada** (hari ini: SQL manual). **TD-206 TETAP OPEN**, statusnya turun jadi *"jalur baru tertutup, data lama belum tertolong"*. ⚠️ Label opsi (a)/(b) di TD-206 sempat bertabrakan antar-sesi — baris itu kini memakai **nama deskriptif**, jangan rujuk hurufnya. ~~**Blokir guard-nya sendiri belum aktif di produksi** selama migrasi `20260821000001` belum dijalankan (gate UI-nya sudah aktif, itu FE).~~ **[KOREKSI 24 Agu 2026: migrasi SUDAH dijalankan (21 Agu) — guard-nya AKTIF di produksi. TD-206 kini punya konsekuensi nyata, bukan lagi teoretis.]**
- **[24 Agu 2026, globalisasi roles → pola query FE]** ⚠️ **Memfilter `.eq('company_id', …)` pada `roles`/`departments`/`positions`/`branches` SEKARANG SELALU salah** — keempat tabel sudah global (`company_id = NULL`) sejak migrasi `20260821000002`/`…000003` (21 Agu); query yang masih memfilternya mengembalikan **nol baris tanpa error** (gagal senyap). Scoping pindah ke **`user_roles.company_id`** (masih `NOT NULL`) — **jangan ikut dicabut**. Pola benar + jebakannya: `03_DATA_MODEL.md` **gotcha #18**. **TD-207** (3 query CRM, RESOLVED — roster salesperson/assignee + notifikasi approver Lead Pool yang diam-diam mati 21→24 Agu). **TD-209** (**HIGH**, OPEN — **7 titik lain belum diperbaiki**; yang paling perlu dicek duluan: **dropdown Role/Departemen/Posisi/Cabang di halaman User Access & User Edit** [`useUserAccess.js:233/245/257/269`] — kalau kosong, **tak ada role yang bisa di-assign lewat UI**; menyusul: notifikasi approver HRGA, eskalasi CEO BNF, notifikasi CEO MOM, roster Sales Report `CRMReportPage.jsx:161-162` yang **jalan untuk super_admin tapi kosong untuk role lain**). ⚠️ **Status TD-209 = deduksi kode, BELUM tes runtime.** **TD-208** (MEDIUM, OPEN — nol file CRM yang mengonsumsi `activeCompanyId`; efeknya **baru muncul setelah fix TD-207**: user multi-entitas dapat roster entitas HOME, bukan yang dipilih di CompanySwitcher. **Jangan diperbaiki sepotong-sepotong.**)
- **[2 Sep 2026, Item SP Security + Finance level SP]** ⚠️ **Ada TIGA sumbu izin berbeda yang bertemu di halaman Detail SP — jangan digabung jadi satu flag:** `canWarehouseOps` (BTB, generate picking, tenggat SP, TTF — **tak berubah**) · **`canWriteSpItem`** (baru, cermin `is_sp_item_writer()` = super_admin/admin/manager/**operations**; ceo/gm/gm_bd VIEW-ONLY) · **`canEditFinanceDocs`** (baru, cermin `set_sp_finance_docs` = super_admin/finance_controller/finance, **tanpa manager** — matrix menaruh manager di R untuk Finance). Ketiga role `ceo`/`gm`/`gm_bd` kini **konsisten antara matrix dan kode**: `ceo`/`gm_bd` sudah lama R/tanpa-akses di matrix (kode menyusul), dan **baris `gm` pada matrix Logistics diubah CRUD → R per keputusan Den 2 Sep 2026** (matrix menyusul kode). Nol divergensi tersisa di ketiganya. Blast radius nyata 1 user (`gm_bd`). Role `supervisor` **sengaja tak dimasukkan** (tak eksis di tabel `roles`, TD-106 — memasukkannya = izin nganggur). **6 kolom finance (`inv`/`fp`/`submit`/`kirim`/`submit_date`/`email_status`) kini di header `sp_orders` dan HANYA boleh ditulis `set_sp_finance_docs()`** — ia men-sync turun ke SEMUA `sp_items` se-SP, dan sinkronisasi itulah yang menjaga `groupBySP`/`financePct`/KPI FinancePage tetap benar; `update_sp_item_dual` sudah dicabut haknya (pola sama dgn `expired_date`/TD-201). Efek samping disetujui: **`financePct` kini biner per-SP** (0/25/50/75/100%), nilai spt 60% mustahil lagi. **RLS `sp_items` DELETE tak lagi `USING(true)`** (di-`REVOKE` + policy dipersempit) — **SELECT/INSERT/UPDATE-nya masih longgar**, TD-173 utuh. **TD-180 tetap PARTIAL**: 6 policy `sp_btb`/`dc_master` ditutup, tapi sisir baru menemukan **198 policy varian tunggal di 76 tabel** (`payment_terms_*` masih OPEN) — ⚠️ dua kali berturut-turut (18 Agu, 2 Sep) kelas bug ini ketahuan dari **keluhan user**, bukan audit, karena gagalnya senyap (**nol baris / embed NULL, BUKAN error**). **+TD-215** (MEDIUM — `FinancePage`/`OutstandingPage` kini menampilkan baris berulang per-item bernilai identik; konsekuensi SADAR, bukan regresi) **+TD-216** (LOW, kosmetik). ⚠️ **Fase 0 (gate tulis/hapus item SP) NOL tes runtime** — hanya Fase 1 yang dikonfirmasi lolos smoke test produksi.
- **[5 Sep 2026, Laporan Per Barang Storbit]** **✅ [KOREKSI 11 Sep 2026: TD-217 & TD-218 RESOLVED — migrasi `20260910000001_finance_read_access` LIVE, terverifikasi akun Elvira 11 Sep; `sp_invoices_insert`/`_update` tetap singular (masuk sisir TD-180). Paragraf di bawah dipertahankan sebagai jejak — JANGAN dibaca sebagai bug terbuka.]** ⚠️ **Dua bug akses RLS BARU, keduanya gagal SENYAP dan keduanya ketahuan dari PEMAKAIAN NYATA (akun Elvira Nurhuda), bukan audit — ini kali ketiga berturut-turut (18 Agu → 2 Sep → 5 Sep).** **TD-217 (HIGH, OPEN)** — `prospects_read` (tabel `accounts`) tak memuat role `finance` maupun `finance_controller`, dan keduanya juga tak lolos `is_manager_or_above()` → **Finance TIDAK BISA membaca nama customer sama sekali**; kolom **CUSTOMER tampil KOSONG di SELURUH tabel Storbit**, termasuk tabel lama yang sudah ada jauh sebelum sesi ini (**bukan regresi, cuma baru ketahuan**). Sudah dikonfirmasi ke Den: Finance **SEHARUSNYA** bisa membaca → **KELUPAAN, bukan by-design**. Usulan: ikut pola `operations`, dibatasi `account_status='customer'`; masuk batch besar RLS (TD-173/TD-39). **TD-218 (HIGH, OPEN)** — `sp_invoices_read` masih `get_user_company_id()` **singular** (`schema_snapshot.sql:17867`): Elvira punya role aktif di SOA tapi home MSI, jadi kartu **Outstanding Piutang tampil Rp 0** untuknya sementara Super Admin melihat Rp 47,3 jt. **Instance konkret ketiga TD-180** — jangan digarap sendirian, satukan dgn sisir sistematis 198 policy / 76 tabel. **TD-219 (LOW, dimitigasi)** — ligature `fi` `@react-pdf/renderer` 4.5.1 **tak bisa dimatikan lewat style prop apa pun** (`getFragments` tak pernah mengisi atribut `features`); PDF-nya sendiri BENAR (`ToUnicode` → `<0066 0069>`, copy-paste tetap "defisit"), murni keterbacaan di px(8.5)–px(9). Solusi: pecah run shaping lewat `<Text>` bersarang (helper `noLig`). ⚠️ **ZWNJ U+200C TIDAK aman** — memetakan ke glyph `space`, hasilnya "def isit". **TD-220 (LOW)** — `exceljs ^4.4.0` → 2 advisory moderate lewat `uuid <11.1.1`; **jangan `npm audit fix --force`** (men-downgrade ke `exceljs@3.4.0`, breaking). **Keputusan desain yang JANGAN dibalik tanpa sengaja:** sumber angka laporan = **`sp_items`** bukan `sp_order_items` (divergensi diukur 0 baris, risikonya teoretis — **jangan "diperbaiki" sebagai bagian laporan**) · syarat "invoice hidup" **hanya `status <> 'void'` TANPA `deleted_at`**, sengaja identik dgn guard `create_invoice` · **PPN `1.11` LITERAL** mencerminkan `0.11` literal di `create_invoice` (dua tempat, gerakkan bersama) · satuan **`unit` primer / `uom` fallback** wajib `NULLIF(btrim(…),'')` karena 2 produk ber-`unit` **string kosong** · filter `NOT IN ('CANCELLED','DRAFT')` saat ini **INERT** (0 baris) tapi **tetap dipertahankan sbg penjaga**. **Sudah diselidiki, JANGAN diulang:** 21 SP `MENUNGGU_KONFIRMASI_DC` dgn `shipped_qty = qty` **BENAR by design** (SJ masih `in_transit`, belum `delivered`). ⚠️ **`get_table_columns` sudah tak lagi ter-GRANT ke `anon`** — perubahan DB nyata tanpa migrasi yang merekamnya, **perlu konfirmasi siapa/kapan** (TD-24 dikoreksi 3 RPC → 2).
- **[7 Sep 2026, PRF + Storbit]** ⚠️ **TD-180 instance KEEMPAT — dan yang PERTAMA memblokir KERAS** (`RAISE EXCEPTION`, bukan gagal senyap): 4 RPC PRF menolak procurement lintas-entitas di **seluruh 285 PRF**. **Ditutup** migrasi `20260907000001` (LIVE + terverifikasi runtime), **tapi TD-180 TETAP PARTIAL** — 198 policy / 76 tabel belum disisir. ⚠️⚠️ **Yang wajib dibawa ke sisir berikutnya: `20260821000004` sudah memperbaiki hal yang sama untuk dua orang yang sama tapi hanya menyentuh policy, NOL RPC — keempat fungsi `SECURITY DEFINER` tak ikut terbantu dan bug bertahan 17 hari.** Sisirnya harus mencakup `pg_proc.prosrc`, bukan cuma `pg_policies.qual` (`03_DATA_MODEL.md` gotcha #26). ⚠️ **Perbaikannya BUKAN tukar singular→jamak** — `has_role()`/`is_manager_or_above()` sama-sama mengabaikan company, jadi tukar polos **memutus keterkaitan dua syarat**; bentuk benar = **uji role di entitas baris itu**. **TD-231** (MEDIUM, baru — `is_bnf_authorized()` **guard singular sejati** DAN **satu-satunya guard tanpa `SET search_path`**; kerabat sekelas yang BELUM ditutup — bagian `search_path`-nya murah & nol perubahan perilaku, bagian `company_id`-nya **mengubah cakupan akses** jadi ukur blast radius dulu). **TD-232** (LOW, baru — 4 RPC PRF tanpa `REVOKE ALL FROM PUBLIC`; tak eksploitatif karena `auth.uid()` NULL utk `anon`, tapi perlindungannya bersandar pada **isi fungsi, bukan izin**). **TD-233** (MEDIUM, baru — daftar role manajerial kini hidup di **EMPAT tempat**: `is_manager_or_above()`, `mark_delivery_delivered`, `prf_release`, `prf_select_offer`. ⚠️ **Duplikasinya DISENGAJA** — itu harga supaya role bisa diikat ke entitas; **JANGAN "perbaiki" dgn mengembalikan panggilan ke `is_manager_or_above()`**, itu membatalkan alasannya. Perlakukan sbg **checklist**: ubah daftar role = sentuh keempatnya dalam satu migrasi). **TD-234** (MEDIUM, baru — laporan lintas-produk butuh **RPC agregat**, bukan ulang RPC per-produk 38×; mitigasi murah = naikkan `TOP_PRODUCT_ROWS`). **TD-235** (LOW, baru — baris SP & baris produknya di `RekapSection` PDF **tanpa `wrap={false}` bersama**; **sengaja tidak ditambal**, perbaikannya bisa memaksa halaman baru). **TD-217 dipersempit:** hanya mengenai **Finance** — `ceo`/`gm`/`gm_bd`/`manager` sudah lolos lewat `is_manager_or_above()`. **Keputusan desain yang JANGAN dibalik tanpa sengaja:** 4 kunci status **sengaja tanpa `_value`** · 3 kategori rekap **sengaja `NULL` bukan 0** (jangan dikoersi — `SUM()`/`reduce()` diam-diam menganggapnya nol) · `btb_terbit_value` ≠ Outstanding Tagih **by design** (selisih `shipping_price`) · ketiga dropdown cakupan **sengaja tanpa opsi "semua"** (kategori saling tumpang tindih). **Sudah diselidiki, JANGAN diulang:** 33 SP `BTB_TERBIT` ber-`shipped_qty`=0 (**belum dipastikan by design** → `09_ROADMAP.md` **#41**) · SP `SOA-0001` **BUKAN data uji** · selisih rekap Gudang 53 vs Nexus 44 = **cara memotongnya berbeda**. ✅ **Tabrakan nomor migrasi lintas-branch — SUDAH DITUTUP 8 Sep 2026.** `20260907000001` & `20260907000002` sempat dipakai dua kali (`main` = PRF/Storbit; branch `feature/crm-v3-batch-persiapan` = lifecycle) sehingga rujukan ke nomor itu berhenti punya arti tunggal. **Sisi branch dinomori ulang jadi `20260908000001`/`20260908000002`** — **penomoran `main` yang menang, branch yang mengalah**, aturan yang sama dengan renomori TD-215…221 (6 Sep). Nol baris SQL berubah, header `Status:` `_dual_write` tetap LIVE. ⚠️ **KOREKSI 8 Sep 2026 — klaim semula MENYESATKAN.** Yang tertulis semula: *"rujukan di KEDUA sisi sudah disunting (12 di `main`, nol di branch)"*. Angka **nol** di sisi branch itu **BUKAN** berarti "tidak ada yang perlu disunting", melainkan **BELUM DISISIR**. `569e5c6` hanya mengganti **nama file** + **satu baris** header `-- Migration:` per file (2 insertions / 2 deletions untuk dua file); badan filenya tak tersentuh, menyisakan **18 rujukan `20260907*` di 15 baris** (9 di `_dual_write`, 9 di `_drop_legacy`). Bahayanya nyata, bukan kerapian: di `main` kedua prefix lama itu **milik migrasi lain** (`_prf_multi_company_guard` / `_storbit_nilai_per_status`), jadi rujukan yang tertinggal menunjuk file yang **isinya beda total**. **Disisir 8 Sep 2026** (dikerjakan di branch `feature/crm-v3-batch-persiapan`, tempat kedua file migrasi itu hidup). Angka **12 di `main` memang benar sudah disunting** (`PROGRESS.md` + `09_ROADMAP.md` lewat `a5ec55c`) — `main` tak punya file migrasi lifecycle-nya sama sekali, jadi di sini yang bisa basi hanya dokumen. **5 rujukan nomor lama di `08_TECH_DEBT.md` §Renomori MIGRASI SENGAJA dipertahankan** — itu peta lama→baru, nomor lamanya harus tetap terbaca. ⚠️⚠️ **LANGKAH MANUAL DB BELUM JALAN:** menyunting file migrasi **TIDAK** memperbaiki COMMENT yang sudah mendarat. `_dual_write` dijalankan 7 Sep saat masih bernomor lama, jadi **3 COMMENT di skema produksi masih menyebut `20260907000002`** — `sync_lifecycle_columns()`, `accounts.account_status`, `accounts.lifecycle_stage` (terbukti `schema_snapshot.sql` baris 4065/4280/4287, refresh `af18626`). Perlu **3 `COMMENT ON` manual di SQL Editor + refresh snapshot**. ⚠️ **Acuan teksnya PRODUKSI, BUKAN file migrasi:** COMMENT `accounts.lifecycle_stage` di produksi sudah **berbeda** dari file (produksi merujuk **Keputusan Terbuka #37**; teks itu **tidak ada di file `.sql`/`.md` mana pun di repo**, jadi ia lahir dari penyuntingan manual saat eksekusi yang tak pernah dibalikkan ke file). Menyalin dari file migrasi akan **diam-diam membatalkan** perbaikan itu. Dua COMMENT lainnya identik file↔produksi. Peta lama→baru: `08_TECH_DEBT.md` §Renomori MIGRASI (sisi branch) · `09_ROADMAP.md` §TABRAKAN NOMOR MIGRASI. ⚠️ **Pelajarannya tetap berlaku:** dua jalur doc-keeper menomori per-tanggal di hari yang sama tanpa saling melihat, jadi **polanya bisa terulang** setiap dua branch aktif menulis migrasi di tanggal yang sama — konvensi penomorannya sendiri belum diperlebar.
- **✅ [KOREKSI 18 Sep 2026: keempat butir (1)-(4) SUDAH DIKERJAKAN di hari merge dan TD-19 RESOLVED — lihat entri Recent 18 Sep; sesi 11-13 Sep MASIH belum terdokumentasi (peringatan terakhir bullet ini tetap berlaku).]** **[14 Sep 2026, pra-merge CRM v3 + TD-19]** ⛔ **Empat hal yang MENGIKAT hari merge `feature/crm-v3-batch-persiapan`** (lampiran per-file: `09_ROADMAP.md` §Pekerjaan Sinkron Branch): **(1)** `DealDetailPage.jsx:752` (branch) `canCreatePRF … ['sales', …]` **lolos auto-merge TANPA konflik** (main memperbaiki baris yang sama di posisi lain) → tombol Buat PRF hilang untuk semua sales; ganti `canCreatePrf(erpRoles)`. **(2)** Tiga file **ReferenceError/white-screen** kalau blok konfliknya diselesaikan "ambil main" mentah — `DealDetailPage` (`MANAGER_OR_ABOVE` dihapus main, pemakainya di luar blok), `CRMDashboardPage` (destructure `useAuth()` **wajib gabung** `{ profile, erpRole, erpRoles, activeCompanyId }`), `InquiryListPage` (`canScopeToggle`). **(3)** `20260830000001_crm_menu_permissions_sales` **SUPERSEDED — jangan dijalankan** (role `sales` nol pemegang; 6 key CRM sudah diseed `bd_*` oleh `20260912000005`). **(4)** `20260830000003_inquiries_rls_owner_based` (branch, dikoreksi `1cb2edd`) **hanya boleh jalan serempak** dengan ganti `p_scope_own` `created_by`→`owner_id` di `crm_stage_conversion`/`crm_stage_age` (TD-249, branch) — dan **bandingkan dulu dengan `pg_policies` produksi** (gotcha #35: file ini sempat basi 15 hari, akan menimpa `is_procurement_functional()`). ⚠️ **TD-19 BELUM ditutup** — `20260914000001_drop_customers_table` ditulis tapi **BELUM dijalankan**; guard-nya menolak jalan tanpa `customers_backup_20260614` di DB yang sama (baru ada di staging, 0 baris; produksi belum, akan 2 baris); deploy `58643a3` (SchemaManager tanpa `customers`) harus LIVE lebih dulu (belum terverifikasi). ⚠️ **Sesi 11 (sore)–13 Sep belum terdokumentasi** — `04_ROLE_PERMISSION_MATRIX.md` (14 kode role), TD-233 (daftar nama manajerial), dan #52 (`supervisor` tak ada di `roles`) **basi** oleh `roles.level` + 11 role baru; jangan dipakai sebagai dasar keputusan sebelum sesi-sesi itu dicatat.
- **⚠️ [18 Sep 2026: snapshot `9f6e232` MEMUAT objek migrasi ini — indikasi kuat sudah jalan di produksi, TANPA laporan eksekusi; PERLU KONFIRMASI DEN sebelum bullet ini dianggap tertutup.]** **[15 Sep 2026, Penerimaan Barang]** ⛔ **`PenerimaanBarangPage.jsx` di `main` memanggil RPC `create_goods_receipt` yang di PRODUKSI belum ada** — migrasi `20260914000002` baru LIVE di **staging** (`Status:` header `:3`). Sampai dijalankan di produksi, Konfirmasi di produksi gagal keras di tiap submit (`function create_goods_receipt does not exist`), dan halaman **tak punya fallback** ke jalur lama (INSERT langsung `stock_ledger` sudah dihapus — sengaja). **Jalankan migrasi produksi SEBELUM atau segera sesudah deploy** (TD-261 catatan; gotcha #36); lalu langkah terpisah `REVOKE INSERT ON stock_ledger FROM authenticated` (ekor migrasi `:388`, prasyaratnya sudah terpenuhi) + refresh snapshot. ⚠️ Instance baru **TD-180**: 4 policy + 2 guard RPC singular di migrasi ini — user ber-home ≠ SOA dengan role `operations`/manager di SOA **ditolak keras**. ⚠️ Temuan laten (belum TD/Keputusan): default privileges DB memberi `TRUNCATE/REFERENCES/TRIGGER` ke `authenticated` untuk tiap tabel baru; hari ini 99 tabel `GRANT ALL` + 29 `TRUNCATE` eksplisit (TRUNCATE tak tunduk RLS; paparan PostgREST nol, `exec_sql` service_role-only) — migrasi ini hanya mencabutnya untuk 2 tabel barunya.
- **[18 Sep 2026, pasca-sinkron CRM v3 + SJ `signed_date`]** ⛔ **TD-265 (HIGH, pekerjaan tertunda PRIORITAS): STAGING belum punya `delivery_notes.signed_date` maupun overload `mark_delivery_delivered(uuid, date)`** — FE `main` (`db.js:828` selalu memanggil versi 2-argumen) di staging **gagal keras** di "Tandai Terkirim" sampai `20260917000001` dijalankan di sana; kebalikan kasus TD-261/gotcha #36 (di sini produksi yang duluan). **TD-263 (MEDIUM):** overload LAMA `mark_delivery_delivered(uuid)` masih hidup (snapshot `:3037`) dengan ACL `GRANT ALL … TO authenticated` **tanpa `REVOKE … FROM PUBLIC`** (`:20062`) dan badannya **tak mensyaratkan `signed_date`** → siapa pun bisa menandai terkirim tanpa tanggal lewat panggilan 1-argumen; panggilan yang hanya mengirim `p_delivery_note_id` **ambigu** antar-overload. DROP-nya = **Keputusan Terbuka #59**. **TD-264 (LOW):** cek "tidak masa depan" di RPC memakai `current_date` **UTC** — 00:00-06:59 WIB ditolak kalau RPC dipanggil langsung; FE sudah WIB (`getTodayWIB`). **TD-262 (MEDIUM):** whitelist status `canGeneratePicking` (`SalesOrderDetailPage.jsx:1535`) hardcode & sudah dua kali ketinggalan status (26 Agu, 17 Sep); backend `generate_picking_from_sp` tak mengecek `sp_orders.status` → ganti ke cek langsung (ada outstanding + tak ada picking aktif); `INVOICED`/`SUBMITTED` = **#57** (Finance); efek samping: headline Detail SP tetap "BTB Terbit" saat picking/pengiriman susulan — progres hanya di tab Shipment/Items. ⚠️ **Kepemilikan deal (`inquiries.owner_id`) kini sumbu RLS di produksi** (`inquiries_read`/`inquiries_update` `owner_id = auth.uid()`, `20260830000003` LIVE) — filter FE `created_by` di CRM adalah sumbu LAMA (TD-223); **`quotations` tetap `created_by`** (tabel itu tak punya `owner_id`). **Nol pertahanan lapis DB untuk `owner_id` kosong** (tanpa DEFAULT/trigger) — insiden 121 baris 7-17 Sep terjadi karena satu jalur INSERT FE lupa mengisinya; kalau ada jalur INSERT baru (RPC/import), bisa terulang → **#58**. ⛔ **JANGAN jalankan ulang `20260909000006`** (mengembalikan `p_scope_own` ke `created_by`, memutus pasangan TD-249). ⚠️ **PERLU KONFIRMASI DEN: status `20260914000002` (`create_goods_receipt`) di PRODUKSI** — snapshot `9f6e232` memuat objeknya (`goods_receipts` `:7231`, RPC `:338`, trigger `:12976`) padahal 15 Sep dicatat staging-only dan tak ada laporan eksekusi; sampai dikonfirmasi, bullet 15 Sep di bawah tetap berlaku. `REVOKE INSERT ON stock_ledger FROM authenticated` **belum** (snapshot `:21751` masih `GRANT ALL`). **Batch 3C** (pemangkasan nilai legacy `pipeline_stage` + CHECK) **belum dikerjakan** — prasyaratnya (`pipeline_stage` beku) baru terpenuhi 18 Sep; `20260908000002_accounts_lifecycle_drop_legacy` tetap ⛔ STOP (TD-246 `prospects_read` cabang `operations` masih `account_status`).
- **MEDIUM/LOW** — Dua sistem permission belum sinkron (TD-06, **PARTIAL 7 Agu 2026**: axis FE kini satu — `role_permissions`/`hasPermission()` pensiun dari kode, `hasMenuPermission` dapat resolusi 3-tingkat baru `role_menu_permissions`+`user_menu_permissions.effect` — axis RLS tetap terpisah, TD-01 utuh; **[koreksi 9 Agu 2026]** klaim "satu axis" cuma benar utk menu-gating — sistem KETIGA `can()`/`PERMISSIONS` yang independen dari keduanya masih hidup, lihat TD-182 di atas; **[20 Agu 2026] sistem lama kini dihapus FISIK**, jadi "satu axis FE utk menu-gating" benar secara struktural — TD-06 tetap PARTIAL krn axis RLS & `can()` tak berubah). Tabel dormant `sales_calls`/`sales_visits`/~~`customers`~~ + kolom `profiles.role` belum di-drop (TD-18/~~19~~/20 — **`customers` SUDAH DI-DROP 18 Sep 2026, TD-19 RESOLVED**); App.jsx god-file **5.530 baris** (~~5.421~~ ~~5.274~~ — angka lama basi, dihitung ulang doc-keeper 2 Sep 2026 via `wc -l`; pecah setelah ada test; TD-12). **[2 Sep 2026] TD-20 RESOLVED** — kolom `profiles.role` + enum `user_role_legacy` ternyata SUDAH di-drop (terbukti dari snapshot: `profiles` 27 kolom tanpa `role`, nol `CREATE TYPE user_role_legacy`); ⚠️ kapan persisnya dieksekusi tak diketahui. `sales_calls`/`sales_visits`/`customers` (TD-18/19) tetap dorman.
- **FLAG (RBAC — JANGAN diperbaiki sekarang; perlu bahas terpisah)** — Menu `input` (Input SP) di-gate `module:'logistics'` → `hasPermission('logistics','view')`, dan role-def-nya masih meng-include `sales`. Konsekuensi: kalau RBAC grant `logistics.view` ke role **sales**, sales bisa lihat "Input SP" (sidebar) + "Buat SP" (home) → mungkin **tak diinginkan** (sales harusnya tak bikin SP). **Ini isu konfig data-permission/RLS (di luar scope FE)**, bukan bug home-gating (home 3.0C sudah konsisten dgn sidebar). Perbaikan = ubah role-def/permission `logistics.view` utk sales, berisiko → tunda sampai dibahas. Selain itu halaman `InputPage`/`InputSPPage` **tak punya page-level guard** (hanya `canAccessActiveMenu` yg kasar) → idealnya tambah guard halaman saat RBAC dirapikan. **⚠️ [VERIFIKASI 7 Agu 2026, doc-keeper — kemungkinan STALE, PERLU KONFIRMASI]** Diverifikasi grep `src/App.jsx` hari ini (turunan kerja retirement `hasPermission`): kedua definisi menu `id:'input'` (baris ~620, ~1001) **tidak punya properti `module:` sama sekali**, dan `MENU_KEY_MAP['input'] = 'logistics_input'` — jalur gate AKTUAL saat ini tampak `hasMenuPermission('logistics_input','view')`, BUKAN `hasPermission('logistics','view')` seperti tertulis di atas. Belum ditelusuri KAPAN mekanismenya berubah (kemungkinan besar sudah stale SEBELUM sesi 7 Agu, bukan akibat kerja hari ini — dead branch `hasPermission` fallback di `canSeeMenuItem` yang dihapus hari ini sudah dikonfirmasi 0 menu items yang masih memicunya). **Paragraf di atas TIDAK diubah/dihapus** (menghormati "JANGAN diperbaiki sekarang") — hanya ditandai perlu verifikasi ulang sebelum dipakai sbg dasar keputusan. **⚠️ [VERIFIKASI 20 Agu 2026, doc-keeper — FLAG ini kini DEFINITIF STALE di DUA titik, bukan lagi "kemungkinan"]** **(a)** Mekanisme `hasPermission('logistics','view')` yang jadi premis seluruh paragraf **sudah tidak ada sama sekali** — fungsinya dihapus fisik dari `AuthContext.jsx` (sesi 20 Agu, lihat entri Recent teratas), dan **nol** item menu yang punya properti `module:` (`grep "module: '" src/App.jsx` = 0 hit). Jalur gate aktual = `hasMenuPermission('logistics_input','view')` (`MENU_KEY_MAP` `App.jsx:1278`). **(b)** Klaim *"halaman `InputPage`/`InputSPPage` tak punya page-level guard (hanya `canAccessActiveMenu` yg kasar)"` **juga stale** — guard `canInputSP` (`App.jsx:1879`) ADA dan menuntut **DUA syarat sekaligus**: `canRenderPage('input')` **DAN** `role ∈ ['admin','manager','operations','super_admin']` — daftar itu **tidak memuat `sales`**. Jadi kekhawatiran inti FLAG ("sales bisa bikin SP") kini tertahan di titik render, walau item sidebar-nya bisa saja tetap terlihat kalau `logistics_input.view` di-grant. Guard itu masuk `47db537` (1 Jul 2026, *"page-level guards for Input SP + Shipment/Finance/Outstanding (F5)"*) — artinya **FLAG ini sudah stale sejak sebelum catatan 7 Agu ditulis**. **Paragraf asli TETAP tidak diubah** — tapi jangan dipakai sebagai dasar keputusan tanpa membaca dua koreksi ini lebih dulu; kalau dibahas, yang perlu diputuskan tinggal soal **visibilitas menu** untuk `sales`, bukan kemampuan membuat SP.
