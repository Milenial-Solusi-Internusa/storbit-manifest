# ROLE PERMISSION MATRIX — Nexus by MSI

> Matrix RBAC. Sumber: `CLAUDE.md` (Roles & Permission Structure, kini di `docs/00_DEV_JOURNEY.md`), `docs/03_DATA_MODEL.md` (RLS), `docs/08_TECH_DEBT.md` (gaps). ⚠️ RLS DB belum sepenuhnya sinkron dengan matrix UI — lihat §5.

---

## 1. Daftar Role

14 **ERP role** (sumber: tabel `roles`, di-assign via `user_roles`). ~~Struktur sama di 3 entitas kecuali `gm_bd` (baru — MSI single-entity; lihat catatan).~~ **[KOREKSI 24 Agu 2026 — kalimat di atas sudah tidak akurat sejak 21 Agu 2026]** keempat belas kode role kini **GLOBAL**: satu baris per kode ber-`roles.company_id = NULL` (berlaku Group-wide), duplikat per-company sudah di-soft-delete, referensi di-repoint ke survivor (migrasi `20260821000003_globalize_roles.sql`; detail skema: `03_DATA_MODEL.md` §3 RBAC + gotcha #18). Yang **menentukan entitas** sekarang **bukan lagi baris `roles`**, melainkan baris **`user_roles.company_id`** (masih `NOT NULL`). ⚠️ **Efek praktisnya tetap sama untuk `gm_bd`** — ia cuma muncul untuk user MSI karena hanya user MSI yang punya baris `user_roles` gm_bd, **bukan** lagi karena baris `roles`-nya milik MSI. ⚠️ **Kode `supervisor` tidak punya baris sama sekali di tabel `roles`** (0 baris, sebelum maupun sesudah migrasi — kondisi pre-existing), padahal ia disebut di hierarki §2 dan di beberapa daftar roster §3.1.

| Code | Nama | Level | Deskripsi |
|------|------|-------|-----------|
| `super_admin` | Super Admin | System | IT/Developer; full akses lintas-entitas (bypass RLS via `is_super_admin()`). |
| `admin` | Admin | System | Master Data admin (per-entitas). |
| `ceo` | CEO / Executive | 1 | Full view + final approve. |
| `gm` | GM / Senior GM | 2 | GM SCM (Supply Chain). Approve + report. |
| `gm_bd` | GM Business Development | 2 | GM BD; setara/dekat `gm` di hierarki, fokus commercial/CRM. Full CRM + akses reporting (incl. Riwayat Visit) + approve MOM. **TIDAK** dapat akses gudang/finance (by design). |
| `manager` | Manager | 4 | Kelola departemen + approve. |
| `finance_controller` | Finance Controller | 4 | Full akses finance. |
| `finance` | Finance Staff | 7 | Finance Jr. Manager + Staff. |
| `operations` | Operations | 7 | Logistic, Console, Warehouse. |
| `sales` | Sales / BD | 7 | BD, Sales Forwarding, Account Exec, Digital. |
| `procurement` | Procurement | 7 | Direct + Indirect Procurement. |
| `hrga` | HRGA | 7 | HRGA, Personnel, People Dev, GA. |
| `it` | IT Staff | 7 | IT Developer + Helpdesk. |
| `supervisor` | Supervisor | 6 | Supervisor lintas-dept. |
| `viewer` | Viewer | — | Read-only semua modul. |

**Catatan role lain:**
- `gm_bd` — role baru (Paket 1, 9 Jul 2026). Dieksekusi di DB MSI (tabel `roles` + permission + fungsi `is_manager_or_above()` sudah +`gm_bd`, dijalankan user manual). Single-entity MSI; RLS lintas-3-entitas = Paket 2 (belum). Prioritas frontend: disisipkan di `ERP_ROLE_PRIORITY` setelah `gm`, sebelum `manager` (`AuthContext.jsx:11`). **Roster CRM: lihat §3.1** (gm_bd boleh di-assign sbg pelaksana, TIDAK dihitung sbg performa sales). ⚠️ `gm_bd` absen dari map `PERMISSIONS` FE → `can()` = `false` untuk semua aksi; fix sudah ada (`App.jsx:289`) tapi **belum di-commit** — lihat **TD-64** / `18_…:D-01`.
- `sales_head` — muncul di fungsi RLS `is_manager_or_above()` tapi tidak ada di daftar role aktif standar. [TODO: konfirmasi apakah `sales_head` masih dipakai sebagai role aktif atau sisa migrasi].
- `operator` — adalah **position level** (Operator: driver/OB), **bukan** role RBAC.
- Legacy enum `profiles.role` (`super`/`logistic`/`management`) — **deprecated**, frontend & Edge Functions tak baca lagi (lihat TECH_DEBT TD-20).

---

## 2. Hierarki Role

```
super_admin            (System — bypass RLS lintas-entitas)
   └─ admin            (System — master data per-entitas)
        └─ ceo         (Lvl 1 — full view + final approve)
             └─ gm / gm_bd   (Lvl 2 — gm=SCM, gm_bd=BD; approve + report)
                  └─ manager / finance_controller   (Lvl 4)
                       └─ supervisor                (Lvl 6)
                            └─ sales / finance / operations / procurement / hrga / it   (Lvl 7)
                                 └─ viewer          (read-only)
```

**Cakupan akses data (RLS):**
- `super_admin` → **lintas entitas** (semua company).
- `admin` & lainnya → **single entity** (`company_id` sendiri). ⚠️ Frontend `isAllEntities=['super_admin']` saja (admin = single-entity, selaras RLS).
- `sales`/`operations` → hanya row miliknya (`assigned_to`/`created_by`).
- `manager` ke atas (`is_manager_or_above`) → seluruh entitasnya.

---

## 3. Permission Matrix per Modul

Sumber: matrix permission `CLAUDE.md`. **CRUD** = full, **R** = read-only, **-** = no access.

> **`gm_bd`** (kolom belum ditambahkan ke tabel di bawah): aksesnya kira-kira setara `gm` untuk **CRM (CRUD)** + **Reporting (R)**, TAPI **TIDAK** dapat Logistics/Finance/Assets/HRGA (by design — fokus BD). Peta visibilitas menu presisi untuk `gm_bd` ada di tabel "Menu → Halaman → Role-gate" di bawah.

| Module | super_admin | ceo | gm | manager | finance_controller | finance | operations | sales | procurement | hrga | it | viewer |
|--------|:-----------:|:---:|:--:|:-------:|:------------------:|:-------:|:----------:|:-----:|:-----------:|:----:|:--:|:------:|
| Master Data | CRUD | R | R | R | R | R | R | R | R | R | CRUD | - |
| CRM | CRUD | R | CRUD | CRUD | R | R | R | CRUD | R | - | R | R |
| Logistics | CRUD | R | R | CRUD | R | R | CRUD | R | R | - | R | R |
| Finance | CRUD | R | R | R | CRUD | CRUD | R | R | R | - | R | R |
| HRGA | CRUD | R | R | CRUD | R | R | - | - | - | CRUD | R | R |
| Assets | CRUD | R | R | R | R | R | R | R | CRUD | R | CRUD | R |
| Admin | CRUD | - | - | - | - | - | - | - | - | - | CRUD | - |

> **⚠️ Tabel di atas berbicara di level MODUL. Beberapa AKSI di dalam modul digerbang lebih sempit** oleh fungsi DB tersendiri — daftarnya di §3.0 tepat di bawah. Kalau matrix dan fungsi DB berbeda, **fungsi DB yang mengikat** (ia yang menolak di server); matrix ini pernyataan niat.

### 3.0 Override konteks-spesifik (gate DB lebih sempit dari matrix modul)

**[BARU 2 Sep 2026]** Dua gate baru lahir di sesi Item SP Security + promosi Finance. Keduanya **konteks-spesifik**: mereka TIDAK mengubah `is_manager_or_above()` (fungsi global yang dipakai banyak gate lain di luar Logistics) — itu keputusan sadar, bukan kelalaian.

| Aksi | Fungsi/guard DB | Role yang LOLOS | Catatan |
|------|-----------------|-----------------|---------|
| **Edit / Hapus baris item SP** (`update_sp_item_dual`, `delete_sp_item_dual`, policy `sp_items_delete`) | **`is_sp_item_writer()`** (migrasi `20260902000001`) | `super_admin`, `admin`, `manager`, `operations` | **`ceo`, `gm`, `gm_bd` → VIEW-ONLY** di konteks ini walau ketiganya lolos `is_manager_or_above()`. Untuk `ceo` (matrix Logistics = **R**) dan `gm_bd` (matrix = **tanpa akses Logistics**) ini **menghapus divergensi lama** — kode akhirnya menyusul matrix. Untuk `gm` arahnya sempat sebaliknya (matrix dulu menulis Logistics = CRUD, gate ini menolaknya): **[DISELESAIKAN 2 Sep 2026 — keputusan Den] baris `gm` pada tabel modul §3 diubah CRUD → R, matrix disesuaikan ke kode.** Jadi kini tak ada lagi divergensi tersisa di ketiga role — bukan lagi "belum diputuskan". **Blast radius nyata: 1 user** (ber-role `gm_bd`) — nol user aktif ber-role `ceo`/`gm`. **`supervisor` sengaja TIDAK dimasukkan** ke daftar lolos: role itu tak eksis di tabel `roles` (TD-106), memasukkannya = izin nganggur yang otomatis menyala kalau role itu kelak dibuat. Cermin FE: `canWriteSpItem` (`SalesOrderDetailPage.jsx:1040`). |
| **Ubah status dokumen finance SP** (`set_sp_finance_docs` — `inv`/`fp`/`submit`/`kirim`/`submit_date`/`email_status`) | **`set_sp_finance_docs`** (migrasi `20260902000004`) | `super_admin`, `finance_controller`, `finance` | **SENGAJA TANPA `is_manager_or_above()`** — matrix baris **Finance** menaruh `manager` di **R**, bukan CRUD, jadi guard ini konsisten dengan matrix. Sumbu FINANCE, **bukan** sumbu gudang: ini bukan superset maupun subset `is_sp_item_writer()` — dua sumbu berbeda yang kebetulan bertemu di halaman yang sama, **jangan digabung jadi satu flag**. Freeze bila SP `CANCELLED`. Cermin FE: `canEditFinanceDocs` (`SalesOrderDetailPage.jsx:1049`). |

> **Bug yang ditutup gate kedua:** sejak 25 Agu 2026, `FinanceModal` (halaman Finance/Outstanding) memanggil `update_sp_item_dual` yang guard-nya menolak `finance` → **Finance bisa membuka & men-toggle, tapi Save PASTI gagal server-side, tanpa UI menjelaskan kenapa**. Efek samping tak sengaja dari migrasi security yang niatnya cuma mengunci qty/harga. Modal-nya kini di-repoint ke `set_sp_finance_docs`, bukan dihapus.

### 3.1 Roster CRM — OPERASIONAL vs LAPORAN (dua konsep, sengaja terpisah)

> **Ini BUKAN gate menu** (siapa boleh buka halaman), tapi **isi dropdown/agregasi** (siapa muncul sbg pilihan / siapa dihitung). Keputusan bisnis, 17 Jul 2026 — **jangan disatukan.**

| Roster | Kriteria (`roles.code`) | Sumber | Dipakai di | `gm_bd`? |
|---|---|---|---|---|
| **OPERASIONAL** — siapa yang boleh **di-assign / dipilih sbg pelaksana** | `['sales','gm_bd']` = `OPERATIONAL_ROSTER_ROLES` | **`src/modules/crm/salesRoster.js`** → `fetchOperationalRoster(companyId)` | dropdown salesperson visit (`CRMDashboardPage` AddVisitModal) + filter dashboard · assignee `ActivitiesPage` · salesperson `SalesCallsPage` (dead code, TD-69) · filter `ActivityLogPage` | ✅ **MASUK** (BD ikut visit customer) |
| **ASSIGNEE DEAL** — siapa boleh **pegang deal** | `['sales','manager','supervisor','gm_bd']` | **`DealPanels.jsx:138` `fetchAssignees`** (**daftar sendiri**, lebih luas) — ⚠️ lokasi lama `DealDetailPage.jsx:105` sudah tidak akurat; file itu kini **mengimpor**-nya (`:28`) | dropdown assignee EditDealModal → `accounts.assigned_to`. **DUA konsumen**, bukan satu: `DealDetailPage.jsx:440` **dan** `CustomerDetailPage.jsx:1026` | ✅ **MASUK** |
| **KAM** — siapa boleh **pegang akun strategis** | `['sales','manager','gm_bd']` | `StrategicHandoverModal.jsx:75` (**daftar sendiri**, lebih luas) — ⚠️ **komponennya YATIM per 24 Agu 2026** (nol importer; komentar `PipelineKanbanPage.jsx:633`: *"TIDAK dihapus, hanya tidak lagi dipanggil dari sini"*) **dan masih memakai filter `roles.company_id` yang sudah mati** — sengaja tak diperbaiki (TD-207) | dropdown KAM di form Strategic Handover (deal WON > Rp100jt) — **tak ada pintu masuknya hari ini** | ✅ **MASUK** — **kontrak mewajibkan BD memegang customer tier A** |
| **LAPORAN** — performa sales siapa yang **DIHITUNG** | `['sales','supervisor','manager']` | `CRMReportPage.jsx:159` `fetchReportSales` (query `roles` di `:161-162`) | Sales Report (KPI / trend / per-sales). ⚠️ **[24 Agu 2026] MASIH RUSAK, sengaja tak ikut diperbaiki** — filternya bersyarat (`if (!isSuper) rolesQ = rolesQ.eq("company_id", companyId)`), jadi **super_admin masih jalan sementara role lain dapat roster KOSONG**; mudah dikira masalah hak akses (`08_TECH_DEBT.md` **TD-209** titik 8) | ❌ **TIDAK** — **BD tidak dihitung sbg performa sales** |

**Ringkas: `gm_bd` ada di 3 dari 4 roster** — semua roster **operasional/assignment** (operasional, assignee deal, KAM). Yang satu (**LAPORAN**) **dikecualikan dengan sengaja**, bukan kelupaan.

- **Kenapa dipisah:** menyatukannya = diam-diam memasukkan BD ke **angka Sales Report**. Menambah `gm_bd` ke roster laporan **mengubah angka**, jadi itu keputusan bisnis, bukan refactor.
- **⚠️ Kenapa `DealDetailPage` & `StrategicHandoverModal` TAK memakai helper** (perangkap nyata — jangan "dirapikan" jadi satu): daftar keduanya **lebih luas** dari `OPERATIONAL_ROSTER_ROLES` (`['sales','gm_bd']`). Ditukar helper → **`DealDetailPage`: manager & supervisor hilang** · **`StrategicHandoverModal`: manager hilang** dari dropdown KAM (regresi senyap — dropdown tetap terisi, cuma kurang orang). Keduanya sudah diberi komentar peringatan di kode (`DealPanels.jsx:133-136` — ⚠️ nomor lama `DealDetailPage.jsx:99-102` sudah bergeser, fungsinya pindah file; `StrategicHandoverModal.jsx:70-74`). Pola: **role ditambahkan ke daftar milik masing-masing**, bukan disatukan ke helper.
- **Scope entitas [DIREVISI 24 Agu 2026 — cara kerjanya berubah, hasilnya tidak]:** roster **tetap company-scoped**, tapi scoping-nya kini **HANYA di `user_roles.company_id`** — langkah `roles` **tidak boleh lagi** memfilter `company_id` (baris role sudah global ber-`company_id = NULL` sejak 21 Agu 2026, jadi filter itu mengembalikan **nol baris** dan seluruh dropdown ikut kosong secara senyap). Pola yang benar: `roles` → `.in('code', …)` + `.is('deleted_at', null)`; `user_roles` → `.eq('company_id', …)` + `.eq('is_active', true)` + `.is('revoked_at', null)`; `profiles` → `.eq('active', true)`. `gm_bd` **tetap cuma muncul untuk user MSI**, sebabnya saja yang bergeser (lihat §1). ⚠️ Riwayat & sisa titik yang belum diperbaiki: `08_TECH_DEBT.md` **TD-207**/**TD-209**; gotcha lengkapnya `03_DATA_MODEL.md` #18.
- **Menambah role ke roster operasional:** ubah `OPERATIONAL_ROSTER_ROLES` di `salesRoster.js` — **satu tempat**. (Sebelum 17 Jul: fungsi `fetchSalesProfiles` di-copy-paste identik di 4 file, semuanya hardcode `'sales'` → itu sebabnya `gm_bd` tak pernah muncul.)
- **Di luar roster (jangan tertukar):** `LeadPoolPage.jsx:150` `['manager','supervisor']` = `notifyManagers`, daftar **penerima notifikasi approver**, **bukan roster salesperson** → `gm_bd` **tidak relevan** di sini; daftar rolenya nol sentuhan. ⚠️ **[24 Agu 2026]** query-nya sendiri ikut diperbaiki (filter `roles.company_id` dicabut) — sejak 21 Agu notifikasi ke manager **tak pernah terkirim** padahal sales tetap menerima toast sukses. **Belum diuji runtime** (`08_TECH_DEBT.md` TD-207).

### Peta Menu → Halaman → Role-gate (visibilitas)

> **Beda dari matriks granular di bawah:** ini peta **visibilitas** (menu → halaman + siapa boleh lihat), dari `ERP_MENU_GROUPS`/`NEXUS_NAV` + `canSeeMenuItem` (**default-deny**) di `App.jsx`. Jenis gate: `public` · `menuKey` (via `hasMenuPermission`) · ~~`module` (via `hasPermission`)~~ · `role[...]` (array). **[KOREKSI 20 Agu 2026]** Gate `module` **sudah tidak ada** — `hasPermission` dihapus fisik dari kode (lihat §5 poin 1), dan **nol** item menu yang masih punya properti `module:` (`grep "module: '" src/App.jsx` = 0 hit). Sisa satu-satunya: `navHasGate` (`App.jsx:1445`) masih menyebut `item.module` sebagai salah satu penanda "item ini ber-gate" — selalu falsy hari ini, inert, sengaja dibiarkan. **📋 soon** = `PLANNED_MODULES` → ComingSoonPage. *(Sumber terkini: `App.jsx` `ERP_MENU_GROUPS`/`NEXUS_NAV`.)*
>
> ⚠️ **[20 Agu 2026] Sinyal "izin sudah selesai dimuat" — dua guard sempat MATI untuk user yang aksesnya murni dari `role_menu_permissions` (tier 3).** Dua efek di `App.jsx` memakai ekspresi lokal `permsLoaded = role === 'super_admin' || userPermissions.length > 0 || menuPermissions.length > 0`. Ekspresi itu **tak pernah bernilai true** untuk kelompok user tersebut: `userPermissions` sudah mati (sistem `hasPermission`, lihat §5 poin 1) dan `menuPermissions` **hanya memuat override per-user** (`user_menu_permissions`) — sedangkan akses tier-3 justru datang dari `role_menu_permissions`, yang **tidak pernah masuk ke kedua array itu**. Akibatnya: (a) efek "FIX B" (validasi `activeMenu` hasil restore dari `localStorage`) — redirect guard tak pernah jalan; (b) efek adopsi deep-link `?menu=` — `urlMenuReady` tak pernah true, sehingga deep-link tak diadopsi **DAN** efek penulis URL (mirror `activeMenu` → URL) ikut mati. **Fix:** keduanya kini `if (permissionsLoading || bnfAuthLoading) return;` — sinyal yang memang sudah dipakai benar oleh `canAccessActiveMenu` (`App.jsx:3066`). `bnfAuthLoading` **sengaja DITAMBAHKAN** ke titik kedua (dulu tak ada): `canRenderPage('bnf'/'meeting-mingguan')` resolve lewat `isBnfAuthorized`, jadi berjalan sebelum RPC `is_bnf_authorized()` settle akan **menolak deep-link `?menu=bnf` yang sah lalu mengunci penolakan itu permanen** via `setUrlMenuReady(true)`. Dep array ikut dikoreksi dan kini bersandar pada identitas `hasMenuPermission` (`useCallback` ber-dep `[menuPermissions, roleMenuPermissions, erpRoleCode]`) — **lebih benar dari sebelumnya**, karena dep array lama tak pernah menangkap perubahan `roleMenuPermissions` sama sekali. ⚠️ **Ini prasyarat sebelum Fase B3 (seeding `role_menu_permissions`)** — tanpa fix ini, seeding justru akan mematahkan URL-sync & redirect guard untuk user yang baru dapat akses lewat role default. **NOL tes runtime** (sesi FE-only tanpa kredensial login).
>
> ℹ️ **Restruktur menu CRM Tahap 1 (19 Jul 2026, branch `feat/crm-nav-refactor`, belum commit — fondasi, NOL perubahan gate/izin):** daftar item CRM kini bersumber **tunggal** dari konstanta bersama **`CRM_MENU_ITEMS`** (`App.jsx`), dirujuk verbatim oleh `ERP_MENU_GROUPS` (pohon gate) DAN `NEXUS_NAV nav-crm` (sidebar) → tak bisa drift lagi. Induk grup CRM kini ber-id **`crm-group`** (gateless, dulu bertabrakan dengan id anak `crm-dashboard`); anak "Dashboard" TETAP `crm-dashboard` (menuKey `crm_dashboard` tak berubah). **Item, jumlah, dan gate per role di tabel di bawah IDENTIK** sebelum vs sesudah (objek gate-nya harfiah dipindah, bukan diubah); urutan render sidebar juga tak berubah.
>
> ℹ️ **Restruktur menu CRM Tahap 2a (19 Jul 2026, branch `feat/crm-nav-tahap2a` dari `main`@4e541b3, sudah di-commit sebagai `f2bbd76` & merge ke `main` — gabung menu Customer, NOL perubahan gate/izin/DB):** 5 entri "Master Customer" (`crm-customers` induk + 4 anak `crm-customers-msi/-jci/-soa/-free`) di `CRM_MENU_ITEMS` digabung jadi SATU leaf **`crm-customers`** berlabel **"Customer"**. Sumbu **entitas** (Semua/MSI/JCI/SOA) & **status** (Semua/Customer/Free Agent) kini jadi **filter di dalam `CustomerListPage`**, bukan submenu. `MENU_KEY_MAP` — 4 entri anak mati dihapus; `crm-customers` + `customer-detail` tetap → `crm_customers` (**gate & menuKey tak berubah, tak ada role yang kehilangan/mendapat akses**). Prop `entityFilter`/`ENTITY_HEADER` dibiarkan **dormant** (backward-compat, tak dihapus). Free Agent kini terjangkau lewat filter status (fetch `account_status IN ('customer','free_agent')`; di produksi `free_agent`=0 baris). Tahap 2b (Pipeline/Prospects/Lead Pool) & 2c (Activities/Activity Log) belum dikerjakan.
>
> ℹ️ **Restruktur menu CRM Tahap 2b (19 Jul 2026, branch `feat/crm-nav-tahap2b` dari `main`@f2bbd76, sudah di-commit sebagai `b857adf` & merge ke `main` — gabung 4 menu ber-tabel `accounts`, NOL perubahan gate/izin/DB/katalog menu):** 4 menu CRM yang membaca tabel `accounts` — **Pipeline** (`crm-pipeline`), **Prospects** (`crm-prospects`), **Lead Pool** (`crm-lead-pool`), **Approval Lead Pool** (`crm-lead-pool-approval`) — digabung jadi SATU leaf sidebar **`crm-account`** berlabel **"Account"** dengan **4 TAB**. **`activeMenu` tetap = id tab** (bukan id induk sintetis) → semua deep-link/back-button/localStorage/redirect-guard/content-gate lama bekerja tanpa diubah (beda dari Tahap 2a yang id anaknya benar-benar hilang + butuh normalisasi). **Gate per tab VERBATIM** (objek gate dipindah harfiah ke `children` node `crm-account` di `CRM_MENU_ITEMS`; `findMenuItemById(tabId)` tetap menemukan gate tiap tab); **visibilitas menu "Account" = OR gate anak** (`navChildGate`) → muncul iff user berhak ≥1 tab. Tab-bar (`AccountTabBar`) & sidebar (`LeafRow` special-case) memfilter dengan fungsi gate yang sama (`canSeeMenuItem`); tab default = tab pertama yang user berhak. **Tak ada role yang kehilangan/mendapat akses** (mis. `sales` → Lead Pool ✅, Approval ❌ = 3 tab; `crm_pipeline`/`crm_prospects` menuKey tak berubah). Deep-link notifikasi `lead_pool` → `crm-lead-pool` mendarat tepat di tab Lead Pool. Tab bar disembunyikan saat `ProspectFormPage` terbuka (satu-satunya sub-view in-flow full-page) agar isian tak hilang; sub-view lain = overlay `position:fixed` (self-protecting). Isi keempat halaman tak diubah. `MENU_KEY_MAP` tak berubah. Tahap 2c (Activities/Activity Log) belum dikerjakan.
>
> ℹ️ **Restruktur menu CRM Tahap 2c (19 Jul 2026, branch `feat/crm-nav-tahap2c` dari `main`@b857adf [= commit Tahap 2b], belum commit — penutup tahap 2; gabung 3 menu + 1 menu PINDAH GRUP, NOL perubahan gate/izin/DB/katalog menu/`MENU_KEY_MAP`):** **Activities** (`crm-calls`, CRM), **Activity Log** (`crm-activity-log`, CRM), dan **Riwayat Visit** (`riwayat-visit`, sebelumnya grup **Reporting**) digabung jadi SATU leaf sidebar **`crm-aktivitas`** berlabel **"Aktivitas"** dengan **3 TAB** (Jadwal & Tugas / Riwayat / Riwayat Visit). **`riwayat-visit` PINDAH grup Reporting → CRM** (dihapus dari `ERP_MENU_GROUPS` grup Reporting + dari `NEXUS_NAV nav-report`; gate `role[super_admin,ceo,gm_bd]` dipindah **verbatim** ke `children` node `crm-aktivitas`). Pola identik Tahap 2b: **`activeMenu` tetap = id tab** → deep-link/back/localStorage/redirect-guard/content-gate lama jalan tanpa diubah; **gate per tab VERBATIM** (`findMenuItemById(tabId)` tetap menemukan gate); **visibilitas "Aktivitas" = OR gate anak** → muncul iff user berhak ≥1 tab. Komponen tab bar Tahap 2b `AccountTabBar` **di-rename `MenuTabBar`** (kini dipakai 2 menu bertab; perilaku tak berubah); sidebar `LeafRow` special-case digeneralisasi menangani `crm-account` **dan** `crm-aktivitas`. Content-gate `canRenderPage('riwayat-visit')` **dipertahankan** di sub-blok riwayat-visit; catch-all ComingSoon allow-list `riwayat-visit` **dipertahankan** (bukan prefix `crm-`). **Visibilitas: 3 tab** (super_admin/ceo/gm_bd) — **2 tab** tanpa Riwayat Visit (admin/gm/manager/supervisor/sales) — **menu tersembunyi** (operations/finance/procurement/hrga/it/viewer). Tab default = Jadwal & Tugas. Deep-link notifikasi `activity` → `crm-calls` mendarat di tab Jadwal & Tugas. **Tak ada role yang kehilangan/mendapat akses.** Isi ketiga halaman tak diubah (kecuali 1 kata breadcrumb `RiwayatVisitPage` "Reporting"→"CRM"). Tahap 3 (panel detail inquiry) belum dikerjakan.
>
> ℹ️ **Restrukturisasi menu CRM — batch #1 (26 Jul 2026, dikerjakan di branch `feat/menu-restructure`) — Rate List keluar dari grup CRM, id kembar `crm-customers` dihapus, rename label tab Aktivitas. NOL perubahan gate/izin/DB.** Ini **BUKAN** kelanjutan Tahap 1/2a/2b/2c di atas — batch terpisah, dari rencana besar restrukturisasi CRM yang berbeda (melebur Customer+Prospects jadi "Akun", menaikkan Pipeline jadi menu sendiri — **belum dikerjakan**, di luar batch ini). **(1)** `crm-rate-list` (**RateListPage**) dipindah keluar dari `CRM_MENU_ITEMS` ke grup top-level BARU **"Shared Reference"** di `ERP_MENU_GROUPS` + leaf `nav-rate-list` di `NEXUS_NAV` grup "Shared Services" — **role-gate TAK BERUBAH** (`role[super_admin,admin,ceo,gm,gm_bd,manager,sales]`, tetap TANPA `procurement`); lihat baris **SHARED REFERENCE** di bawah. **(2)** id kembar/mati `crm-customers` di grup Logistics (label "Master Customer", tak pernah tercapai kode manapun — diverifikasi via `findMenuItemById`) di-rename `ref-crm-customers-pointer` — housekeeping murni, nol efek visibilitas. **(3)** label tab `crm-activity-log` (menu "Aktivitas") `'Riwayat'` → **`'Log Aktivitas'`** + heading internal `ActivitiesPage`/`ActivityLogPage` diselaraskan — **teks saja, gate/izin tak berubah**. Detail: `PROGRESS.md` 2026-07-26 + `08_TECH_DEBT.md` TD-136/TD-137.
>
> ℹ️ **Restrukturisasi menu CRM — batch #2 (27 Jul 2026, dikerjakan di branch `feat/akun-merge`) — Pipeline & Approval Lead Pool ditarik keluar dari tab "Account" jadi menu top-level. NOL perubahan gate/izin/DB.** Lanjutan rencana besar yang sama dengan batch #1 di atas, tapi task terpisah (tak saling bergantung). **Peleburan Customer+Prospect+Lead Pool jadi satu halaman berpreset adalah batch KETIGA — BELUM dikerjakan**, tidak disinggung lebih lanjut di sini. **(1)** `crm-pipeline` (**PipelineKanbanPage**) dipindah dari `children` node `crm-account` jadi entri top-level tersendiri di `CRM_MENU_ITEMS`, tepat setelah `crm-dashboard`. **Id & gate TAK BERUBAH** (tetap `menuKey crm_pipeline` → `hasMenuPermission`, granular per-user — bukan role array, meski sekarang top-level). Label diganti `'Pipeline / Leads'` → **`'Pipeline'`**; icon diganti `Users` → **`TrendingUp`** (hindari kembar ikon dgn menu "Account" di sebelahnya). Lihat baris **CRM › Pipeline** di bawah. **(2)** `crm-lead-pool-approval` (**LeadPoolApprovalPage**) dipindah dari `children` node `crm-account` jadi entri top-level tersendiri, di posisi PALING BAWAH `CRM_MENU_ITEMS`. **Id, label, icon, DAN role-gate dipindah verbatim, nol perubahan.** Lihat baris **CRM › Approval Lead Pool** di bawah (dipindahkan ke akhir blok CRM di tabel ini juga, mengikuti posisi barunya di kode). **(3)** `crm-account` ("Account") kini tinggal **2 tab** (Prospects, Lead Pool) — turun dari 4. Mekanisme tab/sidebar (generik terhadap panjang `children`) tak disentuh sama sekali. Detail: `PROGRESS.md` 2026-07-27 + `08_TECH_DEBT.md` TD-138.

> ℹ️ **AdminHub — unifikasi Master Data + Admin Settings (9 Agu 2026, 2 tahap, commit `c461b99`+`96ae561`).** `AdminShell.jsx` (Master Data) dan `AdminSettingsHub.jsx`+9 sub-halaman (Admin Settings) — sebelumnya DUA entry point top-level terpisah — melebur jadi **satu** `AdminHub.jsx` (landing card-grid, 25 destinasi). Kedua file lama **pensiun dari `App.jsx`, filenya dibiarkan di disk** (orphan — `08_TECH_DEBT.md` **TD-181**). Detail lengkap gate per-destinasi: baris FOUNDATION di bawah (disederhanakan, bukan 25 baris terpisah — sumber presisi = `src/pages/foundation/AdminHub.jsx`). **Sesi yang sama:** redirect legacy `'users'` kini landing TEPAT di card User Access (bukan default landing hub); tab "Permissions" `UserEditPage` digate `isSuperAdmin`; Picking List & Surat Jalan (baris LOGISTICS di bawah) masuk sistem `hasMenuPermission`/`role_menu_permissions` granular, lepas dari array `role[]` hardcode.

| Grup | Menu / Halaman | Komponen | Role-gate | Status |
|---|---|---|---|---|
| CORE | Command Center · Home | Dashboard · HomeDashboard | public | LIVE |
| CRM | CRM Dashboard | CRMDashboardPage | menuKey `crm_dashboard` | LIVE |
| CRM | Pipeline | PipelineKanbanPage | menuKey `crm_pipeline` | LIVE (**naik jadi menu top-level, batch #2 27 Jul 2026** — id/gate/komponen verbatim tak berubah, sebelumnya tab "Account › Pipeline / Leads" [Tahap 2b]. Label diganti `'Pipeline / Leads'`→**`'Pipeline'`**; icon `Users`→`TrendingUp`. **Heading `<h1>`/breadcrumb internal halaman masih `'Pipeline / Leads'`** [file sengaja tak disentuh batch ini] — lihat `08_TECH_DEBT` **TD-138**) |
| CRM | Account › Lead Pool | LeadPoolPage | role[super_admin,admin,ceo,gm,gm_bd,manager,supervisor,sales] | LIVE (**Tahap 2b — tab dalam menu "Account"; kini salah satu dari 2 tab sisa** sejak Pipeline & Approval Lead Pool naik jadi menu top-level tersendiri, batch #2 27 Jul 2026) |
| CRM | Account › Prospects | ProspectListPage/FormPage | module `crm` + role[…,ceo,gm,…] (gm_bd via crm module) | LIVE (**Tahap 2b — tab dalam menu "Account"; kini salah satu dari 2 tab sisa** sejak Pipeline & Approval Lead Pool naik jadi menu top-level tersendiri, batch #2 27 Jul 2026) |
| CRM | Inquiry | InquiryListPage/FormPage/DealDetailPage | via crm | LIVE |
| CRM | ↳ **Tandai Kalah** (aksi di Detail Inquiry) | DealDetailPage (`markInquiryLost`) | **TIDAK ada role-array di FE** — gate tampilnya **status dokumen** (allowlist `OPEN`/`IN_REVIEW`/`QUOTED`/`NEGOTIATION`, fail-closed; WON/LOST/CANCELLED → tombol **tak dirender**). Penegak izin sebenarnya = **RLS `inquiries_update`**: `company_id` cocok **AND** (`is_manager_or_above()` **OR** `created_by = auth.uid()`) **OR** `is_super_admin()` | LIVE 22 Jul 2026 (batch 3B-1) — **konsekuensi: sales hanya bisa menandai kalah inquiry BUATANNYA SENDIRI**; manager+ bisa se-company. **✅ TES RUNTIME LOLOS dgn akun `sales` ASLI (bukan bypass super admin)** — tombol muncul untuk inquiry OPEN, tanpa alasan ditolak, dengan alasan tersimpan (`status='LOST'`+`lost_reason`) & berjejak `audit_logs`; **akun pemiliknya nol perubahan**. *(pengamatan Den)* |
| CRM | ↳ Pindah Stage / Edit Deal (Detail Account & Detail Deal) | CustomerDetailPage · DealDetailPage · DealPanels | role-gate **tak berubah**; yang berubah = **himpunan NILAI yang boleh ditulis** (NEW/CONTACTED/QUALIFIED saja) + gate BANT di semua jalur | LIVE 22 Jul 2026 (batch 3B-1) — **tak ada role yang kehilangan/mendapat akses**; RLS `accounts` tak disentuh. **✅ Terbukti runtime di Detail Account** (akun sales asli): Pindah Stage 3 opsi · Edit Deal 7 opsi 4 disabled · gate BANT 0/12 menolak. **Jalur yang sama di `DealDetailPage` belum diuji runtime**. **[30 Jul 2026]** Jalur tulis yang SAMA (`saveDealUpdate`, `DealPanels.jsx`) kini juga dipakai aksi baru **"Edit BANT"** (card "Kriteria BANT", tab "BANT & Pipeline" — 4 dimensi `bant_budget/authority/need/timeline`, dulu tak pernah tampil+editable di Detail Account); **role-gate identik** (nol guard kepemilikan FE baru, otorisasi tetap 100% RLS `prospects_update`). **Sesi yang sama menutup bug silent-fail RLS di `saveDealUpdate`** (dulu `.update()` tanpa `.select()` melaporkan sukses palsu saat RLS menolak) — perbaikan ini **retroaktif juga berlaku ke Pindah Stage & Edit Deal di baris ini**, keduanya sudah lama memakai fungsi yang sama. Lihat `08_TECH_DEBT.md` **TD-161** (fix)/**TD-162** (CHECK constraint BANT belum ada). NOL tes runtime untuk "Edit BANT". |
| CRM | ↳ **Buat PRF** (dulu berlabel "Cetak PRF" — tombol di-rename 29 Jul 2026, gate & mekanisme di kolom ini TAK berubah; aksi di Detail Inquiry & Detail Account tab Riwayat) | DealDetailPage (`PrfListCard canCreate`) · CustomerDetailPage (`canCreatePRF`) | role[sales,gm_bd,**super_admin**] — **`super_admin` ditambah 27 Jul 2026** (`feat/prf-audit`, dua array identik `['sales','gm_bd']`→`['sales','gm_bd','super_admin']`, `DealDetailPage.jsx:483`+`CustomerDetailPage.jsx:724`) | LIVE — **dikonfirmasi kelewatan, bukan keputusan sadar**: `super_admin` sebelumnya tak bisa memicu pembuatan PRF dari kedua entry point resmi meski RLS `prf_insert` **dan** gate menu registry `'prf'` (`App.jsx:695`, 7 role) sama-sama sudah mengizinkannya. Gate lain (RLS, `canRenderPage`, registry menu) **tak disentuh**. Lihat `08_TECH_DEBT` TD-105 (array yang sama = contoh andalan TD itu; penambahan ini **widening sempit**, BUKAN penyelesaian akar TD-105 — user multi-role spt Endang Kumolo Ratih tetap kehilangan hak `sales`-nya). ⚠️ Belum tes runtime. **[28 Jul 2026]** Mekanisme GATE-nya sendiri (bukan cuma isi array) diganti: `erpRole` (role primer tunggal) → `erpRoles?.some((r) => ['sales','gm_bd','super_admin'].includes(r.roles?.code))` (mengecek SELURUH baris `user_roles` aktif — pola PERSIS `has_role()` RLS `prf_insert`), di KEDUA file (`DealDetailPage.jsx`+`CustomerDetailPage.jsx`). Dipicu kasus Endang Kumolo Ratih (manager+sales): `manager` menutupi `sales` di `erpRole`, sehingga gate lama tetap menolaknya meski array sudah memuat `sales`. **Addendum TD-105** (bukan TD baru) — sub-instance ini genuinely diperbaiki dgn pola benar, TAPI TD-105 tetap OPEN sistemik (~106 pemakaian `erpRole` lain di `src/` tak tersentuh; `AuthContext.jsx` tak diubah). ⚠️ Belum diverifikasi runtime (perlu Endang login sendiri). **[29 Jul 2026]** Label tombol berubah "Cetak PRF"→**"Buat PRF"** (dikonfirmasi doc-keeper: nol komponen print/PDF PRF ada di `src/` — tombol ini selalu shortcut membuat PRF baru dari inquiry, bukan mencetak; nama lama menyesatkan). **Gate/mekanisme kolom ini (role array + `erpRoles?.some()`) TAK berubah** — murni rename teks tombol, komponen `DealPanels.jsx` `PrfListCard` juga ganti ikon `FileText`→`Plus`. Detail: `PROGRESS.md` 2026-07-28 (gate) + `PROGRESS.md` 2026-07-29 (rename). |
| CRM | Quotation | QuotationList/Detail/FormPage | via crm | LIVE |
| CRM | ~~Rate List~~ | ~~RateListPage~~ | — | **PINDAH ke grup baru "Shared Reference"** (batch #1, 26 Jul 2026 — role-gate tak berubah; lihat baris SHARED REFERENCE) |
| CRM | Customer (+ Detail) | CustomerListPage · CustomerDetailPage | menuKey `crm_customers` | LIVE (**Tahap 2a, 19 Jul** — 5 entri lama [induk + 4 anak MSI/JCI/SOA/Free] digabung jadi SATU menu "Customer"; MSI/JCI/SOA/Free Agent kini **filter di halaman**, bukan submenu. menuKey & gate tak berubah — **tak ada role yang kehilangan/mendapat akses**). **[26 Jul 2026]** tab baru "Kontak" (tabel `contacts`) di dalam `CustomerDetailPage` **tak punya gate sendiri** — siapa pun yang lolos gate `crm_customers` (buka halaman ini) bisa Tambah/Edit/Hapus/Jadikan-Primary kontak; penegak di DB juga longgar (RLS `contacts` mewarisi visibilitas `accounts` via EXISTS, tanpa restriksi role tambahan; hapus selalu lewat soft-delete UPDATE sehingga batas "DELETE super_admin-only" di DB tak pernah tersentuh jalur ini) — dikonfirmasi Den sbg desain. |
| CRM | Aktivitas › Jadwal & Tugas / Log Aktivitas | ActivitiesPage · ActivityLogPage | role[super_admin,admin,ceo,gm,gm_bd,manager,supervisor,sales] | LIVE (**Tahap 2c, 19 Jul** — tab dalam menu "Aktivitas"; gate tak berubah. **Label sub-tab kedua "Riwayat"→"Log Aktivitas"** diganti batch #1, 26 Jul 2026 — teks saja, lihat `08_TECH_DEBT` **TD-137** soal tab-label vs heading halaman yang tak terjaga sinkron) |
| CRM | Aktivitas › Riwayat Visit | RiwayatVisitPage | role[super_admin,ceo,gm_bd] | LIVE (**Tahap 2c, 19 Jul — PINDAH dari grup Reporting** jadi tab; gate `[super_admin,ceo,gm_bd]` tak berubah — **gm_bd ditambah 9 Jul; gm SENGAJA tidak**) |
| CRM | Approval Lead Pool | LeadPoolApprovalPage | role[ceo,gm,gm_bd,manager,supervisor,admin,super_admin] | LIVE (**naik jadi menu top-level, batch #2 27 Jul 2026** — id/label/icon/role-gate verbatim tak berubah, sebelumnya tab "Account › Approval Lead Pool" [Tahap 2b; ceo/gm/gm_bd ditambah 9 Jul — sebelumnya CEO kelewat]. Heading internal halaman [`'Approval Lead Pool'`] kebetulan sudah persis sama dgn label menu barunya — **tak kena TD-138**. **Catatan konteks (belum dieksekusi):** ada wacana menu ini diganti alur tarik-langsung — belum dikonfirmasi, dipertahankan apa adanya batch ini) |
| SHARED REFERENCE | Rate List | RateListPage | role[super_admin,admin,ceo,gm,gm_bd,manager,sales] | LIVE (**dipindah dari grup CRM, batch #1 26 Jul 2026** — role-gate TAK BERUBAH. Grup baru sengaja BUKAN bagian "Procurement & Vendor" di `ERP_MENU_GROUPS` meski `NEXUS_NAV` menaruh leaf-nya `nav-rate-list` bertetangga dgn modul Procurement — gate `crm-rate-list` tak memuat `procurement`. Registrasi kini di DUA tempat terhubung hanya lewat kecocokan string id [bukan lagi array tunggal `CRM_MENU_ITEMS`] — lihat `08_TECH_DEBT` **TD-136**) |
| LOGISTICS | **Dashboard Storbit** (Shipping Manifest + Warehouse) | StorbitDashboardPage | menuKey **`logistics_sp` (REUSE, bukan key baru)** | LIVE (**BARU 18 Agu 2026**) — route `storbit-dashboard` **sengaja memakai ULANG** menu key `logistics_sp`. Alasannya struktural, bukan malas: `hasMenuPermission` **default-deny**, jadi menu key baru butuh seeding katalog `module_menus`/`menu_actions`/`role_menu_permissions` lebih dulu — tanpa itu halaman **tak terlihat siapa pun kecuali super_admin**, dan itu bug yang baru ketahuan saat orang lain login (kerabat: Fase B3 yang `role_menu_permissions`-nya masih kosong total). Datanya bersumber sama dgn SP Manifest sehingga batas aksesnya memang tepat sama. Preseden pola "dua route id berbagi satu menu key" sudah ada: `'customer-detail' → 'crm_customers'`. **Nol perubahan RLS/role/permission dari sesi ini.** |
| LOGISTICS | Sales Order / SP (list + detail) | SalesOrderPage · SalesOrderDetailPage | menuKey `logistics_sp` | LIVE |
| LOGISTICS | ↳ Hapus SP (Danger Zone, Detail) | SalesOrderDetailPage | **role `super_admin` + status `DRAFT`** (RPC `delete_sp_dual` guard `is_super_admin()` + DRAFT) | ⚠️ RPC belum live (dijalankan manual). Dulu `[super_admin,operations]` tanpa gate status → operations dicabut akses hapus |
| LOGISTICS | ↳ Batalkan SP (header actions, Detail) | SalesOrderDetailPage | role[super_admin,operations,manager,gm] + status `DRAFT` (`set_sp_status 'cancelled'`, alasan wajib) | LIVE |
| LOGISTICS | ↳ **Edit item SP** (pensil, tabel Items di Detail) | SalesOrderDetailPage · EditItemModal | **`is_sp_item_writer()`** = [super_admin, admin, manager, operations] — lihat §3.0. Cermin FE `canWriteSpItem` | LIVE (**BARU 2 Sep 2026**). Sebelumnya tombolnya digate `canWarehouseOps` (tak termasuk `finance`) sementara `EditItemModal` sendiri **nol role check** — gate seluruhnya di luar modal |
| LOGISTICS | ↳ **Hapus SATU item SP** (tombol Hapus di sebelah pensil) | SalesOrderDetailPage (`deleteSpItem` → RPC `delete_sp_item_dual`) | **`is_sp_item_writer()`** + status ∈ {DRAFT, CONFIRMED, MENUNGGU_STOK} + bukan baris terakhir | LIVE (**BARU 2 Sep 2026**). ⚠️ **Sebelumnya TANPA GATE SAMA SEKALI** — hard-delete langsung lewat RLS `USING(true)`, siapa pun yang login bisa menghapus baris item SP siapa pun, dan hapusnya meninggalkan baris hantu `sp_order_items` yang membuat SP itu **tak bisa di-invoice selamanya** |
| LOGISTICS | ↳ **Finance & Dokumen** (kartu tab Overview Detail SP · `FinanceModal` di Finance/Outstanding) | SalesOrderDetailPage · App.jsx `FinanceModal` | **RPC `set_sp_finance_docs`: `is_super_admin() OR has_role('finance_controller') OR has_role('finance')`** — **tanpa** manager, lihat §3.0. Cermin FE `canEditFinanceDocs`; freeze bila SP `CANCELLED` | LIVE (**BARU 2 Sep 2026**, smoke test produksi dilaporkan LOLOS). Menggantikan 6 field dokumen yang dulu diedit per-item lewat `EditItemModal` |
| LOGISTICS | ↳ Terbitkan / Submit Invoice (card Invoice, Detail SP) | SalesOrderDetailPage | RPC `create_invoice`/`submit_invoice`: `is_super_admin() OR is_manager_or_above() OR has_role('finance_controller')` (+guard `Σshipped=Σqty` utk terbit) | LIVE (8 Agu 2026) |
| LOGISTICS | ↳ **Terima Pembayaran** (inline card Invoice, Detail SP) | SalesOrderDetailPage | **RPC `record_payment`: HANYA `is_super_admin() OR has_role('finance_controller')`** — `is_manager_or_above()` **SENGAJA dikecualikan** (mencatat uang masuk bukan wewenang manajer lini; **lebih ketat dari `create_invoice`** — jangan diseragamkan tanpa keputusan). Gate FE `canRecordPayment` cermin ini, dibaca dari `erpRoles` (seluruh role aktif) BUKAN prop `role` primer. Form hanya tampil saat `sp_invoices.status ∈ {issued,submitted,partial}` | ✅ LIVE 17 Agu 2026 |
| LOGISTICS | ↳ **Tandai TTF Diterima / Edit TTF** (inline card Invoice, Detail SP) | SalesOrderDetailPage | **RPC `mark_ttf_received`: `is_super_admin() OR is_manager_or_above() OR has_role('finance_controller')`** — **lebih longgar** dari `record_payment` (menerima faktur = aktivitas operasional). Gate FE `canMarkTtf` cermin ini. Blok tampil saat status invoice `∈ {issued,submitted,partial,paid}`. RPC **UPSERT** → Edit tak melahirkan TTF kedua | ✅ LIVE 17 Agu 2026 |
| LOGISTICS | ↳ Baca jurnal AR (`journal_entries`/`journal_entry_lines`) | — (belum ada halaman) | RLS **read-only** `FOR SELECT TO authenticated`, company-scoped `is_super_admin() OR company_id IN (SELECT get_user_company_ids())`. **Nol policy INSERT/UPDATE/DELETE**, GRANT tabel cuma `SELECT` → tulis mustahil lewat PostgREST | ✅ RLS LIVE 17 Agu 2026; **belum ada UI pembaca** |
| LOGISTICS | Input SP | InputSPPage | module `logistics` + **canInputSP** (permission AND operational role) | LIVE |
| LOGISTICS | Picking List (+Detail) | PickingListPage · DetailPage | menuKey `logistics_picking` **[9 Agu 2026, BARU]** | LIVE. Sebelumnya gate murni `role[super_admin,admin,ceo,gm,manager,operations]` hardcode — **nol jejak** di `MENU_KEY_MAP`/sistem permission granular, satu-satunya cara mengubah akses adalah edit kode. Migrasi `20260809000001` mendaftarkan menu (`module_menus`) + action `view` (`menu_actions`) + mereplikasi array role lama ke `role_menu_permissions` (super_admin tak perlu baris, selalu bypass true). ⚠️ **Data migrasi belum diverifikasi independen ke DB live** oleh doc-keeper (`schema_snapshot.sql` schema-only, tak memuat data) — dipercaya dari isi file migrasi + commit message. **[20 Agu 2026]** Array `role: [...]` lama yang masih tertinggal di kedua item menu (`App.jsx`) **DIHAPUS** — bukan perubahan gate, melainkan pembersihan sisa kerja 9 Agu: `canSeeMenuItem` memeriksa `MENU_KEY_MAP` **lebih dulu lalu langsung `return`**, jadi `item.role` di kedua item ini **tak pernah dievaluasi** sejak menuKey-nya didaftarkan (pola identik `admin-settings`, terdokumentasi di `App.jsx:1857-1871`). **Nol perubahan perilaku user-facing.** Diganti komentar peringatan agar array itu tidak dikembalikan. Diverifikasi doc-keeper: dari 18 item ber-`role:[]` sebelum perubahan, **hanya 2 ini** yang tumpang tindih `MENU_KEY_MAP` — 16 sisanya bersih & tak disentuh. |
| LOGISTICS | Surat Jalan (+Detail) | DeliveryNotePage · DetailPage | menuKey `logistics_surat_jalan` **[9 Agu 2026, BARU]** | LIVE. Sama persis catatan Picking List di atas (satu migrasi yang sama, dua menu sekaligus). |
| LOGISTICS | Shipment Mgmt | ShipmentPage (inline) | module `logistics` + role | LIVE |
| LOGISTICS | General Trading · Job · Freight · Customs(PPJK) | ComingSoon | — | 📋 soon |
| INVENTORY | Dashboard · Stok Barang · Penerimaan | InventoryDashboard · StokBarang · PenerimaanBarangPage | via inventory | LIVE |
| INVENTORY | Pengeluaran · Transfer · Opname | — | — | 📋 menu ada, tanpa render block |
| FINANCE | AR/Collection · Outstanding · Finance Docs | ARTracker · Outstanding · FinancePage (inline) | module `finance` + role | LIVE |
| FINANCE | Job Costing · Billing · AP · Cash/Bank · Accounting | ComingSoon | — | 📋 soon |
| SERVICE | HRGA Request | HrgaShell (My/Buat/Semua[role]/Pending Approval[role]/Arsip) | mixed per sub-page | LIVE |
| SERVICE | Asset Management | AssetShell (16 sub-objek) | inherit | LIVE |
| SERVICE | IT Service Mgmt | ComingSoon | — | 📋 soon |
| PROCUREMENT | PRF (Price Request Form — form) | PRFFormPage | role[sales,gm_bd,procurement,manager,ceo,admin,super_admin] | LIVE (Fase 1; menu terlihat semua role di list, tapi **hanya sales/gm_bd bisa Submit/Draft** — dijaga RLS `prf_insert`; role lain submit → toast error) |
| PROCUREMENT | PRF — Detail + Jawaban Harga (cost build-up) | PRFDetailPage | **edit: `['procurement','super_admin'].includes(erpRole)`** (cermin RLS `prf_update_status` + `prf_cost_items`); lihat: siapa pun yang berhak baca PRF (`prf_select`: own OR procurement OR manager+ OR super); **tombol "Buat Quotation" + panel "Quotation dari PRF Ini" [20 Jul 2026]: `hasMenuPermission('crm_quotation','view')`** (fail-closed `AuthContext.jsx:271-282`; himpunan = menu Quotation sidebar → **procurement tak melihat keduanya**, konsisten TD-90) | LIVE (20 Jul 2026; dibuka dari list "Forwarding (MSI)"; sales/manager = LIHAT saja, panel read-only [terbukti runtime 20 Jul — Karina]; **RLS = penegak**; ~~write butuh `status='SUBMITTED'`~~ **[KOREKSI 27 Jul 2026 sore]** sejak migrasi `20260727000004` (commit `e1c7ff1`) write butuh `status IN ('SUBMITTED','ACKNOWLEDGED') AND (acknowledged_by IS NULL OR acknowledged_by=auth.uid())` — procurement yang meng-klaim PRF (`prf_claim`, lihat baris "Penawaran Vendor" di bawah) tetap bisa memakai panel ini sesudahnya → lihat `08_TECH_DEBT` TD-109 (~~usulan turun MEDIUM→LOW~~ **[DITARIK 27 Jul 2026, Batch 3C]** tetap MEDIUM — ~~begitu PRF dinyatakan `QUOTED` via `prf_mark_quoted` [kini punya UI, tombol "Nyatakan Penawaran Siap"], panel ini kembali terkunci utk PRF itu, TANPA UI pengganti~~ **[KOREKSI SUSULAN, doc-keeper 27 Jul 2026]** migrasi `20260727000006_prf_update_status_terima_quoted.sql` melebarkan `prf_update_status` SEKALI LAGI ke `status IN ('SUBMITTED','ACKNOWLEDGED','QUOTED')` (daftar status di awal kalimat ini juga sudah usang, kini termasuk QUOTED); diverifikasi lewat sesi procurement ASLI (ROLLBACK, bukan super_admin): UPDATE `prf.pricing_notes` pada PRF QUOTED BERHASIL — panel ini TIDAK lagi terkunci saat QUOTED. TD-109 TETAP MEDIUM, tapi alasannya kini asimetri `prf_vendor_offers_update` yang tak menyaring status PRF sama sekali (lihat `08_TECH_DEBT` TD-109 blok terbaru)); ~~tombol Buat Quotation juga butuh `answered_at` + `suggested_rate>0` + status bukan CANCELLED/EXPIRED — positive case belum tes runtime).~~ **[KOREKSI 28 Jul 2026]** gerbang tombol "Buat Quotation" kini menerima DUA jalur independen: **(a) BARU** `prf.selected_offer_id` terisi (sales sudah memilih penawaran vendor — harga jual jadi wilayah sales, bukan lagi disyaratkan dari `suggested_rate` procurement) **ATAU (b) LAMA** `answered_at`+`suggested_rate>0` (klaim lama di atas, panel "Jawaban Harga" lama, tak disentuh) — plus status bukan CANCELLED/EXPIRED di kedua jalur. Variabel `hasOffers` (ada sejak Batch 3C 27 Jul) SENGAJA ikut membuka render-area tombol supaya alasan blokir "Sales belum memilih penawaran" (Batch 3C) tetap tampil sbg tombol disabled+alasan, bukan hilang total. **✅ Positive case kini TERVERIFIKASI RUNTIME PENUH** (Den, PRF uji dibersihkan pasca-tes): sebelum pilih → disabled+alasan; sales pilih penawaran di Detail Inquiry → badge "DIPILIH SALES" → tombol procurement AKTIF; klik → form Quotation ter-prefill cost price nyata (dari penawaran terpilih) + sell price KOSONG (bukan 0, mengundang sales mengisi sendiri). **+TD-157 baru (MEDIUM)** — nol validasi cegah quotation Rp 0 tersimpan/ter-submit (bukan celah baru, cuma titik masuknya bergeser). **+TD-158 baru (LOW)** — pesan blokir di `PRFDetailPage.jsx` buntu bagi sales (tak menunjuk ke Detail Inquiry tempat pemilihan sebenarnya terjadi). Detail: `PROGRESS.md` 2026-07-28 + `05_WORKFLOW_MAP` §Procurement — PRF. **[21 Jul 2026 — panel jadi MULTI-VENDOR, gate role TIDAK berubah]** yang bertambah = **penghalang NON-role** pada tombol "Buat Quotation": dirender tapi **disabled + alasan tertulis** bila `rate_currency ≠ IDR` atau ada mata uang ter-award non-IDR yang kursnya belum diisi (**TD-120**) — ini pembatas **bentuk data**, bukan izin. Panel edit multi-vendor **terbukti runtime dengan akun `procurement` asli** — smoke test 5 langkah **seluruhnya lolos**: validasi pra-submit menyala & memblokir simpan · badge award `final` (navy) **dan** `pending` (abu) sama-sama tampil benar · **PRF warisan tetap bisa disimpan** (validasi tak memblokirnya). **Jalur tulis ke DB juga TERVERIFIKASI** — 3 skenario dikonfirmasi lewat `SELECT` langsung ke `prf_cost_items` (backward-compat UI lama di atas RPC baru · award 2 kartu · 3 kartu termasuk USD dgn `exchange_rate` tersalin ke baris). Sisa: simpan ulang **setelah** perbaikan UI terakhir — perbaikan itu tak mengubah payload submit. **[29 Jul 2026 — halaman ini dirombak jadi 9 section bernomor, "edit" di kolom Gate ini TETAP soal panel Jawaban Harga saja]** `PRFDetailPage.jsx` redesign visual besar (9 section: Identitas & Urgensi/Customer & Kontak/Rute & Ringkasan Kargo/Detail Layanan/Dangerous Goods/Niaga & Catatan/Penawaran Vendor/Jawaban Harga/Quotation dari PRF Ini) + field baru ditampilkan (`goods_name`/`submitted_at`/alamat akun/`selected_by`+`selected_at`) — **gate `edit` panel Jawaban Harga di kolom ini TAK berubah**. Sesi yang sama JUGA menambah kapabilitas BARU dan BEDA — "Edit PRF (draft)" — lihat baris terpisah di bawah, JANGAN disamakan dengan gate `edit` kolom ini (dua "edit" berbeda pada halaman yang sama: satu utk jawaban harga saat SUBMITTED+, satu utk field PRF sendiri saat DRAFT). Detail: `PROGRESS.md` 2026-07-29 + `05_WORKFLOW_MAP` §Procurement — PRF. |
| PROCUREMENT | PRF — **Edit PRF (draft)** [BARU, 29 Jul 2026, TD-76] | PRFDetailPage (tombol "Edit PRF") → PRFFormPage (mode edit, prop `editPrfId`) | `canEditDraft = prf.status==='DRAFT' && (prf.created_by===profile?.id \|\| erpRole==='super_admin')` — **cermin PERSIS** USING RLS `prf_update_draft` (`is_super_admin() OR (company + created_by=auth.uid() AND status='DRAFT')`, `schema_snapshot.sql:13052`), **TANPA bypass manager** (beda sengaja dari gate `canRelease`/`canManageOffers` di baris "Penawaran Vendor" bawah yang punya `MANAGER_OR_ABOVE` — `prf_update_draft` sendiri memang tak punya klausa manager). **Ini gate BARU DAN BERBEDA dari `edit` panel Jawaban Harga di baris di atas** — kapabilitas ini mengedit FIELD PRF SENDIRI (Section 01-06) saat status masih DRAFT, bukan mengisi jawaban harga. | **VERIFY (kode, 29 Jul 2026) — belum tes runtime.** Menutup poin (b) TD-76 (form dulu create-only). Reachable HANYA lewat list "Forwarding (MSI)" (role menu `proc-inquiry-fwd-msi` = `[sales,gm_bd,procurement,manager,ceo,admin,super_admin]` — sales BISA reach) → klik baris draft milik sendiri → tombol "Edit PRF". **Belum ada jalur ke-2** dari kartu `PrfListCard` (Detail Inquiry/Detail Account — kartu itu cuma render "Buat PRF" untuk CREATE, bukan edit draft). ⚠️ **TD-159 baru (LOW)** — RLS `prf_update_draft` tak punya jalur manager-override (draft milik staf resign/cuti tak bisa diedit siapa pun kecuali super_admin). Detail: `PROGRESS.md` 2026-07-29 + `08_TECH_DEBT` TD-76/TD-159. |
| PROCUREMENT | PRF — Penawaran Vendor (`prf_vendor_offers` + 4 RPC transisi status) | ~~— (**DB-only, NOL komponen/menu FE** — belum ada leaf/entry point apa pun)~~ **[KOREKSI 27 Jul 2026 sore]** `PRFDetailPage` (bukan leaf/menu baru — komponen yang SAMA dgn baris di atas, dapat tambahan tombol + kartu, lihat kolom Status) · **[+27 Jul 2026, Batch 3C]** `DealDetailPage`/`DealPanels.jsx` (`PrfListCard`/`OfferMiniCard`, panel Detail Inquiry sisi SALES — bukan leaf/menu baru, komponen yang sudah dipakai kartu "Daftar PRF") | **Bukan gate menu** (tak ada `role[]`/`menuKey` — halaman belum dibangun). Penegak satu-satunya hari ini = **RLS 4 policy `prf_vendor_offers_*`**, semua `TO authenticated`, semua top-level `is_super_admin() OR (…)`: **select** — company match AND (`has_role('procurement')` OR `is_manager_or_above()` OR PRF-nya dibuat user ini sendiri → sales pemilik PRF bisa lihat penawaran vendor untuk PRF miliknya); **insert** — company match AND `has_role('procurement')` AND `created_by=auth.uid()` AND PRF-nya SEDANG dipegang user ini (`acknowledged_by=auth.uid()`, hanya terisi via RPC `prf_claim()`); **update** — USING sama seperti insert, **WITH CHECK identik dengan USING** (keduanya `has_role('procurement')` — celah lama [WITH CHECK tak mengulang role check] **sudah diperbaiki di produksi**, per pernyataan Den 27 Jul 2026); **delete** — super_admin only. Ditambah **4 RPC SECURITY DEFINER** (`prf_claim`/`prf_release`/`prf_mark_quoted`/`prf_select_offer`) yang menegakkan alur transisi `prf.status` di luar jangkauan RLS biasa (lihat `03_DATA_MODEL` §5). **[TAMBAHAN 27 Jul 2026 sore, Batch 3A]** tombol render "Ambil PRF Ini"/"Lepas PRF" digate FE (RPC tetap penegak sebenarnya): `canClaim = canEdit && status==='SUBMITTED' && !acknowledged_by`; `canRelease = status==='ACKNOWLEDGED' && (acknowledged_by===profile.id || MANAGER_OR_ABOVE.includes(erpRole))` — const lokal `MANAGER_OR_ABOVE` (7 role: super_admin/admin/ceo/gm/gm_bd/manager/supervisor) disalin **BENAR** dari `is_manager_or_above()` DB, kontras dgn mirror SALAH `ActivitiesPage.jsx:489` yg ketinggalan `gm_bd` → `08_TECH_DEBT` **TD-155** baru. **[+27 Jul 2026, Batch 3B]** tombol Tambah/Edit/Hapus penawaran digate `canManageOffers = canEdit && prf.acknowledged_by === profile?.id` — ⚠️ **BEDA dari `canRelease`**: TANPA fallback `MANAGER_OR_ABOVE`, cermin PERSIS `prf_vendor_offers_insert/update` yang `acknowledged_by=auth.uid()` TANPA `IS NULL OR` (`schema_snapshot.sql:13079-13101`) — **manager-ke-atas BISA melepas PRF orang lain tapi TAK BISA kelola penawarannya**. Hapus tambahan-disabled bila `prf.selected_offer_id===o.id` (keputusan bisnis, bukan gate role). **[+27 Jul 2026, Batch 3C]** tombol "Nyatakan Penawaran Siap" (`prf_mark_quoted`) digate `canMarkQuoted = canEdit && status==='ACKNOWLEDGED' && acknowledged_by===profile.id && vendorOffers.length>=1`; <3 penawaran wajib alasan lewat dialog `MinOffersWaiverModal` sebelum RPC dipanggil. Tombol "Pakai/Ganti Penawaran Ini" (`prf_select_offer`, di `DealDetailPage.jsx`) digate `canSelectOffer = created_by===profile.id || MANAGER_OR_ABOVE.includes(erpRole)` — const `MANAGER_OR_ABOVE` BARU di file itu, mirror (bukan re-export) dari const nama sama `PRFDetailPage.jsx`, 7 role, cocok syarat DB `prf_select_offer` (`v_owner=v_uid OR is_manager_or_above()`) | ~~**⚠️ RLS LIVE, UI BELUM DIBANGUN (27 Jul 2026).** RLS-nya sudah bisa ditegakkan/diuji sekarang (mis. lewat SQL Editor/JWT-impersonation) **terlepas ada UI atau tidak** — dijalankan manual Den, direkam migrasi arsip `20260727000001`–`000003`. **NOL UI**: diverifikasi grep `src/` — nol pemanggil untuk `prf_vendor_offers`/`prf_claim`/`prf_release`/`prf_mark_quoted`/`prf_select_offer`.~~ **[KOREKSI 27 Jul 2026 sore — SEBAGIAN USANG, "Batch 3A" `feat/prf-audit` belum di-commit]** `PRFDetailPage.jsx` kini punya: baris header "Sedang dikerjakan oleh {nama}", tombol "Ambil PRF Ini" (`prf_claim`), tombol "Lepas PRF" (`prf_release`), kartu baca "Penawaran Vendor" (`prf_vendor_offers` SELECT, read-only, nol tombol tulis). ~~**CAKUPAN SEMPIT — masih genuinely NOL UI:** `prf_mark_quoted`/`prf_select_offer` (diverifikasi grep ulang `src/` 27 Jul sore = 0 hit keduanya) + form tambah/edit/hapus `prf_vendor_offers` (batch 3B, belum dikerjakan).~~ **[KOREKSI 27 Jul 2026, sesi terpisah SETELAH ini — Batch 3B]** form tambah/edit/hapus `prf_vendor_offers` **SUDAH ADA** (`PRFVendorOfferModal.jsx`, gate `canManageOffers` — lihat kolom Gate); ~~`prf_mark_quoted`/`prf_select_offer`/panel Detail Inquiry sales (3C) **TETAP genuinely nol UI**.~~ **[KOREKSI 27 Jul 2026, sesi terpisah SETELAH ini — Batch 3C]** ketiganya **SUDAH ADA** — tombol "Nyatakan Penawaran Siap" (`prf_mark_quoted`, `PRFDetailPage.jsx`) + tombol "Pakai/Ganti Penawaran Ini" (`prf_select_offer`, `DealDetailPage.jsx`) + panel Detail Inquiry sales menampilkan penawaran vendor (`PrfListCard`/`OfferMiniCard`, `DealPanels.jsx`) — lihat kolom Gate untuk gate barunya. **Yang MASIH genuinely nol:** notifikasi PRF; transisi `CANCELLED`/`EXPIRED` (TD-154). **+TD-156 (baru, MEDIUM) → RESOLVED (kode) 27 Jul 2026 Batch 3C** — kalkulasi "Buat Quotation" TAK LAGI basis `is_awarded` murni; dasar hitung penawaran-linked kini `prf.selected_offer_id` (baris warisan `offer_id IS NULL` tetap difilter `is_awarded=true`, keputusan sadar — lihat `08_TECH_DEBT` TD-156 untuk nuansa lengkap). Struktur tabel + kolom lengkap: `03_DATA_MODEL` entri `prf_vendor_offers`. Kaitan tech debt: `08_TECH_DEBT` TD-109 (~~update, usulan turun MEDIUM→LOW~~ **[KOREKSI SUSULAN 27 Jul 2026]** tetap MEDIUM — migrasi `20260727000006` menutup risiko RAISE saat QUOTED, tapi severity dipertahankan karena asimetri `prf_vendor_offers_update`, lihat blok update terbaru)/TD-122 (REOPENED, masih soal model LAMA `prf_cost_items` — beda dari baris ini, TAK BERUBAH oleh Batch 3C)/TD-154/TD-155/TD-156 (RESOLVED kode). Detail: `PROGRESS.md` 2026-07-27 (entri Batch 3A + Batch 3B + Batch 3C + entri susulan doc-keeper). |
| PROCUREMENT | Vendor Management › Vendor List (Master Vendor) | VendorListPage | role[procurement,manager,supervisor,gm,gm_bd,ceo,admin,super_admin] — **cermin persis RLS `vendors_*`** = `has_role('procurement')` + ketujuh kode `is_manager_or_above()` (`schema_snapshot.sql:956`); **`sales` sengaja dikecualikan**. Gate lewat `role[]` array, BUKAN menuKey (`MENU_KEY_MAP` tak punya entri `proc-vendor-list`) | LIVE (21 Jul 2026 — CRUD master data). ✅ **TES RUNTIME DONE (21 Jul 2026)** — login **akun `procurement` asli** (`scm@msi.com`; super_admin **dicabut permanen** dari akun itu → benar-benar uji role procurement, **bukan** bypass super admin): menu muncul & leaf aktif (tanpa `soon`) · halaman terbuka tanpa error konsol · **insert lolos** · **update lolos** · **soft delete lolos** (vendor terarsip hilang dari list) · **negative case: menu TIDAK muncul untuk `sales`**. ⚠️ **KOREKSI klaim lama:** "list company-scoped benar (user MSI lihat vendor MSI, bukan 4 seed SOA)" **BUKAN bukti RLS** — query list memasang `.eq('company_id', …)` **eksplisit** (`VendorListPage.jsx:103`) → company-scoping **terbukti di KODE, belum terbukti di RLS**. ⚠️ **Penyaringan vendor terarsip kini di QUERY, bukan RLS** (syarat `deleted_at IS NULL` dicabut dari USING `vendors_select` karena memblokir soft delete — `08_TECH_DEBT` **TD-115**). ⚠️ `role` di `canSeeMenuItem` = **role primer** (`erpRole`) → user `sales`+`procurement` / `finance`+`procurement` **tak melihat menu** meski RLS mengizinkan (`has_role()` menguji semua role) → gate FE **lebih ketat dari RLS**, lihat **TD-105**. `supervisor` dalam daftar = ikut cermin-RLS tapi **mati** (tak ada di tabel `roles`) → **TD-106** |
| PROCUREMENT | Vendor Management › Onboarding · Catalog · Performance/Scorecard | — | — | 📋 soon (leaf `soon:true` di `NEXUS_NAV`) |
| PROCUREMENT | PR · PO | ComingSoon | — | 📋 soon |
| REPORTING | Sales Report | CRMReportPage | role[super_admin,admin,ceo,gm,gm_bd,manager,supervisor] | LIVE |
| REPORTING | ~~Riwayat Visit~~ | ~~RiwayatVisitPage~~ | — | **PINDAH ke grup CRM** (Tahap 2c, 19 Jul — kini tab "Aktivitas › Riwayat Visit"; lihat baris CRM) |
| REPORTING | Indomarco Dashboard | IndomarcoDashboardPage | role[super_admin,admin,ceo,gm,gm_bd,manager,supervisor] (manager-or-above) | LIVE |
| REPORTING | MOM (menu) | MOMListPage/FormPage/DetailPage | role[super_admin,admin,ceo,gm,gm_bd,manager,supervisor,sales,operations] | LIVE |
| REPORTING | MOM — lihat semua (SEE_ALL) | MOMListPage | role[super_admin,admin,ceo,gm,gm_bd,manager,supervisor] | LIVE |
| REPORTING | MOM — approve (APPROVER) | MOMDetailPage | role[ceo,gm_bd,admin,super_admin] | LIVE (**gm_bd ditambah 9 Jul; gm SENGAJA tidak**) |
| REPORTING (Governance) | Briefing Harian [BARU, 10-12 Agu 2026] | BriefingHarianPage | `public: true` **genuinely terbuka** — semua user login, tanpa syarat role/`is_bnf_authorized()` | LIVE (kode). Menu id `briefing-harian`, gate murni "sudah login". Overview tab-nya sendiri bercabang internal ke `is_bnf_authorized()` (bukan gate menu) — lihat baris "BNF" di bawah + `05_WORKFLOW_MAP.md`. Tak masuk sistem `hasMenuPermission` (nol entri `MENU_KEY_MAP`). |
| REPORTING (Governance) | BNF (Bad News First) [gate DIUBAH, 12 Agu 2026] | BNFListPage | **RPC `is_bnf_authorized()`** (via `AuthContext.jsx` `isBnfAuthorized`) — **BUKAN** lagi `public:true` tanpa syarat | LIVE. **Pembalikan keputusan disengaja** (dikonfirmasi Den): komentar kode lama eksplisit menyebut "BNF harus tetap terbuka semua staff" sbg keputusan produk — dibalik krn RLS `bnf_reports_select`/`_insert` (lihat `03_DATA_MODEL.md` §BNF) sudah lebih dulu diperketat ke `is_bnf_authorized()`, staff tanpa akses nyata akan mendarat di halaman kosong/rusak kalau menu tetap `public`. Tak masuk sistem `hasMenuPermission` (nol entri `MENU_KEY_MAP` utk `bnf`) — gate murni special-case `canSeeMenuItem` di `App.jsx`. |
| REPORTING (Governance) | Meeting Mingguan [BARU, 12 Agu 2026] | MeetingMingguanPage | **RPC `is_bnf_authorized()`** — sama persis gate BNF, RPC di-reuse (tak fetch ulang) | LIVE (kode). Menu id `meeting-mingguan`, satu grup sidebar dgn BNF/Briefing Harian ("Reporting & Governance" › "Governance"). Tak masuk sistem `hasMenuPermission`. |
| REPORTING | Reports · Performance · Audit | ComingSoon | — | 📋 soon |
| FOUNDATION | **Master Data & Admin Settings** (AdminHub) [RESTRUKTUR 9 Agu 2026] | `AdminHub.jsx` (baru — ganti `AdminShell`+`AdminSettingsHub`) | mount gate `App.jsx`: `canRenderPage('admin-hub') OR canAdminSettings` — union, **satu-satunya tempat** union ini boleh dievaluasi (di dalam `AdminHub` sendiri, tiap 25 card mereplikasi gate ASLI-nya masing-masing, tak pernah baca union ini) | LIVE. Landing card-grid, 6 grup (Organisasi/Akses Kontrol/Dokumen&Konfigurasi/Finance/Produk&Harga/Sistem), 25 destinasi. Gating 2-lapis per card (`view`=visibilitas grid, `open`=render-guard tunggal) — 24/25 destinasi `view===open` (replikasi persis kondisi lama); **1 pengecualian sengaja: Schema Manager** (`view` via `hasMenuPermission('foundation_schema','view')`, longgar, TAPI `open` tetap hardcode `role==='super_admin'` — bug pre-existing DIWARISI apa adanya, bukan diperbaiki sesi ini). Lihat baris ↳ di bawah utk sub-gate spesifik; daftar 25 destinasi × gate lengkap: `src/pages/foundation/AdminHub.jsx` (`HUB_GROUPS`+objek `G`), tak diulang penuh di sini. |
| FOUNDATION | ↳ User Access | UserAccessPage · UserEditPage | sama dgn baris di atas (`gMaster`) | LIVE. **[9 Agu 2026]** Redirect legacy `activeMenu==='users'` kini set `adminInitialSection='user-access'` sebelum `navigateTo('admin-hub')` — landing TEPAT di card User Access (dulu landing di card-grid default). **Tab "Permissions" `UserEditPage`** kini digate `isSuperAdmin` (tab switcher + body tab, defense-in-depth ganda, pola sama Role Defaults) — sebelumnya tab ini tampil & bisa diklik utk role `admin` biasa padahal RLS `user_menu_permissions` (`ump_admin_all`) cuma izinkan tulis dari `super_admin` (trap-UX: klik Save akan gagal RLS). |
| FOUNDATION | ↳ Role Defaults [BARU, 7 Agu 2026] | RoleDefaultsPage | **super_admin only** (`gMaster && role==='super_admin'`, dicek `AdminHub.jsx G['role-defaults']` — bukan lagi `AdminShell.jsx`) — cermin RLS `role_menu_permissions` yang juga super_admin-only utk tulis | LIVE (kode) — atur default `role_menu_permissions` per role + "Salin dari role lain" (destructive replace). ~~**⚠️ Tabel datanya KOSONG TOTAL**~~ **[KOREKSI 9 Agu 2026]** tidak lagi 100% kosong — migrasi `20260809000001` (baris Picking List/Surat Jalan §LOGISTICS) mengisi baris utk 2 menu spesifik × 5 role, TAPI **Fase B3 (isi default komprehensif SEMUA menu × role) tetap belum dimulai**. Belum tes manual runtime. |
| FOUNDATION | ↳ Products & Services (+Detail) | ProductsPage · ProductDetailPage | canRenderPage — **dual-access**, tetap top-level `NEXUS_NAV`/`ERP_MENU_GROUPS` DAN card di AdminHub | LIVE |
| FOUNDATION | ↳ Update Harga Massal / Schema Manager / BNF — Kepala & Direktur | BulkEditPricePage / SchemaManagerPage / BNFOrgRolesPage | role[super_admin] / **super_admin only** (lihat asimetri `view`/`open` di atas) / role[super_admin,admin] | LIVE. **[9 Agu 2026]** Ketiganya pindah dari leaf top-level tersendiri jadi card DI DALAM AdminHub — leaf lama di `NEXUS_NAV`/`ERP_MENU_GROUPS` DIHAPUS, TAPI 3 blok render `App.jsx` (`activeMenu==='bulk-edit-price'`/`'bnf-org-roles'`/`'schema-manager'`) **tidak ikut dihapus & tidak masuk daftar redirect legacy** — `08_TECH_DEBT.md` **TD-181** (2 dari 3 kini `AccessDenied` yang SALAH utk stale bookmark `nexus_last_menu`; `schema-manager` aman krn gate-nya independen). **[12 Agu 2026]** `BNFOrgRolesPage` dapat section KETIGA "Akses BNF Tambahan" (grant/revoke `bnf_authorized_users`) — gate section ini **LEBIH SEMPIT** dari gate `role[super_admin,admin]` halaman induknya: **HANYA `super_admin`** (`isSuperAdmin`, cermin RLS `bnf_authorized_users_read` yang juga super_admin-only). `admin` biasa TETAP bisa buka halaman (2 section pertama: Direktur Divisi/Kepala Departemen, tak berubah), tapi section ketiga ini disembunyikan TOTAL utknya (bukan ditampilkan read-only kosong). Detail: `03_DATA_MODEL.md` §BNF Layer 4. |

### Matrix granular per action (VIEW/CREATE/EDIT/DELETE/APPROVE/EXPORT/PRINT)

Model granular sebenarnya disimpan di DB: **`modules` → `module_menus` → `module_actions` / `menu_actions` → `user_menu_permissions`** (per-user-per-menu-action), diakses frontend via `hasMenuPermission(menuKey, action)`. ~~Juga ada `permissions` + `role_permissions` (per-role) via `hasPermission(module, action)`.~~ **[KOREKSI 20 Agu 2026]** `hasPermission()` **dihapus dari kode** (bukan lagi vestigial) — tabel `permissions`/`role_permissions` masih ada di DB tapi **nol pembaca di jalur aplikasi**. Lihat §5 poin 1.

> [TODO: isi tabel granular VIEW/CREATE/EDIT/DELETE/APPROVE/EXPORT/PRINT per menu — **tidak tersedia di `schema_snapshot.sql`** karena snapshot `--schema-only` (tanpa data). Perlu query langsung: `SELECT m.key, ma.code FROM module_menus m JOIN menu_actions ... ` atau export data RBAC tables. Action set yang ada di kode: `view`, `create`/`add`, `edit`, `delete`, `approve`, `export`, `print` — verifikasi via `module_actions`/`menu_actions`.]

---

## 4. RLS Functions

| Fungsi | Siapa yang masuk (true) | Efek |
|--------|-------------------------|------|
| `is_super_admin()` | `super_admin` (via `user_roles`, is_active, not revoked) | Bypass semua filter company → akses lintas-entitas. **Wajib top-level `OR`**, jangan nest di filter `company_id`. |
| `is_admin_or_above()` | **`super_admin`, `admin` SAJA** | ⚠️ TIDAK termasuk manager/ceo/gm — dipakai ~51 policy. Sumber bug akses (CEO ke-block, dropdown sales kosong utk manager). |
| `is_manager_or_above()` | super_admin, admin, ceo, gm, **gm_bd**, manager, sales_head | Cakupan "lihat seluruh tim/entitas" (RLS accounts/activities). `gm_bd` ditambah 9 Jul 2026 (dieksekusi user di DB MSI). |
| `get_user_company_id()` | — | `SELECT company_id FROM profiles WHERE id=auth.uid()`. Null sebelum backfill / di SQL Editor. |
| **`get_user_company_ids()`** [BARU, 9 Agu 2026] | — (jamak — SEMUA company tempat user punya `user_roles` AKTIF) | `RETURNS SETOF uuid`, SECURITY DEFINER — `SELECT DISTINCT company_id FROM user_roles WHERE user_id=auth.uid() AND is_active=true`. Fungsi TERPISAH dari `get_user_company_id()` (singular, tak disentuh — banyak policy lain memang harus tetap home-company-only). Dipakai bypass tambahan `OR company_id IN (SELECT get_user_company_ids())` — LIVE di `companies_read_own`/`roles_read`, **BELUM** di `payment_terms_read` (dan mungkin tabel lain) — `08_TECH_DEBT.md` **TD-180**. |
| `has_permission(module, action)` | query `user_roles→roles→role_permissions→permissions` | ⚠️ Flagged broken/unseeded (TECH_DEBT TD-02). **[7 Agu 2026]** Dikonfirmasi 0 pemakaian — 0 hit di RLS policy manapun, 0 hit `rpc()` dari FE (frontend `hasPermission()`/`role_permissions` resmi pensiun dari kode, TD-06 kini PARTIAL). **[20 Agu 2026]** Sisi FE naik satu tingkat lagi: `hasPermission`/`isCrossEntity`/`userPermissions`/`refreshPermissions`/`fetchPermissionsForRoleId` **dihapus fisik** dari `AuthContext.jsx` → **nol pembaca `role_permissions` di jalur aplikasi**. Satu-satunya penulis yang tersisa adalah halaman ORPHAN `RolesPage.jsx` (tak di-route dari mana pun — **TD-204**). Fungsi SQL `has_permission()` sendiri **tidak disentuh**, tetap 0 pemakaian. |
| `has_role(role_code)` / `get_user_role_code()` | cek role di `user_roles` | Helper. |
| **`is_admin_tier_role(p_role_id uuid)`** [BARU, 7 Agu 2026] | — (cek role code TARGET, bukan pemanggil) | SECURITY DEFINER. Dipakai `user_roles_insert`/`user_roles_update` (WITH CHECK/USING) — menolak admin biasa (`is_admin_or_above()` non-super) menulis baris `user_roles` yang menargetkan `role_id` tier `super_admin`/`admin`. Menutup **TD-170** (CRITICAL → RESOLVED), privilege escalation via RLS. |
| **`is_bnf_authorized()`** [BARU, 10 Agu 2026, diperluas 12 Agu 2026] | `super_admin` · kepala departemen BNF (`bnf_departments.head_profile_id`) · direktur divisi BNF (`bnf_divisions.director_profile_id`) · role aktif `ceo`/`gm`/`gm_bd`/`manager`/`finance_controller` (awalnya cuma `ceo`, diperluas 12 Agu) · grant non-revoked `bnf_authorized_users` | `LANGUAGE sql STABLE SECURITY DEFINER`. Gate akses gabungan modul BNF/Briefing Harian/Meeting Mingguan — RLS `bnf_reports_select`/`_insert`, `bnf_report_action_items_*`, `weekly_meetings_all`/`weekly_meeting_items_all`, trigger guard `daily_report_items`, DAN gate 2 menu FE (`bnf`/`meeting-mingguan` — `briefing-harian` TETAP `public:true`, tak pakai fungsi ini utk gate menunya sendiri). Role FUNGSIONAL (`hrga`/`it`/`operations`/`procurement`/`sales`/`finance`) sengaja TIDAK otomatis lolos. Detail: `03_DATA_MODEL.md` §BNF. |
| `check_similar_accounts(p_name, p_company_id)` ✅ **berjejak `20260725000004`** (sudah dijalankan DAN TESTED — `SELECT check_similar_accounts('ALLIANCE COSMETIC', <uuid MSI>)` balik 3 baris similarity 1.0/0.842/0.619; smoke test UI 25 Jul 2026: warning fuzzy *"Mirip dengan: ALLIANCE COSMETICS"* muncul di jalur Prospect & Customer CRM — **[KOREKSI 25 Jul 2026 atas klaim lama "belum tes runtime"]**; ⚠️ belum di snapshot) | — (bukan predikat role; **SECURITY DEFINER** → menembus RLS `accounts`) | **Pre-check kemiripan nama akun** untuk peringatan duplikat di form. **Sengaja menembus RLS**: `prospects_read` membatasi sales ke akun **miliknya sendiri**, sehingga pre-check dari klien biasa **buta terhadap akun sales lain** dan akan menjanjikan "aman" untuk nama yang justru ditolak index `uq_accounts_norm_name_per_entitas`. **Batas kebocorannya dijaga dua lapis:** (a) hasil di-scope **satu entitas** lewat argumen `p_company_id`; (b) yang dikembalikan **hanya `id` + `name` + skor** — kolom lain milik akun sales lain **tidak** ikut terbuka. `GRANT EXECUTE TO authenticated`. ⚠️ **Ini pelonggaran visibilitas yang DISENGAJA** — kalau kelak dipakai untuk hal lain, tinjau ulang dulu. *(Atribusi: hasil SELECT + smoke test UI = laporan Den 25 Jul 2026, bukan verifikasi doc-keeper langsung ke DB/browser.)* |

**Pola RLS accounts (acuan utama):**
`USING ((company_id = get_user_company_id() AND (is_manager_or_above() OR assigned_to=auth.uid() OR created_by=auth.uid())) OR is_super_admin())`.

Catatan: `profiles_read = USING(true)` (stopgap — semua authenticated baca profiles; TD-04).

---

## 5. Known Gaps

1. **TIGA sumber kebenaran permission yang tidak sinkron** (TECH_DEBT TD-06). **[UPDATE 7 Agu 2026 — kini DUA axis aktif, bukan tiga; TD-06 PARTIAL]:**
   - **RLS DB** — pakai role hardcode (`is_admin_or_above()` ~51 policy, `is_manager_or_above()`). TAK BERUBAH — TD-01 tetap prasyarat penuh.
   - ~~**`hasPermission(module, action)`** — frontend, baca `role_permissions`/`permissions`.~~ **PENSIUN DARI KODE (7 Agu 2026)** — fetch datanya dimatikan, tak lagi dipanggil di mana pun (dead branch `App.jsx canSeeMenuItem` juga dihapus). **[KOREKSI 20 Agu 2026 — kalimat lama "struktur JS dibiarkan ada (vestigial)" SUDAH TIDAK BERLAKU]** strukturnya kini **DIHAPUS FISIK**: `hasPermission` · `isCrossEntity` · `userPermissions`/`setUserPermissions` · `refreshPermissions` · `fetchPermissionsForRoleId` (satu-satunya pembaca `role_permissions` di jalur auth) + 4 entri `value` yang di-export — semuanya hilang dari `AuthContext.jsx` (429→391 baris). Di `App.jsx`, parameter `hasPermission` dicabut dari **5 fungsi** (`canSeeMenuItem`/`navChildGate`/`navModuleVisible`/`isMenuAccessible` + props `NexusSidebar`) berikut 23 call site + dep array + 2 prop JSX `hasPermission={hasPermission}` (sidebar desktop + drawer mobile); tanda tangan kini seragam `(item, role, hasMenuPermission, isBnfAuthorized)`. **Diverifikasi doc-keeper via grep `src/`:** `isCrossEntity`/`refreshPermissions`/`fetchPermissionsForRoleId` = **0 hit**; `hasPermission` (di luar `hasMenuPermission`) = **1 hit**, komentar historis di file orphan `AdminShell.jsx:129`; `userPermissions` = **2 hit**, keduanya baris komentar penjelas baru di `App.jsx:2151-2153`. **Nol hit fungsional.** Objek DB (`role_permissions`/`permissions`) tetap dibiarkan ada, kini **nol pembaca** — penulisnya tinggal halaman orphan `RolesPage.jsx` (`08_TECH_DEBT.md` **TD-204**).
   - **`hasMenuPermission(menuKey, action)`** — frontend, kini **resolusi 3-tingkat**: super_admin → user override (`user_menu_permissions.effect`, grant/deny) → role default (tabel baru `role_menu_permissions`, union semua role aktif) → deny. Lebih canggih dari sebelumnya (dulu cuma baca `user_menu_permissions` tanpa fallback role-level).
   → UI bisa mengizinkan aksi yang RLS tolak (atau sebaliknya) — axis RLS vs axis FE **TETAP** dua sumber kebenaran terpisah, TAK disatukan sesi ini. Konsolidasi PENUH (termasuk RLS) = bagian migrasi RLS RBAC-driven (TD-01), belum dikerjakan.
2. **`is_admin_or_above()` tak kenal manager/ceo** → memicu stopgap (`profiles_read USING(true)`, dropdown sales kosong utk manager sudah di-fix di frontend tapi RLS tetap perlu dibenahi).
3. **`has_permission()` broken/unseeded** (TD-02) — tabel `permissions`/`role_permissions` ada di snapshot tapi status seed belum dikonfirmasi.
4. **Audit CRUD/DELETE policy belum lengkap** (TD-03) — hanya ~4 dari ~50 tabel punya DELETE policy; UPDATE "admin-only" pernah nyangkut owner-edit.
5. **Migrasi RLS proper (RBAC-driven, 4-fase)** — BESAR, risiko tinggi, eksekusi sesi fresh, prasyarat HRIS (TD-01).
