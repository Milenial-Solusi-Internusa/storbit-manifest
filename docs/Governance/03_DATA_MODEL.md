# DATA MODEL — Nexus by MSI

> Referensi database. Sumber kebenaran struktur = **`supabase/schema_snapshot.sql`** (`pg_dump` full). Migrasi formal berhenti 3 Jun 2026 — banyak perubahan via SQL Editor (snapshot bisa lag; lihat §6).
>
> **Diperbarui 2026-08-24 — konsekuensi globalisasi `roles`/`departments`/`positions`/`branches` terhadap pola query FE (dokumentasi menyusul kode; FE-only 3 file, NOL perubahan DB/RLS).** Sejak migrasi `20260821000002`/`20260821000003` (21 Agu 2026) keempat tabel master itu memakai `company_id = NULL` untuk baris survivor, jadi **memfilter `<tabel>.company_id` di frontend sekarang SELALU mengembalikan nol baris** — gagal senyap, bukan error. Dicatat permanen sbg **gotcha #18** (baru) karena polanya gampang terulang di modul mana pun. Konsekuensi yang sudah ditambal + yang masih terbuka: `08_TECH_DEBT.md` **TD-207**/**TD-209**. ⚠️ **Nol perubahan skema** di sesi ini — yang berubah cuma dokumentasi + 3 query FE. Detail: `PROGRESS.md` 2026-08-24.
>
> Sebelumnya **2026-08-21 — Storbit: partial picking dibuka (1 fungsi DB + 2 file FE). Dikerjakan 21 Agu 2026.** ⚠️ **SQL BELUM DIJALANKAN** **[KOREKSI 24 Agu 2026, doc-keeper: SUDAH DIJALANKAN — header migrasi `Status: LIVE`, guard baru terbaca di `schema_snapshot.sql:795`; hanya "NOL tes runtime" yang masih berlaku]** — migrasi `20260821000001_partial-picking-guard.sql` ditulis **SEBELUM** eksekusi (disiplin yang benar, kebalikan pola retroaktif `20260817000001`), jadi `schema_snapshot.sql` **masih memuat guard LAMA** (`status <> 'cancelled'`, `:713`) dan itu memang kondisi DB hari ini — **bukan** snapshot basi. Yang berubah di dokumen ini: entri `picking_lists`/`picking_list_items` §3 (kolom `qty_picked`/`qty_short` + nilai enum `'short'` akhirnya punya penulis), baris `generate_picking_from_sp` §5 (guard idempotensi 1 blok → 2 blok), **gotcha #17 baru** (`dispatched_at IS NOT NULL` vs `status IN ('in_transit','delivered')` = DUA pertanyaan berbeda) + catatan utang eksekusi di gotcha #10. Konsekuensi yang DISENGAJA (picking `done` tanpa SJ = jalan buntu) dicatat `08_TECH_DEBT.md` **TD-206**. **NOL tes runtime.**
>
> Sebelumnya **2026-08-18 — Dashboard Storbit (Shipping Manifest + Warehouse): 2 kolom baru + 3 RPC agregasi/drill-down. Dikerjakan 18 Agu 2026.** **SELURUH SQL DIKONFIRMASI LIVE & SNAPSHOT SEGAR** — doc-keeper membaca langsung `schema_snapshot.sql`: `products.reorder_point` (`:6256`, COMMENT `:6306`) · `sp_orders.price_category` + CHECK (`:6931`) + `GRANT UPDATE(price_category)` per-kolom (`:18462`, wajib karena `sp_orders` tak punya table-level GRANT UPDATE sejak fix TD-175 — tanpa itu wiring UI gagal senyap/403, persis pola TD-166) · 3 RPC baru `get_storbit_dashboard_stats` (`:789`) / `get_storbit_sp_drilldown` (`:913`) / `get_storbit_stock_drilldown` (`:985`), **semuanya SECURITY INVOKER + STABLE** (read-only, RLS pemanggil tetap berlaku), ACL pola FASE 5 (`REVOKE ALL … FROM PUBLIC` + `GRANT … TO authenticated`, **nol baris `anon`** — sengaja TIDAK meniru `indomarco_dashboard_stats`/`storbit_sp_customers` yang ter-GRANT ke anon; default privileges Supabase meng-GRANT fungsi baru ke anon OTOMATIS, jadi REVOKE eksplisit bukan formalitas) · 2 index baru `idx_sp_btb_sp_order_live` (`:9997`) / `idx_sp_orders_company_status` (`:10025`). Ketiganya direkam migrasi `20260818000001`–`20260818000003`, **ditulis SEBELUM eksekusi lalu headernya dikoreksi jadi `Status: LIVE` setelah dijalankan** (kebalikan pola retroaktif `20260817000001`; disiplin migrasi sesi ini beres dua-duanya — file direkam DAN snapshot di-refresh). **Kedua kolom sengaja TIDAK di-backfill:** `reorder_point` menunggu angka SOP Storbit (Finance/Warehouse Controller), `price_category` NULL untuk seluruh SP existing (tipe SP historis tak diketahui sistem; menebaknya lebih buruk daripada mengakui tak tahu). **Redefinisi konsep:** `expired_date` = **tenggat WAJIB KIRIM (risiko pinalti)**, BUKAN validitas harga → `05_WORKFLOW_MAP.md §Logistics`. **TD-178 bertambah satu kasus** (hardcode SOA kini juga di FE `StorbitDashboardPage.jsx:53` — perbaikan sadar atas bug senyap, komentar kodenya sudah dikoreksi jadi rekaman alasan) + 3 TD baru (**TD-200/TD-201/TD-202**) + **koreksi TD-199** (sisa langkah manualnya sudah lunas) — `08_TECH_DEBT.md`. Tes runtime browser = **klaim Den yang di-relay**, BUKAN verifikasi doc-keeper; yang diverifikasi doc-keeper sendiri: isi snapshot + `npm run lint` net-zero (171/149/22). Detail: `PROGRESS.md` 2026-08-18.
>
> Sebelumnya **2026-08-17 — Storbit SP FASE 5: pembayaran (LUNAS) + jurnal AR minimal + TTF sbg dokumen transmittal, plus gate anti double-entry `ar_btbs`. Dikerjakan 16-17 Agu 2026.** **SELURUH SQL DIKONFIRMASI LIVE** — doc-keeper membaca langsung `schema_snapshot.sql` pasca-refresh (bukan grep sepintas): 2 tabel baru **`journal_entries`**/**`journal_entry_lines`** (`CREATE TABLE` baris 5416/5440), `sp_payments` **+2 kolom** `bukti_potong_url`/`bukti_potong_no` (baris 6704-6705), `ar_ttfs` **+1 kolom** `diterima_oleh` (baris 3090) + 2 index FK menyusul kolom FASE 4 (`idx_ar_ttfs_invoice_id`/`idx_ar_ttfs_sp_order_id`, baris 8695/8702). 2 RPC baru **`record_payment`** (baris 1796, dibaca penuh) / **`mark_ttf_received`** (baris ~1480, dibaca penuh) + 2 RPC di-`CREATE OR REPLACE`: `sp_recompute_status` (+cabang `WHEN v_paid THEN 'LUNAS'` di PUNCAK CASE, baris 2432) & `create_invoice` (+blok jurnal `invoice_issued` 4 baris, baris 413-436). **Mesin status 12 tahap kini LENGKAP s/d LUNAS** — tahap terakhir yang sejak FASE 0 tercatat "belum dibangun sama sekali" kini punya RPC+UI+jurnal. **Jurnal AR sengaja MINIMAL & auto-post tanpa approval:** cuma 2 pemicu, dikunci CHECK `journal_entries_reference_type_check` ke persis `invoice_issued`/`payment_received`; **tanpa kolom `updated_at` — by design, koreksi = jurnal pembalik, BUKAN UPDATE baris lama**; `journal_entry_lines_sign_check` menolak baris ber-debit DAN credit sekaligus. **RLS jurnal READ-ONLY** (`journal_entries_read`/`journal_entry_lines_read`, `FOR SELECT TO authenticated`) — **NOL policy INSERT/UPDATE/DELETE**, GRANT tabel cuma `SELECT` → PostgREST tak bisa menulis jurnal sama sekali, tulis hanya lewat RPC SECURITY DEFINER. Keduanya pakai **`get_user_company_ids()` varian JAMAK** (sengaja, supaya user multi-company tak terkunci — pola TD-180, §4). **Migrasi `20260817000001_fase5_jurnal_ar_payments_ttf.sql` ADA tapi RETROAKTIF** (SQL dijalankan manual di SQL Editor lebih dulu, file ditulis setelahnya dari isi snapshot — BUKAN utk di-run ulang); **snapshot SUDAH di-refresh** untuk FASE 5 (bukan utang, beda dari pola berulang gotcha #10). **TD-176 → PARTIAL** (tertutup HANYA utk `sp_payments`), **TD-180 → PARTIAL**, +2 TD baru (**TD-197** casing `dppPPN`/`dppPpn`, **TD-198** kolom mati `sp_orders.confirmed_at/by`) — `08_TECH_DEBT.md`. **Tes end-to-end satu siklus penuh dilaporkan LOLOS oleh Den (klaim di-relay, BUKAN verifikasi independen doc-keeper)** — detail & batasannya di `PROGRESS.md` 2026-08-17. Detail: `PROGRESS.md` 2026-08-17 + `05_WORKFLOW_MAP.md` §Logistics.
>
> Sebelumnya **2026-08-12 — BNF extension 4-lapis (Layer 0-4): Briefing Harian (modul baru) + BNF diperluas (gate `is_bnf_authorized()`) + Meeting Mingguan (modul baru) + "Akses BNF Tambahan" (grant/revoke). Dikerjakan 10 & 12 Agu 2026 (jeda 1 hari tanpa commit, 11 Agu).** **Governance docs 03/04/05 SEBELUMNYA NOL dokumentasi untuk seluruh modul BNF** (termasuk fondasi 3-5 Agu 2026 — lihat gap yang sudah ditandai `CLAUDE.md` entri 2026-08-04/05) — seluruh entri BNF di bawah (§3 baru, §5 RPC baru, gotcha #15 baru) net-new, BUKAN amandemen. **7 tabel baru** (Layer 0, migrasi `20260810000001`-`20260810000004`, semua GRANT lengkap ke `authenticated` — TAK ada insiden GRANT-hilang spt TD-166 sesi ini): `bnf_authorized_users` (grant/revoke akses BNF di luar jalur struktural head/direktur/CEO), `bnf_division_scopes`/`bnf_department_scopes` (model home+scope utk posisi group-wide, mis. BD GM home MSI scope JCI+SOA — gantikan tombol "Salin ke entity lain" yang **dibatalkan**; **doc-keeper TAK menemukan jejak commit tombol itu di git log, tercatat sbg klaim Den yang belum terverifikasi independen**), `daily_report_items` (backing **Briefing Harian**, kategori task/aktivitas/insiden + carry-forward), `bnf_report_action_items` (checklist tindak lanjut, `assigned_to` wajib), `weekly_meetings`+`weekly_meeting_items` (backing **Meeting Mingguan**). Fungsi gate baru **`is_bnf_authorized()`** (`LANGUAGE sql STABLE SECURITY DEFINER`) — true utk `is_super_admin()`, `head_profile_id`/`director_profile_id` (`bnf_departments`/`bnf_divisions`), role aktif `ceo` (lalu **diperluas 12 Agu 2026** ke `gm`/`gm_bd`/`manager`/`finance_controller` — keputusan sadar: seluruh staff akan dilatih SOP BNF, gate per-assignment terlalu sempit; role FUNGSIONAL `hrga`/`it`/`operations`/`procurement`/`sales`/`finance` SENGAJA dikecualikan), atau grant `bnf_authorized_users` non-revoked. Dipakai RLS `bnf_reports_select`/`_insert` (**diperketat** — staff biasa TAK LAGI baca/tulis `bnf_reports` langsung kecuali lolos gate ini, beda dari perilaku "terbuka semua staff" sebelumnya) + trigger guard `daily_report_items` + gate 3 menu FE (`bnf`/`briefing-harian` [TETAP public]/`meeting-mingguan`). RPC baru **`get_linked_bnf_status(p_daily_report_item_id)`** (`SECURITY DEFINER`, self-gating internal) — badge status BNF di Briefing Harian tanpa expose nomor laporan ke reporter non-authorized; tanpa GRANT eksplisit (Postgres default PUBLIC-executable, dikonfirmasi TAK ada di daftar REVOKE) — pola sama `is_bnf_authorized()`. **2 bug produksi ditutup sesi ini:** badge "Jadi BNF" tak tampilkan nomor (akar: RLS blokir embed `bnf_reports` utk reporter non-authorized — PostgREST resolve NULL senyap, BUKAN join rusak — fallback `'BNF'` menutupinya; fix pakai RPC di atas, badge kini "Jadi BNF · Status") + default filter tab Overview BNF `'active'`→`'all'` (laporan Closed sempat tampak hilang — gap *discoverability* filter dropdown, BUKAN bug logika; KPI strip sudah benar sejak awal, dihitung dari fetch tak terfilter). **Seluruh SQL dikonfirmasi live** langsung baca `schema_snapshot.sql` (6 migrasi `20260810000001`-`20260811000002`, semua direkam file). **NOL tes runtime independen oleh doc-keeper** — Den melaporkan smoke test penuh Passed, di luar kapasitas verifikasi sesi dokumentasi. +11 tech debt baru **TD-184…TD-194** (`08_TECH_DEBT.md`). Detail: `PROGRESS.md` 2026-08-10 & 2026-08-12.
>
> Sebelumnya **2026-08-08, KOREKSI SUSULAN SAMA HARI (snapshot di-refresh — commit `8117127`, 23:33 WIB, "chore(db): refresh schema snapshot — Fase 4 Invoice, RLS hardening, dual-write", 2186 insersi, +416 statement `GRANT`):** Banner di bawah ini awalnya ditulis saat snapshot MASIH basi (sebelum `8117127`) dan menandai area (3)/(4)/(5) "SQL BELUM DIJALANKAN" — **itu bacaan yang JUJUR terhadap bukti yang ada SAAT ITU, tapi kini SUDAH TIDAK BENAR.** Doc-keeper memverifikasi ULANG langsung ke `schema_snapshot.sql` pasca-refresh (bukan sekadar percaya klaim) — **hasilnya: SELURUH SQL 6 area di bawah kini TERKONFIRMASI LIVE**, KECUALI dua hal yang **masih genuinely OPEN**: **(a)** GRANT kolom-level utk `sp_order_items`/`sp_items`/`sp_invoice_lines`/`sp_payments` — diverifikasi ULANG, keempatnya **masih** `GRANT ALL ON TABLE ... TO authenticated` (bukan cuma UPDATE — lebih longgar dari perkiraan awal), **TD-176 tetap OPEN**; **(b)** backfill DATA historis `ar_ttfs.sp_order_id`/`invoice_id` (step M7) — kolom+FK constraint-nya ADA (diverifikasi `schema_snapshot.sql:9655-9667`), tapi **snapshot schema-only tak bisa membuktikan baris-baris lama sudah terisi** — status M7 TETAP "belum dikerjakan" per catatan sesi asli, TIDAK diverifikasi ulang di sini. **Rincian per area, semua diverifikasi doc-keeper baca LANGSUNG isi fungsi/tabel di `schema_snapshot.sql`, bukan cuma grep sepintas:** **(1) DC Master** — tak terpengaruh isu snapshot (FE+RLS lama), tetap seperti semula. **(2) DcPicker** — idem. **(3) FASE 4 Invoice — LIVE.** `sp_invoices`/`sp_invoice_lines`/`sp_payments` ADA (`CREATE TABLE`, baris 5799/5815/5977); `ar_ttfs` +2 kolom FK ADA (`ar_ttfs_sp_order_id_fkey`/`ar_ttfs_invoice_id_fkey`). RPC `create_invoice` (baris 314) & `submit_invoice` (baris 1934) ADA, dibaca penuh — guard `Σshipped=Σqty` persis seperti diklaim; kode entitas invoice **dikonfirmasi dinamis** (`SELECT code INTO v_entity_code FROM companies WHERE id=v_company_id`, BUKAN hardcode "SBI"); otorisasi persis `is_super_admin() OR is_manager_or_above() OR has_role('finance_controller')`. `sp_recompute_status` (baris 1856) — **CREATE OR REPLACE TERKONFIRMASI LIVE**: guard kini **`IF v_status IN ('CANCELLED','LUNAS') THEN RETURN;`** (bug lama sungguh sudah diperbaiki), cabang `WHEN v_submitted THEN 'SUBMITTED'` & `WHEN v_has_invoice THEN 'INVOICED'` ADA di prioritas teratas (di atas BTB_TERBIT). **(4) RLS/GRANT hardening — TD-175 kini RESOLVED** (lihat detail verifikasi di entri RPC/gotcha #9 + `08_TECH_DEBT.md` TD-175); **TD-176 tetap OPEN** (lihat poin (a) di atas). **(5) Dual-write trilogy — LIVE.** `dispatch_delivery` (baris 475) & `cancel_delivery` (baris 173) — blok sync/reversal `sp_order_items.shipped_qty` via `legacy_sp_item_id` ADA persis seperti diklaim (ditandai komentar `-- ===== BARU:` di source). `update_sp_item_dual` (baris 2056) ADA — **dual-write KELIMA field terkonfirmasi** (`qty`, `sla_days`, `estimated_delivery_date`, `shipping_price`, `notes`, bukan cuma 2 seperti dugaan awal doc-keeper) — `sp_order_items` (baris 5915) memang punya kelima kolom itu sbg kolom asli (`sla_days integer`, `estimated_delivery_date date`, `notes text` — sebelumnya tak lengkap tercatat di sini, dikoreksi di entri `sp_order_items` di bawah). **Nuansa verifikasi (lihat entri RPC §5 utk detail persis):** pemetaan kolom **terverifikasi BENAR via pembacaan langsung body fungsi LIVE** (lebih kuat dari "code review" — ini bacaan ke apa yang tersimpan di DB sungguhan) untuk kelima field; TAPI **eksekusi dengan data uji nyata** baru terbukti utk `notes` (skrip data ZZZTEST sesi ini) — `sla_days`/`estimated_delivery_date` belum pernah dieksekusi dgn data uji, meski pemetaannya sendiri sudah terverifikasi benar. Backfill satu-kali 115 baris (terpisah dari mekanisme sinambung di atas) **tetap seperti semula: LIVE**. **(6) InvoicePDF** — tak terpengaruh isu snapshot (murni FE), tetap seperti semula (NPWP SOA kosong = data belum diisi bukan bug; QR placeholder = keputusan belum diambil). **Yang TIDAK berubah oleh koreksi ini:** hardcode UUID SOA di `sp_recompute_status` (**TD-178**) **masih ada** (`v_company uuid := 'd2e5e565-...'`, baris 1861) — **DAN ditemukan SATU LAGI instance yang sama** di `dispatch_delivery` (baris 479, dipakai jalur `stock_ledger`/fallback `warehouse_id`) — TD-178 diperluas cakupannya, lihat `08_TECH_DEBT.md`. **NOL tes runtime/UI tetap NOL** — SQL live ≠ ada yang sudah mengklik tombolnya di browser; klaim itu TIDAK berubah oleh temuan ini. Detail penuh koreksi: `PROGRESS.md` 2026-08-08 (entri susulan).
>
> Sebelumnya **2026-08-08 (draft awal, ditulis SAAT snapshot masih basi — dipertahankan sbg jejak, isinya SEBAGIAN sudah dikoreksi banner di atas, JANGAN dibaca sbg fakta terkini tanpa cross-check ke koreksi di atas):** Storbit SP Fase 4 Invoice + DC Master CRUD + DcPicker + RLS GRANT hardening + dual-write trilogy `sp_items`↔`sp_order_items` + InvoicePDF — 6 area saling terkait. **(1) DC Master** — `dc_master` (§3 Foundation, entri di bawah) kini CRUD penuh via halaman admin baru `DcMasterPage.jsx` + hook `useDcMaster.js` (`AdminShell` › Configuration) — sebelumnya read-only (cuma dropdown `InputSPPage`); 45 DC awal berasal dari migrasi otomatis FASE 0. **(2) DcPicker** — field "Distribution Center" `InputSPPage` diganti combobox searchable `DcPicker.jsx` (pola `AccountPicker`, filter kode/nama) menggantikan `<select>` native 45+ opsi; filter per-customer TAK berubah. **(3) FASE 4 Invoice (INVOICED/SUBMITTED):** 3 tabel baru `sp_invoices`/`sp_invoice_lines`/`sp_payments` (skema `DESIGN_SP_SCHEMA.md` §2.5, migrasi step **M2** = pembuatan tabel saja; `sp_payments` dibuat tapi FASE 5/payment BELUM dibangun) + 2 kolom baru `ar_ttfs.sp_order_id`/`invoice_id` (FK, **TANPA backfill** — itu step **M7**, belum dikerjakan) + RPC baru `create_invoice`/`submit_invoice` + `sp_recompute_status` (`CREATE OR REPLACE`) dapat cabang INVOICED/SUBMITTED **DAN fix bug guard nyata**: guard freeze lama ikut memuat `INVOICED`/`SUBMITTED` sendiri, sehingga status yang sudah INVOICED **macet permanen**, tak pernah bisa naik ke SUBMITTED — bug ditemukan Den (bukan doc-keeper), guard dipersempit ke `CANCELLED,LUNAS` saja. Kartu "Invoice" baru di `SalesOrderDetailPage.jsx` (Terbitkan Invoice/Submit/Download PDF). Kode entitas invoice **diresolusi dinamis dari `companies.code`** saat runtime (BUKAN hardcode "SBI" — sempat diduga hardcode pertengahan sesi, diinvestigasi & dikoreksi). **(4) RLS/GRANT hardening:** ditemukan celah `authenticated` bisa `UPDATE sp_orders.status` LANGSUNG lewat PostgREST, membypass total `sp_recompute_status`/mesin status 12-tahap — fix: REVOKE UPDATE penuh dari `sp_orders`, GANTI GRANT kolom-spesifik (24/25 kolom, `status` dikecualikan); `sp_invoices` serupa (GRANT kolom-spesifik, kecuali 6 kolom system-computed). Kolom SEJENIS yang SENGAJA belum ditambal sesi ini: `sp_order_items.shipped_qty`/`sp_items.sp_status`/`sp_invoice_lines.dpp,ppn,qty` → **TD-175** (fix, `08_TECH_DEBT.md`)/**TD-176** (deferred, baru) — axis GRANT kolom-level ini BEDA dari axis RLS baris-level **TD-173**/**TD-39** (saling melengkapi, bukan duplikat). **(5) Dual-write trilogy `sp_items`↔`sp_order_items`:** `dispatch_delivery`/`cancel_delivery` (`CREATE OR REPLACE`) + RPC baru **`update_sp_item_dual`** (dipakai `updateSpItem`, `db.js`) dirancang menyinkronkan `sp_order_items` dari `sp_items` secara sinambung — **backfill satu-kali 115 baris SUDAH DIJALANKAN** (1 baris dikecualikan: SP 2181355, status CANCELLED). Ini akar sebab Invoice FASE 4 tak bisa dipakai utk SP lama manapun SEBELUM backfill (`sp_order_items.shipped_qty` selalu 0/basi → guard `Σshipped=Σqty` RPC `create_invoice` mustahil lolos) → **TD-40 diperbarui**. **(6) InvoicePDF** — dokumen baru `@react-pdf/renderer` (`src/modules/logistics/InvoicePDF.jsx`), identitas visual ungu/krem/serif **SENGAJA beda** dari Picking List/Delivery Note (navy/orange) — keputusan desain Den, bukan inkonsistensi/oversight. NPWP entitas SOA (`companies.tax_id`) kosong di DB — **bukan bug**, data belum diisi Den. QR code placeholder di PDF sengaja kosong — tujuan link belum diputuskan, follow-up terpisah. Detail penuh: `PROGRESS.md` 2026-08-08.
>
> Sebelumnya **2026-08-07 (RBAC hardening — fix eskalasi role `user_roles` [LIVE] + sistem `role_menu_permissions`/`user_menu_permissions.effect` baru [LIVE DB, snapshot BELUM refresh] + pensiun `role_permissions`/`has_permission()` dari kode [objek DB dibiarkan ada]):** Fungsi baru **`is_admin_tier_role(uuid)`** (§4) + validasi role-tujuan di RLS `user_roles_insert`/`update` (§3) menutup privilege escalation CRITICAL (admin biasa dulu bisa assign `role_id=super_admin`) — `08_TECH_DEBT.md` **TD-170 RESOLVED**. Tabel baru **`role_menu_permissions`** (default menu-permission per role, role-only-grant) + kolom baru **`user_menu_permissions.effect`** (`grant`/`deny`) jadi fondasi resolusi 3-tingkat `hasMenuPermission` (user override > role default > deny) — keduanya direkam migrasi `20260807000002_role_menu_permissions_and_effect_column.sql` tapi **BELUM masuk `schema_snapshot.sql`** (utang `pg_dump` baru — lihat gotcha #10 update lanjutan). `role_permissions`/`permissions`/`has_permission()` dikonfirmasi 0 pemakaian (RLS maupun FE) — lihat §4, §6 gotcha #5, `08_TECH_DEBT.md` TD-02/TD-06. **Terpisah, sesi sama, TAK terkait RBAC:** audit RLS/RBAC menyeluruh menemukan 19 tabel CRITICAL ber-RLS longgar (belum dieksekusi, `08_TECH_DEBT.md` **TD-173**) + fix double-write `accounts.pipeline_stage` (**TD-171**, PARTIAL — test D/E belum jalan). Detail: `PROGRESS.md` 2026-08-07.
>
> Sebelumnya **2026-08-06 (koreksi klaim salah trigger `track_stage_change()`/`trg_z_track_stage_change` — DOKUMEN-ONLY, NOL perubahan DB/kode; ditemukan sesi audit read-only `REPORTING_READINESS.md`, cross-check independen `OVERDUE_STATUS_AUDIT.md`, keduanya root repo):** Baris **Trigger fns** (§5, tabel RPC, di bawah) menulis klaim salah bahwa `track_stage_change()` "log pipeline stage → `activity_logs`". **Diverifikasi langsung ke source function** (`schema_snapshot.sql:1898-1908`): isinya **HANYA** `IF NEW.pipeline_stage IS DISTINCT FROM OLD.pipeline_stage THEN NEW.stage_changed_at = now(); END IF;` — **TIDAK ADA statement INSERT ke tabel mana pun**. Trigger ini semata-mata menstempel ulang `accounts.stage_changed_at` — **BUKAN riwayat**, nilai lama tertimpa, tak bisa direkonstruksi dari trigger ini. Alasan struktural tambahan kenapa klaim lama mustahil benar: `activity_logs.activity_id` adalah kolom **`NOT NULL`** ber-FK wajib ke `activities` (`schema_snapshot.sql:2028-2036`, diverifikasi ulang doc-keeper) — tabel itu didesain untuk audit status **activity**, bukan event `accounts`. Entri **Trigger fns** dikoreksi in-place di §5 di bawah. **Klaim yang sama juga salah di `05_WORKFLOW_MAP.md` (dua titik, §CRM/Sales Flow)** — dikoreksi terpisah di dokumen itu, tak diulang di sini. → `PROGRESS.md` 2026-08-06.
>
> Sebelumnya **2026-07-27 (lanjutan SAMA HARI — Batch 3C: RPC `prf_mark_quoted`/`prf_select_offer` [sudah ada sejak fondasi pagi, BUKAN baru] akhirnya dapat UI + dasar hitung "Buat Quotation" pindah ke `selected_offer_id`; 4 file FE `feat/prf-audit` belum di-commit [`src/lib/auditLogger.js`, `src/modules/procurement/PRFDetailPage.jsx`, `src/modules/crm/DealPanels.jsx`, `src/modules/crm/DealDetailPage.jsx`]; NOL perubahan DB/RLS/migrasi sesi ini — kedua RPC + kolom `min_offers_waiver_reason` sudah ada sejak migrasi `20260727000003`):** **NOL kolom/tabel/RPC baru** — sesi ini murni FE memberi kedua RPC pemanggil pertamanya. Tombol "Nyatakan Penawaran Siap" (`PRFDetailPage.jsx`, procurement, `prf_mark_quoted`) menampilkan `min_offers_waiver_reason` (kolom yang sudah ada tapi tak pernah di-`SELECT` file ini sebelumnya) sebagai teks oranye begitu terisi. Tombol "Pakai/Ganti Penawaran Ini" (`DealDetailPage.jsx`, sales, `prf_select_offer`) — RLS `prf_vendor_offers_select`/`prf_cost_items_select` (diverifikasi `schema_snapshot.sql:13018-13020`/`:13088-13090`) SUDAH mengizinkan `created_by=auth.uid()` membaca lewat EXISTS, TANPA policy baru. **Dasar hitung modal "Buat Quotation"** (`PRFDetailPage.jsx`) pindah dari filter `is_awarded` murni ke formula UNION: baris `offer_id IS NULL` (warisan) **tetap** `is_awarded=true` (keputusan sadar — cegah baris vendor kalah PRF lama tergelembungkan; filter ini gugur bareng saat `is_awarded` dibuang di Fase 4) + baris `offer_id=prf.selected_offer_id` **tanpa syarat `is_awarded` sama sekali** → menutup **`08_TECH_DEBT` TD-156 (RESOLVED kode)**. **Koreksi klaim lama "`prf_mark_quoted`/`prf_select_offer` MASIH genuinely nol pemanggil FE / nol hit"** (ditulis berulang di banner-banner di bawah + entri `prf_vendor_offers` §3) — **sudah tidak benar**, ditutup batch ini. **Cakupan TETAP SEMPIT:** notifikasi PRF MASIH nol; transisi `CANCELLED`/`EXPIRED` (TD-154) MASIH nol RPC; `prf.selected_by`/`selected_at` (kolom yang sudah ada di DB) MASIH tak di-select/ditampilkan di mana pun (diverifikasi diff — hanya `selected_offer_id`+`min_offers_waiver_reason` yang ditambah ke select). **NOL tes runtime.** Detail: `PROGRESS.md` 2026-07-27 (entri Batch 3C) + `05_WORKFLOW_MAP` §Procurement — PRF (subbagian "Penawaran Vendor") + `08_TECH_DEBT` TD-156.
>
> Sebelumnya **2026-07-27 (lanjutan SAMA HARI — Batch 3B: sisi TULIS modul "Penawaran Vendor" — tambah/edit/hapus penawaran vendor + rincian biaya; 3 file FE `feat/prf-audit` belum di-commit [2 diubah `src/lib/auditLogger.js`+`src/modules/procurement/PRFDetailPage.jsx`, 1 baru `src/modules/procurement/PRFVendorOfferModal.jsx` 299 baris]; NOL perubahan DB/RLS/migrasi sesi ini — seluruh RLS dipakai sudah live; PLUS satu prasyarat yang SUDAH DI-COMMIT TERPISAH sebelum sesi ini, commit `a404aac`, genuinely nol dokumentasi sebelum entri ini):** `a404aac` memperbaiki panel "Jawaban Harga" LAMA supaya HANYA memproses baris warisan `prf_cost_items.offer_id IS NULL` (sebelumnya memproses SEMUA baris → begitu Batch 3B mulai menulis baris ber-`offer_id`, panel lama akan menampilkannya dobel & menuliskannya ulang tanpa `offer_id` saat disimpan — akar masalah ditutup lebih dulu). Batch 3B sendiri: modal baru `PRFVendorOfferModal.jsx` — form + tabel biaya, simpan **DUA FASE NON-ATOMIK** (INSERT/UPDATE offer, lalu REPLACE `prf_cost_items` — delete-lalu-insert, pola sama `save_prf_pricing`). **⭐ Tiap baris `prf_cost_items` baru WAJIB isi `offer_id` DAN `prf_id`** — diverifikasi `schema_snapshot.sql:13009-13031`: ketiga policy `prf_cost_items_insert/update/delete` mencari induk lewat `prf_id`, BUKAN `offer_id`. **⭐ `is_awarded` di-set EKSPLISIT `false`** (bukan default kolom `true`, `:4841`) — kalkulasi "Buat Quotation" (`costTotalIdr`) masih basis `is_awarded` (model LAMA), belum pindah ke `selected_offer_id`; dibiarkan default → modal terhitung membengkak tiap penawaran baru. **PRASYARAT WAJIB Batch 3C** (instruksi eksplisit Den) → **`08_TECH_DEBT` TD-156 (baru, MEDIUM)**. Tombol Tambah gate **`canManageOffers`** (HANYA pemegang PRF sendiri, TANPA fallback manager — beda dari `canRelease` — cermin PERSIS RLS `prf_vendor_offers_insert/update` yang `acknowledged_by=auth.uid()` TANPA `IS NULL OR`, `:13079-13101`). Hapus **DIBLOKIR** saat `prf.selected_offer_id` menunjuk offer itu (keputusan bisnis: penawaran terpilih = dasar harga jual quotation yang mungkin sudah dikirim sales); hard-DELETE `prf_cost_items` + soft-DELETE `prf_vendor_offers`. `ENTITY_TYPES` audit TETAP `PRF` (bukan entity offer terpisah — supaya riwayat satu PRF tak terpecah dua `entity_type`). **Koreksi klaim lama "form tambah/edit `prf_vendor_offers` MASIH nol UI"** (ditulis di banner Batch 3A di bawah + entri `prf_vendor_offers`) — **TAK LAGI BENAR**, ditutup batch ini. **Cakupan TETAP SEMPIT:** `prf_mark_quoted`/`prf_select_offer` MASIH genuinely nol pemanggil FE (diverifikasi grep ulang `src/`); panel Detail Inquiry sales (3C) MASIH nol UI. **NOL tes runtime.** Detail: `PROGRESS.md` 2026-07-27 (entri Batch 3B) + `05_WORKFLOW_MAP` §Procurement — PRF (subbagian "Penawaran Vendor") + `08_TECH_DEBT` TD-156 (baru).
>
> Sebelumnya **2026-07-27 (lanjutan SAMA HARI — Batch 3A: tombol Klaim/Lepas PRF [RPC `prf_claim`/`prf_release`] + kartu baca `prf_vendor_offers` read-only, UI PERTAMA modul "Penawaran Vendor"; 2 file FE `feat/prf-audit` belum di-commit; NOL DB dari sesi FE itu sendiri — TAPI mendahului 2 file ini, DUA commit DB-only susulan `604430f`+`e1c7ff1` juga terjadi hari yang sama):** `e1c7ff1` (migrasi `20260727000004_prf_rls_acknowledged.sql`) memperlebar RLS `prf_update_status`+3 policy `prf_cost_items_*` dari `status='SUBMITTED'` jadi **`status IN ('SUBMITTED','ACKNOWLEDGED') AND (acknowledged_by IS NULL OR acknowledged_by=auth.uid())`**, dan mem-patch `save_prf_pricing` (DELETE rincian kini `AND offer_id IS NULL`, melindungi baris `prf_cost_items` milik `prf_vendor_offers` dari kehapus diam-diam oleh panel Jawaban Harga LAMA). Snapshot self-refresh (nol utang `pg_dump` dari sesi ini). Detail lengkap + KONTRADIKSI baru soal `prf_cost_items.prf_id` di "Fase 4" (dua pernyataan Den hari sama, saling bertentangan, PERLU KONFIRMASI): `08_TECH_DEBT` TD-109 (usulan turun MEDIUM→LOW)/TD-122 (item 9c) + `PROGRESS.md` 2026-07-27. **Koreksi klaim "UI: NOL"/"nol pemanggil FE" untuk `prf_claim`/`prf_release`/`prf_vendor_offers`(SELECT)** di entri `prf_vendor_offers` + tabel RPC §5 di bawah — **cakupan SEMPIT**: ~~form tambah/edit penawaran (3B),~~ **[KOREKSI 27 Jul 2026, sesi terpisah SETELAH ini — lihat banner "Diperbarui" Batch 3B di atas]** form tambah/edit/hapus penawaran **SUDAH ADA** (`PRFVendorOfferModal.jsx`); `prf_mark_quoted`, `prf_select_offer` TETAP nol UI, jangan dibaca "UI lengkap". +TD-155 (baru, LOW — mirror `is_manager_or_above()` di `ActivitiesPage.jsx` ketinggalan `gm_bd`, ditemukan saat membandingkan dengan `MANAGER_OR_ABOVE` baru di `PRFDetailPage.jsx`). Detail Batch 3A: `PROGRESS.md` 2026-07-27.
>
> Sebelumnya **2026-07-27 (fondasi modul "Penawaran Vendor" PRF — dua commit DB-only `190fb5e`+`5c87119`; TIGA migrasi arsip `20260727000001`/`000002`/`000003`, SQL sudah dijalankan manual Den sebelum commit; NOL kode FE):** Model award vendor PRF **DIBALIK** — dulu procurement MEMILIH pemenang (`prf_cost_items.is_awarded`, constraint tingkat himpunan yang jadi akar **TD-122**); kini procurement **MENYEDIAKAN OPSI** (tabel baru **`prf_vendor_offers`**, banyak baris per PRF, `pros`/`cons` NOT NULL — justifikasi wajib ditegakkan DB) dan **SALES/MANAGER YANG MEMILIH** (`prf.selected_offer_id`, kolom FK skalar, via RPC baru `prf_select_offer`). ~~**TD-122 BUBAR OTOMATIS** (bukan "diperbaiki") — constraint himpunan yang jadi masalahnya tak punya padanan di model baru.~~ **[KOREKSI, Den 27 Jul 2026 — TD-122 TETAP OPEN, kesimpulan "bubar" di atas SALAH]** Kolom model LAMA (`prf_cost_items.vendor_id`/`item_group`/`is_awarded`) **TIDAK di-drop** — kedua model hidup berdampingan di skema, panel Jawaban Harga live (`PRFDetailPage.jsx`) masih 100% pakai model lama, dan **lubang double-award yang jadi akar TD-122 masih hidup di produksi hari ini**. TD-122 baru gugur di **Fase 4** (kutipan Den: saat `is_awarded` DAN `prf_cost_items.prf_id` dibuang). Detail lengkap kutipan Den + pertanyaan terbuka: entri `prf_cost_items` di bawah + `08_TECH_DEBT` TD-122. **+6 kolom `prf`** (`selected_offer_id` FK ON DELETE SET NULL, `selected_by`, `selected_at`, `min_offers_waiver_reason`, `acknowledged_by` FK→profiles, `acknowledged_at`) **+1 kolom `prf_cost_items.offer_id`** (FK ON DELETE CASCADE, nullable). **+4 RPC SECURITY DEFINER** (`prf_claim`/`prf_release`/`prf_mark_quoted`/`prf_select_offer`, §5) — satu procurement pengerja per PRF tanpa fungsi takeover, minimum 3 penawaran dengan jalan keluar tertulis. **✅ Snapshot SEGAR untuk kedua commit** (diverifikasi doc-keeper: token `\restrict`/`\unrestrict` berubah di `git show --stat` keduanya = `pg_dump` asli, bukan sunting manual) — **nol utang `pg_dump`**. **⚠️ TAPI +2 tabel backup BARU** (`backup_prf_20260727` 13 baris + `backup_prf_cost_items_20260727` 5 baris, klaim Den di header migrasi) — tabel backup di snapshot kini **SEPULUH**, bukan delapan seperti catatan 26 Jul. ~~**⚠️ UI BELUM ADA SAMA SEKALI** (nol pemanggil FE, diverifikasi grep `src/`)~~ **[KOREKSI 27 Jul 2026 sore — SEBAGIAN USANG, lihat banner "Diperbarui" di atas + entri `prf_vendor_offers`]** `prf_claim`/`prf_release`/`prf_vendor_offers`(SELECT) kini punya UI (Batch 3A); `prf_mark_quoted`/`prf_select_offer` masih nol. **Transisi `CANCELLED`/`EXPIRED` juga belum ada** (nol RPC — klaim Den: PRF/MSI/2026/VII/004 nyangkut permanen). Detail: entri `prf_vendor_offers` (baru) + `prf` + `prf_cost_items` di bawah, §5, gotcha #10, `05_WORKFLOW_MAP` §Procurement — PRF (subbagian "Penawaran Vendor"), `08_TECH_DEBT` TD-109 (update)/TD-122 (REOPENED 27 Jul — sempat salah ditandai usang, dikoreksi Den)/TD-154 (baru), `PROGRESS.md` 2026-07-27.
>
> **Diperbarui 2026-07-26 (lanjutan — Fase 1 `contacts` TUNTAS: migrasi arsip direkam + kunci `pic_*` di 10 file CRM; NOL SQL dijalankan sesi ini):** migrasi arsip **`20260726000001_arsip_contacts_fase1.sql`** merekam SQL yang **sudah dijalankan manual Den SEBELUM file ini ditulis** — merekam ulang DDL `contacts` (yang lahir sesi sebelumnya hari yang sama, lihat entri `contacts` di §3 — **melunasi utang "belum direkam migrasi" untuk tabel itu sendiri**), **+2 kolom BARU** `activities.contact_id`/`inquiries.contact_id` (FK→`contacts`, struktural, **`inquiries.contact_id` TIDAK ikut di-backfill**, tetap NULL semua baris), dan **backfill data** (satu DO-block ber-guard tunggal, replay-safe): **951 kontak baru** dari `accounts.pic_name/pic_email/pic_phone` (akun yang belum punya kontak sama sekali, `is_primary=true`) + **194 `activities.contact_id` ditautkan** ke kontak existing (3 tingkat prioritas: nama persis → HP digit-saja → fuzzy `pg_trgm` ≥0.6) + **40 kontak baru** dari `activities.contact_name` tak-tertaut → **991 kontak total, 239/240 activities tertaut**. **Menyusul (10 file FE, NOL perubahan DB):** seluruh jalur baca `accounts.pic_name/pic_phone/pic_email` di CRM dipindah ke **kontak primary tabel `contacts`** (`WHERE account_id=X AND is_primary AND deleted_at IS NULL`), dan seluruh jalur tulis ke `pic_*` **dikunci** (field read-only di form, payload tak lagi menyertakannya) — `accounts.pic_name/pic_phone/pic_email` **TETAP ADA di DB, tak di-drop**. **⚠️ Utang `pg_dump` MELEBAR, bukan berkurang:** diverifikasi doc-keeper via grep `schema_snapshot.sql` — **0 hit untuk `contacts`, `activities.contact_id`, DAN `inquiries.contact_id`** (ketiganya, snapshot terakhir refresh commit `c1062f4` 25 Jul, sebelum `contacts` lahir). Detail penuh (per-file, tech debt TD-131/132/133, temuan `DealDetailPage.jsx:165` fetched-but-unused): `PROGRESS.md` 2026-07-26 (entri "Fase 1 `contacts` TUNTAS") + entri `contacts`/`activities`/`inquiries` di §3 + gotcha #10 (update lanjutan) + `08_TECH_DEBT` TD-131/132/133 + `05_WORKFLOW_MAP` §CRM.
> **Diperbarui 2026-07-25 (arsip migrasi dedup akun — susulan, NOL perubahan DB):** empat objek dedup yang kemarin dicatat "LIVE tapi 0 jejak repo" **kini BERJEJAK MIGRASI** — `20260725000001`/`…000002` (merge 8 grup, skip-guard) · `…000003` (`pg_trgm` + `normalize_account_name` + `uq_accounts_norm_name_per_entitas`, idempotent) · `…000004` (`check_similar_accounts`, idempotent; per Den sudah dijalankan). **Utang JEJAK lunas** → entri `accounts`, RPC `check_similar_accounts`, dan gotcha #10 dikoreksi in-place ("belum berjejak" → "berjejak"). **⚠️ TAPI snapshot BELUM di-refresh** — `schema_snapshot.sql` masih tak memuat keempatnya → **"berjejak migrasi" ≠ "ada di snapshot"**; sampai `pg_dump` jalan, sumber kebenaran keempat objek = file migrasinya. Sisa 2 utang MANUAL (butuh Den): SATU `pg_dump` + drop 6 tabel backup lama (18–23 Jul). Lihat **TD-128**.
> **Diperbarui 2026-07-25 (lapisan UX dedup akun; 3 file FE, NOL perubahan DB dari sesi itu):** entri **`accounts`** dapat blok **HARD-BLOCK nama unik per entitas** — `uq_accounts_norm_name_per_entitas` (UNIQUE partial atas `company_id` + `normalize_account_name(name)`, `WHERE deleted_at IS NULL`) + fungsi `normalize_account_name` + `pg_trgm`, **dipasang manual & sudah LIVE**. Tiga konsekuensi wajib dipegang saat menulis kode: penegakan **tak peduli `account_status`** · akun **soft-deleted tak menghalangi** pakai-ulang nama · `23505`-nya menyebut **NAMA INDEX** (partial) sehingga handler **wajib mencocokkan nama index** — `accounts` juga punya `accounts_code_unique`, tanpa itu bentrok **kode** salah dilaporkan sebagai bentrok **nama**. +RPC **`check_similar_accounts`** masuk tabel RPC, ~~ditandai **⚠️ BELUM DIJALANKAN** (pemanggilnya sudah ada di 3 file FE → saat ini **gagal senyap**)~~ **[KOREKSI SUSULAN 25 Jul 2026 — USANG]** RPC **sudah dijalankan DAN TESTED** hari yang sama: `SELECT check_similar_accounts('ALLIANCE COSMETIC', <uuid MSI>)` balik **3 baris** (similarity **1.0 / 0.842 / 0.619**); smoke test UI membuktikan warning *"Mirip dengan: ALLIANCE COSMETICS"* **muncul** di jalur Prospect **dan** Customer CRM — fuzzy pre-check TERBUKTI JALAN di produksi, bukan lagi gagal senyap (detail lengkap: entri tabel RPC di §3 + gotcha #10 di bawah). **⚠️⚠️ Gotcha #10 diperluas: utang jejak kini EMPAT hal, TIGA sudah LIVE tanpa rekaman apa pun** — dan **(2)+(3) membuat snapshot MENYESATKAN secara semantik** untuk `accounts` (memperlihatkan nama tanpa penjagaan unik, padahal hard-block sudah aktif), bukan sekadar bikin hitungan meleset. Sebelumnya **2026-07-24 (fix aging Lead Pool; 1 Edge Function + DUA migrasi DATA, NOL objek DB baru, NOL perubahan RLS):** entri **`accounts`** dapat aturan tegas soal kelima kolom **`pull_*`** — **penanda SIKLUS parkir yang sedang berjalan, BUKAN riwayat permanen**; EF `aging-pipeline` kini **me-reset kelimanya ke `null`** tiap kali akun masuk pool lagi. Sebelum itu cap `pull_status='approved'` dari siklus lama terbawa ke siklus baru → akun **terkunci total** karena `LeadPoolPage` dan `LeadPoolApprovalPage` membaca **sumbu berbeda**. 27 baris korban dibersihkan (`20260724000000`, backup `backup_leadpool_trap_20260724`; `pipeline_stage`/`account_status` **sengaja tak disentuh**), lalu **gelombang kedua** (`20260724000001`, opsi C1) membebaskan **15 akun** lagi — **`pull_*` TIDAK disentuh** di gelombang itu (sudah `null`), `pull_status='pending'` **dikecualikan**, dan migrasinya **TIDAK idempoten** (kontras dgn yang pertama). **+TD-127 (MEDIUM):** `is_in_lead_pool` bisa **tertinggal `true`** saat akun maju ke `WON` (kasus INDO KARYA ANUGERAH) — baris dibersihkan, **akar belum ditutup**; kerabat **TD-94** tapi kolom & arahnya berbeda. Catatan `stage_changed_at` ditambah: kolom itu **beku permanen** untuk stage `PROPOSAL`/`NEGOTIATION` sejak keduanya dipensiun 22 Jul → alasan kedua stage dicabut dari aturan aging. **⚠️ DUA KLAIM USANG DIKOREKSI (dokumen bertentangan dgn snapshot yang ada di repo):** entri **`prf_cost_items`** dan **gotcha #10** masih menulis *"keempat kolom vendor/award BELUM masuk snapshot, `pg_dump` PERLU dijalankan"* — **tidak benar lagi**; snapshot memuat `vendor_id`/`item_group`/`is_awarded`/`exchange_rate` (`:4337-4340`) + FK (`:9636`), dan **sumber kebenaran = snapshot**. Gotcha #10 juga dicatat: **7 tabel backup menumpuk** → drop sekaligus, lalu **satu** `pg_dump`. Sebelumnya **2026-07-23 (fix `CustomerFormModal` — promosi lifecycle diam-diam; FE-only 1 file, NOL perubahan DB):** entri `accounts` dikoreksi soal **jalur tulis `account_status`** — modal Customer dulu **menimpa `account_status` jadi `customer` tanpa syarat** tiap kali akun apa pun diedit (bug TD-123, `account_status` + `code` kini dicabut dari UPDATE, keduanya INSERT-only). **Konsekuensi jalur tulis:** kini satu-satunya penulis `account_status` di `src/` = INSERT modal Customer yang hardcode `'customer'`; **`free_agent` tak punya penulis FE lagi** (rujukan `CustomerListPage.jsx:356` usang, dikoreksi in-place). Sebelumnya **2026-07-22 (Fase 3 — batch 3B-1 FE-only + LIMA SQL manual yang kini DIREKAM sebagai migrasi `20260722000006`–`20260722000010`):** **(a) `accounts.pipeline_stage`** — sumbu deal pindah ke `inquiries.status`; tak ada lagi jalur FE yang menulis `PROPOSAL`/`NEGOTIATION`/`WON`/`LOST`; **kolomnya belum dipangkas & belum ber-CHECK** (batch 3C). **(b) `inquiries`** — `status` = sumbu deal + tiga trigger penggeraknya (**`trg_inquiry_won` SUDAH DIREVISI**: pemicunya kini **SO berstatus `SENT`**, event **AFTER INSERT OR UPDATE**) + **tiga kolom deal baru** (`won_reason`/`lost_reason`/`estimated_value`, **nullable tanpa default — sengaja beda dari `accounts.estimated_value` yang DEFAULT 0**) + **trigger baru `trg_set_customer_on_inquiry_won`** yang mengambil alih konversi akun→customer (trigger lama di `accounts` **masih hidup berdampingan** sampai F3-8/3C). ~~**⚠️ SNAPSHOT BASI — UTANGNYA KINI TINGGAL `pg_dump`**~~ **[KOREKSI 22 Jul 2026 — USANG, jangan dihidupkan lagi] ✅ `pg_dump` SUDAH DIJALANKAN sesudah seluruh SQL manual Fase 3 → `schema_snapshot.sql` SEGAR.** Terverifikasi memuat: tiga kolom deal `inquiries` · fungsi+trigger **`set_customer_on_inquiry_won`** (`:7892`) · **`trg_inquiry_won` versi `AFTER INSERT OR UPDATE`** (`:7829`) · dua tabel backup. **Tidak ada utang `pg_dump` untuk Fase 3.** ⚠️ Snapshot kini memuat **empat tabel backup sementara** (`accounts_fase3_backup_20260722`, `sales_orders_backup_20260722`, `inquiries_status_backup_20260722`, `accounts_dedup_backup_20260722`) — itu sebabnya hitungan tabel naik ke **103**; sesudah keempatnya di-drop nanti, snapshot perlu di-refresh sekali lagi. **Aturan umum tetap berlaku: "snapshot bilang objek tidak ada ≠ objeknya tidak ada"** — cek tanggal refresh vs tanggal SQL manual terakhir, lalu TANYA. Sebelumnya **2026-07-21 (migrasi keempat & kelima hari itu — `20260721000003_prf_cost_items_vendor_award_fx.sql` + `20260721000004_save_prf_pricing_vendor_award_fx.sql`, SUDAH DIJALANKAN & LIVE):** **`prf_cost_items` +4 kolom** — `vendor_id uuid NULL REFERENCES vendors(id)`, `item_group text NULL`, `is_awarded boolean NOT NULL DEFAULT true`, `exchange_rate numeric NOT NULL DEFAULT 1` — **semua sengaja LONGGAR** supaya RPC & UI lama tetap jalan tanpa disentuh (`is_awarded` default `true` → seluruh baris lama otomatis dianggap pemenang, angka modal PRF lama **tidak berubah**; `exchange_rate` default 1 → baris lama IDR). `item_group` **SET NOT NULL + partial unique index DITUNDA ke Tahap D**, setelah UI + RPC baru live dan backfill selesai. **RPC `save_prf_pricing` di-`CREATE OR REPLACE`** → menulis keempat kolom itu + `prf.exchange_rates` dari `p_header`, semua dibaca **toleran** (`COALESCE`/`NULLIF`) sehingga pemanggil lama menghasilkan perilaku identik; **+guard baru: RAISE bila >1 vendor ter-award per PRF** (`count(DISTINCT vendor_id)` **hanya menghitung `vendor_id` non-null yang `is_awarded=true`** → baris tanpa vendor, termasuk PRF lama, tidak pernah memicunya). **`schema_snapshot.sql` STALE untuk kedua migrasi ini** — terverifikasi: definisi `CREATE TABLE public.prf_cost_items` di snapshot (`:3894-3906`) **belum memuat** keempat kolom, dan body `save_prf_pricing` di snapshot masih versi lama; **sumber kebenaran sementara = file migrasinya**, bukan snapshot. **`pg_dump` sudah DIRENCANAKAN Den sebelum commit → bukan utang terbuka**, hanya langkah yang belum dieksekusi saat catatan ini ditulis. **`prf.exchange_rates` kini DIPAKAI UI** (tidak lagi "belum dibaca kode mana pun") — lihat entri `prf` + `prf_cost_items` §Procurement dan `05_WORKFLOW_MAP` §Procurement — PRF. Sebelumnya **2026-07-21 (migrasi ketiga hari itu — `20260721000002_vendors_select_drop_deleted_at.sql`, SUDAH DIJALANKAN & LIVE):** syarat **`deleted_at IS NULL` DICABUT dari klausa USING `vendors_select`** → policy kini `is_super_admin() OR company_id = get_user_company_id()` saja (`schema_snapshot.sql:12581`). **Alasan = perilaku terukur, bukan teori:** soft delete vendor (`UPDATE deleted_at`) **ditolak `42501` "new row violates row-level security policy"** saat dijalankan dengan **role `procurement` asli**; terbukti lewat **A/B terkontrol di SQL Editor** (user sama, baris sama): `UPDATE city` **lolos**, `UPDATE deleted_at` **ditolak**; setelah syarat itu dicabut **di dalam transaksi**, `UPDATE deleted_at` **lolos** dengan `RETURNING` 1 baris. ⚠️ **MEKANISMENYA BELUM FINAL — jangan tulis sebagai fakta:** `vendors_update` **tidak menyebut `deleted_at` di WITH CHECK** (`:12588`), sehingga model standar (USING → baris LAMA, WITH CHECK → baris BARU) **tidak menjelaskan** error itu. Yang dicatat = **gejala + perbaikan yang terbukti**; sebabnya **pertanyaan terbuka**. **Konsekuensi struktural:** penyaringan vendor terarsip **pindah dari RLS ke query di kode** → setiap query `vendors` BARU wajib memasang `.is('deleted_at', null)` sendiri (**TD-115**); akses by-id di `VendorListPage` tanpa guard (**TD-116**); baris terarsip **tak bisa dipulihkan** + kode vendor hangus permanen (**TD-117**). ✅ **`schema_snapshot.sql` SEGAR** — `pg_dump` sudah dijalankan, **diff bersih: 1 policy + token `\restrict`, nol efek samping**; **tidak ada utang `pg_dump`.** Temuan sampingan (**catatan audit, belum dinilai bug**): `warehouses_select ON public.warehouses FOR SELECT USING (true)` **tanpa `TO`** (`:12617`) = pola sama dengan lubang `vendors` yang baru ditambal → **TD-119**, kandidat audit berikutnya bersama **TD-55**. Lihat entri `vendors` §Foundation. Sebelumnya **2026-07-21 (dua migrasi DB-only, SUDAH DIJALANKAN & LIVE — rombak RLS `vendors` + currency PRF):** **(1)** RLS **`vendors`** dirombak dari **LIMA policy tumpang tindih** → **EMPAT** (`vendors_select`/`vendors_insert`/`vendors_update`/`vendors_delete`; semua `TO authenticated`, company-scoped, role gate `is_manager_or_above() OR has_role('procurement')`, super admin bypass, **DELETE super admin only**) — menutup role legacy `procurement_head`/`procurement_staff`, `vendors_select USING(true)` (bocor BACA lintas entitas **antar user login**; `anon` TIDAK terdampak — tak punya GRANT), dan **`vendors_insert` tanpa cek role/company** (paling berbahaya, luput dari audit & TD lama) → **`08_TECH_DEBT` TD-47 RESOLVED**; rekaman `20260721000000_vendors_rls_overhaul.sql`. **(2)** **`prf_cost_items.currency` kini ber-FK → `currencies(code)`** (dulu free text tanpa FK/CHECK) + **kolom baru `prf.exchange_rates jsonb NOT NULL DEFAULT '{}'`** (pola `quotations.exchange_rates`; **belum dibaca kode mana pun**); rekaman `20260721000001_prf_currency_fk_exchange_rates.sql`. `quotation_items.currency` **sengaja belum dikunci** (data sudah banyak) → **TD-113**/**TD-114**. ✅ **`schema_snapshot.sql` SEGAR untuk kedua migrasi** — terverifikasi grep pada snapshot: 4 policy `vendors` baru ADA, `vendors_read`/`vendors_modify` **0 hit**, `prf_cost_items_currency_fkey` ADA, `prf.exchange_rates` ADA. **Jangan `pg_dump` untuk hal ini.** ⚠️ **Multi-vendor `prf_cost_items` BELUM ADA** — kolom `vendor_id`/`item_group`/`is_awarded`/`exchange_rate` **sempat ditambahkan lalu di-DROP** (urutan salah: DB duluan sebelum kode + RPC siap). Lihat entri `vendors` §Foundation + `prf`/`prf_cost_items` §Procurement. Sebelumnya **2026-07-20 ("Buat Quotation dari PRF" — rantai Inquiry → PRF → Quotation tersambung):** `quotations` **+1 kolom `prf_id`** (FK → `prf`) + **trigger `trg_quotation_prf_consistency`** (menolak INSERT bila `prf_id` diisi tapi `inquiry_id` ≠ `prf.inquiry_id`; mengisi `inquiry_id` otomatis bila kosong) — **keduanya dibuat & dijalankan MANUAL oleh Den, LIVE sebelum batch FE**; ~~⚠️ BELUM masuk `schema_snapshot.sql` … & BELUM direkam file migrasi.~~ **[KOREKSI 21 Jul 2026 — klaim ini USANG]** ✅ keduanya **sudah masuk snapshot** (terverifikasi grep: kolom `quotations.prf_id` `:4189`, FK `quotations_prf_id_fkey` `:9352`, fungsi `guard_quotation_prf_consistency` `:638`, trigger `trg_quotation_prf_consistency` `:7527`) **dan sudah direkam migrasi** `supabase/migrations/20260720000003_quotations_prf_id.sql`. **+Koreksi cakupan trigger:** trigger sebenarnya **`BEFORE INSERT OR UPDATE`**, bukan INSERT saja — jadi jalur EDIT pun dijaga konsistensinya (bukan hanya create). Trigger juga menolak bila PRF rujukan **tak punya `inquiry_id`** ("PRF % tidak punya inquiry_id…"), selain menolak mismatch `inquiry_id`. `prf_id` ditulis HANYA jalur CREATE FE (`insertPayload`); RPC `save_quotation` (jalur EDIT) tak tahu `prf_id` → edit tak menghapusnya. `currency_code` kini DIKIRIM saat create (`'IDR'` default = perilaku lama; dari PRF = `rate_currency`). **Koreksi klaim snapshot batch PRF Pricing:** snapshot TERNYATA sudah segar untuk `prf_cost_items`/`save_prf_pricing`/kolom jawaban `prf` (pg_dump 20 Jul, terverifikasi grep) — peringatan "snapshot belum memuat" di entri lama DIKOREKSI. Lihat entri `quotations` + `prf` di §3. Sebelumnya **2026-07-18 (FASE 2 lifecycle-split — DB SELESAI & LIVE):** backfill + trigger gerbang `set_prospect_on_inquiry` (SECURITY DEFINER, `AFTER INSERT ON inquiries`) + default kolom `account_status='lead'` + **CHECK `accounts_account_status_check`** (7 nilai `lead`/`mql`/`sql`/`prospect`/`customer`/**`free_agent`**/`lost`) **dijalankan manual & terverifikasi via `pg_proc`/`pg_trigger`/`pg_constraint`**, direkam migrasi `20260718000001_lifecycle_split_fase2.sql`. **Gerbang FINAL:** akun jadi `prospect` **hanya bila ada inquiry** (promosi via TRIGGER DB, bukan FE); `is_in_lead_pool` = penanda parkir tunggal; `'lead_pool'` **sudah tak ada di data**. ✅ `schema_snapshot.sql` **sudah selaras per 18 Jul 2026** (memuat keempat objek, di-refresh `pg_dump`); `free_agent` belum teruji runtime (0 baris). Sisa TD-91 = cleanup FE kosmetik. Lihat entri `accounts` di §2. Sebelumnya **(FASE 1.5):** kosakata FINAL `accounts.account_status` = `lead`/`mql`/`sql`/`prospect`/`customer`/**`free_agent`**/`lost`; 10 file FE TANPA ubah DB. Sebelumnya **2026-07-18:** **+tabel `sales_orders`** (dokumen perintah kerja Sales → Procurement; §Procurement) — sudah dibuat manual, direkam `20260718000000_sales_orders.sql`, **sudah masuk snapshot** (`:4184`). NOL perubahan DB dari sisi UI. Sebelumnya **2026-07-17:** `quotations` **+1 kolom `exchange_rates` jsonb** (tabel kurs manual per-quotation) + RPC `save_quotation` kini memetakannya — **dua-duanya LIVE, sudah direkam migrasi** (`20260717000000_…` + `20260717000001_…`) **dan sudah masuk snapshot** (`pg_dump` 17 Jul: kolom `:4044`, body RPC `:1013`). Sisa **TD-74** tinggal keputusan desain (tabrakan nama + `usd_rate` vestigial). ⚠️ **Awas tabrakan nama:** ada **tabel** `public.exchange_rates` (master FX history) yang **berbeda & dorman** — lihat §2 `quotations` + gotcha #13. Sebelumnya **2026-07-16:** `sp_items` +2 kolom (`price_category`, `review_status` [SEMENTARA]) — ALTER manual sudah direkam migrasi `20260716000000_…` **dan sudah masuk snapshot** (`:4295-4296`); sisa **TD-72** tinggal domain nilai `price_category`. Sebelumnya **2026-07-10 (audit CRM E2E) — koreksi CRM:** `accounts` (pipeline = tabel ini, tak ada `deals`; `estimated_value`; kolom Lead Pool/aging), mesin status `quotations` (DRAFT→SUBMITTED→SENT), `rate_sheets` RLS bocor, `notification_rules` yatim, trigger `sync_last_activity`; gotcha #10-12 (snapshot stale lagi + data historis CRM + pooler host). Sebelumnya (8 Jul): FASE 0-3 mesin status SP. Hitungan tabel/RPC kanonik: `supabase/schema_snapshot.sql`.

---

## 1. Overview

- **Supabase project ref:** `untmpqceexwxzuhlmyrg`
- **Pooler:** `aws-1-ap-northeast-2.pooler.supabase.com:5432` (region Seoul)
- **Total tabel:** ~60+ tabel business (schema `public`) — bertambah sejak FASE 0 (SP: `sp_orders`, `sp_order_items`, `sp_btb`, `dc_master`). Hitungan persis: `supabase/schema_snapshot.sql`.
- **Fungsi DB:** ~37 RPC/fungsi (mesin status SP menambah ~10-12) · **RLS policy:** ~60 tabel ber-RLS (⚠️ ~48 policy `USING(true)` → isolasi company via filter aplikasi, bukan DB) · **Trigger:** ~42 (mayoritas `set_updated_at` + 7 `trg_z_*`)
- Skema: multi-company (semua tabel business punya `company_id` / `owner_company_id`), soft-delete (`deleted_at`), RLS company- + role-scoped.

---

## 2. Entity UUIDs

| Entity | Nama | UUID |
|--------|------|------|
| MSI | PT Milenial Solusi Internusa (Freight Forwarding) | `0e1840d8-e6fb-4190-bd09-88338e68b492` |
| JCI | PT Jago Custom Indonesia (PPJK/Customs) | `42569e7c-531b-4d2b-832a-d5a7268c455b` |
| SOA | PT Stuja Orbit Abadi (General Trading; eks SBI/Storbit) | `d2e5e565-5f67-4954-b8d9-5979a2a0c697` |

Branch MSI Head Office: `ef2594db…` (asset_locations "Head Office BSD" terkait). [TODO: UUID branch lain bila perlu]

---

## 3. Tabel Utama per Modul

### CRM & Sales
- **`accounts`** — **master customer tunggal** (pipeline = tabel ini; **TIDAK ada tabel `deals`/`pipeline`**). **Tiga sumbu status berbeda (dulu bertumpuk, dipisah 18 Jul 2026):** (1) `account_status` = **LIFECYCLE akun**, **kosakata FINAL** `lead`/`mql`/`sql`/`prospect`/`customer`/**`free_agent`**/`lost` — **`free_agent` RESMI bagian kosakata lifecycle final** (~~nilai hidup, ditulis `CustomerListPage.jsx:356` saat customer di-lepas jadi Free Agent~~ **[KOREKSI 23 Jul 2026 — jalur tulis itu SUDAH TIDAK ADA]** dropdown "Status" di `CustomerFormModal` yang jadi satu-satunya cara menyetel `free_agent` **dicabut** saat fix promosi-diam-diam [TD-123] — komponen itu ber-sumbu salah & jadi keran bug. **Konsekuensi: `free_agent` kini punya NOL penulis di seluruh `src/`** [diverifikasi grep 23 Jul: satu-satunya penulis `account_status` tersisa = INSERT `CustomerFormModal` yang hardcode `'customer'`; `InquiryFormPage:223` hanya membaca]. Produksi tetap **0 baris `free_agent`**, jadi tak ada regresi data — tapi **kemampuan menandai Free Agent dari UI hilang; perlu konfirmasi Den apakah disengaja**; nilai tetap sah di CHECK & dipakai read/filter. Detail: **TD-123** + **TD-91**); **`lead_pool` DIHAPUS dari kosakata lifecycle** (jadi penanda parkir terpisah, lihat sumbu 3). Kelompok kerja: **PRA-CUSTOMER** = `lead`/`mql`/`sql`/`prospect` (+`lead_pool` selama transisi, ber-`TODO`); **CUSTOMER-SIDE** = `customer`/`free_agent`. (2) `pipeline_stage` = **deal-stage** (`NEW`…`WON`/`LOST`) — **⭐ SEJAK 22 Jul 2026 sumbu ini MENYUSUT jadi sumbu LEAD**: sumbu deal pindah ke **`inquiries.status`** (Fase 2, tiga trigger DB), dan **Fase 3 batch 3B-1 menghentikan SEMUA penulisan nilai lama dari FE** — hanya **`NEW`/`CONTACTED`/`QUALIFIED`** yang boleh ditulis (konstanta `ACTIVE_STAGE_KEYS`, `src/modules/crm/DealPanels.jsx`; daftar render `STAGES` **tetap 7 nilai** — memangkas daftar render akan menghilangkan akun warisan dari layar). **KOLOM BELUM DIPANGKAS**: nilai `PROPOSAL`/`NEGOTIATION`/`WON`/`LOST`/`NURTURE` **masih ada di data**, **belum ada CHECK** — pemangkasan + CHECK = **batch 3C** setelah backfill. **⚠️ Guard FE memeriksa stage TUJUAN, bukan asal** → akun yang sekarang `WON` masih bisa dipindahkan **turun** (drift **TD-94**; per keputusan Den **tutup sendiri di 3C**, tak ditindak sekarang). **⭐ KONVERSI OTOMATIS TIDAK MATI — PEMICUNYA PINDAH (jangan dibaca sbg regresi):** benar bahwa tak ada lagi penulis `WON` dari FE sehingga `trg_set_customer_on_won` (di `accounts`) tak bisa dipicu dari aplikasi — **itu memang TUJUAN batch 3B-1**. Penggantinya sudah live: **`trg_set_customer_on_inquiry_won`** (di `inquiries`, `20260722000008`). Rantai penuhnya: **SO jadi `SENT` → `trg_inquiry_won` → `inquiries.status='WON'` → `trg_set_customer_on_inquiry_won` → `accounts.account_status='customer'`** (+ penerbitan kode customer lewat trigger `code` yang sudah ada). Trigger **lama tetap hidup berdampingan** sampai dicabut di **F3-8 (batch 3C)**. *Bahwa rantai ini bekerja = verifikasi Den 22 Jul 2026, bukan verifikasi doc-keeper.* Pembuatan customer **manual** lewat form Customer juga tetap jalan. Detail: `05_WORKFLOW_MAP` §CRM + `PROGRESS.md` 2026-07-22; (3) `is_in_lead_pool` (boolean) = **SATU-SATUNYA penanda** akun sedang parkir di Lead Pool. **Aturan gerbang (FINAL, LIVE 18 Jul 2026):** akun boleh `account_status='prospect'` **hanya setelah punya inquiry** — inquiry = gerbang; promosi `lead/mql/sql → prospect` dipicu **trigger DB `trg_set_prospect_on_inquiry`** (fungsi `set_prospect_on_inquiry()`, SECURITY DEFINER, guard `account_status IN ('lead','mql','sql')` → tak menurunkan customer/prospect) `AFTER INSERT ON inquiries`, **bukan** kode FE. Trigger + default kolom + CHECK **sudah LIVE & terverifikasi** (`pg_proc`/`pg_trigger`/`pg_constraint`), direkam migrasi `supabase/migrations/20260718000001_lifecycle_split_fase2.sql`. **⚠️ FASE 1.5 (18 Jul, 10 file FE, TANPA ubah DB):** buka deadlock gerbang (dropdown inquiry `InquiryFormPage` kini bisa pilih akun `lead`/`mql`/`sql`), read-selector diperluas ke PRA-CUSTOMER + picker CUSTOMER-SIDE (`customer`/`free_agent`), `ProspectFormPage` kini **insert `'lead'`** (akun baru lahir sebagai lead). **✅ FASE 2 SELESAI & LIVE (18 Jul 2026):** backfill lifecycle (`WHERE deleted_at IS NULL`; hasil lead 643 · mql 182 · sql 112 · prospect 93 · customer 42 · lost 8) + trigger gerbang `set_prospect_on_inquiry` + default kolom `account_status` **`'lead'`** (dulu `'prospect'`) + **CHECK `accounts_account_status_check`** = `account_status IN ('lead','mql','sql','prospect','customer','free_agent','lost')` (**7 nilai, `free_agent` termasuk**) — **semua dijalankan manual & terverifikasi via katalog sistem**, direkam migrasi `20260718000001_lifecycle_split_fase2.sql`. **Nilai `'lead_pool'` sudah TIDAK ADA di data** (backfill memindahnya); kompensasi FE `lead_pool` ber-`TODO` = sisa cleanup kosmetik (**TD-91**). Backup: `public.accounts_lifecycle_backup_20260718` (1089 baris, boleh di-drop setelah aman). **⚠️ JUJUR:** `free_agent` terverifikasi via KODE saja, **belum runtime** (0 baris di produksi). **✅ `schema_snapshot.sql` sudah selaras per 18 Jul 2026** (memuat fungsi/trigger/default/constraint baru; di-refresh `pg_dump` di luar sesi). ⚠️ Snapshot kini **memuat definisi tabel backup** `public.accounts_lifecycle_backup_20260718` — kalau backup itu di-drop nanti, refresh `pg_dump` sekali lagi. Detail: `08_TECH_DEBT` **TD-91** + `PREFLIGHT_LIFECYCLE.md`. **⭐ NAMA AKUN KINI UNIK PER ENTITAS — HARD-BLOCK di level tabel (LIVE 25 Jul 2026, dipasang manual; ⚠️ BELUM terlihat di snapshot).** Aturan bisnis final **"satu perusahaan = satu akun, cakupan PER-ENTITAS"** ditegakkan **UNIQUE index partial** `uq_accounts_norm_name_per_entitas` **ON accounts (`company_id`, `normalize_account_name(name)`) WHERE `deleted_at` IS NULL**, memakai fungsi **`normalize_account_name(text)`** (IMMUTABLE — buang PT/CV/TBK + non-alfanumerik + lowercase) + extension **`pg_trgm`**. **Konsekuensi yang harus dipegang saat menulis kode:** (a) penegakan **tidak peduli `account_status`** — bentrok bisa datang dari akun lifecycle **apa pun**, bukan cuma pra-customer; (b) **partial** `WHERE deleted_at IS NULL` → akun **soft-deleted tidak menghalangi** pemakaian ulang nama; (c) INSERT yang bentrok melempar **`23505`** yang menyebut **NAMA INDEX** (bukan nama constraint, karena partial) → **handler wajib mencocokkan nama index**, sebab `accounts` **juga** punya `accounts_code_unique` (`code`) yang dipicu jalur create yang sama; tanpa itu bentrok **kode** salah dilaporkan sebagai bentrok **nama** (pola terpakai di `ProspectFormPage`, `CustomerListPage`, `App.handleSaveCustomer`). **✅ KINI BERJEJAK MIGRASI (25 Jul 2026, arsip — KOREKSI atas klaim "NOL JEJAK DI REPO"):** `normalize_account_name` + `uq_accounts_norm_name_per_entitas` + `pg_trgm` direkam di **`supabase/migrations/20260725000003_dedup_hardblock.sql`** (idempotent). Merge duplikat yang jadi prasyarat index direkam di **`20260725000001_dedup_merge_7_grup.sql`** + **`20260725000002_dedup_merge_alliance.sql`** (skip-guard). **⚠️ TAPI snapshot BELUM di-refresh** — `schema_snapshot.sql` **masih** tak memuat objek-objek ini (utang `pg_dump`, lihat gotcha #10 + **TD-128**). **Bedakan tegas: "berjejak migrasi" ≠ "ada di snapshot".** Bagi pembaca yang mengandalkan snapshot, `accounts` **masih tampak** tanpa penjagaan unik → **cek file migrasi 25 Jul, jangan snapshot**, sampai `pg_dump` dijalankan. *(Keberadaan objek di DB = pernyataan Den; yang doc-keeper verifikasi = keempat file migrasi ADA + snapshot belum memuatnya.)* Kolom kunci: `name, legal_name, customer_type, source, pipeline_stage, account_status, owner_company_id, company_id, code, tier, assigned_to, created_by, estimated_value` (**nilai deal disimpan di sini, bukan `deal_value`**)`, estimated_closing_date, payment_terms_id, bant_*, won_reason, lost_reason, became_customer_at, converted_at, vat_rate?, stage_changed_at, last_activity_at, is_in_lead_pool, lead_pool_*, pull_*, is_odoo_customer`. **RLS aktif** (`prospects_read/insert/update`): `company_id = get_user_company_id() AND (is_manager_or_above() OR assigned_to=auth.uid() OR created_by=auth.uid()) OR is_super_admin()`. **Gotcha:** WON → auto-`customer` via trigger `trg_set_customer_on_won` (**terbukti sehat** audit 10 Jul: 33 WON, nol `account_status` bukan 'customer', nol `became_customer_at` NULL); **⚠️ trigger SATU-ARAH** (hanya set `customer` saat masuk WON, `schema_snapshot.sql:1080`/`:7388`) — **tak ada revert saat KELUAR WON** → memindahkan akun `customer` dari WON→NEW meninggalkan `account_status='customer'` dgn `pipeline_stage='NEW'` (drift). Relevan sejak Tahap 3a: kontrol Pindah Stage kini muncul juga di Detail Account. Lihat `08_TECH_DEBT.md` **TD-94** (keputusan bisnis, belum ditindak); FK constraint masih bernama `prospects_*` (lihat §6). **Penanda Lead Pool kini SATU kolom** — `is_in_lead_pool` (boolean). Desain lama (dua kolom `account_status='lead_pool'` + `is_in_lead_pool`, disinkron per TD-50) **di-SUPERSEDE 18 Jul 2026**: EF `aging-pipeline` (masuk pool) & `LeadPoolApprovalPage` (tarik keluar) **berhenti menulis `account_status`** — hanya toggle `is_in_lead_pool`. Lihat `08_TECH_DEBT.md` **TD-50** (catatan supersede) + **TD-91**. **⭐ Kelima kolom `pull_*` (`pull_status`, `pull_requested_at`, `pull_justification`, `pull_approved_at`, `pull_approved_by`) = penanda SIKLUS parkir yang sedang berjalan, BUKAN riwayat permanen (aturan ditegakkan 24 Jul 2026).** Sejak fix aging, EF `aging-pipeline` **me-RESET kelimanya ke `null`** dalam UPDATE yang sama saat menyalakan `is_in_lead_pool=true` → siklus parkir baru selalu mulai bersih. Sebelum itu cap `pull_status='approved'` dari siklus **sebelumnya** ikut terbawa saat akun ter-aging ulang, dan karena `LeadPoolPage` (`canPull`, `:228`) dan `LeadPoolApprovalPage` (filter `pending`, `:86`) membaca **sumbu yang berbeda**, akun jadi **terkunci total** — tak ada tombol di halaman mana pun. **27 baris korban sudah dibersihkan** (migrasi rekaman `20260724000000_fix_leadpool_stale_pull_status.sql`, SQL dijalankan manual; backup `public.backup_leadpool_trap_20260724`); **`pipeline_stage` & `account_status` sengaja TIDAK disentuh** di pembersihan itu. **Gelombang KEDUA hari yang sama** (`20260724000001_liberate_leadpool_c1_won.sql`, opsi C1) membebaskan **15 akun lagi** (≥2 quotation hidup = 14, + 1 anomali customer WON) — **tapi hanya me-reset `is_in_lead_pool` + `lead_pool_at`/`lead_pool_reason`; kolom `pull_*` TIDAK disentuh** karena akun-akun itu `pull_status`-nya **sudah `null`** (bukan korban trap); akun ber-`pull_status='pending'` **dikecualikan** (menunggu approval); backup `public.backup_leadpool_c1_won_20260724`. ⚠️ **Migrasi kedua TIDAK idempoten** (predikat `count(quotations)>=2` dievaluasi ulang tiap jalan) — kontras dgn yang pertama yang idempoten; **jangan disamaratakan**. **⚠️ TD-127 (MEDIUM) — `is_in_lead_pool` bisa TERTINGGAL `true` saat akun maju ke `WON`:** ditemukan **INDO KARYA ANUGERAH** ber-`pipeline_stage='WON'` + `account_status='customer'` + `is_in_lead_pool=true` **berbarengan** (ter-aging saat `PROPOSAL`, lalu naik ke WON tanpa flag pool ter-clear). Barisnya sudah dibersihkan, **guard strukturalnya belum ada**. Kerabat **TD-94** tapi **bukan duplikat** — TD-94 = `account_status` tertinggal saat **KELUAR** WON; TD-127 = `is_in_lead_pool` tertinggal saat **MASUK** WON. **Penulis `pull_status` (diverifikasi grep 24 Jul):** hanya **tiga** di `src/` — `LeadPoolPage.jsx:176` (`'pending'`), `LeadPoolApprovalPage.jsx:122` (`'approved'`), `:141` (`'rejected'`) — plus EF (menulis `null`); kolomnya **tanpa DEFAULT**. ⚠️ **RLS `prospects_update` tak column-guard `pull_status`** → approval hanya dijaga di client (**TD-77**, tetap OPEN). Detail alur: `05_WORKFLOW_MAP` §Aging Pipeline. **`stage_changed_at`** 727/997 = timestamp backfill identik 25 Jun (sejarah pra-25 Jun hilang, **TD-51**) — ⚠️ **catatan tambahan 24 Jul:** untuk akun ber-stage `PROPOSAL`/`NEGOTIATION` kolom ini **beku permanen** sejak stage-nya dipensiun 22 Jul (tak bisa ditulis dari UI) → itulah alasan kedua stage **dicabut dari aturan aging**. **`last_activity_at`** dulu tak jujur (ditulis sekali saat prospect dibuat) → **kini di-maintain trigger `trg_z_sync_last_activity`** dari `activities` (**TD-52/TD-59**). **`is_odoo_customer`** = nol/997 true (kolom mati, **TD-57**).
- **`inquiries`** — `inquiry_no, prospect_id, customer_id, service_type, route, goods_name, status, deleted_at` (~~`commodity`~~ **DI-DROP 25 Jul 2026 [batch B4]** — dulu ada di list ini, kolomnya sungguh sudah tak ada; `goods_name` kini satu-satunya field nama barang, lihat blok B1→B3→B4 di akhir entri ini). FK `inquiries_prospect_id_fkey` / `inquiries_customer_id_fkey` → `accounts`. **Dirujuk balik** oleh `prf.inquiry_id` (FK `prf → inquiries(id)`) — relasi `inquiry ↔ prf` kini aktif (Cetak PRF dari inquiry; lihat entri `prf` di §Procurement). **⭐ `status` = SUMBU DEAL (sejak Fase 2, 22 Jul 2026)** — CHECK `inquiries_status_check` (`schema_snapshot.sql:3605`) = `OPEN`/`IN_REVIEW`/`QUOTED`/`NEGOTIATION`/`WON`/`LOST`/`CANCELLED`, DEFAULT `'OPEN'`. Digerakkan **tiga trigger DB** (migrasi `20260722000004`): `trg_inquiry_review` (PRF `SUBMITTED` → `OPEN`→`IN_REVIEW`, snapshot `:7688`) · `trg_inquiry_quoted` (quotation `SENT` → `OPEN`/`IN_REVIEW`→`QUOTED`, snapshot `:7681`) · **`trg_inquiry_won`** → **⚠️ SUDAH DIREVISI, jangan pakai deskripsi lama.** Sejak **`20260722000009` (F3-EX1)** pemicunya **BUKAN lagi "SO dibuat"** melainkan **SO berstatus `SENT`**, dan triggernya kini **`AFTER INSERT OR UPDATE ON sales_orders`** (dulu `AFTER INSERT` saja). Alasan: SO yang masih **DRAFT internal bukan kemenangan**. Perubahan **event** itulah yang membuat SO yang lahir DRAFT lalu **berubah** jadi SENT tetap tertangkap — karena itu triggernya **harus di-DROP lalu CREATE ulang**, tak bisa lewat `CREATE OR REPLACE`. +guard `IF TG_OP='UPDATE' AND OLD.status='SENT' THEN RETURN` (menyunting SO yang sudah SENT tak menembak ulang). ~~**⚠️ Snapshot masih memuat versi LAMA**~~ **[KOREKSI 22 Jul — USANG] ✅ snapshot sudah di-refresh:** trigger versi baru ada di `schema_snapshot.sql:7829` (`AFTER INSERT OR UPDATE ON public.sales_orders`) — **snapshot kembali jadi sumber kebenaran**, sejalan dgn migrasi `20260722000009`. Semua SECURITY DEFINER + klausa "hanya naik" (`status IN ('OPEN','IN_REVIEW','QUOTED','NEGOTIATION')`) → **`LOST`/`CANCELLED` tak pernah tersentuh trigger**; **`NEGOTIATION` belum punya penulis otomatis**. **Batasan yang diketahui (tak berubah):** SO di-soft-delete → inquiry **TETAP WON** (pembatalan setelah menang = `CANCELLED` manual, bukan penurunan otomatis). **Frontend:** `InquiryFormPage` hanya menyetel `status:'OPEN'` saat INSERT (jalur EDIT-nya tak menyentuh `status`) → **satu-satunya penulis TRANSISI status dari FE = tombol "Tandai Kalah"** (`DealDetailPage`, 22 Jul 2026) yang menulis `status='LOST'` + `lost_reason`; penegak izinnya **RLS `inquiries_update`** (company + manager-atau-pemilik + super) — **sales hanya bisa menandai kalah inquiry buatannya sendiri**. **⭐ TIGA KOLOM DEAL BARU (`20260722000007`, F3-2):** **`won_reason text`**, **`lost_reason text`**, **`estimated_value numeric`** — pindahnya kolom deal dari `accounts` ke sini, karena **satu akun bisa punya banyak inquiry** sehingga "alasan menang/kalah" + "nilai deal" yang tunggal per akun tak bisa mewakili deal yang berbeda-beda. **⭐ KEPUTUSAN DESAIN — ketiganya nullable TANPA default, SENGAJA BERBEDA dari `accounts.estimated_value` yang `DEFAULT 0`:** di sini **"belum diisi" harus bisa dibedakan dari "nol"**; `DEFAULT 0` membuat keduanya tampak sama dan **ikut mencemari rata-rata/total nilai pipeline**. Aditif murni (nol pembaca & nol baris tersentuh saat dijalankan). **Kolom lama di `accounts` TIDAK di-drop & TIDAK di-backfill** — itu batch 3C. Penulis FE pertama = tombol "Tandai Kalah" (mengisi `lost_reason`). ~~**⚠️ SNAPSHOT STALE:**~~ **[KOREKSI 22 Jul — USANG] ✅ ketiganya kini TERMUAT snapshot** (`pg_dump` sesudah seluruh SQL manual Fase 3) **dan** direkam migrasi `20260722000007`. **Jangan pakai lagi `estimated_volume text` sebagai penanda "kolom nilai belum ada"** — `estimated_volume` (volume) dan `estimated_value` (nilai) kini hidup berdampingan di tabel yang sama; **nama mirip, sumbu beda.** **⭐ TRIGGER BARU `trg_set_customer_on_inquiry_won`** (`20260722000008`, F3-3) — **`AFTER INSERT OR UPDATE ON inquiries`**, fungsi `set_customer_on_inquiry_won()` (SECURITY DEFINER, `search_path=public`): inquiry yang **menang** kini yang mengkonversi akunnya jadi **customer**. Guard: `status='WON'` · bukan soft-delete · pada UPDATE **berhenti bila `OLD.status='WON'`** (tak menstempel ulang tanggal konversi); akun = **`COALESCE(prospect_id, customer_id)`** (`prospect_id` didahulukan karena itu yang diisi `InquiryFormPage`; NULL keduanya → no-op); `UPDATE accounts` hanya menyentuh akun yang **belum `customer` & belum soft-delete**; `became_customer_at`/`converted_at` pakai `COALESCE` (timestamp asli dipertahankan). **⚠️ `set_customer_on_won` LAMA di `accounts` SENGAJA MASIH HIDUP** — dua jalur berdampingan selama transisi; yang lama baru dicabut di **F3-8 (batch 3C)**. ~~**Nol hit di snapshot**~~ **[KOREKSI 22 Jul — USANG] ✅ fungsi + trigger sudah termuat snapshot** (`:7892`). **⭐ [25 Jul 2026] `commodity` DI-DROP, konsolidasi ke `goods_name` (satu-satunya field nama barang).** Rangkaian: **(B3, commit `260e9ae` 23 Jul)** kode CRM (`InquiryFormPage`/`InquiryListPage`/`CustomerDetailPage`/`DealDetailPage`/`InquiryPDF`) berhenti baca/tulis `commodity` — `goods_name` jadi satu-satunya field nama barang di form & tampilan (baris "Komoditas" yang redundan dihapus di 3 tempat). Kolom DB **tetap ada** saat itu (drop menyusul terpisah). **(B4, 25 Jul)** kolom di-DROP sungguhan: backup penuh (`backup_b4_inquiries_20260725`) → backfill `goods_name = commodity` untuk baris yang `goods_name`-nya kosong tapi `commodity`-nya terisi (fill-empty-only, skip `WON`/`CANCELLED`, skip soft-deleted) → **80 baris ter-update** → `ALTER TABLE inquiries DROP COLUMN commodity`. Migrasi arsip: `supabase/migrations/20260725000006_b4_drop_inquiries_commodity.sql` (3-step, idempotent). **Diverifikasi (`AUDIT_B4_READINESS.md`, 25 Jul) SEBELUM drop:** nol pembaca `inquiries.commodity` tersisa di `src/`/Edge Functions, nol CHECK/FK/index/view/trigger DB-side bergantung padanya. **⚠️ OPEN ITEM (belum dikerjakan, bukan bug):** `goods_name` **TIDAK wajib diisi** di `InquiryFormPage` (`validate()` tak mengeceknya) — per audit, **19 inquiry aktif punya `goods_name` kosong** (pasca-backfill). Keputusan mewajibkan field ini belum diambil. `prf.commodity` (enum kategori kargo PRF) **tak disentuh**, tetap hidup terpisah — lihat entri `prf` di bawah untuk `goods_name`/`un_number`/`imo_class` versi PRF (**B1**, 23 Jul). **+`contact_id`** [BARU 26 Jul 2026, migrasi `20260726000001`] FK→`contacts(id)`, nullable — **murni struktural, TIDAK di-backfill, TIDAK dipakai jalur tulis FE mana pun** (beda dari `activities.contact_id` yang di-backfill 239/240 baris — lihat entri `contacts`/`activities` di atas). Kolom ini tetap NULL di semua baris per hari ini. Detail: `PROGRESS.md` 2026-07-25 (entri PRF batch B1 + Inquiry B3 + drop kolom B4) + `08_TECH_DEBT` TD-107 (koreksi item `commodity`).
- **`quotations`** — `quotation_no, revision, inquiry_id, prf_id, prospect_id, customer_id, service_type, route, valid_until, quote_date, payment_terms_id, currency_code, exchange_rates, usd_rate, vat_rate (DEFAULT 0.011), discount_pct, subtotal, tax_amount, total_amount, status, notes, terms, internal_notes, margin_floor, pricing_done_at, quote_sent_at`. **`prf_id`** [BARU 20 Jul 2026 — **DB manual oleh Den**, LIVE; ⚠️ **BELUM di snapshot & BELUM direkam migrasi**] = jejak "quotation ini lahir dari PRF mana" (FK → `prf`), diisi HANYA saat quotation dibuat via tombol "Buat Quotation" di `PRFDetailPage` (jalur CREATE `insertPayload`; flow lain → null). **Konsistensi dijaga trigger `trg_quotation_prf_consistency`** (DB manual, LIVE): INSERT dgn `prf_id` terisi tapi `inquiry_id` ≠ `prf.inquiry_id` → **RAISE (insert ditolak**, pesan sampai ke user via toast FE); `inquiry_id` kosong → diisi otomatis dari `prf.inquiry_id`. **Jalur EDIT (RPC `save_quotation`) tak tahu `prf_id`** → edit tak menghapusnya. **Arah relasi:** `quotation → prf → inquiry` (quotation menunjuk PRF; PRF menunjuk inquiry) — panel "Quotation dari PRF Ini" di `PRFDetailPage` query balik `quotations WHERE prf_id` (informasional, satu PRF boleh banyak quotation — keputusan desain). Lihat `05_WORKFLOW_MAP` §Procurement — PRF. **Tiga kolom kurs/currency hidup berdampingan — jangan tertukar:** **(1) `exchange_rates`** [BARU 17 Jul 2026] `jsonb NOT NULL DEFAULT '{}'::jsonb` = **tabel kurs manual per-quotation**, bentuk **object map** `{"USD":16200,"SGD":12000}`; **IDR implisit = 1 → TIDAK disimpan** di map. **Diisi manual per-quotation — TIDAK ada lookup FX / master kurs.** Kurs baris (`quotation_items.exchange_rate`) = **turunan read-only** dari map ini, dijaga koheren lewat **write-through** di form (ubah kurs header → semua baris ber-currency itu ikut ter-update) → `QuotationDetailPage`/`QuotationPDF` tetap baca `quotation_items.exchange_rate` seperti biasa. ✅ ALTER manual **sudah direkam migrasi** (`supabase/migrations/20260717000000_quotations_exchange_rates.sql`) **dan sudah masuk snapshot** (`CREATE TABLE public.quotations` → `exchange_rates jsonb DEFAULT '{}'::jsonb NOT NULL`, `schema_snapshot.sql:4044`; COMMENT `:4052`). ⚠️ **JANGAN dikelirukan dgn tabel `public.exchange_rates`** (nama sama persis, entitas beda — master FX history company-scoped `from_currency`/`to_currency`/`rate`/`effective_date`, `schema_snapshot.sql:3045`; **DORMAN**, 0 query di `src/`; lihat gotcha #13 + **TD-74(c)**). **(2) `currency_code`** (DEFAULT `'IDR'`) = **semi-pasif** — tak dirender & bukan penggerak kalkulasi baris; **sejak 20 Jul 2026 CREATE mengirimnya** (`insertPayload`: `header.currency_code || 'IDR'` — flow non-PRF tetap `'IDR'` = identik default kolom lama, nol perubahan perilaku; flow "Buat Quotation dari PRF" mengisinya dgn `prf.rate_currency`). EDIT tetap tak menyentuhnya. Percobaan menghidupkannya sbg "currency header penggerak baris" (17 Jul) **dibatalkan & dibongkar**. **(3) `usd_rate`** (DEFAULT 16000) = **vestigial** — masih ditulis kedua jalur simpan tapi **tak dipakai `calcRowTotal`**; kini tumpang-tindih konsep dgn `exchange_rates` (**TD-74**). FK → `accounts` (`quotations_prospect_id_fkey`/`_customer_id_fkey`). RLS: read/insert company-scoped; `quotations_update` = owner (`created_by`) ATAU manager ATAU super. **Mesin status:** `DRAFT → SUBMITTED → SENT` (SUBMITTED via "Submit Quotation" `QuotationFormPage.jsx:730,790`, disabled sampai `inquiry_id` terisi; SENT via "Kirim ke Customer" `QuotationDetailPage.jsx:258`). **`ACCEPTED`/`REJECTED`/`EXPIRED` = label display, TANPA transisi UI** → **nol baris ACCEPTED** di DB → trigger `sync_deal_value_on_quotation_accept` tak pernah jalan → 88% deal WON `estimated_value` kosong (`08_TECH_DEBT.md` **TD-54**). Detail alur: `05_WORKFLOW_MAP`.
- **`quotation_items`** — `quotation_id, sort_order, description, qty, unit, unit_price, notes, group_name, currency, unit_label, exchange_rate, total, cost_price`. **Punya policy DELETE** (fix 2.8J — sebelumnya delete-then-insert silent gagal). Save via RPC `save_quotation` (atomik). **`currency` tetap per-baris (multi-currency).** **`exchange_rate` sejak 17 Jul = turunan `quotations.exchange_rates`** (read-only di form, di-write-through dari tabel kurs header) — **tetap disimpan di kolom ini** (bukan dihitung on-the-fly) supaya Detail/PDF/laporan tak perlu tahu soal map di header.
- **`activities`** — modul Activity terpadu (gantikan `sales_calls`+`sales_visits`). `type` (call/whatsapp/visit/meeting/email/followup), `status` (todo/done/cancelled), `account_id`/`inquiry_id`/`quotation_id`, `assigned_to`, `scheduled_for`, `details jsonb`, `migrated_from`. RLS niru accounts. **+`contact_id`** [BARU 26 Jul 2026, migrasi `20260726000001`] FK→`contacts(id)`, nullable — **backfill historis mengisi 239/240 baris** (3 tingkat prioritas matching by nama/HP/fuzzy ke tabel `contacts`, lihat entri `contacts` di atas); **belum dipakai jalur tulis FE mana pun** (kolom murni struktural untuk fitur masa depan, backfill-nya adalah satu-satunya penulis sejauh ini).
- **`activity_logs`** — audit status activity: `activity_id` (FK CASCADE), `changed_by`, `from_status`, `to_status`, `notes`. RLS via parent activity (EXISTS), bukan `company_id`.
- **`user_login_logs`** — sumber feed "Login" (`user_id, logged_in_at, ip, …`). **Tanpa `company_id`** → RLS sendiri (manager+/super/own). Diisi fungsi `capture_login_session()`.
- **`rate_sheets`** — rate card / pricing sheet dinamis (`columns`/`rows` jsonb, `valid_until`, `note`, `company_id` NOT NULL); soft-delete. Modul Rate List (CRM). ⚠️ **RLS `rate_sheets` TAK filter `company_id`** (policy = `created_by=auth.uid() OR is_manager_or_above()`) → manager entitas lain bisa baca lintas-entitas. Laten (2 baris, keduanya MSI) tapi **WAJIB fix sebelum JCI/SOA input rate** (`08_TECH_DEBT.md` **TD-55**, pola sama TD-47 `vendors`).
- **`deal_handovers`** — form handover deal WON (`handover_type` light/strategic by nilai deal, `status`); gate WON → customer (lihat `05_WORKFLOW_MAP`). **⚠️ [22 Jul 2026] TIDAK ADA PENULIS LAGI** — mesin tulis WON di papan akun dicabut (Fase 3 batch 3B-1) dan `LightHandoverModal`/`StrategicHandoverModal` kini **nol importer** di `src/` → tak ada baris baru yang bisa masuk tabel ini dari aplikasi. Tabel + data lama **tidak disentuh**. **Perlu keputusan Den:** gate handover Rp100jt masih aturan bisnis atau tidak (kalau masih → pasang ulang di sumbu baru, mis. saat SO lahir). Lihat `05_WORKFLOW_MAP` §Gate & Approval + `09_ROADMAP` Keputusan Terbuka.
- **`top_requests`** — pengajuan Terms of Payment customer (`status='submitted'`; approval finance downstream). Dari CustomerDetailPage / WON flow.
- **`contacts`** [BARU 26 Jul 2026 — **tabel dibuat MANUAL oleh Den, di luar sesi FE**; ⚠️ **BELUM di `schema_snapshot.sql` (0 hit grep) & BELUM direkam migrasi (0 hit `supabase/migrations/`)** — dua utang manual sekaligus, pola persis TD-20 lama / B1 23 Jul] — kontak/PIC **per akun, multi-kontak** (berbeda dari `accounts.pic_name`/`pic_phone`/`pic_email` yang tetap ADA apa adanya, **tak disentuh** batch ini — dua model hidup berdampingan sementara ini, belum ada keputusan konsolidasi). Kolom: `account_id` (FK→`accounts`, NOT NULL), `company_id` (NOT NULL), `name` (NOT NULL), `position`, `email`, `phone`, `role_type` (CHECK: `decision_maker`/`requester`/`finance`/`operations`/`other`/NULL), `is_primary` (boolean DEFAULT false), `is_active` (boolean DEFAULT true), `notes`, `created_by` (DEFAULT `auth.uid()`), `created_at`, `updated_at` (DEFAULT `now()` — **tanpa trigger auto-update**, lihat **TD-134**), `deleted_at`. **UNIQUE index partial `uq_contacts_one_primary ON contacts(account_id) WHERE is_primary AND deleted_at IS NULL`** — satu akun maksimal satu kontak primary; jalur yang bisa memicu unique violation-nya sengaja dipersempit ke satu aksi ("Jadikan Kontak Utama"), form Tambah/Edit tak pernah menulis `is_primary` sama sekali. **RLS mewarisi visibilitas `accounts`** (policy EXISTS ke tabel induk — pola sama `activity_logs`/`prf_cost_items`, bukan filter `company_id` langsung); **DELETE (SQL DELETE asli) super_admin-only** — tapi satu-satunya jalur hapus dari UI adalah **soft-delete via UPDATE**, jadi batasan DELETE itu tak pernah tersentuh aplikasi (dikonfirmasi Den sbg desain: siapa pun yang lolos visibilitas `accounts` boleh soft-delete kontaknya, tak ada gate role tambahan). **Pintu masuk UI pertama: tab "Kontak" di `CustomerDetailPage`** (26 Jul 2026, `src/modules/crm/CustomerDetailPage.jsx`) — list + Tambah/Edit (modal `ContactFormModal`) + Jadikan Kontak Utama (dua UPDATE sequential: demote lalu promote, wajib karena unique index partial) + Hapus (soft delete). *(Atribusi: struktur tabel/RLS/constraint = deskripsi diteruskan ke doc-keeper, BUKAN hasil query DB langsung — snapshot belum memuat tabel ini untuk diverifikasi silang.)* **⭐ [Lanjutan 26 Jul 2026, sesi yang sama, hari yang sama — Fase 1 TUNTAS] Migrasi arsip `20260726000001_arsip_contacts_fase1.sql` merekam DDL tabel ini (melunasi utang "belum direkam migrasi" di atas) + BACKFILL:** **951 kontak** dari `accounts.pic_name/pic_email/pic_phone` (akun tanpa kontak sama sekali, `is_primary=true`) + **40 kontak** dari `activities.contact_name` tak-tertaut (`is_primary=false`) → **991 kontak total**. **+2 kolom FK struktural baru:** `activities.contact_id` (**194 baris ditautkan** ke kontak via 3 tingkat prioritas: nama persis → HP digit-saja → fuzzy `pg_trgm` ≥0.6, `DISTINCT ON` deterministik → **239/240 activities tertaut**, 1 sisa ber-`account_id` NULL memang tak bisa ditautkan) dan `inquiries.contact_id` (**TIDAK di-backfill, tetap NULL semua baris** — murni struktural, disiapkan untuk fitur masa depan). **Menyusul di 10 file FE (CRM), `contacts` kini benar-benar jadi sumber kebenaran PIC di seluruh app** (bukan cuma tab Kontak halaman ini) — kontak *primary* (`is_primary AND deleted_at IS NULL`) dibaca menggantikan `accounts.pic_name/pic_phone/pic_email` di: kartu/list `CustomerListPage`/`ProspectListPage`/`PipelineKanbanPage` (pola seragam: PostgREST embedded select `contacts(...)` TANPA filter server-side pada embed — supaya pasti LEFT JOIN, filter `is_primary`/`deleted_at` dilakukan CLIENT-SIDE via helper `primaryContactOf()`, demi menghindari ketidakpastian perilaku filter-on-embed PostgREST yang tak bisa diverifikasi sesi ini), detail `QuotationDetailPage`/cetak `QuotationPDF` (satu query tambahan per-quotation, bukan bulk — halaman detail, bukan list), kartu grid Storbit "Master Customer" (`App.jsx`, ditenagai `listCustomers()`/`db.js`). **Jalur TULIS `pic_*` DIKUNCI di 3 form** (`CustomerFormModal`/`ProspectFormPage`/`CustomerModal` App.jsx) — field jadi read-only (tetap tampil, bukan dihapus, demi menunjukkan nilai lama), payload insert/update tak lagi menyertakan `pic_name`/`pic_phone`/`pic_email`. **`accounts.pic_name/pic_phone/pic_email` TETAP ADA di DB** (drop kolom = batch destruktif terpisah, belum dikerjakan); `db.js customerFromDb()`/`standardAppKeys` **sengaja tak diubah** (masih memetakan `picName`/`picEmail` supaya field read-only tsb tetap menampilkan nilai lama, bukan kosong). **⚠️ Utang `pg_dump` MELEBAR** (bukan sekadar tetap) — diverifikasi doc-keeper via grep: **0 hit untuk `contacts`, `activities.contact_id`, DAN `inquiries.contact_id`**, ketiganya. **+TD-131/TD-132/TD-133** (lihat `08_TECH_DEBT`). Detail: `PROGRESS.md` 2026-07-26 (entri "Fase 1 `contacts` TUNTAS") + `05_WORKFLOW_MAP` §CRM (Halaman Detail Account) + `08_TECH_DEBT` **TD-134/TD-131/TD-132/TD-133** + `09_ROADMAP` (baris Master Customer). **⭐ [27 Jul 2026 — PRF jadi konsumen ke-11, FE-only 1 file `PRFDetailPage.jsx`, NOL perubahan DB]** Modul PRF (Procurement) kini juga fetch kontak *primary* akun (`.eq('account_id', prf.account_id).eq('is_primary', true).is('deleted_at', null).maybeSingle()`) untuk kartu baru **"Customer & Kontak"** di layar Detail PRF — pola disalin verbatim dari `CustomerDetailPage.jsx:769-778` (duplikasi helper, bukan diekstrak ke hook bersama — konsisten pola yang sudah berulang di 10 file CRM lain, bukan tech debt baru). Menutup klaim inti **usulan `AUDIT_PRF_FIELDS.md` TD-153** ("modul PRF tidak menyentuh tabel `contacts` sama sekali") — kini salah. **TAPI TD-153 hanya PARTIAL**: NPWP (`accounts.tax_id`)/alamat (`accounts.address`) yang juga disebut TD-153 **masih tak ditampilkan** di Detail PRF (hanya nama+kode akun +nama/posisi/telepon/email kontak). Lihat `08_TECH_DEBT` TD-153. **[KOREKSI 27 Jul 2026 — TEMUAN DOC-KEEPER, insidental, TIDAK terkait sesi PRF di atas]** Klaim **"Utang `pg_dump` MELEBAR ... 0 hit untuk `contacts`, `activities.contact_id`, DAN `inquiries.contact_id`"** di paragraf atas **SUDAH TIDAK BENAR.** Commit `37ac4fd` (2026-07-26 21:31:30 — commit YANG SAMA yang menulis paragraf itu) ternyata JUGA menyertakan refresh `pg_dump` (`git show --stat 37ac4fd -- supabase/schema_snapshot.sql` → **+136/−3 baris**, termasuk token `\restrict` baru = bukti `pg_dump` asli dijalankan, bukan sunting manual). **Diverifikasi ulang doc-keeper hari ini (grep `schema_snapshot.sql` di HEAD):** `CREATE TABLE public.contacts` ADA (`:3157`) lengkap dgn seluruh kolom+CHECK; `activities.contact_id` ADA (kolom + FK `activities_contact_id_fkey`, `:8400`); `inquiries.contact_id` ADA (kolom). **Utang `pg_dump` untuk KETIGA objek ini LUNAS sejak commit itu sendiri** — paragraf di atas menulis hasil grep yang dilakukan SEBELUM `pg_dump`-nya sendiri jalan dalam sesi yang sama, lalu ter-commit bersama tanpa disinkronkan ulang. **Sisa utang `pg_dump` lain** (dedup akun 25 Jul, drop tabel backup lama) **TIDAK diverifikasi ulang di sini** — di luar cakupan temuan ini, tetap perlu konfirmasi Den terpisah. Detail lengkap: gotcha #10 (update 27 Jul) + `CLAUDE.md` Quick Reference (koreksi serupa).
- **`inquiry_comments` + `inquiry_comment_mentions`** [BARU 31 Jul 2026 — tabel dibuat MANUAL oleh Den di Supabase SEBELUM sesi kode "Chatter" (commit `5529540`), skema+RLS+GRANT sekaligus (GRANT sempat kelewat lalu dibetulkan manual, per commit message); ⚠️ **BELUM di `schema_snapshot.sql` (0 hit grep) & BELUM direkam migrasi (0 hit `supabase/migrations/`)** — dua utang manual sekaligus, pola sama persis preseden `contacts` di atas & **TD-128** (dedup akun)] — komentar bebas + @mention per inquiry, dipakai fitur **Chatter** (kolom kanan persisten tab Detail Inquiry, `src/modules/crm/DealDetailPage.jsx` via `InquiryChatter.jsx`). *(Struktur kolom di bawah ini HANYA diketahui dari kode FE — BELUM diverifikasi lewat schema/snapshot langsung, sama persis `contacts` sebelum migrasi arsipnya ditulis.)* `inquiry_comments`: `inquiry_id`, `company_id`, `body`, `created_by`, `created_at`, `updated_at`, `deleted_at` (soft-delete — dipakai jalur "hapus komentar sendiri"; RLS **diasumsikan** membatasi UPDATE ke `created_by=auth.uid()` berdasar perilaku UI [guard `data.length===0` → pesan "Tidak ada izin..." saat edit/hapus, pola sadar-RLS sama dgn fix TD-161], **BUKAN diverifikasi via teks policy langsung** — tabel tak ada di snapshot untuk dibaca). `inquiry_comment_mentions`: `comment_id`, `user_id` (insert-only dari kode FE, tak pernah di-`SELECT` balik oleh `InquiryChatter.jsx`). Notifikasi mention menumpang tabel generik `notifications` (`event_type:'inquiry_mention'`, `reference_type:'inquiry'`) — kolom itu `character varying` TANPA CHECK (`schema_snapshot.sql:4554-4566`), jadi NOL perubahan skema `notifications`. **Konsumen: hanya `InquiryChatter.jsx`**, hanya dipasang di `DealDetailPage.jsx` (belum di tab Riwayat `CustomerDetailPage.jsx`). **⚠️ `InquiryChatter.jsx` tak menerima prop `showToast`** — seluruh jalur tulis (submit/edit/hapus komentar, insert mention, insert notifikasi) gagal senyap ke `console.error` saja, lihat **TD-164**. Detail: `PROGRESS.md` 2026-07-31 + `08_TECH_DEBT.md` **TD-163** (utang migrasi+snapshot) + **TD-164** (jalur tulis gagal senyap) + gotcha #10 (addendum di bawah).
- **DORMANT:** `sales_calls`, `sales_visits`, `sales_visit_logs` (digantikan activities/activity_logs sejak cutover 2.9D; belum di-drop). `customers` (digantikan `accounts`; pensiun, tak dihapus).

### Foundation / Master Data
- **`companies`** (3 entity; **`aging_enabled boolean NOT NULL DEFAULT false`** — flag per-entitas apakah aging-pipeline memindahkan lead idle ke Lead Pool. Default `false` = aktivasi harus keputusan sadar. Saat ini **MSI=true, JCI=false, SOA=false**. Dibaca EF `aging-pipeline` [service role] untuk `.in('company_id', …)`; lihat `05_WORKFLOW_MAP` §Aging Pipeline. ⚠️ `service_role` diberi GRANT SELECT eksplisit pada `companies` — 46 tabel lain belum, lihat TD-62), **`branches`**, **`departments`** (`code, name, parent_id, company_id`), **`positions`** (`code, name, level [Staff/Supervisor/Manager/Head/Director], department_id`; **UNIQUE(company_id, code)**), **`profiles`** (user; **`active` boolean, TIDAK ada `deleted_at`**; `reports_to` self-FK untuk org chart; `avatar_url`, `notification_preferences jsonb`, `display_preferences jsonb`; legacy `role` kolom deprecated). **`products`**, **`document_types`**, **`payment_terms`**, **`taxes`**, **`status_catalog`**, **`vendors`**, **`warehouses`**.
- **`currencies`** — `code, name, symbol, decimal_places, is_active`. Isi: USD/IDR/EUR/SGD/JPY/MYR. RLS `currencies_read_all` = `USING(true)` (semua authenticated baca).
- **`exchange_rates`**, **`code_counters`** (counter generate code customer), **`document_sequences`** / **`document_numbering`** / **`document_templates`**.
- **Entity settings:** `entity_bank_accounts`, `entity_signatories`, `entity_finance_settings` (Admin Settings). **[9 Agu 2026]** `entity_finance_settings` +kolom baru **`default_payment_term_id uuid REFERENCES payment_terms(id) ON DELETE SET NULL`**. Kolom lama **`default_payment_terms integer` DIPERTAHANKAN** (tidak di-drop) tapi berhenti ditulis dari form — `FinanceDefaultsPage.jsx` field "Termin Pembayaran Default" berubah dari `NumberStepper` manual jadi dropdown `KitSelect` isi `payment_terms` AKTIF milik company yang sedang dibuka (`is_active=true`, `deleted_at IS NULL`). `src/lib/db.js` (`getInvoicePdfData`) baca dgn resolusi **3-tingkat**: FK aktif (`payment_terms.days_due`, kalau `payment_terms.is_active`) → kolom lama `default_payment_terms` → literal `30`. RLS `payment_terms_read` **HANYA** dapat bypass `is_super_admin()` (top-level, benar) — **TANPA** `get_user_company_ids()`, beda dari `companies_read_own`/`roles_read` yang dapat keduanya sesi yang sama — lihat gotcha #10 + `08_TECH_DEBT.md` **TD-180**. Kolom baru **belum direkam migrasi** (`supabase/migrations/`), hanya termuat via refresh `schema_snapshot.sql`.
- **`products`** — **4 tier harga** (FASE 0): `default_price` + **`price_semester`/`price_tahunan`/`price_project`** (nullable). Riwayat perubahan `default_price` → `product_price_history` (trigger `trg_z_products_price_history`); harga kategori di-log manual via RPC `set_product_category_prices` (§5). Lokasi rak per (product × warehouse) → `product_warehouse_location`. **[+18 Agu 2026, LIVE — `schema_snapshot.sql:6256`, COMMENT `:6306`]** +**`reorder_point numeric`** nullable — **STRUKTUR SAJA, sengaja nol isi**: angkanya ditentukan Finance/Warehouse Controller lewat SOP Storbit, bukan dihitung sistem, dan belum ada UI untuk mengeditnya. TERPISAH dari `min_order_qty` (text bebas, mis. "10 pcs" → tak bisa dibanding numerik). Dipakai kartu "Danger Stock"/"ROP Belum Diisi" di Dashboard Storbit — selama kolom ini kosong, Danger Stock selalu 0 dan ROP Belum Diisi = seluruh produk aktif.
- **`dc_master`** [BARU FASE 0] — master Distribution Center + wilayah (dipakai `sp_orders.dc_id`; `company_id`, `customer_id` [FK→`accounts`, nullable — dasar filter DC per-customer], `kode`, `nama`, `wilayah`, `alamat`, `is_active`, `created_by`, soft-delete `deleted_at`). **[8 Agu 2026] Kini CRUD penuh** — halaman admin `DcMasterPage.jsx` (`AdminShell` › Configuration, gate sama seperti Companies/Document Types/Taxes/Payment Terms lain: `module foundation + role[super_admin,admin,it]`, TANPA gate baru) + hook `useDcMaster.js` (list server-side paginated+search+filter-by-customer, create/update — **TANPA delete/archive**, `is_active` toggle satu-satunya jalur nonaktifkan). Sebelumnya READ-ONLY (cuma dropdown di `InputSPPage`); 45 DC awal berasal dari migrasi otomatis FASE 0. Field "Distribution Center" di `InputSPPage` sendiri diganti **`DcPicker.jsx`** (combobox searchable, pola `AccountPicker`, filter kode/nama, tampilkan seluruh list saat fokus kosong) menggantikan `<select>` native 45+ opsi — perilaku filter per-customer (`dcOptions`) TAK berubah.

### RBAC / Access Control
- **`user_roles`** — sumber role user (13 ERP role). `user_id` (→ `auth.users`, BUKAN profiles), `role_id`, `company_id`, `is_active`, `revoked_at`, `valid_until`. RLS: own row, atau admin se-company, atau super. **[7 Agu 2026] `user_roles_insert`/`user_roles_update` kini juga memvalidasi ROLE TUJUAN** — admin biasa (`is_admin_or_above()` non-super) TAK BISA lagi menulis baris yang menetapkan `role_id` ke tier `super_admin`/`admin` (dulu bisa — privilege escalation, TD-170 RESOLVED); ditegakkan via fungsi baru `is_admin_tier_role(role_id)` (lihat §4) yang dicek di `WITH CHECK` (insert) dan `USING`+`WITH CHECK` (update, supaya baris admin-tier milik orang lain juga tak bisa disentuh admin biasa). **[9 Agu 2026] Kedua policy DIRESTRUKTUR lagi** — fix TD-170 (7 Agu) menaruh bypass `is_super_admin()` DI DALAM klausa `AND` (`… AND (is_super_admin() OR NOT is_admin_tier_role(role_id))`), yang berarti bahkan **super_admin TETAP terkunci ke `company_id = get_user_company_id()` miliknya sendiri** — ditemukan lewat audit akses sesi ini (bukan lagi teori: super_admin secara struktural tak bisa assign/ubah role user di company LAIN, padahal UI `UserEditPage` tak membatasi dropdown company). Sekarang `is_super_admin()` jadi bypass **TOP-LEVEL** murni: `is_super_admin() OR (company_id = get_user_company_id() AND is_admin_or_above() AND NOT is_admin_tier_role(role_id))` — dikonfirmasi live (`schema_snapshot.sql:15569,15583`); klausa `is_admin_tier_role` TD-170 tetap utuh, cuma posisinya yang berubah. **Belum direkam migrasi** (SQL manual, hanya termuat via refresh snapshot).
- **`roles`** (`code, name, level`), ~~**`role_permissions`**, **`permissions`**~~, **`role_permission_templates`**. **[7 Agu 2026]** `role_permissions`/`permissions` + fungsi `has_permission()` **PENSIUN DARI KODE** (frontend berhenti membaca/memanggil, 0 hit RLS policy) — tabel & fungsi TETAP ADA di DB (sengaja tak di-drop), lihat `08_TECH_DEBT.md` TD-02/TD-06. **[20 Agu 2026 — status naik dari "pensiun" ke "nol pembaca"]** Pembaca terakhir `role_permissions` di jalur aplikasi (`fetchPermissionsForRoleId`, `AuthContext.jsx`) **dihapus fisik** bersama `hasPermission`/`isCrossEntity`/`userPermissions`/`refreshPermissions`. ⚠️ **Yang tersisa adalah satu jalur TULIS** dari halaman ORPHAN `RolesPage.jsx` (`SELECT`/`INSERT`/`UPDATE`/`DELETE`, tak di-route dari `AdminHub.jsx` maupun `App.jsx`) — `08_TECH_DEBT.md` **TD-204**. **Konsekuensi praktis kalau kelak di-drop:** hentikan/hapus `RolesPage.jsx` LEBIH DULU (urutan wajib `CLAUDE.md`: deploy kode yang berhenti menyentuh kolom/tabel dulu, baru drop). **[9 Agu 2026]** `roles_insert`/`roles_update` mendapat perbaikan **SAMA KELAS** dgn `user_roles` di atas — bedanya di sini bypass `is_super_admin()` sebelumnya **TIDAK ADA SAMA SEKALI** (bukan cuma salah-posisi seperti `user_roles`); sekarang `is_super_admin() OR (company_id = get_user_company_id() AND is_admin_or_above())`, dikonfirmasi live (`schema_snapshot.sql:14958,14972`). `roles_read` JUGA diperluas ke multi-company: `company_id = get_user_company_id() OR company_id IN (SELECT get_user_company_ids()) OR is_super_admin()` (`:14965`) — lihat fungsi baru `get_user_company_ids()` di §4 dan `08_TECH_DEBT.md` **TD-180** (pola yang sama BELUM ditambal di `payment_terms_read`, kemungkinan ada tabel lain senasib). **[21 Agu 2026 — PERUBAHAN STRUKTURAL, dicatat menyusul 24 Agu 2026] `roles` kini CROSS-ENTITY: `company_id` NULLABLE, `NULL` = berlaku Group-wide** (migrasi `20260821000003_globalize_roles.sql`, dijalankan manual). Untuk **14 kode role** (`super_admin`, `admin`, `ceo`, `gm`, `gm_bd`, `manager`, `operations`, `finance`, `finance_controller`, `procurement`, `sales`, `hrga`, `it`, `viewer`): satu baris survivor di-set `company_id = NULL`, duplikat per-company **di-soft-delete** (bukan dihapus), dan seluruh referensi (`user_roles`, `role_permissions`, `role_permission_templates`, `role_menu_permissions`, `approval_rules.approver_role_id`) **di-repoint ke survivor**. Constraint `roles_company_code_unique` diganti index parsial `roles_company_code_active_uidx` (`COALESCE(company_id, '00000000-…')`, `WHERE deleted_at IS NULL`); +policy `roles_select_global` (`company_id IS NULL AND deleted_at IS NULL`). ⚠️ **Konsekuensi untuk SEMUA kode yang memfilter `roles.company_id` — lihat gotcha #18 §6** (gagal senyap; `08_TECH_DEBT.md` TD-207/TD-209). ⚠️ **`user_roles.company_id` TIDAK ikut diglobalkan** — masih `NOT NULL`, dan **di situlah** company-scoping sekarang ditegakkan. `departments`/`positions`/`branches` mendapat perlakuan sejenis di migrasi kembarannya `20260821000002_globalize_org_structure.sql` (+ perbaikan data: merge GA+HR jadi HCGA, anomali profil, branch Semper duplikat, backfill `job_title`).
- **Hierarki menu:** `modules` → `module_menus` → `module_actions` → `menu_actions`; `user_menu_permissions` (granular per-user-per-menu-action, kini +kolom **`effect`** `'grant'`/`'deny'` default `'grant'` — lihat di bawah). Dipakai `hasMenuPermission`. **[9 Agu 2026]** +2 menu baru **`logistics_picking`**/**`logistics_surat_jalan`** (`module_menus`, module `logistics`) — Picking List & Surat Jalan sebelumnya digerbang MURNI array `role[]` hardcode di `App.jsx` (nol jejak di sistem permission granular manapun); migrasi `20260809000001_picking_surat_jalan_menu_permissions.sql` menambah kedua menu + action `view` + baris `role_menu_permissions` utk `admin`/`ceo`/`gm`/`manager`/`operations` (mereplikasi PERSIS array role lama, `super_admin` tak perlu baris krn selalu bypass true) — **data-only, doc-keeper tak bisa verifikasi baris benar-benar ter-INSERT** (`schema_snapshot.sql` schema-only, tanpa data; percaya isi file migrasi + commit message "sekarang digerbang", belum diverifikasi independen ke DB live).
- **`role_menu_permissions`** [BARU, 7 Agu 2026] — default menu-permission PER ROLE, cerminan struktural `user_menu_permissions` tapi `role_id`-keyed (bukan `user_id`) dan **SENGAJA TANPA kolom `effect`** — role cuma bisa GRANT, tak ada konsep deny di level role (keputusan desain). 2 partial unique index (`role_id`+`menu_action_id` XOR `role_id`+`module_action_id`, konstrain `rmp_one_action_required`). RLS: tulis (INSERT/UPDATE/DELETE) `is_super_admin()` only; baca = siapa pun yang role itu jadi salah satu role AKTIF-nya sendiri (EXISTS ke `user_roles`). `role_id` sudah company-scoped lewat `roles.roles_company_code_unique`, tak perlu kolom `company_id` sendiri. ~~**⚠️ Tabel ini KOSONG TOTAL saat ini**~~ **[KOREKSI 9 Agu 2026]** tidak lagi 100% kosong per migrasi `20260809000001` di atas (Picking/Surat Jalan × 5 role) — TAPI ini scoped-fix utk 2 menu spesifik, **BUKAN** Fase B3 (isi default komprehensif utk SEMUA menu × role) yang **tetap belum dimulai**. **`hasMenuPermission` (`AuthContext.jsx`) resolusi 3-tingkat:** super_admin selalu true → user override (baris `user_menu_permissions` yang cocok, KALAU ADA, menang mutlak sesuai `effect`-nya) → union `role_menu_permissions` dari SEMUA role AKTIF user (union, BUKAN role primer — sengaja beda dari pola `get_user_role_code()` yang non-deterministic utk user multi-role) → default deny. **[9 Agu 2026]** Query tier-3 ini (`fetchMenuPermissions`, `AuthContext.jsx`) kini JUGA difilter ke `roleIds` milik `activeCompanyId` saja (dulu union SEMUA company tanpa filter) — lihat bullet `activeCompanyId` di bawah. Halaman admin: `RoleDefaultsPage.jsx` (super_admin only, gate di titik render). Rekaman: `supabase/migrations/20260807000002_role_menu_permissions_and_effect_column.sql` — **⚠️ BELUM masuk `schema_snapshot.sql`** (diverifikasi doc-keeper: 0 hit `role_menu_permissions`, kolom `effect` juga absen dari definisi `user_menu_permissions` di snapshot per 7 Agu 2026 pagi) — utang `pg_dump`.
- **`profiles.role`** (legacy enum `user_role_legacy`) — deprecated, frontend/EF tak baca lagi; drop kolom = tahap final pending.
- **`activeCompanyId` / `setActiveCompanyId` / `myCompanyIds`** [BARU, 9 Agu 2026 — Fase 1 multi-entity plumbing; BUKAN tabel DB, murni state `src/contexts/AuthContext.jsx`] — `activeCompanyId` = company "sedang aktif" bagi user (default `profile.company_id`/home company, override via `setActiveCompanyId`; **session-only, TIDAK persist localStorage**, direset ke `null` otomatis tiap login/logout lewat listener auth-state yang sudah ada). `myCompanyIds` = array SEMUA `company_id` tempat user punya baris `user_roles` AKTIF (turunan `erpRoles`, data yang SUDAH di-fetch sebelumnya — cuma belum dipakai apa pun sebelum ini). `pickPrimaryErpRole(erpRoles, activeCompanyId)` (fungsi lokal, dulu `pickPrimaryErpRole(erpRoles)`) kini **SCOPE resolusi role ke company aktif dulu** (`filter(r => r.company_id === activeCompanyId)`), baru pilih privilege tertinggi DI DALAM company itu — sebelumnya lintas-company tanpa filter (privilege tertinggi user itu DI MANA PUN, dipakai jadi `erpRole`/`role` di ratusan tempat `App.jsx`). **0 perubahan behavior yang diklaim hari ini** — tapi klaim itu TERBUKTI TIDAK SELALU BENAR sesi yang sama: kasus Elvira Nurhuda (Finance permanen di MSI+SOA sekaligus) adalah exercise PERTAMA kode ini ketemu data multi-company nyata (lihat gotcha #10 + `08_TECH_DEBT.md` **TD-180**). Konsumen: `hooks/useProducts.js`/`hooks/useAppSettings.js` (fallback `companyId ?? activeCompanyId`, ganti dari `profile?.company_id`, masuk dependency array effect fetch masing-masing), UI switcher pertama = `src/components/CompanySwitcher.jsx` (lihat `04_ROLE_PERMISSION_MATRIX.md`).

### Finance
- `chart_of_accounts` (COA, unique per company), `cost_centers`, `taxes`, `currencies`, `exchange_rates`.

### Approval Engine
- `approval_rules`, `approval_workflows`, `approval_workflow_steps`, `approval_logs`, `approval_delegations`. (Sebagian UI Admin Settings sudah disambung; lihat ROADMAP.)

### HRGA
- `hrga_requests`, `hrga_request_items`, `hrga_request_types`, `hrga_request_approvals` (INSERT-only audit), `hrga_request_attachments`, `hrga_approval_configs` (**UNIQUE(request_type_id, level)**; selalu filter `company_id`), `hrga_notification_queue`, `hrga_offboarding_checklists`, `hrga_offboarding_items`.

### Procurement
- **`prf`** [BARU FASE 0, 10 Jul 2026] — **Price Request Form** (greenfield). Satu tabel dgn child fields Sea/Air/Inland/Custom/Project sebagai kolom nullable (opsi A; discriminator = `service_type`). **52 kolom.** Sistem: `id`, `company_id`, `prf_no`, `status` (DEFAULT `DRAFT`, CHECK ∈ `DRAFT/SUBMITTED/ACKNOWLEDGED/CANCELLED/QUOTED/EXPIRED`), `created_by` (=Nama Sales), `updated_by`, `submitted_at`, `acknowledged_by/at`, `created_at`, `updated_at`, soft-delete `deleted_at`. Informasi Dasar: `customer_source` (customer/prospect/inquiry), `account_id` (FK `accounts`), `account_name_manual`, `stream`, `deadline_quotation`. Inquiry Details: `direction`, `commodity` (+**4 opsi B1**, 23 Jul: `perishable`/`live_animal`/`valuable`/`pharma`, form-only tanpa CHECK), **`goods_name`** (text, nullable — **B1**, prefill dari `inquiries.goods_name`), `hs_code`, `msds_available`, **`un_number`**/**`imo_class`** (**B1**, tampil saat `commodity='dg'`, prefill fill-empty-only dari inquiry), `service_type` (DISCRIMINATOR), `incoterms` (single), `commercial_value` numeric(14,2), `commercial_currency`, `origin`, `destination`, `pickup_address`, `delivery_address`, `add_on_services` text[] (**11 opsi** [koreksi Fase 2 — sebelumnya doc keliru catat 6]: custom_clearance, inland, import_license_undername, import_license_pi, export_license, ls, warehouse_umum, warehouse_reefer, tkbm, insurance, others — **5 customs-family** [custom_clearance/import_license_undername/import_license_pi/export_license/ls] disabled saat direction=domestic; sisanya tidak), `add_on_others`, `cargo_ready_date`, `notes`. Child (**Fase 2 [10 Jul 2026] — kolom sudah ada sejak Fase 0 DAN sekarang sudah di-form** di Section 03 "Detail Layanan", dinamis per `service_type`): `sea_freight_type` (FCL/LCL), `sea_container_types` text[] (kode `20`/`40`/`40HC`/`20RF`/`40RF`), `sea_container_qty` jsonb (qty per tipe FCL, mis. `{"20":2,"40HC":1}`), `sea_lcl_gw/dimension/volume/koli`; `air_gw/dimension/volume/koli`; `inland_fleet_types` text[] (25 opsi armada) + `inland_pickup_address/delivery_address` (wajib) + `inland_gw/dimension` (opsional); `custom_doc_type` (AUTO PIB/PEB dari direction — hanya saat service=custom DAN add-on Custom Clearance dicentang); `project_freight_types` text[] + `project_qty`. **UNIQUE(company_id, prf_no)** + 5 FK (company/created_by/updated_by/acknowledged_by/account_id). Trigger `set_prf_updated_at` (reuse `set_updated_at()`). Nomor `prf_no` dirakit di FE: `PRF/{ENTITAS}/{TAHUN}/{ROMAWI}/{URUT}` via `increment_document_sequence` (`p_document_type='PRF'`, `p_department_code='PROC'`, `p_month`, reset per-bulan per-entitas). **RLS single-entity + bypass super_admin** (tiru `hrga_requests`): `prf_insert` (company + `created_by=auth.uid()` + `has_role('sales') OR has_role('gm_bd')`), `prf_select` (company + own OR `has_role('procurement')` OR `is_manager_or_above()`), `prf_update_draft` (own + DRAFT), `prf_update_status` (procurement, saat SUBMITTED). **Keempat policy dibungkus `is_super_admin() OR (…)`** sesuai jenisnya (insert=WITH CHECK saja, select=USING saja, update=USING & WITH CHECK) → super_admin bypass company scope (standar, konsisten semua modul; berarti super_admin LIHAT PRF lintas-3-entitas). **Cabang `is_super_admin` ditambahkan setelah Fase 0** (Fase 0 keliru tanpa cabang ini → super_admin ditolak saat INSERT); direkam `supabase/migrations/20260710000003_prf_rls_super_admin.sql` (verified pg_policies count=4). Yang **masih ditunda ke Fase 3b** = cross-entity inbox untuk role `procurement` (butuh cabang RLS custom) — BUKAN super_admin. Rekaman Fase 0: `supabase/migrations/20260710000001_prf_fase0.sql`. Kolom `inquiry_id uuid` + FK `→ inquiries(id)` DITAMBAH manual oleh Den setelah Fase 0 — direkam di `supabase/migrations/20260710000002_prf_add_inquiry_id.sql`. **Status relasi `inquiry ↔ prf` (update 18 Jul 2026):** FK ini dulu tercatat ADA tapi **hampir tak dipakai** (Fase 1 hanya menyalin `account_id` saat `customer_source='inquiry'`); **kini DIPAKAI aktif** oleh fitur "Cetak PRF dari inquiry" (mandat MI) untuk: **(a)** prefill form PRF dari inquiry (field non-cabang: hs_code/pickup/delivery/pol→origin/pod→destination/deadline/incoterms — NOL perubahan DB), **(b)** panel "Daftar PRF" per-inquiry di `DealDetailPage` (query `prf` WHERE `inquiry_id`), **(c)** list "Forwarding (MSI)" (`ProcInquiryForwardingPage`, embed `inquiries`), **(d)** jejak `inquiry_id` saat INSERT PRF. Lihat `05_WORKFLOW_MAP` §Procurement + `08_TECH_DEBT` **TD-79** (sebagian teraddress) / **TD-76** (~~list read-only ada, form tetap create-only~~ **[KOREKSI 29 Jul 2026 — USANG]** form TAK LAGI create-only; PRF berstatus DRAFT kini bisa dibuka & diedit lewat tombol "Edit PRF" di `PRFDetailPage.jsx` — TD-76 → VERIFY [kode], lihat `08_TECH_DEBT` TD-76 untuk detail lengkap + caveat yang tersisa). Fase 1 (form Section 01/02 + menu + nomor auto) = KODE only (`PRFFormPage.jsx`). **Fase 2 (10 Jul 2026)** = KODE only (same file) — Section 03 Detail Layanan (child fields dinamis per `service_type`) + koreksi daftar add-on (6→11); **tanpa ubah DB** (kolom child sudah ada sejak Fase 0). Trigger Inland penuh bila `service_type='inland'` ATAU add-on `inland` dicentang. **[+ Pricing Answer, 20 Jul 2026 — branch `feat/prf-pricing-answer`; SQL DIJALANKAN MANUAL oleh Den di SQL Editor, rekaman `supabase/migrations/20260720000000_prf_pricing_answer.sql`, non-destruktif/idempotent]** +7 kolom **jawaban harga** (diisi procurement, ditulis via UPDATE `prf` → dijaga policy `prf_update_status` yang ADA = procurement + `status='SUBMITTED'`; **NOL policy prf baru**): `suggested_rate` numeric(18,2), `rate_currency` text **NOT NULL DEFAULT 'IDR'**, `valid_from` date, `valid_until` date, `pricing_notes` text, `answered_by` uuid (FK `prf_answered_by_fkey → profiles(id)`), `answered_at` timestamptz. Semua nullable kecuali `rate_currency`. **Menyimpan jawaban TIDAK mengubah `prf.status`** (satu-satunya penulis status = form create `PRFFormPage:~372`; tak ada transisi) → procurement bisa mengoreksi ulang selama PRF `SUBMITTED`. **Total Modal / Untung / Margin % dihitung saat render, TIDAK disimpan.** ✅ kolom-kolom jawaban **sudah masuk snapshot** (pg_dump 20 Jul 2026 — koreksi 20 Jul: klaim lama "snapshot belum memuat" usang, terverifikasi grep `suggested_rate`/`rate_currency`/`answered_by` di snapshot). **Dirujuk balik oleh `quotations.prf_id`** [20 Jul 2026] — PRF terjawab bisa diturunkan jadi quotation via tombol "Buat Quotation" di `PRFDetailPage` (lihat entri `quotations` §CRM & Sales + `05_WORKFLOW_MAP` §Procurement — PRF). **[+ kolom `exchange_rates`, 21 Jul 2026 — SQL manual Den, rekaman `supabase/migrations/20260721000001_prf_currency_fk_exchange_rates.sql`]** `exchange_rates` **jsonb NOT NULL DEFAULT `'{}'::jsonb`** — **tabel kurs manual per-dokumen**, object map `{"USD":16200}`, **IDR implisit = 1 (TIDAK disimpan)**; meniru **pola `quotations.exchange_rates`** (`20260717000000`) sebagai sumber kebenaran kurs per-dokumen, input manual tanpa lookup FX. ~~⚠️ **BELUM DIBACA KODE MANA PUN** — kolom disiapkan lebih dulu, aktif dipakai saat batch multi-vendor + currency PRF dijalankan.~~ **[KOREKSI 21 Jul 2026 — klaim itu USANG]** kolom ini **kini DIBACA & DITULIS UI**: panel "Jawaban Harga" (`PRFDetailPage.jsx`) menampilkan **tabel kurs "Kurs ke IDR"** di header panel (pola sama form quotation: peta `{code: rate}`, IDR implisit 1, "+ Tambah Mata Uang…", peringatan kurs belum diisi), dan `handleSave` mengirimnya lewat `p_header.exchange_rates` → ditulis RPC `save_prf_pricing` (`20260721000004`). **Kurs di sini TIDAK dipakai menghitung total di layar PRF** (total sengaja ditampilkan terpisah per mata uang, tanpa konversi) — perannya: (a) disalin ke `prf_cost_items.exchange_rate` per baris saat simpan, (b) satu-satunya bahan konversi ke IDR untuk `cost_total` yang dikirim ke quotation. ⚠️ **Awas tabrakan nama:** ada **tabel** `public.exchange_rates` (master FX history) yang **berbeda & dorman** — lihat gotcha #13 + **TD-74**. ✅ kolom ini **sudah masuk snapshot** (terverifikasi grep, dalam definisi tabel `prf` sebelum `prf_status_check`). ~~**⚠️ Multi-vendor BELUM ADA di rantai PRF:** kolom `vendor_id`/`item_group`/`is_awarded`/`exchange_rate` … **sempat ditambahkan lalu di-DROP kembali** … **Jangan anggap multi-vendor tersedia.**~~ **[KOREKSI 21 Jul 2026 — multi-vendor KINI ADA]** keempat kolom itu **dipasang ulang** (Tahap A `20260721000003`) dengan urutan yang benar — **kode + RPC disiapkan lebih dulu, DB menyusul** (pelajaran dari percobaan sebelumnya) — lalu RPC diperluas (Tahap B `20260721000004`) dan panel FE dirombak (21 Jul 2026). Detail kolom + guard: entri **`prf_cost_items`** di bawah. ⚠️ **Snapshot belum memuat keempat kolom** (lihat banner atas + gotcha #10). **[+ Nama Barang & detail DG, 23 Jul 2026 — batch B1, commit `1235f4a`]** +3 kolom baru di **Inquiry Details**: **`goods_name`** (text, nullable — prefill dari `inquiries.goods_name` via `applyInquiryData`, TIDAK mandatory), **`un_number`** (text) + **`imo_class`** (dropdown, nilai IDENTIK `InquiryFormPage` supaya prefill cocok opsi) — keduanya tampil saat `commodity='dg'`, berdampingan dgn `msds_available`, prefill fill-empty-only dari inquiry (`un_number`/`imo_class`) meski field-nya baru TAMPIL saat DG dipilih. Ketiganya **nullable, tanpa default, tanpa CHECK**. Enum `commodity` PRF (form-only, `text` tanpa CHECK di DB) **+4 opsi**: `perishable`/`live_animal`/`valuable`/`pharma`. Kewajiban field lain ikut berubah: `cargo_ready_date` jadi opsional, **Incoterm jadi WAJIB saat Submit**, Volume Air jadi opsional (Volume Sea-LCL tetap wajib). SQL prasyarat (`ALTER TABLE prf ADD COLUMN goods_name/un_number/imo_class`) dijalankan manual 23 Jul bersamaan commit, **direkam migrasi arsip** `supabase/migrations/20260725000005_arsip_b1_prf_goods_fields.sql` (25 Jul, idempotent). **Diverifikasi `AUDIT_B4_READINESS.md` (25 Jul):** dump terakhir (`92af2c1`, 25 Jul 11:58) terjadi SETELAH B1 (23 Jul 13:49) → ketiga kolom **sudah ADA** di snapshot saat ini, **nol utang `pg_dump` untuk ketiganya** — yang tadinya bolong hanya jejak migrasinya (kini lunas). Detail: `PROGRESS.md` 2026-07-25 (entri PRF batch B1 + Inquiry B3 + drop kolom B4) + `08_TECH_DEBT` TD-107 (koreksi item `commodity`). **⭐ [27 Jul 2026 — Detail PRF akhirnya menampilkan Section 03 + DG, FE-only 1 file `PRFDetailPage.jsx`, NOL perubahan DB]** Kedua audit read-only `AUDIT_PRF.md`+`AUDIT_PRF_FIELDS.md` (27 Jul 2026) menemukan: seluruh **19 kolom Section 03** (`sea_freight_type`…`project_qty`, sudah ada sejak Fase 0/10 Jul) + **`msds_available`/`un_number`/`imo_class`** (`msds_available` sejak Fase 0; `un_number`/`imo_class` sejak B1/23 Jul) **sudah ADA di DB & snapshot**, tapi `PRFDetailPage.jsx` **tak pernah men-select-nya** — datanya tersimpan sejak create, layar baca tak pernah menampilkannya. Batch ini memperluas `PRF_SELECT` (`PRFDetailPage.jsx:69`) +19 kolom Section 03 +3 kolom DG +`account_id` + embed `account.code` (dulu hanya `name`) → 2 kartu baru **"Detail Layanan"** (kondisional per `service_type`, meniru struktur form create — field kosong satu-satu disembunyikan, bukan tampil "—") dan **"Dangerous Goods (DG)"** (kondisional, seluruh kartu disembunyikan bila `!un_number && !imo_class && !msds_available`; `msds_available` SELALU ditampilkan Ya/Tidak begitu kartu tampil, karena `false` pun informasi penting untuk kargo DG). **NOL kolom baru, NOL migrasi, NOL `pg_dump`** — seluruh kolom yang kini dibaca sudah lama live & di snapshot (diverifikasi doc-keeper: grep `schema_snapshot.sql` untuk `sea_freight_type`/`msds_available`/dst — semua ADA di definisi `CREATE TABLE public.prf`, `:4463` dst). Menutup **usulan `AUDIT_PRF_FIELDS.md` TD-151/TD-152** di kode (belum tes runtime) — lihat `08_TECH_DEBT` TD-151/TD-152. Detail: `05_WORKFLOW_MAP` §Procurement — PRF + `PROGRESS.md` 2026-07-27. **⭐ [27 Jul 2026 — model award DIBALIK, fondasi "Penawaran Vendor", DB-only, `190fb5e`+`5c87119`]** +6 kolom baru: **`selected_offer_id`** (FK → `prf_vendor_offers(id)` ON DELETE SET NULL — pemenang yang DIPILIH SALES, bukan lagi flag di `prf_cost_items`), **`selected_by`**/**`selected_at`** (siapa & kapan, diisi RPC `prf_select_offer`), **`min_offers_waiver_reason`** (alasan tertulis kalau PRF di-mark-QUOTED dengan <3 penawaran — lihat RPC `prf_mark_quoted` §5), **`acknowledged_by`**/**`acknowledged_at`** FK → `profiles(id)` (satu procurement PENGERJA per PRF, diisi `prf_claim()`, dikosongkan `prf_release()` — **TIDAK ADA fungsi takeover**, wajib dua langkah: release lalu claim ulang oleh orang lain). **`prf_status_check` TIDAK diubah** — sudah memuat keenam nilai (`DRAFT`/`SUBMITTED`/`ACKNOWLEDGED`/`CANCELLED`/`QUOTED`/`EXPIRED`) sejak **Fase 0** (10 Jul 2026, commit `acc5f01`, diverifikasi `git log -S"prf_status_check"` → hanya SATU commit yang pernah menyentuhnya, tak satu pun dari ketiga migrasi 27 Jul). Yang berubah sesi ini adalah **ALUR** — 4 RPC baru (`prf_claim`/`prf_release`/`prf_mark_quoted`/`prf_select_offer`, §5) membuat nilai yang tadinya sah-secara-skema-tapi-mati jadi (sebagian) terjangkau kode — **`CANCELLED`/`EXPIRED` MASIH TANPA RPC**, nol jalan masuk (klaim Den: PRF/MSI/2026/VII/004 nyangkut permanen; **TD-154** baru). **⚠️ Interaksi dgn panel Jawaban Harga LAMA (`save_prf_pricing`, masih satu-satunya yang dipakai UI live) — temuan doc-keeper:** begitu `prf_claim()` dipanggil (status→`ACKNOWLEDGED`), RLS `prf_update_status` (USING menuntut `status='SUBMITTED'`, `schema_snapshot.sql:13055`) **langsung menolak** UPDATE header oleh `save_prf_pricing` (SECURITY INVOKER) → guard internalnya sendiri RAISE `'PRF tidak ditemukan atau tidak ada izin menyimpan jawaban harga (RLS).'` (`schema_snapshot.sql:1276-1277`, dibaca+diverifikasi langsung dari body fungsi). **Nol dampak HARI INI** (nol pemanggil FE `prf_claim`), tapi ini mengaktifkan risiko yang sudah diperingatkan `08_TECH_DEBT` TD-109 — dulu hipotetis "bila kelak ditambah transisi", kini mekanismenya SUDAH ADA & bisa dipicu kapan saja. Lihat TD-109 (update) + entri `prf_vendor_offers` (baru, di bawah). **[UPDATE 27 Jul 2026 sore — DUA hal berubah, KEDUANYA di luar paragraf ini sendiri.] (1)** `prf_claim` **sekarang PUNYA pemanggil FE** — tombol "Ambil PRF Ini" di `PRFDetailPage.jsx` (Batch 3A, lihat entri `prf_vendor_offers` di bawah) — jadi "nol pemanggil FE" di kalimat sebelumnya **usang**. **(2)** RAISE yang diperingatkan paragraf ini **tidak lagi terjadi untuk pemegang PRF** — migrasi `20260727000004_prf_rls_acknowledged.sql` (commit `e1c7ff1`, SETELAH paragraf ini ditulis) memperlebar `prf_update_status` jadi `status IN ('SUBMITTED','ACKNOWLEDGED') AND (acknowledged_by IS NULL OR acknowledged_by=auth.uid())`, sehingga procurement yang meng-klaim PRF tetap bisa memakai panel Jawaban Harga lama sesudahnya. **[KOREKSI SUSULAN, doc-keeper 27 Jul 2026]** Daftar status di atas (`status IN ('SUBMITTED','ACKNOWLEDGED')`) SUDAH USANG — migrasi `20260727000006_prf_update_status_terima_quoted.sql` melebarkannya SEKALI LAGI ke `status IN ('SUBMITTED','ACKNOWLEDGED','QUOTED')`; diverifikasi lewat sesi procurement ASLI (ROLLBACK, bukan super_admin): UPDATE `prf.pricing_notes` pada PRF QUOTED BERHASIL — panel Jawaban Harga lama TIDAK lagi terkunci begitu PRF `QUOTED`. TD-109 TETAP MEDIUM, tapi kini karena asimetri `prf_vendor_offers_update` yang tak menyaring status PRF sama sekali (bukan lagi risiko RAISE ini). Detail lengkap (termasuk usulan turun severity yang sudah DITARIK KEMBALI, kontradiksi baru soal `prf_cost_items.prf_id`, dan blok update terbaru): `08_TECH_DEBT` TD-109 + TD-122 (item 9c) + `PROGRESS.md` 2026-07-27 (entri susulan DB `604430f`+`e1c7ff1`; entri susulan doc-keeper migrasi `20260727000006`).
- **`vendors`** — master vendor (Foundation; PR/PO belum dibangun). `company_id` NOT NULL (FK `→ companies` ON DELETE RESTRICT) + **UNIQUE(company_id, code)** → **per entitas**, bukan global. ~~**Nol FK masuk** dari tabel mana pun → **belum tersambung ke transaksi** (PRF/PR/PO tak menunjuk vendor).~~ **[KOREKSI 21 Jul 2026 — klaim itu USANG]** kini ada **satu FK masuk**: **`prf_cost_items.vendor_id` → `vendors(id)`** (nullable, tanpa `ON DELETE`; migrasi `20260721000003`), diisi lewat dropdown vendor di panel "Jawaban Harga" PRF. **PR/PO tetap belum ada.** ⚠️ Dropdown itu **wajib** menyaring `company_id` + `is_active` + **`deleted_at IS NULL`** sendiri (**TD-115**) — sudah dipasang di `PRFDetailPage.jsx:139-148`. ~~Satu-satunya pembaca FE = `PenerimaanBarangPage.jsx:332`.~~ **[KOREKSI 21 Jul 2026 — klaim ini USANG]** kini ada **dua konsumen FE**: (1) `PenerimaanBarangPage.jsx:332` (Inventory, dropdown vendor saat penerimaan barang — **pembaca**; sejak 21 Jul 2026 query-nya menyaring **`company_id` + `is_active = true` + `deleted_at IS NULL`**, lihat blok "penyaringan arsip" di bawah); (2) **`src/modules/procurement/VendorListPage.jsx` [BARU 21 Jul 2026] = halaman Master Vendor, PENULIS pertama** (INSERT/UPDATE + soft-delete `deleted_at`; `company_id` dari `profile.company_id`, `created_by`/`updated_by` = `profile.id`, `currency_code` dari dropdown `currencies`). Tabel **tak lagi yatim dari sisi UI**, tapi **tetap yatim dari sisi transaksi**. ⚠️ FK `vendors_created_by_fkey`/`vendors_updated_by_fkey` menunjuk **`auth.users(id)`** (`:9852`/`:9876`) — beda dari `prf`/`quotations` yang menunjuk `profiles(id)`; mengisinya dengan `profile.id` tetap sah karena `profiles.id` **ADALAH** `auth.users.id` (`profiles_id_fkey`, `:9212`, tanpa DEFAULT). ⚠️ `vendor_type` **tak punya CHECK** (`varchar(50)` polos) — keseragaman 6 nilai lowercase dijaga FE saja. Alur + batasan halaman: `05_WORKFLOW_MAP` §Procurement — Master Vendor. **RLS DIROMBAK 21 Jul 2026** — dari **LIMA policy tumpang tindih** (`vendors_select`, `vendors_read`, `vendors_insert`, `vendors_update`, `vendors_modify`, warisan `20260524000014_rls_policy_draft.sql`) jadi **EMPAT**: `vendors_select` / `vendors_insert` / `vendors_update` / `vendors_delete`. Semua **`TO authenticated`**, company-scoped (`company_id = get_user_company_id()`), role gate **`is_manager_or_above() OR has_role('procurement')`**, super admin bypass top-level (`is_super_admin() OR (…)`); SELECT & UPDATE USING juga menuntut `deleted_at IS NULL`; **DELETE = super admin only**. Yang ditutup: **(a)** role legacy `procurement_head`/`procurement_staff` di `vendors_update` dibuang → pakai `has_role('procurement')` sesuai taksonomi aktif; **(b)** `vendors_select USING(true)` **tanpa klausa `TO`** yang menganulir scoping company `vendors_read` (policy permissive di-OR-kan) = **bocor BACA lintas entitas antar user login** — ⚠️ **`anon` TIDAK terdampak** (tak punya GRANT ke tabel ini) → bocor antar-user-login, BUKAN bocor publik; **(c)** `vendors_insert WITH CHECK (auth.uid() IS NOT NULL)` — **tanpa cek role MAUPUN company** → user terautentikasi apa pun bisa menyisipkan vendor ke company mana pun (**paling berbahaya dari kelimanya**; luput dari TD-47 lama & `AUDIT_PRF_VENDOR.md`, keduanya sudah dikoreksi). SQL **sudah dijalankan & LIVE** (Den), rekaman `supabase/migrations/20260721000000_vendors_rls_overhaul.sql`; ✅ **sudah masuk snapshot** (terverifikasi grep: 4 policy baru ada, `vendors_read`/`vendors_modify` + role legacy 0 hit). → `08_TECH_DEBT` **TD-47 RESOLVED**; pola cacat serupa di `rate_sheets` (**TD-55**) **masih OPEN**. **[21 Jul 2026 — PENYARINGAN ARSIP SUDAH PINDAH DARI RLS KE KODE; migrasi `20260721000002_vendors_select_drop_deleted_at.sql` LIVE]** Syarat `deleted_at IS NULL` **SUDAH DICABUT** dari klausa **USING** `vendors_select` → policy kini `is_super_admin() OR company_id = get_user_company_id()` saja (`schema_snapshot.sql:12581`, snapshot **sudah di-refresh**). **Alasan = perilaku terukur:** soft delete vendor (`UPDATE deleted_at`) **ditolak `42501` "new row violates row-level security policy"** saat dijalankan dengan **role `procurement` asli**; terbukti lewat **A/B terkontrol di SQL Editor** — user sama, baris sama: `UPDATE city` **lolos**, `UPDATE deleted_at` **ditolak**; lalu setelah syarat itu dicabut **di dalam transaksi**, `UPDATE deleted_at` **lolos** dengan `RETURNING` 1 baris. ⚠️ **MEKANISMENYA BELUM FINAL — PERTANYAAN TERBUKA, jangan tulis sebagai fakta:** `vendors_update` **tidak menyebut `deleted_at` di WITH CHECK** (`:12588`; migrasi `20260721000000_vendors_rls_overhaul.sql:45-49`) — syarat itu hanya di klausa **USING** (`:27`, `:42`) — sehingga model standar (USING dievaluasi ke baris LAMA, WITH CHECK ke baris BARU) **tidak menjelaskan** kenapa error itu muncul; `.update()` tanpa `.select()` juga tidak meminta representasi baris (postgrest-js 2.105.4). **Yang tercatat = gejala + perbaikan yang terbukti**, bukan sebabnya. **KONSEKUENSI: penyaringan vendor terarsip kini tanggung jawab QUERY di kode, tanpa jaring pengaman DB** — setiap query `vendors` BARU (termasuk **vendor picker di PRF** nanti) wajib memasang `.is('deleted_at', null)` sendiri → **`08_TECH_DEBT` TD-115**. Dua pembaca yang ADA sudah patuh: `VendorListPage.jsx:104` (list) dan dropdown GRN `PenerimaanBarangPage.jsx:336` (tiga filter: `company_id` + `is_active = true` + `deleted_at IS NULL`; filter ini dipasang **lebih dulu**, sebelum RLS dilonggarkan — sebelumnya hanya `company_id` + `is_active`, dan itu **tidak cukup** karena soft delete **hanya menulis `deleted_at`, tidak menyentuh `is_active`** → vendor terarsip tetap `is_active = true`). Titik akses `vendors` **tanpa guard `deleted_at`**: `VendorListPage.jsx:139` (fetch Edit), `:189` (UPDATE), `:219-221` (soft delete) — vendor terarsip bisa dibuka/diedit **kalau id-nya dipegang**, tapi **hari ini belum ada jalur yang menyuplai id semacam itu** (satu-satunya sumber = baris list yang sudah tersaring) → celah *defense-in-depth*, **TD-116**. ⚠️ **`vendors_update` USING MASIH membawa `deleted_at IS NULL`** (`:12588`, sengaja tak disentuh migrasi ini) → baris terarsip **tak bisa dipulihkan** dari klien; diperparah `vendors_company_code_unique (company_id, code)` (`:6071`) yang **bukan unique parsial** → **kode vendor hangus permanen** setelah diarsipkan (tak bisa dipakai ulang, INSERT kena `23505`) → **TD-117** (obatnya satu paket: tombol Pulihkan). Terverifikasi tak ada jalur baca lain: nol embed `vendors!fk`/`vendor:vendors(...)`, nol fungsi/view DB yang menyentuh `vendors`, dan nol RPC pemanggil dari `src/` yang membacanya (`SchemaManagerPage.jsx:32` hanya menyebut namanya sebagai entri `TABLE_GROUPS`, tidak membaca baris).
- **`prf_cost_items`** [BARU 20 Jul 2026, branch `feat/prf-pricing-answer`; SQL manual, rekaman `20260720000000_prf_pricing_answer.sql`] — **tabel anak rincian biaya (cost build-up) jawaban harga PRF.** Kolom: `id`, `prf_id` NOT NULL (FK `→ prf(id)` **ON DELETE CASCADE**), `component` text NOT NULL, `cost_type` text NOT NULL DEFAULT `'vendor'` (CHECK `prf_cost_items_cost_type_check` ∈ `vendor`/`internal`), `amount` numeric(18,2) NOT NULL DEFAULT 0, `currency` text NOT NULL DEFAULT `'IDR'` (**FK `prf_cost_items_currency_fkey → currencies(code)` ditambahkan 21 Jul 2026** — sebelumnya free text tanpa FK/CHECK sehingga `'usd'`/`'IDR '`/typo diterima diam-diam; meniru pola `vendors.currency_code → currencies(code)`. **Kontras: `quotation_items.currency` SENGAJA belum dikunci** karena datanya sudah banyak → risiko FK violation saat migrasi; rantai PRF kini tervalidasi, rantai quotation belum → `08_TECH_DEBT` **TD-113**, kerabat **TD-114**), `sort_order` int NOT NULL DEFAULT 0, `notes` text, `created_at`, `updated_at`. Index `idx_prf_cost_items_prf_id`. Trigger `set_prf_cost_items_updated_at` (reuse `set_updated_at()`). GRANT SELECT/INSERT/UPDATE/DELETE authenticated. **RLS (4 policy) DITURUNKAN dari policy `prf` yang ASLI via `EXISTS` ke induk `public.prf` (pola `quotation_items`/`activity_logs`):** SELECT ← predikat `prf_select` (siapa boleh baca PRF boleh baca rinciannya — termasuk sales creator = view-only); INSERT/UPDATE/DELETE ← predikat **USING** `prf_update_status` (`has_role('procurement')` + `p.status='SUBMITTED'` + `company_id=get_user_company_id()` + `deleted_at IS NULL`), semua dibungkus `is_super_admin() OR (…)`. **Catatan: tabel `prf` TIDAK punya DELETE policy sendiri** — write anak sengaja diikat ke `prf_update_status` (procurement + SUBMITTED), TIDAK dilonggarkan. **Tidak ada role `pricing`** di sistem (hanya `procurement`) → "pricing/procurement" = procurement. **Ditulis dari FE (`PRFDetailPage.jsx`) via RPC atomik `save_prf_pricing(p_prf_id uuid, p_header jsonb, p_items jsonb)`** — **SECURITY INVOKER** (RLS tetap penegak: `prf_update_status` utk header, `prf_cost_items_*` utk rincian; NOL perubahan policy), satu transaksi: UPDATE header (guard `GET DIAGNOSTICS ROW_COUNT=0 → RAISE` = blokir user tak-berwenang SEBELUM delete) → DELETE rincian → INSERT rincian dari `jsonb_array_elements`; `answered_by`=server `auth.uid()`, `answered_at`=now(); RAISE apa pun me-rollback SELURUH transaksi. Rekaman migrasi TERPISAH **`supabase/migrations/20260720000001_save_prf_pricing_rpc.sql`**, lalu **`20260720000002_save_prf_pricing_guard.sql`** (`CREATE OR REPLACE` men-deploy ulang fungsi + guard). **Jejak keputusan:** (1) implementasi awal memakai 3 request client terpisah (UPDATE + DELETE + INSERT) = **TIDAK atomik** (bila DELETE commit lalu INSERT gagal → rincian lama hilang, header terlanjur tersimpan) → diganti RPC atomik (keputusan Den, opsi A); (2) **tes runtime menemukan rincian sempat tak masuk** (`prf_cost_items` 0 baris, header tersimpan, tanpa error) — hilang SETELAH `CREATE OR REPLACE save_prf_pricing` (`20260720000002`, +guard `RAISE` bila `p_items` non-array). **Akar TAK PERNAH dipastikan** — bug hilang sebelum sempat didiagnosis, **guard tak pernah RAISE = nol bukti `p_items` non-array**; hipotesis (unproven) = PostgREST schema-cache stale ter-refresh oleh DDL; FE tak diubah → `08_TECH_DEBT` **TD-110**. ✅ tabel + RPC **sudah masuk snapshot** (pg_dump 20 Jul 2026 — koreksi 20 Jul: klaim lama "snapshot belum memuat" usang, terverifikasi grep `prf_cost_items` 31 hit + `save_prf_pricing` 3 hit di snapshot). **[+4 kolom MULTI-VENDOR, 21 Jul 2026 — rekaman `20260721000003_prf_cost_items_vendor_award_fx.sql` (Tahap A) + `20260721000004_save_prf_pricing_vendor_award_fx.sql` (Tahap B), keduanya SUDAH DIJALANKAN & LIVE]:** `vendor_id` uuid **NULL** (FK → `vendors(id)`; **tanpa** `ON DELETE`), `item_group` text **NULL**, `is_awarded` boolean **NOT NULL DEFAULT true**, `exchange_rate` numeric **NOT NULL DEFAULT 1**. **Semua sengaja LONGGAR** — tujuan eksplisit di migrasi: RPC lama & UI lama tetap jalan tanpa disentuh; `is_awarded` default `true` supaya baris lama otomatis dianggap pemenang (**angka modal PRF lama tidak berubah**), `exchange_rate` default 1 karena baris lama semuanya IDR. **`item_group` SET NOT NULL + penegak award DITUNDA ke Tahap D** (setelah UI+RPC live & backfill) — **Tahap D sampai kini BELUM dikerjakan, dicatat sebagai `08_TECH_DEBT` TD-122 (severity naik ke MEDIUM 21 Jul 2026)** (prasyarat "UI + RPC baru live" sudah terpenuhi 21 Jul; sisanya **backfill `item_group` NULL**). ⚠️ **"Partial unique index" yang disebut komentar migrasi TIDAK BISA dipakai apa adanya — jangan salin istilah itu tanpa mendesain ulang:** indeks naif `UNIQUE(prf_id) WHERE is_awarded` **akan menolak data yang sah**, karena satu PRF **normal** punya **banyak** baris ter-award — seluruh baris kartu pemenang ber-`vendor_id` sama (`PRFDetailPage.jsx:274-288`) **dan** seluruh baris internal ber-`vendor_id` NULL (`:289-300`). Invarian sebenarnya = "paling banyak SATU `vendor_id` DISTINCT non-null di antara baris ter-award per PRF" = **constraint tingkat HIMPUNAN**, tak bisa diungkapkan unique index biasa. **ARAH YANG DIPILIH: TABEL AWARD TERPISAH** (`prf_id` unik + vendor pemenang) → invarian jadi **unique key biasa, TANPA trigger**; **belum dikerjakan**. ⚠️ **Kenapa severity naik:** guard satu-vendor **hanya hidup di dalam RPC**, sementara **RLS mengizinkan `procurement` menulis `prf_cost_items` LANGSUNG lewat PostgREST** (policy write di atas tidak membatasi akses ke jalur RPC) → **nol penegakan di lapisan DB** bagi penulis mana pun yang tak lewat RPC. Jadi **hari ini `item_group` boleh NULL dan tidak ada CHECK**: daftar tetap **`Origin Charges` / `Freight Charges` / `Destination Charges`** adalah **aturan bisnis yang dijaga FE saja** (`PRFDetailPage.jsx:34`), persis seperti `vendor_type` di `vendors`. **`is_awarded` = penanda pemenang award** — satu PRF hanya boleh punya SATU vendor menang (keputusan bisnis: **tak ada split award**). **Guard di RPC (Tahap B):** `count(DISTINCT it->>'vendor_id')` atas item ber-`is_awarded=true` **DAN `vendor_id` non-null**; > 1 → **RAISE** (bukan skip senyap). ⚠️ Konsekuensi penting: **baris tanpa `vendor_id` tidak pernah ikut dihitung guard** → PRF lama (semua `vendor_id` NULL, `is_awarded` true) **tak pernah memicu RAISE** — inilah yang membuat data lama tetap bisa dibuka & disimpan ulang. **`exchange_rate` per baris = SALINAN** dari tabel kurs dokumen (`prf.exchange_rates`) saat simpan, **bukan** penggerak hitungan di layar PRF (total ditampilkan terpisah per mata uang, tanpa konversi). ~~**Keempat kolom ini BELUM masuk `schema_snapshot.sql`** (definisi tabel di snapshot `:3894-3906` masih 10 kolom) → **sumber kebenaran = file migrasi**, sampai `pg_dump` berikutnya~~ **[KOREKSI 24 Jul 2026 — KLAIM DI ATAS SUDAH TIDAK BENAR, jangan dihidupkan lagi]** `pg_dump` yang dijadwalkan itu **sudah dijalankan**. **Terverifikasi doc-keeper (grep file repo):** definisi `CREATE TABLE public.prf_cost_items` (`schema_snapshot.sql:4326`) **memuat keempat kolom** — `vendor_id` (`:4337`), `item_group` (`:4338`), `is_awarded` (`:4339`), `exchange_rate` (`:4340`) — plus FK `prf_cost_items_vendor_id_fkey → vendors(id)` (`:9636`). **Sumber kebenaran = snapshot**, bukan file migrasi; **nol utang `pg_dump`** untuk kolom-kolom ini. Alur + model UI (kartu per vendor, award tunggal, blokir quotation non-IDR): `05_WORKFLOW_MAP` §Procurement — PRF. **⭐ [27 Jul 2026 — +kolom `offer_id`, model award DIBALIK, DB-only]** +1 kolom **`offer_id`** uuid NULLABLE (FK → `prf_vendor_offers(id)` ON DELETE CASCADE — hapus satu penawaran vendor otomatis menghapus rincian biayanya) — menautkan tiap baris rincian ke SATU penawaran vendor tertentu. Baris **Biaya Internal** (`vendor_id IS NULL`, `cost_type='internal'`) sengaja tetap **`offer_id IS NULL` juga** (diverifikasi migrasi backfill `20260727000002` — `INSERT prf_vendor_offers` hanya untuk baris ber-`vendor_id` non-null) — biaya internal melekat ke PRF ITU SENDIRI, bukan ke penawaran vendor mana pun. ~~**⭐ Konsekuensi bagi `08_TECH_DEBT` TD-122 — BUBAR, bukan DIPERBAIKI:** TD-122 mendiagnosis "aturan satu-vendor-ter-award butuh **tabel award terpisah**" karena unique index biasa tak bisa mengungkapkan constraint tingkat himpunan atas `is_awarded`. Model baru (`prf_vendor_offers` + `prf.selected_offer_id`) **tak butuh solusi itu**... **TD-122 status "RESOLVED (usang)"** — premisnya gugur, bukan solusinya ketemu; tabel award terpisah yang diusulkan TD-122 **tidak perlu dibangun lagi**.~~ **[KOREKSI, Den 27 Jul 2026 (sesi susulan) — kesimpulan di atas SALAH, TD-122 KEMBALI OPEN.]** Faktanya (tak berubah, tetap benar): model baru `prf_vendor_offers` + `prf.selected_offer_id` memang tak butuh constraint tingkat himpunan yang jadi diagnosis awal TD-122 (pemenang kini satu kolom FK skalar, bukan flag pada banyak baris). **Tapi menyimpulkan TD-122 karenanya "resolved"/"bubar" itu keliru.** Kata Den (verbatim): *"Alasannya: lubang double-award masih hidup di produksi hari ini. Kolom prf_cost_items.is_awarded masih ada, RLS-nya masih mengizinkan procurement menulis langsung, dan panel 'Jawaban Harga' yang lama masih memakai model itu. Yang dibangun hari ini baru struktur penggantinya, belum ada yang dicabut. TD-122 baru gugur di FASE 4 (saat is_awarded dan prf_cost_items.prf_id dibuang). Menandai RESOLVED sekarang adalah over-claim — sama bahayanya dengan pola under-claim, hanya terbalik arahnya."* **TD-122 TETAP OPEN, severity MEDIUM.** **TAPI: `vendor_id`/`item_group`/`is_awarded` (kolom model LAMA) TIDAK di-drop** — diverifikasi migrasi `20260727000001_prf_vendor_offers_struktur.sql` tak menyentuhnya sama sekali → **kedua model hidup berdampingan di skema** sampai ada keputusan/migrasi pembersihan lanjutan (belum ada — target Fase 4). Panel "Jawaban Harga" LAMA (`PRFDetailPage.jsx`, masih satu-satunya UI live) **masih membaca/menulis kolom model lama sepenuhnya** — tak ada migrasi UI ke model baru sesi ini. ⚠️ **Perlu konfirmasi Den:** kenapa syarat Fase 4 di atas menyebut `prf_cost_items.prf_id` (bukan hanya `vendor_id`/`item_group`/`is_awarded`) — dicatat apa adanya, tidak ditafsirkan ulang. **Apakah remediasi lama (tabel award terpisah, usul asli TD-122) masih layak dibangun untuk model yang sedang dipensiunkan, atau mubazir mengingat arah migrasi ke Fase 4 — pertanyaan terbuka, belum diputuskan Den** (lihat `08_TECH_DEBT` TD-122 + item 9c §Prioritas Fix untuk detail). **[+27 Jul 2026 — Batch 3B, `PRFVendorOfferModal.jsx`]** baris `prf_cost_items` baru yang ditulis modul "Penawaran Vendor" (via modal tambah/edit) **selalu** mengisi `offer_id` DAN `prf_id` (RLS mensyaratkan `prf_id`, bukan `offer_id`) **dan selalu** `is_awarded=false` eksplisit (bukan default kolom `true`) — disiplin ini murni di level KODE PEMANGGIL, **NOL penegakan DB tambahan**; kalau dilanggar oleh jalur tulis lain, kalkulasi "Buat Quotation" (`costTotalIdr`, entri `prf` di atas) akan membengkak diam-diam. Dicatat **`08_TECH_DEBT` TD-156 (baru, MEDIUM)** — kerabat TD-122 (sama-sama "`is_awarded` tanpa penegakan DB", cakupan beda). Lihat entri `prf_vendor_offers` (di atas) + `05_WORKFLOW_MAP` §Procurement — PRF.
- **`prf_vendor_offers`** [BARU 27 Jul 2026, migrasi `20260727000001_prf_vendor_offers_struktur.sql`, SQL dijalankan manual Den sebelum commit, direkam sbg arsip] — **penawaran vendor untuk PRF, banyak baris per PRF** (ganti model lama "satu tabel datar `prf_cost_items` + flag `is_awarded`" — lihat entri `prf`/`prf_cost_items` + `08_TECH_DEBT` TD-122). Kolom: `id`, `prf_id` NOT NULL (FK → `prf(id)` ON DELETE CASCADE), `company_id` NOT NULL (uuid polos, **tanpa FK ke `companies`** — diverifikasi isi migrasi, tak ada `REFERENCES companies`), `vendor_id` NOT NULL (FK → `vendors(id)`, **tanpa `ON DELETE`** — default RESTRICT), `currency`, `valid_from`, `valid_until`, **`pros` NOT NULL**, **`cons` NOT NULL** (justifikasi WAJIB per penawaran — ditegakkan DI DB, bukan validasi form; lihat konsekuensi backfill di bawah), `notes`, `created_by` (FK → `profiles(id)`), `created_at`, `updated_at` (trigger `trg_prf_vendor_offers_updated_at` reuse `set_updated_at()` — BEDA dari `contacts` yang TANPA trigger updated_at, TD-134), `deleted_at` (soft-delete). 3 index (`prf_id` partial WHERE `deleted_at IS NULL`, `company_id`, `vendor_id`). **RLS 4 policy, SEMUA `TO authenticated`, semua top-level `is_super_admin() OR (…)`:** `prf_vendor_offers_select` — company match AND (`has_role('procurement')` OR `is_manager_or_above()` OR PRF-nya dibuat oleh user ini [`EXISTS prf WHERE p.created_by=auth.uid()`]) → **sales pemilik PRF bisa melihat penawaran vendor untuk PRF-nya sendiri**, bukan cuma procurement/manager. `prf_vendor_offers_insert` — company match AND `has_role('procurement')` AND `created_by=auth.uid()` AND **PRF-nya SEDANG dipegang user ini** (`EXISTS prf WHERE p.acknowledged_by=auth.uid()`) → hanya procurement yang sudah `prf_claim()` PRF itu boleh menambah penawaran, bukan procurement mana pun di company yang sama. `prf_vendor_offers_update` — USING sama seperti insert (procurement + `acknowledged_by=self`); WITH CHECK **kini IDENTIK dengan USING** (keduanya memuat `has_role('procurement')`) — **per pernyataan Den, 27 Jul 2026, diperbaiki di produksi** (sesi susulan, di luar sesi kode manapun). ~~Sebelumnya WITH CHECK hanya company match + `acknowledged_by=self`, tanpa mengulang `has_role('procurement')` seperti USING-nya — celah sempit yang sudah tertutup sekarang.~~ Migrasi arsip `20260727000001_prf_vendor_offers_struktur.sql` dan `schema_snapshot.sql` **sedang disesuaikan Den secara terpisah** supaya cocok dengan RLS yang sudah diperbaiki — doc-keeper sengaja **tidak membaca ulang kedua file itu** sesi ini untuk menghindari menangkap state di-tengah-edit (lihat `PROGRESS.md` untuk catatan lengkap). `prf_vendor_offers_delete` — **super_admin only** (satu-satunya operasi hard-DELETE; nol soft-delete dari UI krn UI belum ada). **Header migrasi mencatat:** policy insert/update sempat direvisi (+guard `acknowledged_by`) dalam sesi yang sama sebelum commit — yang terekam adalah keadaan AKHIR. **Backfill (`20260727000002_prf_vendor_offers_backfill.sql`, TIDAK IDEMPOTEN, sudah dijalankan sekali; backup `backup_prf_20260727` 13 baris + `backup_prf_cost_items_20260727` 5 baris — DUA tabel backup baru, lihat gotcha #10):** untuk tiap kombinasi `(prf_id, vendor_id)` di `prf_cost_items` lama ber-`vendor_id IS NOT NULL`, dibuat SATU baris `prf_vendor_offers` (currency dari cost item atau fallback `prf.rate_currency`; `pros`/`cons` diisi **placeholder sentinel** `'Migrasi data lama — belum ada justifikasi'` — konsekuensi langsung `pros`/`cons` NOT NULL pada tabel baru yang diisi data lama tanpa justifikasi asli), lalu `prf_cost_items.offer_id` ditautkan balik ke offer yang cocok, lalu **`prf.selected_offer_id` diisi dari offer yang vendor-nya sama dgn baris `is_awarded=true`** — backfill ini secara eksplisit MENERJEMAHKAN state model LAMA (`is_awarded`) jadi state model BARU (`selected_offer_id`) untuk PRF historis, murni SQL (TANPA lewat RPC `prf_select_offer`) dan **TANPA menyaring status PRF sama sekali** → inilah sumber kombinasi status `SUBMITTED` + `selected_offer_id` terisi yang tak bisa lahir dari RPC (lihat `09_ROADMAP` Keputusan Terbuka #21 — klaim Den, belum diverifikasi query langsung oleh doc-keeper). Hasil backfill (klaim Den di komentar migrasi, belum diverifikasi query langsung oleh doc-keeper): **4 offer, 4 cost item tertaut, 2 `prf.selected_offer_id` terisi, 1 baris Biaya Internal sengaja `offer_id` NULL.** ~~**UI: NOL** — diverifikasi grep `src/` (`prf_vendor_offers`/`prf_claim`/`prf_release`/`prf_mark_quoted`/`prf_select_offer` semuanya 0 hit) — tabel + RPC ini murni fondasi DB, belum ada form/list/tombol yang memakainya.~~ **[KOREKSI 27 Jul 2026 sore — SEBAGIAN USANG, batch "Batch 3A" (`PRFDetailPage.jsx`, 2 file, `feat/prf-audit`, belum di-commit)]** `prf_claim`/`prf_release` kini punya pemanggil FE — header Detail PRF menampilkan "Sedang dikerjakan oleh {nama} · sejak {tanggal}" (`prf.acknowledged_by`/`_at`, embed `holder:profiles!prf_acknowledged_by_fkey`) + tombol **"Ambil PRF Ini"** (gate: `canEdit && status==='SUBMITTED' && !acknowledged_by`) dan **"Lepas PRF"** (gate: `status==='ACKNOWLEDGED' && (pemegang sendiri || manager-atau-di-atasnya)`, const `MANAGER_OR_ABOVE` 7 role disalin BENAR dari `is_manager_or_above()` DB — kontras dgn mirror SALAH di `ActivitiesPage.jsx` yg ketinggalan `gm_bd`, lihat `08_TECH_DEBT` **TD-155** baru). `prf_vendor_offers` (SELECT) juga kini punya pembaca FE — kartu **"Penawaran Vendor" READ-ONLY** (setelah "Detail Layanan", sebelum "Dangerous Goods"): tiap baris tampil vendor/currency/masa berlaku/total biaya (dihitung dari `prf_cost_items` yang dikelompokkan per `offer_id`)/Kelebihan(`pros`)/Kekurangan(`cons`) + badge "DIPILIH SALES" bila `prf.selected_offer_id` cocok. ~~**CAKUPAN KOREKSI SEMPIT — masih genuinely NOL UI:** `prf_mark_quoted` dan `prf_select_offer` (diverifikasi grep ulang `src/` 27 Jul sore: keduanya masih 0 hit)~~ **[KOREKSI 27 Jul 2026, sesi terpisah SETELAH Batch 3B — Batch 3C]** keduanya **SUDAH ADA**: tombol "Nyatakan Penawaran Siap" (`prf_mark_quoted`, `PRFDetailPage.jsx`, procurement) dan tombol "Pakai/Ganti Penawaran Ini" (`prf_select_offer`, `DealDetailPage.jsx`, sales/manager). · ~~form TAMBAH/EDIT/HAPUS `prf_vendor_offers` (kartu barunya murni baca, nol tombol tulis — form = batch 3B, belum dikerjakan)~~ **[KOREKSI 27 Jul 2026, sesi terpisah SETELAH Batch 3A — lihat bullet "Batch 3B" persis di bawah]** form tambah/edit/hapus **SUDAH ADA** · ~~panel pilih penawaran untuk sales (3C)~~ **[KOREKSI Batch 3C]** panel ini **SUDAH ADA** di `DealDetailPage.jsx` (`PrfListCard`/`OfferMiniCard`, `DealPanels.jsx`) — **yang MASIH nol:** notifikasi. **Dasar hitung modal "Buat Quotation" juga sudah dipindah ke `selected_offer_id`** (`PRFDetailPage.jsx`) → `08_TECH_DEBT` TD-156 RESOLVED (kode). Detail: `PROGRESS.md` 2026-07-27 (entri Batch 3C). `logAudit` dgn `ACTION_TYPES.CLAIM_PRF`/`RELEASE_PRF` baru (`src/lib/auditLogger.js`). **NOL perubahan DB/RLS/migrasi dari Batch 3A ini sendiri** — TAPI 2 commit DB-only LAIN (`604430f`+`e1c7ff1`) terjadi hari yang sama SEBELUM Batch 3A, lihat banner atas + entri `prf`/`prf_cost_items`. Detail: `PROGRESS.md` 2026-07-27 (entri Batch 3A) + `05_WORKFLOW_MAP` §Procurement — PRF (subbagian "Penawaran Vendor").

  **⭐ Batch 3B [27 Jul 2026, sesi terpisah SETELAH Batch 3A, FE-only 3 file (1 baru `PRFVendorOfferModal.jsx` 299 baris) `feat/prf-audit`, belum di-commit, NOL perubahan DB/RLS/migrasi dari sesi ini] — SISI TULIS: `prf_vendor_offers`/`prf_cost_items` kini punya penulis FE.** Tombol Tambah/Edit/Hapus di kartu "Penawaran Vendor" → modal baru, gate **`canManageOffers = canEdit && prf.acknowledged_by === profile?.id`** (⚠️ TANPA fallback manager-ke-atas — beda dari `canRelease` — cermin PERSIS `prf_vendor_offers_insert/update` yang `acknowledged_by=auth.uid()` TANPA `IS NULL OR`, `:13079-13101`). Simpan = INSERT/UPDATE `prf_vendor_offers` lalu REPLACE `prf_cost_items` (delete `.eq('offer_id',...)` + insert ulang) — **DUA FASE NON-ATOMIK** (nol RPC baru; task eksplisit melarang perubahan DB sesi ini). **Tiap baris `prf_cost_items` baru WAJIB isi `offer_id` DAN `prf_id`** (ketiga policy tulisnya mencari induk lewat `prf_id`, bukan `offer_id`, `:13009-13031`) **dan `is_awarded` di-set EKSPLISIT `false`** (bukan default kolom `true`, `:4841` — lihat entri `prf_cost_items` di bawah untuk alasan lengkap + `08_TECH_DEBT` **TD-156 baru**). Hapus **DIBLOKIR** saat `prf.selected_offer_id` menunjuk offer itu (hard-DELETE `prf_cost_items` + soft-DELETE `prf_vendor_offers` via UPDATE `deleted_at` — `.delete()` fisik atas `prf_vendor_offers` sendiri TETAP super_admin only, `prf_vendor_offers_delete`). `ENTITY_TYPES` audit TETAP `PRF` (bukan entity offer terpisah). ~~**Cakupan tetap sempit:** `prf_mark_quoted`/`prf_select_offer`/panel pilih untuk sales (3C) MASIH genuinely nol UI.~~ **[KOREKSI 27 Jul 2026, sesi terpisah SETELAH ini — Batch 3C]** ketiganya **SUDAH ADA** (lihat banner "Diperbarui" paling atas dokumen ini). Detail: `PROGRESS.md` 2026-07-27 (entri Batch 3B + Batch 3C) + `05_WORKFLOW_MAP` §Procurement — PRF (subbagian "Penawaran Vendor") + `08_TECH_DEBT` TD-156 (RESOLVED kode).

  Alur & keputusan desain lengkap: `05_WORKFLOW_MAP` §Procurement — PRF (subbagian "Penawaran Vendor").
- **`sales_orders`** [BARU 18 Jul 2026] — **dokumen perintah kerja Sales → Procurement** (SO). Dibuat manual oleh sales dari inquiry, lalu dikirim ke procurement (read-only). **Prinsip: SO MENUNJUK history, tidak menyalin** — nol kolom quotation/prf; history dirender lewat `inquiry_id` saat buka Detail. Kolom inti: `id`, `company_id` (FK `companies`, NOT NULL), `so_no` (text), `status` varchar DEFAULT `DRAFT` CHECK ∈ `DRAFT`/`SENT`, **`inquiry_id`** (FK `→ inquiries(id)`, NOT NULL — jangkar history), **`account_id`** (FK `→ accounts(id)`, NOT NULL — **diturunkan otomatis** dari inquiry: `customer_id ?? prospect_id`), `signed` bool DEFAULT false, `sign_link` text (link tanda tangan customer, bukan upload), `signed_at`, `created_by`/`updated_by` (FK `profiles`), `created_at`, `updated_at`, soft-delete `deleted_at`. **Constraint anti-dobel:** `UNIQUE(company_id, so_no)` + **unique index parsial `sales_orders_inquiry_unique_live` ON `(inquiry_id)` WHERE `deleted_at IS NULL`** → satu inquiry hanya SATU SO live. Trigger `set_sales_orders_updated_at` (reuse `set_updated_at()`). Nomor `so_no` dirakit di FE: `SO/{ENTITAS}/{TAHUN}/{URUT-3digit}` via `increment_document_sequence` (`p_document_type='SO'`, `p_department_code='CRM'`, `p_month=0`) — pola INQ/QUO. **4 RLS policy (single-entity + bypass super_admin):** `sales_orders_insert` (WITH CHECK: super OR (company + `created_by=auth.uid()` + `has_role('sales') OR has_role('gm_bd')`)), `sales_orders_select` (USING: super OR (company AND (creator OR `has_role('procurement')` OR `is_manager_or_above()`))), `sales_orders_update` (creator + `deleted_at IS NULL`), `sales_orders_delete` (creator OR super). GRANT SELECT/INSERT/UPDATE/DELETE authenticated. **Relasi:** `SO → inquiry → quotation/prf` (menunjuk via `inquiry_id`, tak menyalin). **⚠️ Keterbatasan visibilitas quotation utk procurement:** panel History Quotation di SO Detail menarik `quotations` WHERE `inquiry_id`, tapi RLS `quotations_read` = `manager+ OR created_by OR super` — **`procurement` TIDAK termasuk** → panel bisa kosong karena RLS memblokir (bukan karena tak ada quotation); UI pakai pesan netral untuk role non-definitif. Silang: `08_TECH_DEBT` **TD-90** (keputusan forum apakah RLS quotations dilonggarkan untuk procurement pada konteks SO). Rekaman DB: `supabase/migrations/20260718000000_sales_orders.sql` (banner "BUKAN untuk dijalankan lagi"; sudah masuk snapshot `:4184`). UI = 3 file FE (`src/modules/sales-order/`) + `App.jsx` — NOL perubahan DB dari sisi UI. Alur: `05_WORKFLOW_MAP` §Sales → SO → Procurement.

### Asset Management
- `assets` (+ kolom SQL-Editor: `condition`, `department_id` FK, `brand`, `assignment_status`), `asset_categories`, `asset_locations` (`branch_id` NOT NULL), `asset_specifications`, `asset_network`, `asset_software_licenses`, `asset_maintenance_records`, `asset_fuel_logs`. **Belum ada:** `asset_documents`, `asset_work_orders`, `asset_routes` (UI placeholder).

### Inventory & Logistics (Storbit SP + fulfillment)

**Skema SP baru (FASE 0-3, LIVE) — identitas komposit `(customer_id, sp_no)`:**
- **`sp_orders.price_category`** [BARU 18 Agu 2026, LIVE — `schema_snapshot.sql:6931`] — **tipe SP level header** (`semester`/`tahunan`/`project`, CHECK `sp_orders_price_category_check`). **NULL LOLOS constraint** (`NULL = ANY(...)` menghasilkan NULL, bukan false) dan artinya **"belum/tidak dikategorikan"** — COMMENT kolomnya sengaja TIDAK mengklaim label UI apa pun. Ditulis FE lewat **UPDATE terpisah setelah `create_sp_order_dual`** (`db.js` `setSpOrderPriceCategory`, dipanggil `InputSPPage.jsx` dropdown "Tipe SP") — sengaja BUKAN parameter RPC, supaya signature jalur tulis SP paling ramai tak diubah; konsekuensinya non-atomik, tapi failure mode benign (SP tetap lahir, `price_category` NULL — sama spt user yang memang mengosongkan field). **Prasyarat mutlak: `GRANT UPDATE(price_category)` per-kolom** (`:18462`) — `sp_orders` tak punya table-level GRANT UPDATE sejak fix TD-175, jadi tanpa GRANT ini wiring UI gagal senyap/403 (pola TD-166). **Seluruh SP existing NULL, sengaja tak di-backfill** (tipe SP historis tak diketahui sistem). ⚠️ **TIGA nama mirip, JANGAN tertukar:** `sp_orders.price_category` (tipe SP header) · `sp_order_items.price_category` (kategori harga per baris, boleh beda dari header) · **`sp_orders.sp_category`** (kategori PRODUK: reguler/loyang/trolly — makna beda total).
- **`sp_orders`** [BARU] — **header SP** (satu baris per SP). Kolom kunci: `company_id`, `customer_id`, `sp_no`, `sp_date`, `dc_id` (FK `dc_master`, NOT NULL), **`status`** (12-tahap, CHECK — lihat Mesin Status di bawah), `is_disputed`/`dispute_reason`/`disputed_at`/`disputed_by` (overlay dispute), `expired_date`, `sp_category`, `external_url`, `notes`, `confirmed_at/by`, `cancelled_at/by`, `cancel_reason`, **`had_cancelled_picking`** (flag permanen bila picking pernah dibatalkan), `created_by`, soft-delete `deleted_at`. **UNIQUE(customer_id, sp_no)** (nomor SP milik customer; boleh sama antar-customer). RLS company- + role-scoped (`operations`/manager+/`is_super_admin`).
- **`sp_order_items`** [BARU] — **item kanonik** per SP. `sp_order_id` (FK), `company_id`, `product_id`, `product_name`, `sku`, `qty`, `shipped_qty` (CHECK `shipped_qty <= qty`), `unit_price` (snapshot), `price_category` (CHECK null/semester/tahunan/project), `shipping_price` [koreksi kecil 8 Agu 2026 — kolom ini sudah ada sejak `create_sp_order_dual` FASE 0 (dikirim tiap item saat CREATE), sebelumnya tak tercatat lengkap di baris ini], **`legacy_sp_item_id`** (map 1:1 ke `sp_items.id`, dipakai audit/bridge — JANGAN drop). **[KOREKSI 8 Agu 2026, DIVERIFIKASI ULANG SAMA HARI setelah snapshot di-refresh — tiga klaim lama di bawah ini SUDAH TIDAK BENAR, dan mekanismenya kini TERKONFIRMASI LIVE (bukan cuma dirancang):]** ~~`shipped_qty` kanonik belum di-sync dari dispatch~~ — `dispatch_delivery`/`cancel_delivery` (`schema_snapshot.sql` baris 475/173) **TERKONFIRMASI LIVE**: dibaca langsung, blok `-- ===== BARU: jembatan/reversal shipped_qty ke sp_order_items` ADA, menyinkronkan via `legacy_sp_item_id` (dispatch: `LEAST(soi.shipped_qty+qty, soi.qty)`; cancel: `GREATEST(soi.shipped_qty-qty, 0)`). ~~tabel ini tak pernah dibaca siapa pun~~ — kini dibaca `getInvoicePdfData` (`db.js`, via join `sp_invoice_lines→sp_order_items`) dan guard RPC `create_invoice` (`Σshipped=Σqty FROM sp_order_items`, **dikonfirmasi live** — `schema_snapshot.sql` baris 338-342). ~~basi setelah SP diedit, `updateSpItem` cuma menyentuh `sp_items`~~ — `updateSpItem` (`db.js`) memanggil RPC **`update_sp_item_dual`** (`schema_snapshot.sql` baris 2056, **TERKONFIRMASI LIVE**) lebih dulu — dual-write ke `sp_items` (penuh) **dan** `sp_order_items` (`qty`/`sla_days`/`estimated_delivery_date`/`shipping_price`/`notes`, KELIMANYA — dibaca langsung dari body fungsi, bukan dugaan). Backfill satu-kali (115 baris `shipped_qty`, 1 dikecualikan: SP 2181355 CANCELLED) juga tetap live, terpisah dari mekanisme sinambung ini. **Sekarang genuinely sinkron ke depan** (dispatch/cancel/edit baru pun ikut ter-update) — bukan cuma snapshot satu waktu. **Status "sumber kebenaran" — tetap TAK berubah oleh temuan live ini:** `sp_items` TETAP primary reader utk list/detail fulfillment (kodenya sendiri tak disentuh sesi ini, sync hanya menjaga kedua tabel selaras, tak memindahkan reader); tabel ini kini official reader utk domain Invoice. Divergensi TETAP mungkin utk kolom di luar cakupan sync (delete SATU item via `removeRow` — **TD-49**, tetap OPEN, tak tersentuh fix ini). **Nuansa verifikasi `update_sp_item_dual` (BUKAN lagi "PERLU KONFIRMASI" penuh, tapi juga bukan "fully verified" — lihat §5 RPC utk kalimat presisi):** pemetaan kelima kolom **terverifikasi BENAR** via pembacaan langsung fungsi live; eksekusi dgn data uji nyata baru terbukti utk `notes` (skrip ZZZTEST sesi ini) — `sla_days`/`estimated_delivery_date` belum dieksekusi dgn data uji, meski petanya sendiri sudah dikonfirmasi benar. **Jangan migrasikan reader LAIN (list/detail) ke `sp_order_items`** — sync menutup divergensi utk kolom yang dipakai Invoice, bukan konsolidasi penuh; lihat **TD-40** (RESOLVED-kode/LIVE-SQL, diperbarui)/**TD-49** (delete SATU item — tetap tak tersentuh) + `08_TECH_DEBT.md`.
- **`sp_btb`** [BARU] — **BTB (Bukti Terima Barang) entitas benar**, qty-only tanpa nilai pajak. `company_id`, `sp_order_id` (FK NOT NULL), `delivery_note_id` (FK nullable — BTB per batch bila dipetakan), `customer_id`, `btb_no`, `btb_date`, `qty` (nullable, CHECK `>= 0`), `received_at/by`, `remarks`, soft-delete `deleted_at`. **Partial UNIQUE `(customer_id, btb_no) WHERE deleted_at IS NULL`** (FASE 3 — re-issue nomor pasca soft-delete tak bentrok). RLS company/role-scoped.
- **`sp_invoices`** / **`sp_invoice_lines`** / **`sp_payments`** [BARU, FASE 4, 8 Agu 2026 — **LIVE, dikonfirmasi via `schema_snapshot.sql` refresh commit `8117127`** (23:33 WIB — koreksi atas draft awal hari yang sama yang sempat menulis "SQL belum dijalankan" saat snapshot masih basi)] — entitas invoice per SP (skema penuh: `DESIGN_SP_SCHEMA.md` §2.5; migrasi step **M2** = pembuatan tabel saja, tanpa FK/unique strict). **`sp_invoices`** (`CREATE TABLE` baris 5815, 16 kolom): `company_id`, `sp_order_id` (FK, **UNIQUE** — 1 SP → 1 invoice), `invoice_no` (digenerate sistem via `increment_document_sequence`; ~~format `INV/{kode_entitas}/FIN/{tahun}/{4-digit}`~~ **[KOREKSI 17 Agu 2026, doc-keeper — dokumen bertentangan dgn kode]** format LIVE hari ini = **`{kode_entitas}-INV-{bulan romawi}-{tahun}-{4-digit}`** (mis. `SOA-INV-VIII-2026-0001`), dibaca langsung dari body `create_invoice` di `schema_snapshot.sql`. Perubahan format berasal dari kerja 15-16 Agu 2026 yang **belum terdokumentasi di Governance manapun** — lihat "TAK TERDOKUMENTASI" pada laporan sesi 17 Agu; format lama di atas dipertahankan dicoret sbg jejak), `faktur_no`, `invoice_date`, `status` (CHECK `draft/issued/submitted/partial/paid/void` — **beda namespace** dari `sp_orders.status` 12-tahap, jangan tertukar; FE hari ini cuma menangani `issued`/`submitted`), `submitted_at`, `submit_ref`, `total_dpp`/`total_ppn`/`total_amount`, soft-delete `deleted_at`. **`sp_invoice_lines`** (`CREATE TABLE` baris 5799): per baris `sp_order_item_id` (FK) + `btb_id` (FK nullable) + `dpp`/`ppn`/`qty`/`position`. **`sp_payments`**: **[USANG per 17 Agu 2026 — dulu "kosong-fungsi, murni skema"]** kini backing FASE 5. Kolom: `invoice_id` (FK), `payment_date`, `amount`, `pph`, `reference`, **+2 kolom baru 17 Agu 2026** `bukti_potong_url` (COMMENT: "Tautan scan bukti potong (Drive/Storage). **Interim: URL manual**" — belum ada upload terintegrasi) & `bukti_potong_no` (nomor bukti potong PPh 23 dari customer), `created_by`. Tulis HANYA via RPC `record_payment` (policy `sp_payments_insert` **DIHAPUS TOTAL** 17 Agu 2026 → INSERT lewat PostgREST tak mungkin lagi; GRANT tabel `SELECT` + `UPDATE` kolom-level 3 kolom saja: `reference`/`bukti_potong_url`/`bukti_potong_no` — **TD-176 PARTIAL**, lihat §RLS/GRANT di akhir entri ini). RPC **`create_invoice(p_sp_order_id)`** (baris 314, **LIVE, dibaca penuh doc-keeper**) — guard `v_ordered=0 OR v_shipped<>v_ordered` dari `sp_order_items` (WAJIB Σshipped=Σqty), `v_entity_code` **dikonfirmasi diresolusi dinamis** (`SELECT code INTO v_entity_code FROM companies WHERE id=v_company_id` — **BUKAN hardcode "SBI"**, sempat diduga hardcode pertengahan sesi, diinvestigasi & dikoreksi Den; kini diverifikasi doc-keeper langsung ke source), otorisasi persis `is_super_admin() OR is_manager_or_above() OR has_role('finance_controller')`. RPC **`submit_invoice(p_invoice_id)`** (baris 1934, **LIVE**) — guard `status='issued'`, isi `submitted_at`+`status='submitted'` otomatis server-side. **`sp_recompute_status`** (baris 1856, **CREATE OR REPLACE LIVE**) — cabang `WHEN v_submitted THEN 'SUBMITTED'`/`WHEN v_has_invoice THEN 'INVOICED'` dikonfirmasi ADA di prioritas teratas (di atas BTB_TERBIT); detail bug guard yang DITEMUKAN+DIPERBAIKI sesi ini di entri RPC §5. FE: `getSpInvoice`/`createInvoiceRpc`/`submitInvoiceRpc`/`getInvoicePdfData` (`db.js`) + kartu "Invoice" di `SalesOrderDetailPage.jsx`. Cetak: `InvoicePDF.jsx` (`@react-pdf/renderer`, identitas visual ungu/krem/serif **sengaja beda** dari Picking List/Delivery Note navy/orange — keputusan desain Den) — field dari `sp_invoices`/`sp_invoice_lines`/`sp_order_items`/`accounts`/`dc_master`/`companies`/`entity_bank_accounts`/`entity_finance_settings`; NPWP (`companies.tax_id`) kosong utk entitas SOA (data belum diisi, bukan bug); QR code placeholder sengaja kosong (tujuan link belum diputuskan). RLS/GRANT: `sp_orders`/`sp_invoices` **LIVE** (`08_TECH_DEBT.md` **TD-175 RESOLVED**); `sp_order_items`/`sp_items`/`sp_invoice_lines` **TETAP OPEN** (**TD-176**, masih `GRANT ALL`) — **`sp_payments` KELUAR dari daftar itu per 17 Agu 2026** (GRANT ALL → SELECT + UPDATE 3 kolom, policy INSERT dihapus; TD-176 jadi **PARTIAL**). ~~**⚠️ NOL tes runtime/UI** — SQL live tidak sama dengan sudah dicoba di browser; belum ada laporan klik tombol "Terbitkan Invoice" sungguhan.~~ **[USANG per 17 Agu 2026]** Den melaporkan tes end-to-end SP dummy satu siklus penuh (Draft → … → Invoice → Payment parsial → Payment lunas → LUNAS → TTF) **LOLOS**, termasuk jalur "Terbitkan Invoice" ini — **klaim di-relay, BUKAN diverifikasi independen doc-keeper** (nol akses browser/DB sesi dokumentasi). ⚠️ **JANGAN keliru dgn baris "Finance" §Status Modul `09_ROADMAP.md`** ("Billing/Invoice, AR Collection, AP…") — itu modul AR/AP umum LINTAS-domain yang genuinely belum dibangun; `sp_invoices` di sini scoped SEMPIT ke fulfillment Storbit SP (1 invoice per SP), bukan penggantinya.

- **`journal_entries`** / **`journal_entry_lines`** [BARU, FASE 5, 17 Agu 2026 — **LIVE, dibaca langsung dari `schema_snapshot.sql` pasca-refresh commit `35cb1d3`**] — **jurnal AR minimal**, auto-post tanpa approval. **`journal_entries`** (`CREATE TABLE` baris 5416): `company_id` (FK `companies`, ON DELETE RESTRICT), `entry_date`, **`reference_type`** (CHECK mengunci ke **persis 2 nilai**: `invoice_issued` / `payment_received` — `journal_entries_reference_type_check`), `reference_id` (id invoice atau id payment, tanpa FK), `description`, `created_by`. **TANPA kolom `updated_at` — DISENGAJA:** COMMENT tabelnya sendiri menyatakan *"Koreksi = jurnal pembalik, bukan UPDATE"*. Index: `idx_je_company_date`, `idx_je_reference`. **`journal_entry_lines`** (baris 5440): `journal_entry_id` (FK CASCADE), `account_id` (FK `chart_of_accounts`, ON DELETE RESTRICT), `debit`, `credit` — CHECK **`journal_entry_lines_sign_check`** menolak baris yang debit DAN credit terisi bersamaan (`debit=0 OR credit=0`, keduanya `>=0`). Index: `idx_jel_entry`, `idx_jel_account`. **RLS READ-ONLY:** hanya `journal_entries_read`/`journal_entry_lines_read` (`FOR SELECT TO authenticated`), **NOL policy INSERT/UPDATE/DELETE**; GRANT tabel ke `authenticated` cuma `SELECT` → jurnal **mustahil ditulis lewat PostgREST**, satu-satunya penulis = RPC `create_invoice`/`record_payment` (SECURITY DEFINER). Scoping company pakai **`get_user_company_ids()` (JAMAK)** — `journal_entry_lines_read` menuruni parent lewat `EXISTS`. **Bagan akun yang DIPAKAI RPC** (kode di `chart_of_accounts`, di-lookup per `company_id`; tiap lookup `RAISE EXCEPTION` eksplisit kalau akunnya belum ada): `1-1101` Bank · `1-1200` Piutang Usaha · `1-1300` PPh 23 Dibayar Dimuka · `2-1200` PPN Keluaran · `4-1000` Pendapatan · `4-1100` Pendapatan Ongkos Kirim. ✅ **Seed COA entitas SOA = 7 akun, TERVERIFIKASI 18 Agu 2026 lewat query read-only langsung ke `chart_of_accounts`** (bukan lewat `schema_snapshot.sql`, yang schema-only/0 statement `COPY` sehingga isi barisnya memang di luar jangkauan): `1-1100` Kas · `1-1101` Bank · `1-1200` Piutang Usaha · `1-1300` PPh 23 Dibayar Dimuka · `2-1200` PPN Keluaran · `4-1000` Pendapatan Penjualan (Barang) · `4-1100` Pendapatan Jasa Kirim. **Akun ke-7 = `1-1100` Kas: placeholder transaksi tunai, SENGAJA belum dipakai RPC mana pun** — `record_payment` hanya men-debit `1-1101` Bank, kas fisik belum jadi jalur pembayaran yang didukung (keputusan Den, dijawab di sesi yang sama; bukan lagi pertanyaan terbuka). Nama `4-1000`/`4-1100` di DB lebih spesifik dari penyebutan singkatnya di atas. Pemetaan debit/kredit per pemicu: lihat §5 (`create_invoice`/`record_payment`).

**Rantai fulfillment (picking → surat jalan):**
- **`picking_lists`** / **`picking_list_items`** / **`picking_list_materials`** — manifest picking gudang (status `pending/in_progress/done/cancelled`; bawa `sp_no`+`customer_id`+`sp_order_id`). Reservasi stok via `stock_ledger`. **[21 Agu 2026 — partial picking; kode FE selesai, guard DB BELUM dijalankan]** `picking_list_items` sudah lama punya `qty_picked` (`schema_snapshot.sql:5960`, `integer DEFAULT 0 NOT NULL`), `qty_short`, dan CHECK `picking_list_items_status_check` yang mengizinkan **`pending`/`picked`/`short`** (`:5965`) — tapi nilai **`'short'` sejak lahir NOL PENULIS**. Penulis pertamanya adalah `derivePickingItemStatus()` di `src/lib/db.js` (dipakai `setPickingItemPicked`): status **DITURUNKAN dari angkanya**, tak pernah dikirim terpisah (`0`→`pending`, `0<n<requested`→`short`, `n=requested`→`picked`), supaya qty↔status mustahil melenceng; qty di-clamp `0..qty_requested` + `Math.floor`. ⚠️ **DUA "kurang" yang beda arti, jangan tertukar:** `qty_short` = **stok gudang tak cukup saat picking DIBUAT** (dihitung server di `generate_picking_from_sp`: `GREATEST(req − LEAST(req, avail), 0)`) · `status='short'` = **hasil pengambilan NYATA** oleh petugas gudang. Sisi hilir **tidak diubah** dan memang sudah partial-capable sejak awal: `complete_picking` sejak lahir **nol pemeriksaan qty** (hanya cek status, `:304`), dan `generate_delivery_from_picking` sejak lahir memakai `pli.qty_picked` sbg qty surat jalan + hanya butuh ≥1 baris ber-`qty_picked>0` (`:670`, `:671`, `:688-690`).
- **`delivery_notes`** / **`delivery_note_items`** — Surat Jalan (`do_no`, `sp_no`, `picking_list_id`, `customer_id`, `sp_order_id`, status `draft/in_transit/delivered/cancelled`).

**LEGACY (koeksis, belum di-drop):**
- **`sp_items`** — item SP generasi lama; **MASIH DIBACA** reader fulfillment (list/detail, `shipped_qty`) — TAK disentuh sesi 8 Agu 2026. Create SP = **dual-write** (`create_sp_order_dual` tulis `sp_items` LAMA **dan** `sp_orders`/`sp_order_items` baru). **[8 Agu 2026] EDIT item SP kini JUGA dual-write — LIVE**, dikonfirmasi ulang via `schema_snapshot.sql` refresh (commit `8117127`) — `updateSpItem` (`db.js`) memanggil RPC `update_sp_item_dual` (`schema_snapshot.sql` baris 2056, dibaca penuh) yang menulis `sp_items` (penuh) + `sp_order_items` (`qty`/`sla_days`/`estimated_delivery_date`/`shipping_price`/`notes` — KELIMANYA, cakupan sudah dikonfirmasi persis) sekaligus; sebelumnya edit HANYA menyentuh tabel ini — lihat entri `sp_order_items` di atas + gotcha #9 (§6) + `08_TECH_DEBT.md` **TD-40** (diperbarui, mekanisme kini LIVE). FK `sp_items_customer_id_fkey` → `accounts`. RLS ⚠️ `USING(true)`. **+2 kolom BARU** [ALTER dijalankan manual di produksi 2026-07-16; ✅ **sudah direkam migrasi** → `supabase/migrations/20260716000000_sp_items_price_category_review_status.sql`; ✅ **sudah masuk `schema_snapshot.sql`** (`:4295-4296`, dump 17 Jul); sisa TD-72 = domain nilai `price_category`] untuk rencana "Kunci Harga Edit Item SP" (§Task H7 / `10_TASK_BREAKDOWN.md`):
  - **`price_category`** — kategori harga SP. ⚠️ **Domain nilai belum baku** — dipakai `default`/`semester`/`legacy` (mengikuti kategori Master Product + penanda SP lama), **BEDA** dari CHECK `sp_order_items.price_category` (`semester/tahunan/project` saja). Perbedaan domain ini **perlu dikonfirmasi user** (belum ada CHECK constraint di `sp_items.price_category`). ⚠️ **Backfill belum jalan** — kolom masih kosong untuk SP lama.
  - **`review_status`** — penanda **SEMENTARA** rekonsiliasi SP lama (**bisa DI-DROP setelah rekonsiliasi selesai**): `review_default` = `unit_price` cocok `products.default_price` · `perlu_review` = beda → cek kontrak/PKS manual. Sudah ter-label penuh: **587 `perlu_review` + 140 `review_default` = 727 baris (0 NULL)**. **`unit_price` TIDAK diubah** (harga SP = snapshot).
- **`sp_btbs`** — BTB legacy (hanya `sp_no`+`btb_no`, buta identitas); **0 penulis FE** sejak FASE 3 (helper `db.js` tinggal definisi). Data delta sudah dimigrasi ke `sp_btb`; **tinggal di-drop**. RLS ⚠️ `USING(true)`.
- **`ar_ttfs`** (FK `ar_ttfs_customer_id_fkey` → `accounts`), **`ar_btbs`** — AR Tracker (TTF + baris BTB finance) — **domain terpisah** dari `sp_btb` (BTB Storbit). **[8 Agu 2026, FASE 4 — LIVE]** `ar_ttfs` +2 kolom FK baru **`sp_order_id`** (→ `sp_orders`) dan **`invoice_id`** (→ `sp_invoices`), per rencana `DESIGN_SP_SCHEMA.md` §2.5 (TTF dipertahankan sbg dokumen transmittal ber-FK, `ar_btbs` yang dipensiunkan nilainya). **Dikonfirmasi live via `schema_snapshot.sql` refresh (commit `8117127`)** — kolom + FK constraint `ar_ttfs_sp_order_id_fkey`/`ar_ttfs_invoice_id_fkey` keduanya ADA (`schema_snapshot.sql:9655-9667`). **⚠️ TANPA backfill data historis** — itu migrasi step **M7** (`DESIGN_SP_SCHEMA.md` §5); step M2 (kolom+FK) sudah live, tapi **status M7 TIDAK diverifikasi ulang sesi ini** (snapshot schema-only, tak bisa membuktikan isi baris) — per catatan sesi asli, M7 belum dikerjakan. Sampai M7 dikerjakan, kedua kolom kemungkinan besar masih NULL untuk seluruh baris lama. **[17 Agu 2026, FASE 5 — LIVE]** `ar_ttfs` **+1 kolom `diterima_oleh`** (`text`, COMMENT: *"Nama orang di pihak customer yang menerima faktur. Teks bebas — orang di luar sistem Nexus, sengaja BUKAN FK ke `profiles`"*) + **2 index baru** menyusul kolom FK FASE 4 (`idx_ar_ttfs_invoice_id`, `idx_ar_ttfs_sp_order_id`). TTF dari sisi SP kini diisi RPC **`mark_ttf_received`** (§5) yang **UPSERT** — baris `ar_ttfs` pertama (by `created_at`) utk invoice itu di-UPDATE, jadi tombol "Edit" di Detail SP memanggil RPC yang SAMA tanpa melahirkan TTF kedua. ⚠️ **Gate anti double-entry `ar_btbs` (17 Agu 2026, 2 lapis, level APLIKASI — sengaja BUKAN trigger DB):** nilai uang sudah pindah ke sisi invoice (`DESIGN_SP_SCHEMA.md` §2.5), tapi AR Tracker lama masih membiarkan user mengetik DPP+PPN/PPh/Payment langsung ke `ar_btbs` untuk TTF yang **sudah punya `invoice_id`** → dua sumber angka untuk uang yang sama. Lapis 1: `ttfFromDb` (`db.js:162-163`) kini membawa `invoiceId`/`spOrderId` (aditif; `ttfToDb` **tak pernah** menulis balik kedua kolom itu), dan `ARModal` (`App.jsx`, `const btbLocked = !!initial?.invoiceId`, `:5207`) merender SELURUH baris BTB read-only + menyembunyikan hapus-baris/Add BTB + melewati validasi "minimal 1 BTB" (kalau tidak, field header ikut terkunci tak bisa disimpan). Lapis 2: `updateTtf` (`db.js`) menumpangkan `.select('invoice_id').single()` pada UPDATE header dan memakai hasilnya sbg guard — kalau terisi, **SELURUH blok DELETE + re-INSERT `ar_btbs` di-skip** (guard dibaca dari DB, bukan state klien); `insertTtf` sengaja tanpa guard (TTF baru selalu lahir belum tertaut). **Blast radius: NOL data existing** — kedua tabel dilaporkan kosong (`ar_ttfs` 0 baris, `ar_btbs` 0 baris) via COUNT read-only oleh Den; **doc-keeper tak bisa memverifikasi isi baris** (snapshot schema-only), jadi angka ini klaim di-relay. Gate ini murni preventif.
- **`stock_ledger`** (append-only: `reserved`/`unreserved`/`outbound`/`inbound`/`adjustment`), view **`stock_summary`** (on_hand/reserved/available per product × warehouse × company), **`products`**, **`warehouses`**, `product_warehouse_location`.

**Mesin Status SP (12 tahap — LENGKAP s/d LUNAS sejak 17 Agu 2026):**

Kolom `sp_orders.status`, **fact-derived** (di-maintain otomatis oleh event, bukan diketik) via fungsi **`sp_recompute_status(customer_id, sp_no)`** (§5). Progresi:

`DRAFT → CONFIRMED → MENUNGGU_STOK → PICKING → PACKED → DIKIRIM → SAMPAI → TERKIRIM_PENUH → BTB_TERBIT → INVOICED → SUBMITTED → LUNAS` (+ terminal **`CANCELLED`**).

- **Live sekarang:** DRAFT s/d **SUBMITTED**. **[8 Agu 2026] INVOICED/SUBMITTED (FASE 4): tabel+RPC (`create_invoice`/`submit_invoice`)+UI (kartu Invoice `SalesOrderDetailPage.jsx`) — TERKONFIRMASI LIVE** setelah `schema_snapshot.sql` di-refresh (commit `8117127`, 23:33 WIB) — doc-keeper membaca langsung body `create_invoice`/`submit_invoice`/`sp_recompute_status` di snapshot pasca-refresh, ketiganya cocok persis dgn desain yang dijelaskan sesi ini. **[Catatan proses, bukan koreksi fakta lain]** draft awal entri ini (ditulis sebelum `8117127`) sempat bilang "SQL belum dijalankan" — itu bacaan jujur thd bukti saat itu (snapshot memang masih basi), sudah dikoreksi. ~~**LUNAS (FASE 5) TETAP belum dibangun sama sekali**~~ **[USANG 17 Agu 2026 — FASE 5 LIVE]** cabang **`WHEN v_paid THEN 'LUNAS'`** kini ada di **PUNCAK CASE** `sp_recompute_status` (di atas SUBMITTED), dengan `v_paid := EXISTS(SELECT 1 FROM sp_invoices WHERE sp_order_id=v_id AND status='paid')` — dibaca langsung dari body fungsi live. Pemicunya RPC baru **`record_payment`** (§5) yang menaikkan `sp_invoices.status` ke `paid` lalu `PERFORM sp_recompute_status`. **Guard freeze tetap `IF v_status IN ('CANCELLED','LUNAS') THEN RETURN`** — begitu LUNAS, status SP tak pernah mundur lagi (disengaja). ~~**⚠️ SQL live ≠ sudah dites** — NOL laporan tes runtime/klik UI sungguhan utk fitur Invoice~~ **[USANG]** Den melaporkan tes end-to-end SP dummy satu siklus penuh lolos (termasuk Payment parsial → lunas → SP naik LUNAS → TTF, jurnal balance/saldo Piutang net 0, cleanup total & stok kembali persis ke angka semula) — **klaim di-relay, bukan verifikasi independen doc-keeper**. Detail entitas invoice/jurnal: entri `sp_invoices`/`sp_invoice_lines`/`sp_payments` + `journal_entries` di atas.
- **KEPUTUSAN BISNIS:** **BTB_TERBIT = rank TERTINGGI di band terkelola** (di ATAS TERKIRIM_PENUH) — *"puncak sebelum invoice"*, karena invoice ditagih atas BTB bertandatangan customer (Indomarco). Di recompute, cabang `v_has_btb` dicek **paling atas** (mengalahkan TERKIRIM_PENUH). ⚠️ *Array literal di CHECK constraint masih mengurut `…SAMPAI, BTB_TERBIT, TERKIRIM_PENUH…` — inkonsistensi kosmetik; yang menentukan status tampil = urutan CASE recompute, BUKAN urutan array. (`DESIGN_SP_SCHEMA.md` juga masih memuat urutan lama — perlu update terpisah.)*
- **CASE recompute (prioritas tertinggi → terendah), per 17 Agu 2026:** `v_paid` → **LUNAS** · `v_submitted` → SUBMITTED · `v_has_invoice` → INVOICED · `v_has_btb` → **BTB_TERBIT** · `Σshipped ≥ Σqty` (item confirmed) → TERKIRIM_PENUH · DN delivered → SAMPAI · DN in_transit/delivered → DIKIRIM · picking done → PACKED · picking active → PICKING · confirmed + stok kurang → MENUNGGU_STOK · confirmed → CONFIRMED · else DRAFT.
- **Guard (recompute early-return, tak menyentuh) — LIVE HARI INI:** `status IN ('CANCELLED','LUNAS')`. **BTB_TERBIT TIDAK di-freeze** (bisa naik/turun mengikuti fakta BTB). **[8 Agu 2026] Bug lama (ditemukan Den, bukan doc-keeper) SUDAH DIPERBAIKI DAN TERKONFIRMASI LIVE:** guard lama = `status IN ('CANCELLED','INVOICED','SUBMITTED','LUNAS')` — karena `INVOICED`/`SUBMITTED` ikut ada di daftar freeze YANG SAMA, status yang naik ke `INVOICED` akan **macet permanen** (tak bisa naik ke `SUBMITTED`). Fix (`CREATE OR REPLACE`) dipersempit ke `CANCELLED,LUNAS` saja — **dikonfirmasi doc-keeper langsung membaca body fungsi di `schema_snapshot.sql` baris 1870 pasca-refresh** (commit `8117127`): baris persis `IF v_status IN ('CANCELLED','LUNAS') THEN RETURN; END IF;` — guard baru ini SEKARANG yang live, bukan lagi sekadar rancangan.
- **Recompute dipanggil (`PERFORM`)** dari RPC confirm/picking/delivery/BTB (lihat §5). `stock_summary.available` company-wide dipakai untuk MENUNGGU_STOK. **[8 Agu 2026, belum live]** RPC `create_invoice`/`submit_invoice` DIRANCANG ikut memanggil recompute setelah menulis `sp_invoices`.
- **UI:** headline badge Detail SP + pill/tabs/filter list baca `sp_orders.status` (pasca-2E). Tombol Konfirmasi/Tolak hanya `DRAFT`; Generate Picking hanya `CONFIRMED`/`MENUNGGU_STOK`. **[8 Agu 2026]** Kartu "Invoice" baru di `SalesOrderDetailPage.jsx` — tombol "Terbitkan Invoice" (gate FE cermin guard RPC: `Σshipped=Σqty`), "Submit" (muncul saat `sp_invoices.status='issued'`), "Download" (PDF via `getInvoicePdfData`+`InvoicePDF.jsx`). **[17 Agu 2026, FASE 5]** Tiga blok baru **INLINE di dalam card Invoice yang sama** (dipisah `borderTop`, sengaja BUKAN card mandiri): **Terima Pembayaran** (nominal, tanggal bayar, referensi/no. transfer, PPh 23, link + no. bukti potong), **Riwayat Pembayaran**, **Tanda Terima Faktur** (tombol **Edit** prefill dari data existing, memanggil RPC upsert yang sama). Gate FE **sengaja dibaca dari `erpRoles` (seluruh role aktif), BUKAN prop `role`** — `pickPrimaryErpRole` cuma mengembalikan satu role prioritas tertinggi dan `finance_controller` berada DI BAWAH `manager` di daftar itu, jadi user manager+finance_controller akan ter-resolve jadi `manager` dan kehilangan form padahal RPC-nya (`has_role`) meloloskan (`SalesOrderDetailPage.jsx:880-894`). `canRecordPayment = finance_controller|super_admin` · `canMarkTtf = manager-ke-atas|finance_controller|super_admin` — cermin otorisasi kedua RPC (§5). Form pembayaran tampil saat `sp_invoices.status ∈ {issued,submitted,partial}`; blok TTF `∈ {issued,submitted,partial,paid}`. Turunan FE: `sisaTagihan = total_amount − Σ(amount+pph)` (**TIDAK di-clamp ke nol** — lebih bayar sengaja tampil negatif, sistem belum punya konsep overpay) + **saran PPh 23 = total ongkir × 2%** (`totalOngkirInv = total_amount − dpp − ppn`, persis definisi `v_total_amount` di `create_invoice`). Helper `db.js` baru: `recordPayment`/`markTtfReceived`/`getPaymentHistory`/`getTtfStatus`.

### Notifications
- **`notifications`** — notif in-app (bell). `event_type`, `title`, `body`, `reference_type`/`reference_id`, `is_read`, `user_id`, `company_id`. Ditulis langsung oleh kode (mis. EF aging-pipeline `event_type='crm.lead_idle'`, Lead Pool pull `event_type='lead_pool_pull_result'`).
- **`notification_rules`** — ⚠️ **YATIM (tak dibaca kode).** 11 rule semua `is_active=true` + `company_id=MSI`, seed 2026-06-14; **tak ada consumer** (8 notif nyata ditulis tanpa lewat rules). Bangun mesin pembaca atau buang (`08_TECH_DEBT.md` **TD-53**).

### Reporting / Governance (MOM)
- **`meeting_moms`** — header Minutes of Meeting (`mom_no`, `mom_type` weekly/project/probation/board/departmental/adhoc, `status` draft/submitted/approved/rejected). Approval CEO/admin.
- Child: **`mom_action_plans`** (action item: owner/due/priority), **`mom_issues`** (isu), **`mom_progress_updates`** (progress action plan), **`mom_improvements`** (usulan perbaikan). Semua RLS company-scoped.

### BNF (Bad News First), Briefing Harian & Meeting Mingguan

> Modul eskalasi "kabar buruk duluan" (migrasi dari app Google Apps Script lama, fondasi 3-5 Agu 2026, data lama TAK dipindah) + dua modul turunan baru **Briefing Harian** dan **Meeting Mingguan** (dibangun 10 & 12 Agu 2026). **Net-new section** — governance docs 03/04/05 sebelumnya nol dokumentasi untuk modul ini sama sekali, termasuk fondasinya (lihat `CLAUDE.md` entri 2026-08-04/05).

**Fondasi (3-5 Agu 2026):**
- **`bnf_divisions`** / **`bnf_departments`** — struktur organisasi BNF sendiri (BUKAN tabel `departments`/org chart HR biasa). Tiap tabel: `company_id` (home), `code`/`name`, `is_active`, soft-delete `deleted_at`. **`bnf_divisions.director_profile_id`** / **`bnf_departments.head_profile_id`** (keduanya nullable, FK longgar ke `profiles`) — penanda siapa direktur/kepala; dipakai `is_bnf_authorized()` (lihat Layer 0 di bawah). RLS insert/update: `is_super_admin() OR (is_admin_or_above() AND company_id=get_user_company_id())` — **beda gate** dari akses laporan (`is_bnf_authorized()`): ini mengatur siapa boleh SETUP struktur organisasinya, bukan siapa boleh AKSES laporan BNF.
- **`bnf_reports`** — laporan BNF. `report_no`, `division_id`/`department_id` (NOT NULL keduanya), `description`/`root_cause`/`solution`, `target_date`, `escalation_level` (CHECK `manager_direct`/`direktur_divisi`/`ceo`, nullable), `status` (CHECK `Open`/`In Progress`/`Escalated`/`Closed`, default `Open`), `closed_at`, `created_by`/`updated_by`, soft-delete `deleted_at`. Guard trigger **`guard_bnf_reports_field_update`** (`trg_guard_bnf_reports_update`, BEFORE UPDATE) — 4 tingkat: (0) `auth.uid() IS NULL` bypass total (migrasi/SQL Editor/service-role); (1) `id`/`company_id`/`report_no`/`created_by`/`created_at` terkunci mutlak, tak ada pengecualian; (2) `created_by=auth.uid()` ATAU `is_admin_or_above()` boleh edit field isi laporan; (3) siapa pun lain di company yang sama (row-scope RLS `bnf_reports_update`) hanya boleh ubah `status`/`updated_by`/`closed_at`. Trigger ini pre-existing sejak Fase A, **tak disentuh sesi 10/12 Agu** kecuali fix soft-delete di bawah.
- **`bnf_report_logs`** — audit histori perubahan status (`from_status`/`to_status`/`note`/`changed_by`/`changed_at`), INSERT-only, dibaca read-only di detail laporan.
- **`bnf_report_related_departments`** — junction many-to-many "departemen terdampak lain" (chip multi-select "Irisan", Fase G 5 Agu 2026) — menggantikan kolom tunggal `related_department_id` yang di-drop di migrasi yang sama.

**Layer 0 — fondasi baru (10 Agu 2026, migrasi `20260810000001`-`20260810000004`, seluruhnya dikonfirmasi live di `schema_snapshot.sql`):**
- **`bnf_authorized_users`** — grant/revoke akses BNF di luar jalur struktural head/direktur/CEO (mis. staff sedang diassessment kenaikan jabatan). Kolom: `profile_id` (FK `profiles`), `company_id` (FK `companies`), `reason` (nullable), `granted_by` (FK `auth.users`), `granted_at`, `revoked_at` (nullable — **soft-revoke lewat kolom ini, tak pernah DELETE**). RLS `_insert`/`_update`/**`_read` ketiganya `is_super_admin()`-only** — read dibatasi seketat write, **konfirmasi eksplisit ini konstanta yang disengaja** (bukan oversight yang perlu ditambal), lihat Layer 4 di bawah untuk konsekuensi UI-nya.
- **`is_bnf_authorized()`** — `LANGUAGE sql STABLE SECURITY DEFINER`, gate akses gabungan. `SELECT is_super_admin() OR EXISTS(head_profile_id=auth.uid() di bnf_departments, deleted_at IS NULL) OR EXISTS(director_profile_id=auth.uid() di bnf_divisions, deleted_at IS NULL) OR EXISTS(user_roles aktif dgn roles.code IN (…)) OR EXISTS(bnf_authorized_users non-revoked utk company aktif)`. Daftar role di klausa ketiga **diperluas 12 Agu 2026** (migrasi `20260811000001`) dari `('ceo')` saja menjadi `('ceo','gm','gm_bd','manager','finance_controller')` — **keputusan desain eksplisit**: seluruh staff akan dilatih BNF via SOP, gate per-assignment struktural murni dianggap terlalu sempit untuk rollout itu; role FUNGSIONAL `hrga`/`it`/`operations`/`procurement`/`sales`/`finance` **sengaja dikecualikan** (tag departemen/fungsi, bukan level jabatan). SECURITY DEFINER supaya user biasa bisa cek status dirinya sendiri walau RLS `bnf_authorized_users_read` dibatasi super_admin. Dipakai: RLS `bnf_reports_select`/`_insert` (di bawah), trigger guard `daily_report_items` (di bawah), RLS `bnf_report_action_items_*`/`weekly_meetings_all`/`weekly_meeting_items_all`, dan gate menu FE (`AuthContext.jsx` `isBnfAuthorized`/`bnfAuthLoading`, via RPC `is_bnf_authorized`, di-thread eksplisit lewat parameter — bukan hook — ke seluruh chain `canSeeMenuItem`/`navChildGate`/`navModuleVisible`/`isMenuAccessible`/`canRenderPage` di `App.jsx`, pola gating yang sudah mapan di codebase ini).
- **`bnf_reports` RLS diperketat** — `bnf_reports_select`/`bnf_reports_insert` kini mensyaratkan `is_bnf_authorized()` (dikonfirmasi persis dari `schema_snapshot.sql`: `bnf_reports_select FOR SELECT USING (is_super_admin() OR (company_id=get_user_company_id() AND deleted_at IS NULL AND is_bnf_authorized()))`). **Staff biasa TAK LAGI bisa baca/tulis `bnf_reports` langsung** kecuali lolos gate — perubahan perilaku nyata dari sebelumnya (header komentar lama `BNFListPage.jsx` masih menulis "Visible to every logged-in user regardless of role", kini **stale** pasca perubahan ini — bukan disengaja, lihat catatan di laporan doc-keeper).
- **`daily_report_items`** — backing **Briefing Harian**. Kolom: `company_id`, `department_id` (NOT NULL, FK `bnf_departments` — dept BNF, bukan dept HR), `entry_date` (default `CURRENT_DATE`), `created_by`, `category` (CHECK `task`/`aktivitas`/`insiden`), `description`, `task_status` (CHECK `pending`/`selesai`, nullable), `carried_from_id` (self-FK, rantai carry-forward task pending), `insiden_resolution` (CHECK `pending_review`/`resolved_internal`/`pulled_to_bnf`, nullable), `pulled_to_bnf_report_id` (FK `bnf_reports`, nullable), `resolved_by`/`resolved_at`, timestamps. **Dua trigger, dua event berbeda (tak perlu prefix `trg_z_` — tak ada konflik urutan sesama event):** (1) **`trg_guard_daily_report_items_field_update`** (BEFORE UPDATE, fungsi `guard_daily_report_items_field_update`) — menolak perubahan `insiden_resolution`/`resolved_by`/`pulled_to_bnf_report_id` kecuali `is_bnf_authorized()`; kolom LAIN (mis. `description`) tidak dijaga trigger ini. (2) **`trg_set_daily_report_items_defaults`** (BEFORE INSERT, fungsi `set_daily_report_items_defaults`) — isi `task_status='pending'` (kategori `task`) / `insiden_resolution='pending_review'` (kategori `insiden`) kondisional per kategori; **ditambahkan migrasi SUSULAN `20260810000004`** di hari yang sama karena migrasi awal (`…000003`) menyebut default ini di komentarnya tapi tak pernah benar-benar mengimplementasikannya sbg kolom DEFAULT/trigger — ditemukan saat baca schema langsung untuk Layer 1. **RLS lebih longgar dari `bnf_reports`:** `daily_report_items_read` = company match SAJA (BUKAN `is_bnf_authorized()` — siapa pun di company yang sama bisa baca SEMUA entry, bukan cuma miliknya sendiri); `_insert` = baris sendiri; `_update` = company match saja (penjagaan sesungguhnya ada di trigger kolom-spesifik di atas, bukan RLS row-level).
- **`bnf_report_action_items`** — checklist tindak lanjut per laporan BNF. `report_id` (FK `bnf_reports`), `description`, `assigned_to` (**NOT NULL** — PIC wajib, FK `profiles`), `is_done`, `created_by`, `completed_at`/`completed_by`. RLS mengikuti home+scope divisi/departemen laporan induk (reuse `bnf_department_scopes`/`bnf_division_scopes` via `EXISTS` ganda) **DAN** `is_bnf_authorized()`.
- **`weekly_meetings`** + **`weekly_meeting_items`** — backing **Meeting Mingguan**. `weekly_meetings`: `company_id`, `department_id` (FK `bnf_departments`), `week_start_date`, `meeting_date` (nullable — field "Tanggal" asli dari template kertas lama "Ka Ayun", BEDA dari `week_start_date` yang murni key dedup internal, tak pernah ditampilkan sbg "tanggal meeting"), `peserta`, `keterangan`. `weekly_meeting_items`: `weekly_meeting_id`, `source_type` (CHECK `task_pending`/`insiden_resolved`/`bnf_report`), `daily_report_item_id`/`bnf_report_id` (nullable, salah satu terisi sesuai `source_type`), `catatan_meeting`, `action_plan`. RLS `is_bnf_authorized()` + company match (`FOR ALL`, satu policy per tabel). **TIDAK ADA unique constraint DB pada `(department_id, week_start_date)`** — dedup per-minggu murni application-level (SELECT-first sebelum INSERT), race window kecil diterima sbg trade-off sadar (dikonfirmasi Den).
- **`bnf_division_scopes`** / **`bnf_department_scopes`** — model **home+scope**: home tetap `company_id` yang sudah ada di `bnf_divisions`/`bnf_departments`, scope = company TAMBAHAN di luar home yang laporannya juga masuk divisi/departemen itu (contoh dari Den: posisi BD GM home MSI, scope JCI+SOA). Kolom: `division_id`/`department_id`, `company_id` (scope, BUKAN home), `created_by`. Trigger `trg_guard_bnf_division_scope_not_home`/`trg_guard_bnf_department_scope_not_home` (BEFORE INSERT/UPDATE) — RAISE bila `company_id` yang didaftarkan sbg scope ternyata = home company divisi/dept itu sendiri (redundan, ditolak). RLS: baca = company match ATAU `is_super_admin()`; tulis = `is_super_admin()`-only. Dipakai `bnf_departments_read`/`bnf_divisions_read` (union home ∪ scope) dan resolusi scope departemen di `src/lib/bnfOrgScope.js` (lihat Layer 2/3 di bawah). **Menggantikan pendekatan sebelumnya** — tombol "Salin ke entity lain" di `BNFOrgRolesPage.jsx` yang sempat dibangun lalu **dicabut/dibatalkan** setelah model home+scope ini dirancang (comment sumber migrasi menyebutnya eksplisit). **⚠️ Doc-keeper TIDAK menemukan jejak commit untuk tombol "Salin ke entity lain" ini di `git log`** (termasuk pencarian `-S` pada string tsb di riwayat `BNFOrgRolesPage.jsx`) — kemungkinan besar dibangun & dibongkar dalam sesi kerja yang sama sebelum sempat di-commit; dicatat sbg klaim Den (relay), **belum terverifikasi independen**.

**Layer 1 — Briefing Harian** (`src/modules/briefing-harian/BriefingHarianPage.jsx`, menu baru `briefing-harian`, `public:true` — genuinely terbuka utk SEMUA user login, bukan cuma `is_bnf_authorized()`): 2 tab. **"Isi Hari Ini"** — form entry 3 kategori (task/aktivitas/insiden) + daftar entry hari ini sendiri, dikelompokkan per kategori. **Auto carry-forward**: task `pending` dari hari sebelumnya disalin otomatis jadi baris baru bertanggal hari ini (`carried_from_id` menunjuk baris asal) sekali per mount, idempoten by construction (baris berhenti jadi "ujung rantai" begitu penerusnya ada). Insiden yang sudah ditarik ke BNF menampilkan badge status read-only via `get_linked_bnf_status()`. **"Overview"** — bercabang dari `is_bnf_authorized()`: non-authorized lihat entry hari ini + umur task pending miliknya sendiri (jalan mundur rantai `carried_from_id`); authorized (head/direktur/ceo/grantee) lihat rekap per-departemen se-company (hitung task pending per orang, hitung insiden `pending_review`).

**Layer 2 — BNF diperluas** (`src/modules/bnf/BNFListPage.jsx`): gate menu berubah dari `public:true` tanpa syarat (komentar kode lama eksplisit menyebut ini "confirmed product decision") jadi bersyarat `is_bnf_authorized()` — **pembalikan keputusan yang disengaja** (dikonfirmasi Den). Tab baru **"Tarik dari Insiden"** — daftar `daily_report_items` kategori `insiden` + `insiden_resolution='pending_review'`, di-scope ke jangkauan departemen viewer (`resolveMyDeptScope()`, lihat di bawah). Dua aksi per baris: **"Selesai Internal"** (set `resolved_internal`, tanpa sentuh `bnf_reports`) atau **"Tarik jadi Laporan BNF"** (modal → INSERT `bnf_reports` baru + set `pulled_to_bnf`/`pulled_to_bnf_report_id` di baris asal). Section **Action Items** baru di slide-over detail laporan — checklist `bnf_report_action_items`, PIC wajib saat tambah, badge non-blocking **"Siap Closed"** (`readyToClose = actionItems.length>=1 && every is_done` — indikator murni, TAK PERNAH menggerbang kontrol ubah-status yang sesungguhnya).
- **`src/lib/bnfOrgScope.js`** (`resolveMyDeptScope({isAllEntities, profileId, companyId})`, plain async function bukan hook) — resolusi jangkauan departemen: `null` = tanpa batas/company-wide (super_admin, ATAU siapa pun yang bukan head/director — jatuh ke sini termasuk ceo/grantee `bnf_authorized_users`-only, yg tak punya departemen natural), atau array id departemen (dept milik sendiri + dept di bawah divisi yang dia arahi + scope lintas-company via `bnf_department_scopes`/`bnf_division_scopes`). Diekstrak dari `BNFListPage.jsx` (12 Agu 2026) supaya Meeting Mingguan bisa pakai fungsi identik, bukan duplikat.

**Layer 3 — Meeting Mingguan** (`src/modules/meeting-mingguan/MeetingMingguanPage.jsx`, menu top-level baru, gate `is_bnf_authorized()` sama seperti BNF, RPC tak di-fetch ulang). Picker departemen reuse `resolveMyDeptScope()`, auto-pilih bila hanya 1 dept dalam jangkauan — **beda dari pemakaian di BNFListPage**: di sana scope membatasi daftar KANDIDAT insiden saja (form tarik-ke-BNF tetap menawarkan seluruh departemen company), di sini picker departemen HALAMAN ITU SENDIRI yang dibatasi scope (dikonfirmasi sbg perbedaan yang disengaja). 2 tab. **"Susun dari Minggu Ini"** — 3 daftar kandidat sumber minggu berjalan: `task_pending` (ujung rantai carry-forward, algoritma umur sama dgn Overview Briefing Harian, di-scope departemen bukan per-orang), `insiden_resolved` (`insiden_resolution='resolved_internal'`), `bnf_report` (`status IN ('Open','Escalated')`). Tiap kandidat tampilkan badge **"Sudah pernah dibahas"** bila pernah masuk `weekly_meeting_items` KAPAN PUN (lintas minggu). Checkbox multi-select → find-or-create baris `weekly_meetings` minggu ini (`week_start_date` = Senin minggu berjalan, dihitung LOKAL via `mondayOfWeekStr()` — bukan `toISOString()`, menghindari pergeseran tanggal UTC) → INSERT `weekly_meeting_items`. Catatan per-item: `catatan_meeting` selalu ada; PLUS `action_plan` teks bebas (sumber `task_pending`/`insiden_resolved`) ATAU widget checklist Action Items BNF (sumber `bnf_report` — nulis LANGSUNG ke `bnf_report_action_items`, data yang SAMA dgn yang ditampilkan detail laporan BNF, bukan salinan terpisah). Simpan = tombol "Simpan" eksplisit per item, bukan autosave. **`MeetingInfoForm`** (`meeting_date`+`peserta`+`keterangan`) selalu bisa diedit oleh head mana pun yang bisa akses meeting itu, kapan saja — tanpa mode read-only. **"Riwayat Meeting"** — daftar+detail `weekly_meetings` lampau utk departemen terpilih, terbaru dulu, tampilkan `week_start_date` DAN `meeting_date`; catatan level-item read-only di riwayat (disengaja — hanya `meeting_date`/`peserta`/`keterangan` yang tetap bisa diedit "kapan saja").
- **`src/components/BnfActionItemsChecklist.jsx`** — widget checklist Action Items (list/toggle/tambah) diekstrak dari `DetailPanel` `BNFListPage.jsx` (12 Agu 2026) supaya dipakai ulang persis oleh Meeting Mingguan, bukan diduplikasi. Token/helper lokal sendiri (bukan impor dari `BNFListPage.jsx`) — konsisten pola "sibling pages duplikat potongan kecil, bukan cross-import" yang sudah mapan di codebase ini (preseden `BNFOrgRolesPage.jsx`).

**Layer 4 — "Akses BNF Tambahan"** (`src/modules/bnf/BNFOrgRolesPage.jsx`, section/card KETIGA — setelah "Direktur Divisi" dan "Kepala Departemen" — di halaman admin "BNF — Kepala & Direktur", dicapai via Foundation › Master Data & Admin Settings › grup "Sistem"; halaman itu sendiri bisa dibuka `role[super_admin,admin]`, TAPI section ini di dalamnya HANYA super_admin). Daftar seluruh baris `bnf_authorized_users` utk company yang relevan. **super_admin-only:** tombol "+ Beri Akses" (form inline — person picker, dropdown company, textarea alasan opsional) → INSERT grant baru (`granted_by`=user yang memberi); tombol "Cabut" per baris aktif → `revoked_at=now()` (**tak ada tombol hapus permanen di mana pun** — baris revoked tetap terlihat, dicoret, badge "Dicabut", karena ini jejak audit soft-revoke). **Keputusan desain dikonfirmasi Den:** section ini disembunyikan TOTAL utk admin biasa (bukan ditampilkan read-only) — karena RLS `bnf_authorized_users_read` juga `is_super_admin()`-only (Layer 0), rendering read-only utk admin biasa akan SELALU menampilkan list kosong terlepas data sebenarnya, lebih menyesatkan drpd tak menampilkan section sama sekali. Fetch section ini tak punya cabang query isAllEntities-vs-own-company — akan jadi dead code hari ini (cuma super_admin yang pernah sampai ke sini, dan mereka selalu lihat semua company) — dicatat eksplisit sbg hal yang perlu ditinjau ulang JIKA RLS `bnf_authorized_users_read` kelak dilonggarkan utk admin biasa.

**Bug fixes sesi 10/12 Agu 2026:**
1. **RLS `bnf_reports_update`** (10 Agu, migrasi `20260810000001`) — sebelumnya tak ikut cek `deleted_at IS NULL` (beda dari `bnf_reports_select`) → laporan yang sudah soft-delete masih bisa kena UPDATE. Fix: policy `USING`/`WITH CHECK` ditambah `deleted_at IS NULL` (kecuali `is_super_admin()`).
2. **Badge "Jadi BNF" tak tampilkan nomor** (12 Agu) — akar BUKAN join/embed rusak: RLS `bnf_reports_select` (Layer 0) memblokir embed `bnf_reports` dari `daily_report_items` utk reporter yang bukan `is_bnf_authorized()` (kasus umum — siapa pun bisa lapor insiden, hanya sebagian role yang lolos gate) → PostgREST resolve embed itu jadi `null` secara senyap (bukan error), dan fallback `|| 'BNF'` di kode lama menutupi gejalanya. Fix: pakai `get_linked_bnf_status()` (Layer 0) — badge kini "Jadi BNF · Status" (Open/In Progress/Escalated/Closed), BUKAN nomor laporan — **ini keputusan desain, bukan keterbatasan sementara** (lihat gotcha #15).
3. **Default filter tab Overview BNF** (12 Agu) — dilaporkan sbg "laporan Closed hilang total". Diinvestigasi 2 pertanyaan terpisah: (a) strip KPI count SUDAH benar (dihitung dari fetch penuh tak terfilter, bukan dari list terfilter); (b) query fetch sendiri TANPA exclude Closed — satu-satunya yang menyembunyikan laporan Closed adalah dropdown filter status list/tabel, defaultnya `'active'` ("Aktif, bukan Closed"). **Bukan bug logika — gap discoverability** (menutup laporan diam-diam menerapkan ulang default filter itu, tanpa toast/petunjuk). Fix sesuai keputusan Den: default filter diganti `'active'`→`'all'` — laporan Closed langsung terlihat begitu tab dibuka; opsi "Aktif (bukan Closed)" tetap tersedia di dropdown.

---

## 4. RLS Patterns (fungsi)

| Fungsi | Role yang di-cover | Catatan |
|--------|--------------------|---------|
| `is_super_admin()` | `super_admin` (via `user_roles`) | Top-level bypass. JANGAN nest di dalam filter company. |
| `get_user_company_id()` | — | `SELECT company_id FROM profiles WHERE id=auth.uid()`. Null sebelum backfill / di SQL Editor. |
| **`get_user_company_ids()`** [BARU, 9 Agu 2026] | — (jamak — SEMUA company tempat user punya `user_roles` AKTIF, bukan cuma home) | `RETURNS SETOF uuid`, `LANGUAGE sql STABLE SECURITY DEFINER SET search_path TO 'public'` — `SELECT DISTINCT company_id FROM user_roles WHERE user_id=auth.uid() AND is_active=true`. SENGAJA fungsi TERPISAH dari `get_user_company_id()` (singular) — yang TETAP dipakai apa adanya di puluhan RLS policy existing yang memang harus tetap home-company-only (mis. `user_login_logs`), BUKAN pengganti. Dipakai sbg bypass tambahan `OR company_id IN (SELECT get_user_company_ids())` di `companies_read_own`/`roles_read` — **BELUM** di `payment_terms_read` dan kemungkinan tabel lain, lihat `08_TECH_DEBT.md` **TD-180**. **[17 Agu 2026]** Policy BARU **langsung pakai varian jamak ini sejak lahir** (bukan ditambal belakangan): `journal_entries_read` (`company_id IN (SELECT get_user_company_ids())`) + `journal_entry_lines_read` (lewat `EXISTS` ke parent) + policy `sp_payments` — pola yang benar untuk policy baru, jadikan acuan. |
| `is_admin_or_above()` | `super_admin`, `admin` **saja** | ⚠️ TIDAK termasuk manager/ceo — dipakai ~51 policy; sumber banyak bug akses (lihat tech debt). |
| `is_manager_or_above()` | super_admin, admin, ceo, gm, gm_bd, manager, supervisor | Dipakai RLS accounts/activities. (Verified `schema_snapshot.sql` fungsi `is_manager_or_above`, `code IN (…)` — TIDAK ada `sales_head`.) |
| `has_permission(module, action)` | query `user_roles→roles→role_permissions→permissions` | ⚠️ **[7 Agu 2026]** Diverifikasi 0 pemakaian — bukan cuma "flagged broken" (lihat §6 gotcha #5), tapi genuinely orphan dari RLS (0 hit `CREATE POLICY`) DAN dari FE (0 hit `rpc('has_permission'`). |
| `has_role(role_code)` | cek role di `user_roles` | — |
| `get_user_role_code()` | role code user | — |
| **`is_admin_tier_role(p_role_id uuid)`** [BARU, 7 Agu 2026] | — (bukan predikat role pemanggil; cek role code TARGET) | `LANGUAGE sql STABLE SECURITY DEFINER SET search_path TO 'public'`. `SELECT EXISTS(SELECT 1 FROM roles WHERE id=p_role_id AND code IN ('super_admin','admin'))`. Dipakai `user_roles_insert`/`user_roles_update` utk menolak admin biasa menulis baris ber-`role_id` admin-tier (privilege escalation fix, TD-170). SECURITY DEFINER supaya independen dari visibilitas RLS `roles` milik pemanggil. |

**Pola policy accounts (acuan):** `SELECT/UPDATE USING ((company_id = get_user_company_id() AND (is_manager_or_above() OR assigned_to=auth.uid() OR created_by=auth.uid())) OR is_super_admin())`; `INSERT WITH CHECK (company_id = get_user_company_id())`.

**`profiles_read` = `USING(true)`** (stopgap CEO unblock — semua authenticated baca profiles; lihat tech debt).

---

## 5. RPC Functions

| RPC | Signature | Tujuan |
|-----|-----------|--------|
| `save_quotation` | `(p_quotation_id uuid, p_header jsonb, p_items jsonb)` | Simpan quotation atomik: UPDATE header (COALESCE per field) + DELETE+INSERT items dalam 1 txn; RAISE bila RLS tolak / 0-row. Terima `p_header.vat_rate`. **[17 Jul 2026] +`p_header.exchange_rates`** — `CREATE OR REPLACE` (dollar-quote `$fn$`), body identik + 1 baris: `exchange_rates = CASE WHEN p_header ? 'exchange_rates' THEN p_header->'exchange_rates' ELSE exchange_rates END` (pola `p_header ? 'x'` → key absen = **tak menimpa** dgn `{}`). **Sudah LIVE** (dijalankan manual, verifikasi user `rpc_ok: true`), ✅ **direkam migrasi** `supabase/migrations/20260717000001_save_quotation_exchange_rates.sql` (prasyarat: `20260717000000_…` lebih dulu) **dan body di snapshot sudah versi BARU** (`schema_snapshot.sql:1013`). |
| **SP — mesin status & fulfillment (FASE 1-3)** | | *(semua SECURITY DEFINER; recompute REVOKE dari PUBLIC — internal via PERFORM)* |
| `sp_recompute_status` | `(p_customer_id uuid, p_sp_no text)` | Recompute `sp_orders.status` fact-derived (BTB_TERBIT rank tertinggi). **[8 Agu 2026, `CREATE OR REPLACE` — TERKONFIRMASI LIVE** via `schema_snapshot.sql` refresh commit `8117127`, baris 1856-1903, dibaca penuh doc-keeper**]** +cabang CASE `WHEN v_submitted THEN 'SUBMITTED'` / `WHEN v_has_invoice THEN 'INVOICED'`, keduanya di ATAS BTB_TERBIT (prioritas tertinggi→terendah: LUNAS>SUBMITTED>INVOICED>BTB_TERBIT>…, dikonfirmasi persis dari urutan CASE) + **fix bug guard, TERKONFIRMASI LIVE**: guard sekarang **`IF v_status IN ('CANCELLED','LUNAS') THEN RETURN; END IF;`** (baris 1870) — guard LAMA `CANCELLED/INVOICED/SUBMITTED/LUNAS` (yang menahan status macet permanen di INVOICED, bug ditemukan Den) **sudah tidak berlaku**. **Hardcode UUID company — SUDAH ADA sebelum sesi ini, TAK diperbaiki, MASIH ADA pasca-refresh:** `v_company uuid := 'd2e5e565-5f67-4954-b8d9-5979a2a0c697'` (UUID entitas **SOA**, §2 di atas, baris 1861) dipakai di kalkulasi `v_short` (cek `stock_summary.available` utk menentukan MENUNGGU_STOK) — **BUKAN diturunkan dari `sp_orders.company_id` baris yang sedang direcompute**. Nol dampak praktis hari ini (modul SP eksklusif SOA), tapi cacat laten bila SP dipakai entitas lain kelak. **Instance KEDUA dari pola hardcode yang sama ditemukan di `dispatch_delivery` (baris 479)** — lihat baris RPC itu + `08_TECH_DEBT.md` **TD-178** (diperluas cakupannya). **[17 Agu 2026, `CREATE OR REPLACE` lagi — TERKONFIRMASI LIVE, baris 2398-2451 pasca-refresh]** +deklarasi `v_paid` + probe `EXISTS(SELECT 1 FROM sp_invoices WHERE sp_order_id=v_id AND status='paid')` + cabang **`WHEN v_paid THEN 'LUNAS'` di PUNCAK CASE**. Guard freeze **tidak berubah** (`CANCELLED,LUNAS`) — LUNAS satu arah. ⚠️ **Hardcode UUID SOA `v_company` MASIH ADA** (baris 2403) — TD-178 tetap OPEN, sengaja tak disentuh sesi FASE 5. **[17 Agu 2026, `CREATE OR REPLACE` KETIGA — ✅ TERKONFIRMASI LIVE; diverifikasi 18 Agu 2026 via `pg_get_functiondef` langsung ke DB dengan substring match presisi (`position('submitted_at IS NOT NULL AND status <> ''void''' in …)` ketemu, teks lama `status='submitted'` = 0 hit, `pg_proc` cuma punya SATU fungsi bernama ini sehingga nol ambiguitas signature). ⚠️ **`schema_snapshot.sql` BELUM di-refresh untuk perubahan ini** — snapshot masih memperlihatkan versi kedua, jadi JANGAN pakai snapshot sbg rujukan status fix ini (kasus gotcha #10 lagi, arah sebaliknya: kali ini DB lebih maju dari snapshot). Sempat ada dua kesimpulan berlawanan soal status ini — draft dokumentasi sempat menulis "belum dijalankan" karena membawa hasil pembacaan dari sesi sebelum SQL dijalankan lalu menstempelnya dengan tanggal baru; diselesaikan lewat pengecekan ulang substring utuh]** Fix bug **SP mundur `SUBMITTED`→`INVOICED` setelah pembayaran parsial** (`08_TECH_DEBT.md` **TD-199**), migrasi `20260817000002_fix_status_machine_submitted_flag.sql`. **Satu baris** yang berubah: `v_submitted` semula `EXISTS(… AND status='submitted')` → jadi `EXISTS(… AND submitted_at IS NOT NULL AND status <> 'void')`. **Alasannya:** `sp_invoices.status` memikul DUA sumbu dalam satu kolom (tahap dokumen `draft`/`issued`/`submitted` + tahap pembayaran `partial`/`paid`), jadi begitu `record_payment` menulis `'partial'` nilai `'submitted'` tertimpa dan jejak "pernah disubmit" hilang — recompute berikutnya menghitung `v_submitted=false` lalu jatuh ke `INVOICED`. Basis dipindah dari status saat ini (mudah tertimpa) ke fakta historis `submitted_at` yang **satu arah**: hanya `submit_invoice` yang menulisnya (selalu `now()`, tak pernah di-NULL-kan), `record_payment` tak menyentuhnya, dan kolomnya tak ter-GRANT ke `authenticated` (warisan TD-175) sehingga PostgREST tak bisa menghapusnya. Klausa `AND status <> 'void'` menyelaraskan dengan `v_has_invoice` di baris atasnya yang sudah mengecualikan void (skenarionya sendiri masih teoretis — nol jalur kode yang bisa menulis `'void'`). Urutan CASE **tak disentuh** (`v_paid` tetap di atas `v_submitted`). ⚠️ Hardcode UUID SOA tetap dipertahankan — TD-178 masih OPEN. |
| `set_sp_status` | `(p_sp_no text, p_status text, p_reason text, p_customer_id uuid)` → int | Set `sp_items.sp_status` (draft/confirmed/cancelled) per (sp_no,customer_id) + stamp; confirm → recompute, cancel → CANCELLED. |
| `delete_sp_dual` ✅ *(live & terekam)* | `(p_customer_id uuid, p_sp_no text)` | **Hapus SELURUH SP dual-table** per (customer_id, sp_no): `DELETE sp_orders` (`sp_order_items` ikut via FK ON DELETE CASCADE) + `DELETE sp_items`. Guard **`is_super_admin()`** + status **DRAFT strict** (`IF v_status IS DISTINCT FROM 'DRAFT' THEN RAISE` — blokir non-DRAFT & row kanonik tak ada). SECURITY DEFINER (bypass `sp_orders_delete` super-only RLS, guard tetap di dalam). ✅ **Sudah live & terekam** — ada di `schema_snapshot.sql` + migrasi `supabase/migrations/20260710000004_delete_sp_dual_rpc.sql`. |
| `create_sp_order_dual` | `(p_company_id, p_customer_id, p_sp_no, p_sp_date, p_dc_id, p_status, p_expired_date, p_notes, p_items jsonb)` → uuid | Dual-write header+item ke `sp_orders`+`sp_order_items`; RAISE `unique_violation` bila `(customer_id,sp_no)` dobel. |
| `generate_picking_from_sp` | `(p_sp_no text, p_customer_id uuid, p_warehouse_id uuid=NULL)` → `TABLE(picking_list_id, picking_no)` | Buat picking dari SP confirmed + reserve stok → recompute PICKING. **[21 Agu 2026 — guard idempotensi DIREVISI; ⚠️ SQL BELUM DIJALANKAN, snapshot `:713` masih guard LAMA]** LAMA: `status <> 'cancelled'` → **maksimum satu picking non-cancelled per SP, PERMANEN**; picking `done` memblokir generate berikutnya selamanya, jadi partial picking mustahil. BARU (`supabase/migrations/20260821000001_partial-picking-guard.sql`, `CREATE OR REPLACE`): **(1)** picking `status IN ('pending','in_progress')` tetap memblokir — pesan error dipertahankan **VERBATIM** (`'Picking list untuk SP % sudah ada'`, sengaja, supaya penanganan/ingatan yang ada tak berubah); **(2)** picking `status='done'` memblokir **hanya selama surat jalannya belum pernah diberangkatkan** (`NOT EXISTS (SELECT 1 FROM delivery_notes dn WHERE dn.picking_list_id=pl.id AND dn.dispatched_at IS NOT NULL)`), dengan pesan error baru. Yang dijaga blok (2) = **dobel-reservasi stok**: reservasi picking hanya dilepas oleh `cancel_picking` & `dispatch_delivery`, DAN `sp_items.shipped_qty` (dasar hitungan outstanding `req = GREATEST(qty − shipped_qty, 0)`) juga baru bergerak saat dispatch — tanpa blok (2), picking kedua sebelum SJ berangkat akan meminta qty yang **sama persis** untuk kedua kalinya. Alasan memakai `dispatched_at` alih-alih `status IN ('in_transit','delivered')`: **gotcha #17**. Sisa body **byte-identik** dgn snapshot — diverifikasi doc-keeper via `diff` terhadap `:701-753`: satu-satunya beda selain blok guard adalah header `CREATE FUNCTION` → `CREATE OR REPLACE FUNCTION` (numbering, CTE `src`/`av`/`ins_items`, insert `stock_ledger` `'reserved'`, `PERFORM sp_recompute_status` semuanya utuh). Konsekuensi yang disengaja: `08_TECH_DEBT.md` **TD-206**. |
| `complete_picking` | `(p_picking_list_id uuid)` | Picking → done → recompute PACKED. |
| `cancel_picking` | `(p_picking_list_id uuid)` | Picking → cancelled, release reservasi, set `had_cancelled_picking` → recompute mundur. |
| `generate_delivery_from_picking` | `(p_picking_list_id uuid)` → `TABLE(delivery_note_id, do_no)` | Buat Surat Jalan dari picking done. |
| `dispatch_delivery` | `(p_delivery_note_id uuid)` | DN → in_transit; ledger unreserve+outbound; **+`sp_items.shipped_qty` (FASE 2A, LIVE)** → recompute DIKIRIM/TERKIRIM_PENUH. **[8 Agu 2026, `CREATE OR REPLACE` — TERKONFIRMASI LIVE**, `schema_snapshot.sql` baris 475-527 pasca-refresh commit `8117127`**]** +blok sync `shipped_qty` ke **`sp_order_items`** (via `legacy_sp_item_id`, `UPDATE ... SET shipped_qty = LEAST(soi.shipped_qty+qty, soi.qty)` — komentar source `-- ===== BARU: jembatan shipped_qty ke sp_order_items`) — sebelumnya HANYA menulis `sp_items`. **Hardcode UUID SOA jg ada di fungsi ini** (`v_company`/fallback `warehouse_id`, baris 479) — lihat `08_TECH_DEBT.md` TD-178. Lihat entri `sp_order_items` §3 + `08_TECH_DEBT.md` TD-40 (RESOLVED-kode/LIVE-SQL). |
| `mark_delivery_delivered` | `(p_delivery_note_id uuid)` | DN → delivered → recompute SAMPAI. |
| `cancel_delivery` | `(p_delivery_note_id uuid)` | DN → cancelled; reverse outbound + reverse `shipped_qty` → recompute mundur. **[8 Agu 2026, `CREATE OR REPLACE` — TERKONFIRMASI LIVE**, `schema_snapshot.sql` baris 173-216**]** reverse `shipped_qty` (`GREATEST(soi.shipped_qty-qty, 0)`) kini juga menyasar `sp_order_items` (komentar source `-- ===== BARU: reversal shipped_qty ke sp_order_items (simetris)`), simetris dgn fix `dispatch_delivery` di atas. |
| **`update_sp_item_dual`** [BARU, 8 Agu 2026 — **TERKONFIRMASI LIVE**, `schema_snapshot.sql` baris 2056-2085 pasca-refresh commit `8117127`] | `(p_id uuid, p_item jsonb)` | Dual-write EDIT satu item SP ke `sp_items` (penuh) **dan** `sp_order_items` (via `legacy_sp_item_id`) dalam satu RPC — dipanggil `updateSpItem` (`db.js`) SEBELUM `SELECT` balik dari `sp_items` (bentuk return ke caller tak berubah). **Kolom persis yang ditulis ke `sp_order_items`, dikonfirmasi dari body fungsi (bukan lagi dugaan):** `qty`, `sla_days`, `estimated_delivery_date`, `shipping_price`, `notes` — KELIMANYA, dan `sp_order_items` memang punya kelima kolom itu (`schema_snapshot.sql` baris 5915-5936 — `sla_days`/`estimated_delivery_date`/`notes` sebelumnya tak tercatat lengkap di entri §3, dikoreksi). **Nuansa verifikasi (bukan sekadar "live" tanpa syarat):** pemetaan kolom ini **terverifikasi BENAR via pembacaan langsung** body fungsi — status lebih kuat dari "code review" (ini bacaan ke apa yang tersimpan LIVE di DB). TAPI **eksekusi dgn data uji nyata** baru terbukti utk `notes` (skrip data ZZZTEST sesi ini); `sla_days`/`estimated_delivery_date` **belum pernah dieksekusi dgn data uji** — pemetaannya sendiri terverifikasi benar, tapi belum ada bukti round-trip nyata utk kedua field itu. RETURNS void (FE re-SELECT terpisah). |
| **`create_invoice`** [BARU, FASE 4, 8 Agu 2026 — **TERKONFIRMASI LIVE**, `schema_snapshot.sql` baris 314-370] | `(p_sp_order_id uuid)` → uuid (invoice id) | Terbitkan invoice utk satu SP. Guard: `Σshipped=Σqty FROM sp_order_items WHERE sp_order_id=p_sp_order_id` (RAISE bila belum lolos — cermin `canCreateInvoice` FE, `SalesOrderDetailPage.jsx`). Generate `invoice_no` (format `INV/{kode_entitas}/FIN/{tahun}/{4-digit}`) via `increment_document_sequence`; **kode entitas dikonfirmasi diresolusi dinamis dari `companies.code`** (`SELECT code INTO v_entity_code FROM companies WHERE id=v_company_id` — BUKAN hardcode "SBI", sempat diduga hardcode pertengahan sesi, diinvestigasi & dikoreksi Den, kini diverifikasi doc-keeper langsung ke source). Otorisasi: `is_super_admin() OR is_manager_or_above() OR has_role('finance_controller')`. Memicu `sp_recompute_status` (→ INVOICED). **[17 Agu 2026, `CREATE OR REPLACE` — LIVE, blok jurnal dibaca penuh, baris 388-436]** +posting jurnal `invoice_issued` **4 baris**: **D `1-1200` Piutang Usaha = `total_amount`** · **K `4-1000` Pendapatan = `total_dpp`** (bila >0) · **K `4-1100` Pendapatan Ongkos Kirim = `total_ship`** (bila >0) · **K `2-1200` PPN Keluaran = `total_ppn`** (bila >0). Akun ongkos kirim **HANYA di-lookup kalau `v_total_ship > 0`** — SP tanpa ongkir tak dipaksa punya akun `4-1100` (akun AR/Pendapatan/PPN Keluaran di-lookup tanpa syarat). Tiap lookup punya `RAISE EXCEPTION` eksplisit bila akunnya belum ada di `chart_of_accounts` untuk company itu. |
| **`record_payment`** [BARU, FASE 5, 17 Agu 2026 — **TERKONFIRMASI LIVE**, `schema_snapshot.sql` baris 1796-1916, body dibaca penuh] | `(p_invoice_id uuid, p_amount numeric, p_payment_date date DEFAULT CURRENT_DATE, p_reference text, p_pph numeric DEFAULT 0, p_bukti_potong_url text, p_bukti_potong_no text)` → uuid (payment id) | Catat pembayaran invoice + posting jurnal `payment_received`. **Otorisasi LEBIH KETAT dari `create_invoice`/`submit_invoice`: HANYA `is_super_admin() OR has_role('finance_controller')`** — `is_manager_or_above()` **SENGAJA dikecualikan** (mencatat uang masuk bukan wewenang manajer lini; keputusan desain, jangan "diseragamkan" tanpa bahas). Guard: `p_amount > 0`, `p_pph >= 0`, invoice ada & bukan `void`, bukan `draft` (harus diterbitkan dulu). Alur: INSERT `sp_payments` → hitung `v_settled = SUM(amount)+SUM(pph)` seluruh payment invoice itu → status invoice jadi **`paid` bila `v_settled >= total_amount − 1`** (toleransi **Rp 1** utk selisih pembulatan PPN, konstanta `c_tolerance`), **`partial` bila `v_settled > 0`** — **PPh dihitung sbg BAGIAN pelunasan, bukan potongan hangus**. Jurnal: **D `1-1101` Bank = `amount`** · **D `1-1300` PPh 23 Dibayar Dimuka = `pph`** (bila >0) · **K `1-1200` Piutang Usaha = `amount + pph`**. Hanya saat status jadi `paid` fungsi ini `PERFORM sp_recompute_status` (→ SP naik LUNAS). |
| **`mark_ttf_received`** [BARU, FASE 5, 17 Agu 2026 — **TERKONFIRMASI LIVE**, body dibaca penuh] | `(p_invoice_id uuid, p_received_by text, p_ttf_no text, p_notes text)` → uuid (ttf id) | Tandai faktur diterima customer → tulis `ar_ttfs`. **UPSERT, BUKAN INSERT:** cari baris `ar_ttfs` ber-`invoice_id` sama (`ORDER BY created_at LIMIT 1`) — ada → UPDATE (`tanggal_menerima=CURRENT_DATE`, `diterima_oleh`, `no_ttf`/`notes` di-COALESCE agar isian kosong tak menghapus nilai lama, `no_inv`/`no_sp` diisi hanya bila masih `''`), tak ada → INSERT baru. Karena itu tombol "Edit" di UI memanggil RPC yang SAMA tanpa melahirkan TTF kedua. **Otorisasi LEBIH LONGGAR dari `record_payment`: `is_super_admin() OR is_manager_or_above() OR has_role('finance_controller')`** — menerima faktur = aktivitas operasional, bukan pencatatan uang masuk (keputusan desain). Guard: nama penerima wajib (`btrim` tak boleh kosong), invoice ada & bukan `void`/`draft`. **Nol jurnal** (TTF bukan peristiwa akuntansi). |
| **`submit_invoice`** [BARU, FASE 4, 8 Agu 2026 — **TERKONFIRMASI LIVE**, `schema_snapshot.sql` baris 1934-1956] | `(p_invoice_id uuid)` | `sp_invoices.status` `issued`→`submitted`, isi `submitted_at` otomatis server-side. Otorisasi sama seperti `create_invoice`. Memicu `sp_recompute_status` (→ SUBMITTED — fix guard di baris `sp_recompute_status` di atas dikonfirmasi juga sudah live, jadi transisi ini sekarang benar-benar bisa tercapai). |
| `sp_issue_btb` | `(p_customer_id, p_sp_no, p_btb_no, p_qty=NULL, p_btb_date=NULL, p_delivery_note_id=NULL, p_remarks=NULL)` → uuid | Terbitkan BTB ke `sp_btb` (idempoten per btb_no hidup) + recompute → **BTB_TERBIT**. |
| `sp_delete_btb` | `(p_btb_id uuid)` | Soft-delete `sp_btb` + recompute (mundur). |
| `set_product_category_prices` | `(p_product_id, p_semester, p_tahunan, p_project)` | Set harga kategori produk + log `product_price_history`; authz super ATAU admin-same-company (terima NULL utk clear). |
| `bulk_update_product_prices` | `(p_rows jsonb)` → `{updated, skipped}` | Update harga produk massal (super_admin only). |
| `indomarco_dashboard_stats` | `(p_customer_id uuid)` → jsonb | Agregasi KPI Dashboard Indomarco di DB (dipakai `IndomarcoDashboardPage.jsx` via `supabase.rpc`). Return `{total_sp:int (COUNT DISTINCT sp_no), total_ordered:int (SUM qty), total_realized:int (SUM shipped_qty), dc_count:int (COUNT DISTINCT dc trimmed), period_min/period_max:"YYYY-MM-DD", dc_volumes:[{dc,volume}] (1/DC, tak terurut), monthly:[{ym:"YYYY-MM", sp_count}] (1/bulan ada)}` atas `sp_items` per `customer_id`. Menggantikan agregasi client-side yang dulu ber-`.limit(1000)` (understate diam-diam bila baris >1000). ✅ **Sudah termuat di `schema_snapshot.sql`** (baris 811, refresh commit `6c68642`) + direkam migrasi `20260711000000_indomarco_dashboard_stats.sql` (commit `1a22d98`). |
| `storbit_sp_customers` | `()` → jsonb (tanpa argumen) | Daftar customer selector Dashboard Indomarco/Storbit (dipakai dropdown `IndomarcoDashboardPage.jsx` via `supabase.rpc`). Return jsonb array `[{customer_id:uuid, name:text}]` — DISTINCT customer yang punya SP, difilter company **SOA** (`d2e5e565-…`), urut alfabet by name. Sumber: `sp_items` join `accounts`. Menggantikan 2 query FE (scan `sp_items.select("customer_id").limit(5000)` + dedup `Set`, lalu resolve nama via `accounts.in(ids)`) — hilangkan tarik ribuan baris hanya utk daftar customer unik + `.limit(5000)` rapuh (cap default PostgREST 1000). ⚠️ **BELUM termuat di `schema_snapshot.sql`** (dibuat manual di DB, snapshot belum di-refresh) + **belum direkam migrasi** (`supabase/migrations/`). Lihat gotcha #10 (snapshot lag). |
| **`get_storbit_dashboard_stats`** [BARU 18 Agu 2026, LIVE `:789`] | `(p_customer_id uuid=NULL, p_price_category text=NULL, p_company_id uuid=NULL)` → jsonb | **Satu panggilan untuk SELURUH angka Dashboard Storbit** — return `{manifest:{13 kunci}, warehouse:{4 kunci}, generated_at}`. `SECURITY INVOKER + STABLE` (read-only; RLS pemanggil tetap berlaku, konsisten dgn view `stock_summary` yang `security_invoker='true'`). **Filter `company_id` EKSPLISIT, tak cukup andalkan RLS** — 3 sebab: `stock_ledger_select = USING(true)` (TD-173) bikin `stock_summary` nol isolasi entitas; `sp_orders_read`/`products_read` meloloskan super_admin ke SEMUA entitas; dan pemanggilnya kini pin SOA (TD-178). `expired_date` diambil **`MIN(sp_items.expired_date)`, BUKAN `sp_orders.expired_date`** (gotcha #16). `zero_stock` pakai `<= 0` (available bisa NEGATIF saat over-reserve — dgn `= 0` produk itu hilang dari dua kartu sekaligus); CTE stok digerakkan dari `products` **bukan** `stock_summary`, karena produk tanpa histori ledger tak muncul di view itu sama sekali → kalau digerakkan dari view, `zero_stock` under-count senyap. ACL pola FASE 5. |
| **`get_storbit_sp_drilldown`** [BARU 18 Agu 2026, LIVE `:913`] | `(p_category text, p_customer_id uuid=NULL, p_price_category text=NULL, p_company_id uuid=NULL, p_limit int=200)` → TABLE(`sp_no`, `customer_id`, `customer_name`, `dc_nama`, `sp_date`, `status`, `expired_date`) | Daftar SP di balik tiap kartu — **9 kategori, satu bentuk baris**. Sengaja melayani SEMUA kategori termasuk yang sebenarnya bisa dilayani PostgREST, supaya angka kartu & isi tabel lahir dari CTE+WHERE yang SAMA (nol drift — pelajaran TD-168). CTE `sp`/`sp_flag`-nya **cerminan persis** milik `get_storbit_dashboard_stats`: **ubah satu, ubah dua-duanya**. Kategori tak dikenal → **nol baris** (`ELSE false`), bukan seluruh tabel (salah ketik di FE harus tampil "tidak ada data", bukan membocorkan semua SP). `customer_id` dikembalikan khusus untuk navigasi ke Detail SP — jalur existing memakai komposit `{spNo, customerId}`, **bukan** `sp_orders.id`. |
| **`get_storbit_stock_drilldown`** [BARU 18 Agu 2026, LIVE `:985`] | `(p_category text, p_company_id uuid=NULL, p_limit int=200)` → TABLE(`product_id`, `sku`, `product_name`, `available`, `reorder_point`) | 3 kategori warehouse (`danger_stock`/`zero_stock`/`rop_belum_diisi`). **Sengaja TANPA kolom DC** — `products` bukan entitas per-DC dan satu produk bisa berstok di beberapa gudang, jadi kolom DC hanya akan berisi tebakan (menyimpang sadar dari mockup). `product_id` untuk navigasi ke Detail Produk (`ProductDetailPage` men-fetch ulang seluruh datanya `.eq('id', …)`). |

> **Catatan bersama 3 RPC di atas:** ACL-nya **pola FASE 5** — `REVOKE ALL … FROM PUBLIC` lalu `GRANT … TO authenticated` saja, **nol baris `anon`**. Sengaja TIDAK meniru `indomarco_dashboard_stats`/`storbit_sp_customers` yang ter-GRANT ke `anon`; default privileges Supabase meng-GRANT fungsi baru ke `anon` **otomatis**, jadi REVOKE eksplisit bukan formalitas. Kedua RPC drill-down di-**`DROP`+`CREATE`** (bukan `CREATE OR REPLACE`) saat kolom identifier ditambahkan — menambah kolom = mengubah return type, dan `CREATE OR REPLACE` menolaknya. ⚠️ **DROP menghapus GRANT**: kalau di-run ulang, blok REVOKE/GRANT WAJIB ikut, gejalanya kalau lupa = `permission denied for function`, bukan hasil kosong.
| `check_similar_accounts` ✅ **berjejak `20260725000004`** | `(p_name text, p_company_id uuid)` → `TABLE(id uuid, name text, similarity real)` | **Pre-check kemiripan nama akun** untuk peringatan duplikat di form (bukan gerbang — gerbangnya index `uq_accounts_norm_name_per_entitas`). `LANGUAGE sql STABLE SECURITY DEFINER SET search_path TO 'public'`, ambang similarity **0.6** (satu nilai di CTE `param` supaya gampang diubah), `ORDER BY similarity DESC LIMIT 5`, `GRANT EXECUTE … TO authenticated`. **Kenapa SECURITY DEFINER:** RLS `prospects_read` membatasi sales ke akun **miliknya sendiri** → pre-check dari klien biasa **buta terhadap akun sales lain** dan akan menjanjikan "aman" untuk nama yang justru **ditolak index**. DEFINER membaca **lintas-pemilik tapi tetap dalam SATU entitas** (`p_company_id`) dan hanya mengembalikan **id + name + skor** — kolom lain milik akun sales lain **tidak** ikut terbuka. **Kenapa `LANGUAGE sql` bukan plpgsql:** kolom output wajib bernama `similarity`; di plpgsql nama itu **membayangi fungsi `similarity()` pg_trgm**. Bukan konvensi baru — repo sudah punya **10 fungsi `LANGUAGE sql`** (mis. `get_table_columns`, juga `RETURNS TABLE`). **✅ KOREKSI 25 Jul 2026 — kini berjejak migrasi `20260725000004_check_similar_accounts.sql` (idempotent) + SUDAH dijalankan di produksi** → RPC ada, pemanggilnya di 3 file FE (`ProspectFormPage`, `CustomerListPage`, `App.CustomerModal`) **tak lagi gagal-senyap secara struktural**. ~~**⚠️ Tapi lapisan fuzzy BELUM tes runtime** (form di balik login) — "RPC ada" ≠ "warning terbukti muncul".~~ **[KOREKSI SUSULAN 25 Jul 2026 — TESTED, bukan lagi klaim kode]** RPC **dites langsung**: `SELECT check_similar_accounts('ALLIANCE COSMETIC', <uuid MSI>)` → **3 baris**, similarity **1.0 / 0.842 / 0.619**. **Smoke test UI hari yang sama** membuktikan warning fuzzy *"Mirip dengan: ALLIANCE COSMETICS"* **muncul** di jalur **Prospect** dan **Customer** CRM — lapisan fuzzy TERBUKTI JALAN di produksi. **⚠️ Snapshot BELUM memuat RPC ini** (utang `pg_dump`, **TD-128**) → sumber kebenaran = file migrasinya, bukan snapshot. *(Atribusi: hasil SELECT + smoke test UI = laporan Den 25 Jul 2026, bukan verifikasi doc-keeper langsung ke DB/browser; header file `20260725000004` sendiri tak menyatakan eksekusi eksplisit seperti file merge 1&2.)* |
| **PRF — Penawaran Vendor (Fase 3, 27 Jul 2026, migrasi `20260727000003`)** | | *(semua SECURITY DEFINER; preseden pola: `set_prospect_on_inquiry` — RLS `prf_update_status` tak bisa dilonggarkan tanpa membuka kolom lain [TD-77], dan tak bisa dilewati untuk transisi keluar SUBMITTED [TD-109]; ~~⚠️ NOL pemanggil FE, murni fondasi DB~~ **[KOREKSI 27 Jul 2026 sore — SEBAGIAN USANG]** `prf_claim`/`prf_release` kini punya pemanggil FE (Batch 3A, tombol "Ambil PRF Ini"/"Lepas PRF" di `PRFDetailPage.jsx`, lihat entri `prf_vendor_offers` di atas); ~~`prf_mark_quoted`/`prf_select_offer` MASIH nol pemanggil FE~~ **[KOREKSI KEDUA 27 Jul 2026, Batch 3C]** keduanya **KINI PUNYA pemanggil FE** juga — `prf_mark_quoted` via tombol "Nyatakan Penawaran Siap" (`PRFDetailPage.jsx`), `prf_select_offer` via tombol "Pakai/Ganti Penawaran Ini" (`DealDetailPage.jsx`). Seluruh 4 RPC di baris ini kini punya pemanggil FE)* |
| `prf_claim` | `(p_prf_id uuid)` | SUBMITTED→ACKNOWLEDGED, ambil PRF sbg satu-satunya pengerja. Guard: `has_role('procurement')` + status SUBMITTED + `acknowledged_by IS NULL` (RAISE "PRF sudah diambil orang lain" bila tidak). **UI: 27 Jul 2026 sore (Batch 3A)** — tombol "Ambil PRF Ini" (`PRFDetailPage.jsx`). |
| `prf_release` | `(p_prf_id uuid)` | ACKNOWLEDGED→SUBMITTED, kosongkan `acknowledged_by`/`acknowledged_at`. Guard: pemegang sendiri ATAU `is_manager_or_above()` — **BUKAN** procurement lain. Dua-langkah wajib utk pindah tangan: release lalu `prf_claim` ulang — **tak ada fungsi takeover**. **UI: 27 Jul 2026 sore (Batch 3A)** — tombol "Lepas PRF" (`PRFDetailPage.jsx`), gate render pakai const lokal `MANAGER_OR_ABOVE` (7 role, cermin BENAR `is_manager_or_above()` DB). |
| `prf_mark_quoted` | `(p_prf_id uuid, p_waiver_reason text=NULL)` | ACKNOWLEDGED→QUOTED. Guard: hanya pemegang (`acknowledged_by=auth.uid()`); ≥1 penawaran wajib ada; **<3 penawaran WAJIB isi `p_waiver_reason`** (RAISE bila kosong) → disimpan ke `prf.min_offers_waiver_reason`; ≥3 penawaran → kolom itu dipaksa NULL walau `p_waiver_reason` dikirim. |
| `prf_select_offer` | `(p_prf_id uuid, p_offer_id uuid)` | QUOTED-only (RAISE bila status lain). Sales pemilik PRF (`created_by`) ATAU manager memilih SATU `prf_vendor_offers` sbg `prf.selected_offer_id` + `selected_by`/`selected_at`; guard offer harus milik PRF yg sama & belum soft-delete. |
| `add_picking_material` | `(p_picking_list_id, p_product_id, p_qty)` → uuid | Tambah baris material packing ke picking + post `stock_ledger` outbound (atomik). |
| `delete_picking_material` | `(p_material_id)` | Hapus baris material packing + reverse `stock_ledger` (inbound `material_reverse`). |
| `attach_price_contract_info` | `(p_history_id, p_contract_no, p_valid_from, p_valid_until)` | Lampirkan info kontrak/PKS ke baris `product_price_history` (audit harga). |
| `increment_document_sequence` | `(p_company_id, p_document_type, p_department_code, p_year, p_month=0)` | Generate nomor dokumen sekuensial atomik (SECURITY DEFINER). |
| `is_super_admin` / `get_user_company_id` / `is_admin_or_above` / `is_manager_or_above` / `has_permission` / `has_role` / `get_user_role_code` | — | Helper RLS (lihat §4). Bisa dipanggil `supabase.rpc(...)` dari frontend (mis. gating super-admin). |
| `get_table_columns` | `(p_table text)` | Introspeksi kolom (dipakai Schema Manager + custom fields). |
| `exec_sql` | `(sql text)` | Eksekusi SQL arbitrer (Edge Function `manage-schema`; service-role only). |
| `int_to_roman` | `(num integer)` | Helper (code customer pakai angka romawi). |
| **`is_bnf_authorized()`** [BARU, 10 Agu 2026, **diperluas 12 Agu 2026**] | `()` → boolean | `LANGUAGE sql STABLE SECURITY DEFINER`. Gate akses gabungan modul BNF/Briefing Harian/Meeting Mingguan — detail lengkap kondisi & pemakai: §3 "BNF (Bad News First), Briefing Harian & Meeting Mingguan" di atas. Tanpa GRANT eksplisit (default PUBLIC-executable). |
| **`get_linked_bnf_status(p_daily_report_item_id uuid)`** [BARU, 12 Agu 2026] | `(p_daily_report_item_id uuid)` → text | `LANGUAGE plpgsql STABLE SECURITY DEFINER`. Balikin status `bnf_reports` (Open/In Progress/Escalated/Closed) yg ditautkan dari satu `daily_report_items`, TANPA expose `report_no`/isi laporan. Self-gating internal: `NULL` bila item belum ditarik ke BNF (`pulled_to_bnf_report_id IS NULL`), `NULL` bila caller BUKAN `created_by` item itu DAN BUKAN `is_bnf_authorized()` — aman dipanggil siapa pun tanpa cek otorisasi di FE dulu. Tanpa GRANT eksplisit (default PUBLIC-executable, sama pola `is_bnf_authorized()`). Dipakai `BriefingHarianPage.jsx` (badge "Jadi BNF · Status", lihat Bug Fix #2 §3). |
| **Trigger fns — BNF/Briefing Harian/Meeting Mingguan** [BARU, 10 Agu 2026] | — | `guard_daily_report_items_field_update()` (`trg_guard_daily_report_items_field_update`, BEFORE UPDATE `daily_report_items` — tolak ubah `insiden_resolution`/`resolved_by`/`pulled_to_bnf_report_id` kecuali `is_bnf_authorized()`), `set_daily_report_items_defaults()` (`trg_set_daily_report_items_defaults`, BEFORE INSERT — isi default `task_status`/`insiden_resolution` kondisional per `category`), `guard_bnf_division_scope_not_home()`/`guard_bnf_department_scope_not_home()` (BEFORE INSERT/UPDATE `bnf_division_scopes`/`bnf_department_scopes` — RAISE bila `company_id` yg didaftarkan = home company). Ketiga tabel ini masing-masing cuma py 1-2 trigger per event, tak ada konflik urutan → **tak satu pun perlu prefix `trg_z_`** (prefix itu khusus mengatasi urutan alfabetis Postgres saat >1 trigger bentrok di event yang sama). Detail lengkap tiap fungsi: §3 di atas. |
| **Trigger fns** | — | `generate_customer_code()`, `set_customer_on_won()`, `track_stage_change()` (**[KOREKSI 6 Agu 2026]** HANYA `accounts.stage_changed_at = now()` saat `pipeline_stage` berubah nilai — **TIDAK menulis ke `activity_logs` maupun tabel lain mana pun**, klaim lama di baris ini salah; trigger `trg_z_track_stage_change`), `handle_new_user()`, `set_updated_at()`, `capture_login_session()`, `sync_deal_value_on_quotation_accept()` (quotation ACCEPTED → `accounts.estimated_value` — ⚠️ **tak pernah jalan**, nol baris ACCEPTED, **TD-54**), `sync_last_activity()` [BARU 10 Jul, trigger `trg_z_sync_last_activity` di `activities` AFTER INSERT/UPDATE/DELETE → `accounts.last_activity_at = max(COALESCE(completed_at, created_at))`; **TD-52/TD-59**], `sync_profile_email()` (sinkron `profiles.email` ← `auth.users`), `log_product_price_change()` (AFTER UPDATE `products.default_price` → `product_price_history`), `set_prospect_on_inquiry()` [BARU 18 Jul, trigger `trg_set_prospect_on_inquiry` AFTER INSERT ON `inquiries` → promosi `account_status` `lead/mql/sql → prospect` untuk `COALESCE(NEW.prospect_id, NEW.customer_id)`; SECURITY DEFINER + `search_path=public`; guard `IN (lead,mql,sql)` tak menurunkan customer/prospect; migrasi `20260718000001`; **belum di snapshot**]. |

---

## 6. Known Issues & Gotchas

1. **Snapshot lag:** `migrations/` berhenti 3 Jun; perubahan SQL Editor (mis. `assets.condition/brand/department_id/assignment_status`, `quotations.vat_rate`, `accounts.estimated_value`, `currencies`, `activities`/`activity_logs`, `user_login_logs`) masuk lewat snapshot — JANGAN andalkan migrasi untuk struktur terkini. Refresh snapshot via `pg_dump`. Sudah 2× menyebabkan salah-baca schema.
2. **FK constraint pakai nama lama `prospects_*`:** rename tabel `prospects`→`accounts` TIDAK rename constraint. Embed PostgREST tetap pakai nama lama, dengan **alias** supaya consumer tak berubah: `profiles!prospects_assigned_to_fkey`, `companies!prospects_owner_company_id_fkey`, dst. Begitu juga `customers`→`accounts` (embed `customers:accounts!sp_items_customer_id_fkey(name)`). Jika DBA me-rename constraint, update bagian `!constraint`.
3. **`auth.uid()` NULL di SQL Editor** → semua RLS helper false/null. Test RLS hanya di sesi browser.
4. **Trigger ordering `trg_z_`:** `trg_z_gen_customer_code_upd` (BEFORE UPDATE, `WHEN code NULL/'' AND deleted_at IS NULL`) sengaja prefix `z` agar fire **setelah** `trg_set_customer_on_won`. WHEN sudah meng-exclude soft-delete (fix bug "duplicate key accounts_code_unique saat hapus customer"). `accounts_code_unique` = `UNIQUE(code) WHERE code IS NOT NULL`.
5. **`has_permission()` flagged BROKEN** di `CLAUDE.md` ("query tabel `permissions`/`role_permissions` yang tidak ada"). Snapshot saat ini **punya** tabel `permissions` + `role_permissions` → kemungkinan tabel ada tapi **belum ter-seed** / fungsi tak dipakai. [TODO: verifikasi apakah `permissions`/`role_permissions` terisi & `has_permission` benar-benar dipakai RLS]. **[7 Agu 2026] TODO ini kini terjawab utk sisi "dipakai RLS": TIDAK, 0 hit.** Sisi FE juga dikonfirmasi 0 pemakaian (Tema 2, retirement sistem lama) — lihat `08_TECH_DEBT.md` TD-02/TD-06.
6. **`is_admin_or_above()` tidak kenal manager/ceo** → ~51 policy memakainya, tak sinkron dgn RBAC granular UI. Pemicu bug akses (mis. CEO ke-block baca profiles → stopgap `profiles_read USING(true)`; dropdown sales kosong untuk manager). Lihat tech debt RBAC RLS migration. **[7 Agu 2026]** Audit RLS/RBAC menyeluruh mendetailkan cakupan ini jadi 19 tabel CRITICAL + gap lain — lihat `08_TECH_DEBT.md` **TD-173**. Fungsi ini (dan `is_super_admin()`) juga terverifikasi **kehilangan `SET search_path`** meski `docs/security/rls-policy-draft.md` mewajibkannya utk tiap `SECURITY DEFINER` (LOW, bagian dari TD-173).
7. **`profiles` tanpa `deleted_at`** → pakai `active`. **`user_roles.user_id` → `auth.users`** (bukan profiles) → tak bisa embed user_roles dari profiles; query terpisah lalu map.
8. **Dormant tables** belum di-drop: `sales_calls`/`sales_visits`/`sales_visit_logs` (frontend sudah pindah ke activities), `customers` (sudah pindah ke accounts), kolom `profiles.role` (frontend/EF sudah berhenti baca).
9. **Dua generasi SP (transisi FASE 0-4):** create SP = **dual-write** (`sp_items` lama + `sp_orders`/`sp_order_items` baru via `create_sp_order_dual`). Reader fulfillment (list/detail, `shipped_qty`) **masih baca `sp_items`** — TAK disentuh sesi 8 Agu 2026; **headline status baca `sp_orders.status`** (mesin 12-tahap). `sp_btbs` legacy sudah 0-penulis FE (bermigrasi ke `sp_btb`, tinggal drop). **[KOREKSI 8 Agu 2026, DIVERIFIKASI ULANG SAMA HARI setelah `schema_snapshot.sql` di-refresh (commit `8117127`) — tiga klaim di bawah SUDAH TIDAK BENAR, dan mekanismenya kini TERKONFIRMASI LIVE (bukan lagi "dirancang, belum jalan"), lihat `08_TECH_DEBT.md` TD-40 diperbarui]:** ~~`sp_order_items.shipped_qty` kanonik belum di-sync dari dispatch (2D pending)~~ — `dispatch_delivery`/`cancel_delivery` (`CREATE OR REPLACE`, `schema_snapshot.sql` baris 475/173) **dikonfirmasi live**, blok sync ADA. ~~EDIT item SP hanya menulis `sp_items`~~ (`updateSpItem`, `db.js`) → memanggil RPC `update_sp_item_dual` (baris 2056, **dikonfirmasi live**) lebih dulu, dual-write KELIMA kolom overlap (`qty`/`sla_days`/`estimated_delivery_date`/`shipping_price`/`notes`) ke keduanya. ~~tabel itu juga nol pembaca~~ — kini dibaca `getInvoicePdfData` (Invoice PDF, FASE 4, **live**) + guard RPC `create_invoice` (**live**). **Backfill data satu-kali** (115 baris `shipped_qty`, 1 dikecualikan: SP 2181355 CANCELLED) tetap live seperti semula, kini genap dilengkapi mekanisme sinambung yang juga live — jadi `sp_order_items` sekarang sinkron ke DEPAN juga, bukan cuma pada satu titik waktu backfill. **Kesimpulan yang benar hari ini (bukan overstatement ke arah manapun):** `sp_items` TETAP primary reader fulfillment (kodenya sendiri tak disentuh sesi ini — sync menjaga selaras, tak memindahkan reader); `sp_order_items` kini punya pembaca RIIL (Invoice) + mekanisme sync yang LIVE, tapi belum bisa disebut "sumber kebenaran" app-wide selama reader list/detail tak dipindah — divergensi TETAP mungkin utk kolom di luar cakupan sync (delete SATU item via `removeRow` — **TD-49**, tetap OPEN). ⚠️ **Arah `sp_items` vs `sp_order_items` (mana yang dipensiunkan) masih KEPUTUSAN TERBUKA** — jangan diklaim sudah diputuskan; sesi 8 Agu 2026 MEMPERKECIL divergensi (fix bertarget, kini live), BUKAN mengonsolidasi/memensiunkan salah satu. ⚠️ **RLS `sp_items`/`sp_btbs`/picking/delivery = `USING(true)`** → isolasi company bergantung filter aplikasi (lihat `08_TECH_DEBT.md` **TD-39**/**TD-173**, ~48 policy permisif + daftar tabel — TIDAK terpengaruh koreksi GRANT di bawah, axis berbeda). ⚠️ **[8 Agu 2026] GRANT kolom-level (axis BEDA dari RLS baris di atas):** `sp_orders.status` + kolom sistem `sp_invoices` DITAMBAL (REVOKE UPDATE penuh → GRANT kolom-spesifik) — **dikonfirmasi LIVE** (24/25 kolom `sp_orders` ber-GRANT individual, `status` dikecualikan; 6 kolom `sp_invoices` dikecualikan persis sesuai desain) → **TD-175 RESOLVED**; `sp_order_items.shipped_qty`/`sp_items.sp_status`/`sp_invoice_lines.dpp,ppn,qty` **dikonfirmasi ULANG masih `GRANT ALL`** (lebih longgar dari sekadar UPDATE), SENGAJA belum ditambal — **TD-176 tetap OPEN**.
10. **Snapshot status (re-verifikasi menyeluruh 31 Jul 2026 — MENGGANTIKAN seluruh riwayat tarik-ulur stale/segar 10 Jul–27 Jul di bawah; histori lengkapnya diarsipkan di `PROGRESS.md` tanggal-tanggal terkait, tak diulang di sini):** `schema_snapshot.sql` **SEGAR**. Commit terakhir yang menyentuhnya: `61709bf` (27 Jul 2026, self-refresh `pg_dump` dalam commit yang sama dengan migrasi terbaru repo, `20260727000006_prf_update_status_terima_quoted.sql`). Nol migrasi/perubahan skema tercatat setelah tanggal itu. Diverifikasi ulang langsung ke file (31 Jul 2026): `inquiries.commodity` (drop B4, 25 Jul) **ABSEN** dari snapshot ✅; objek dedup akun (`normalize_account_name`/`uq_accounts_norm_name_per_entitas`/`check_similar_accounts`) **ADA** ✅ (9 hit gabungan); RLS `prf_update_status` **mencakup status `QUOTED`** (`:13059`) ✅; 6 tabel backup lama 18–23 Jul (`accounts_lifecycle_backup_20260718` dkk) **terkonfirmasi sudah di-drop** ✅ (dugaan 26 Jul kini terbukti benar). Total tabel snapshot: **110** (100 tabel bisnis + **10 tabel backup** menunggu drop-sekaligus: `backup_b4_inquiries_20260725`, `backup_dedup_accounts_20260725`, `backup_dedup_activities_20260725`, `backup_dedup_alliance_20260725`, `backup_dedup_inquiries_20260725`, `backup_dedup_quotations_20260725`, `backup_leadpool_c1_won_20260724`, `backup_leadpool_trap_20260724`, `backup_prf_20260727`, `backup_prf_cost_items_20260727`). **Tidak ada utang `pg_dump` yang diketahui per 31 Jul 2026.** Aturan yang tetap berlaku (pelajaran berulang 10 Jul–27 Jul, jangan dilupakan lagi): snapshot bilang "objek tidak ada" **≠** objeknya tidak ada di DB — SQL manual bisa jalan kapan saja di antara dua `pg_dump`. Selalu bandingkan tanggal refresh snapshot vs tanggal SQL manual terakhir sebelum menyimpulkan, lalu **TANYA** kalau ragu. **[KOREKSI 31 Jul 2026 malam, doc-keeper — klaim "Tidak ada utang `pg_dump`" barusan SUDAH TIDAK AKURAT, gugur dalam hitungan jam oleh perubahan yang tidak terlihat saat audit di atas ditulis.]** Commit `5529540` (20:00 — ~2 jam 47 menit SEBELUM commit `9bbb2de` yang menulis audit "nol utang" di atas, 22:47) memperkenalkan fitur "Chatter" yang menyebut eksplisit **dua tabel baru `inquiry_comments`/`inquiry_comment_mentions` dibuat MANUAL di Supabase** (skema+RLS+GRANT) **SEBELUM** sesi kode itu sendiri — audit snapshot 31 Jul di atas tak menyinggung kedua tabel ini sama sekali (kemungkinan luput, bukan sengaja dikecualikan). Diverifikasi doc-keeper: **0 hit grep untuk `inquiry_comments`/`inquiry_comment_mentions` di `schema_snapshot.sql` DAN di `supabase/migrations/`** — utang `pg_dump` DAN utang rekam-migrasi sekaligus, pola sama persis TD-128/`contacts`. **Angka "110 tabel" di atas TETAP AKURAT untuk cakupannya sendiri** (itu jumlah tabel yang ADA di file snapshot; kedua tabel baru ini belum masuk snapshot sama sekali, jadi tak mengubah angka 110 itu — tapi berarti populasi tabel NYATA di DB kini melebihi 110). Entri tabel baru: lihat `inquiry_comments` di §3 (CRM & Sales, atas) + `08_TECH_DEBT.md` **TD-163** (baru). **Pelajaran tambahan:** audit "menyeluruh" pun bisa luput satu perubahan manual yang terjadi jam-jam sebelumnya di hari yang sama — bukan cuma "SQL manual di antara dua `pg_dump`" seperti disebut aturan di atas, tapi juga SQL manual yang terjadi SEBELUM sebuah audit ditulis tapi tak ikut ter-cek olehnya. **[UPDATE 7 Agu 2026 — POLA BERULANG LAGI, kali ini dalam SATU hari:** dua migrasi dijalankan manual hari yang sama (`20260807000001` pagi + `20260807000002` sore). `pg_dump` SEMPAT dijalankan **di antara** keduanya (commit `87620fe`, 09:49 — merekam `is_admin_tier_role()` dgn benar, snapshot SEGAR untuk migrasi pertama). TAPI migrasi kedua (`role_menu_permissions` + kolom `user_menu_permissions.effect`, dibuat 16:54 — berjam-jam SETELAH `pg_dump` pagi itu) **TIDAK ikut ter-refresh** — diverifikasi doc-keeper via grep langsung: `role_menu_permissions` **0 hit** di `schema_snapshot.sql`, kolom `effect` **absen** dari definisi `CREATE TABLE public.user_menu_permissions`. Snapshot kini SEGAR untuk sebagian perubahan hari ini, STALE untuk sebagian lain — jangan asumsikan "sudah di-refresh pagi ini" berarti seluruh hari itu ter-cakup.]** **[UPDATE 9 Agu 2026]** Sesi Access Control + multi-entity plumbing + AdminHub — SQL manual dijalankan lagi di **DUA cabang berbeda** (`main` langsung, lalu terpisah di branch `feat/company-switcher-topbar`), masing-masing me-refresh `schema_snapshot.sql` sendiri-sendiri dari `pg_dump`-nya masing-masing (commit `1147a2e` di `main`: fix `companies_read_own`; commit `c3bac1b` di branch: fix `roles_read`) — begitu digabung (`git merge`), file ini sempat berada di tengah **konflik merge**, TAPI konfliknya cuma **2 baris token sesi `pg_dump` random** (`\restrict`/`\unrestrict`, baris 5 & akhir file — bukan bagian SQL apa pun, cuma metadata dump), **seluruh isi SQL substantif dari kedua sisi tergabung otomatis TANPA konflik** (diverifikasi: `grep -c "^<<<<<<<"` = 2, keduanya persis token itu). Doc-keeper membaca isi file dalam kondisi ini (mid-merge, 2 konflik trivial belum diselesaikan Den) dan mengonfirmasi langsung via `grep`: `get_user_company_ids()` **ADA** (`:770`) · `companies_read_own` **DAPAT bypass baru** (`:13124`) · `roles_read` **DAPAT bypass baru** (`:14965`) · `roles_insert`/`roles_update`/`user_roles_insert`/`user_roles_update` **SEMUA dapat bypass `is_super_admin()` top-level** (`:14958,14972,15569,15583`) · `payment_terms_read` **BELUM dapat** `get_user_company_ids()` (`:14374`, cuma `is_super_admin()`) — lihat `08_TECH_DEBT.md` **TD-180**. Kalau merge ini masih belum diselesaikan Den saat dokumen ini dibaca, JANGAN percaya isi `schema_snapshot.sql` mentah-mentah tanpa cek `git status` dulu. Migrasi tercatat sesi ini **hanya** `20260809000001_picking_surat_jalan_menu_permissions.sql` — `get_user_company_ids()`, keenam bypass RLS di atas, dan kolom `entity_finance_settings.default_payment_term_id` **belum direkam migrasi**. **[UPDATE 17 Agu 2026 — kali ini polanya TIDAK berulang, catat sbg preseden yang benar]** SQL FASE 5 juga dijalankan manual di SQL Editor, TAPI keduanya beres di hari yang sama: **snapshot SUDAH di-refresh** (`pg_dump`, commit `35cb1d3`) **dan** perubahannya **direkam file migrasi** `20260817000001_fase5_jurnal_ar_payments_ttf.sql`. ⚠️ **Migrasi itu RETROAKTIF** — ditulis SETELAH SQL jalan, isinya diambil dari `schema_snapshot.sql` (cakupan diturunkan dari diff snapshot `1dcf5ed..35cb1d3`, bukan dari ingatan), dengan 2 penyimpangan disengaja terhadap teks snapshot yang tak mengubah semantik: `CREATE FUNCTION`→`CREATE OR REPLACE FUNCTION` dan dollar-quote `$$`→`$fn$`. **BUKAN untuk di-run ulang.** Penomoran sectionnya **STEP 1–9 lokal, SENGAJA bukan M1–M13** — label M sudah dipakai tabel rencana migrasi `DESIGN_SP_SCHEMA.md` §5 yang isinya berbeda total (di sana M2 = buat tabel FASE 4, M7 = backfill `ar_ttfs`); jangan tertukar. Migrasi terakhir SEBELUM ini: `20260814000003_invoice_due_date.sql` — file FASE 5 ini melengkapi disiplin yang sempat putus di antaranya. **Angka tabel — KOREKSI:** snapshot per 17 Agu 2026 memuat **132 `CREATE TABLE`** (122 tabel bisnis + **10 tabel backup** yang masih menunggu drop-sekaligus), dihitung ulang doc-keeper via `grep -c "^CREATE TABLE public\."`. Angka **"110 tabel"** yang tertulis di paragraf 31 Jul 2026 di atas **dan di `CLAUDE.md` §Quick Reference sudah STALE** — pertumbuhan dari modul BNF/Briefing Harian/Meeting Mingguan (7 tabel, 10-12 Agu) + FASE 4/5 (`sp_invoices`/`sp_invoice_lines`/`sp_payments`/`journal_entries`/`journal_entry_lines`) + lain-lain. Baris `CLAUDE.md` sengaja TIDAK diubah di sesi ini (di luar bagian state/recent yang jadi cakupan doc-keeper) — lihat REMINDER laporan 17 Agu 2026. **[UPDATE 21 Agu 2026 — utang jenis BARU, arahnya TERBALIK dari semua kasus di atas: bukan "SQL jalan dulu, migrasi/dump menyusul", tapi "migrasi ditulis dulu, SQL BELUM jalan".]** `supabase/migrations/20260821000001_partial-picking-guard.sql` (revisi guard `generate_picking_from_sp`) sudah ada di repo TAPI **belum dieksekusi di SQL Editor**. Jadi hari ini `schema_snapshot.sql` justru **AKURAT** — ia memuat guard LAMA (`status <> 'cancelled'`, `:713`) dan itu memang isi DB. Dua konsekuensi praktis: **(a)** jangan membaca kehadiran file migrasi sbg bukti objeknya sudah live — aturan lama *"snapshot bilang objek tidak ada ≠ objeknya tidak ada"* punya **kembaran terbalik**: *"migrasi bilang ada ≠ sudah ada"*; **(b)** begitu Den menjalankannya, snapshot langsung STALE untuk fungsi itu → **`pg_dump` menyusul**, dan header file migrasi (`⚠️ BELUM DIJALANKAN`) dikoreksi jadi `Status: LIVE` mengikuti preseden 18 Agu 2026. **[KOREKSI 24 Agu 2026, doc-keeper — kedua langkah di atas SUDAH DIKERJAKAN, paragraf ini murni histori sekarang]** migrasi `20260821000001` **sudah dijalankan** (header filenya kini `Status: LIVE. Dijalankan manual di SQL Editor oleh Den pada 21 Agustus 2026`) dan snapshot **sudah di-refresh** — `schema_snapshot.sql:795` memuat guard BARU (`AND dn.dispatched_at IS NOT NULL`), bukan lagi `status <> 'cancelled'` di `:713`. **Nol utang `pg_dump` yang diketahui per 24 Agu 2026** untuk perubahan ini; sesi 24 Agu sendiri nol menyentuh DB.
11. **Data historis CRM tak lengkap (audit 10 Jul):** `stage_changed_at` 727/997 = backfill 25 Jun (sejarah stage pra-25 Jun hilang, `audit_logs` nol perubahan pipeline_stage — **TD-51**); `last_activity_at` dulu ditulis sekali saat prospect dibuat (817 akun punya tanggal tapi nol `activities` — **TD-52**, kini di-NULL-kan + di-maintain trigger). Aging-pipeline mengandalkan `MAX(stage_changed_at, last_activity_at)` untuk mengurangi dampak backfill.
12. **Pooler host `pg_dump` yang BENAR = `aws-1-ap-northeast-2.pooler.supabase.com`** (region Seoul). Catatan lama yang menulis `ap-southeast-1` **SALAH** (gagal koneksi saat backup). Supabase project ref TIDAK berubah (`untmpqceexwxzuhlmyrg`) meski project dipindah ke org MSI.
13. **`exchange_rates` = DUA hal berbeda bernama sama (17 Jul 2026).** **(a) Kolom `quotations.exchange_rates`** (jsonb, BARU) = tabel kurs **manual per-quotation**, map `{"USD":16200}`, IDR implisit 1, **tanpa lookup FX** — inilah yang dipakai form/`save_quotation`. **(b) Tabel `public.exchange_rates`** (`schema_snapshot.sql:3044`) = master **FX rate history** company-scoped (`from_currency`/`to_currency`/`rate` numeric(18,6)/`effective_date`, CHECK `rate > 0`, komentar tabel menyuruh "use most recent rate on or before the transaction date") — **DORMAN: 0 query di `src/`** (`grep "from('exchange_rates')"` → 0 hit). Artinya "master kurs" **secara skema sudah ada tapi tak pernah dipakai**, sementara fitur kurs quotation sengaja manual. Saat menulis query/dokumen, **sebut eksplisit** `quotations.exchange_rates` (kolom) vs `public.exchange_rates` (tabel). Kalau FX lookup dibangun nanti, keduanya **wajib direkonsiliasi** (+ `quotations.usd_rate` yang vestigial) — **TD-74**, perlu keputusan.
14. **[BARU, 9 Agu 2026] Pola RLS `is_super_admin() OR company_id = get_user_company_id()` TIDAK CUKUP untuk user yang genuinely multi-company.** `get_user_company_id()` (singular) cuma baca `profiles.company_id` — SATU home company. Kalau user punya >1 baris `user_roles` AKTIF di company berbeda (skema TIDAK melarang ini — lihat `user_roles` §3), pola di atas salah menyaring dia keluar dari data company LAIN tempat dia juga berperan, PADAHAL dia bukan orang asing di sana. Fix yang benar: tambah `OR company_id IN (SELECT get_user_company_ids())` (fungsi jamak baru, §4). **Ditemukan berulang 3× dalam SATU hari** (`companies_read_own`, `roles_read`, dan `payment_terms_read` yang masih OPEN) lewat testing/audit yang masing-masing TERPISAH, bukan satu sisir menyeluruh — kuat dugaan **masih ada tabel lain** dengan pola sama yang belum ketemu. Saat menulis/mengaudit RLS baru: kalau tabelnya mungkin dibaca oleh user non-admin yang bisa genuinely multi-company (Finance/manager-tier lebih mungkin drpd role single-purpose), JANGAN cuma pasang `is_super_admin() OR company_id = get_user_company_id()` dan anggap selesai — cek juga apakah butuh varian jamak. Detail & daftar tabel yang sudah/belum ditambal: `08_TECH_DEBT.md` **TD-180**.
15. **[BARU, 12 Agu 2026] Modul BNF/Briefing Harian/Meeting Mingguan — snapshot dikonfirmasi FRESH untuk seluruh objek sesi 10/12 Agu (Layer 0-4), verifikasi langsung ke `schema_snapshot.sql`, bukan sekadar percaya klaim.** Semua tabel baru (`bnf_authorized_users`, `bnf_division_scopes`/`bnf_department_scopes`, `daily_report_items`, `bnf_report_action_items`, `weekly_meetings`/`weekly_meeting_items`), fungsi (`is_bnf_authorized()` versi diperluas 12 Agu, `get_linked_bnf_status()`), trigger (`trg_guard_daily_report_items_field_update`, `trg_set_daily_report_items_defaults`), dan RLS `bnf_reports_select`/`_insert`/`_update` (fix soft-delete) dikonfirmasi ADA persis sesuai desain — snapshot terakhir di-refresh commit `8f4b679` (12 Agu 2026), SETELAH seluruh 6 migrasi sesi ini. **Terpisah dari itu — fondasi modul (3-5 Agu 2026: `bnf_divisions`/`bnf_departments`/`bnf_reports`/`bnf_report_logs`/`bnf_report_related_departments`) genuinely NOL terdokumentasi di governance docs sampai sesi 12 Agu 2026 ini** (§3 di atas net-new) — beda kelas masalah dari "snapshot lag" (gotcha #1): objeknya sendiri sudah lama live & fresh di snapshot, yang hilang adalah dokumentasi governance-nya, bukan datanya.
16. **[BARU, 18 Agu 2026] `expired_date` ada di DUA tabel dan sudah DIVERGEN — sumber kebenaran = `sp_items`.** `sp_orders.expired_date` diisi **sekali** saat create (`create_sp_order_dual`) lalu **tak pernah di-update**: `update_sp_item_dual` cuma menyinkronkan 5 kolom (`qty`/`sla_days`/`estimated_delivery_date`/`shipping_price`/`notes`), nol sentuhan ke header. Sementara **SELURUH UI membaca DAN MENGEDIT versi item** — `SalesOrderDetailPage.jsx` (EditItemModal + kartu Deadline), kolom "Expired" di `SalesOrderPage.jsx`, mapping `spFromDb` di `db.js`. Artinya begitu ada yang mengubah deadline lewat modal, header diam-diam basi sementara layar menampilkan angka baru. Karena itu RPC dashboard sengaja mengambil **`MIN(sp_items.expired_date)`** (deadline paling awal = yang mengikat; UI sendiri pakai `items.find()` yang bergantung urutan fetch, jadi non-deterministik). Kelas masalah sama dgn **TD-198** (`sp_orders.confirmed_at/by` kolom mati) → dicatat **TD-201**. ⚠️ **Jebakan nama di tabel yang sama:** `sp_orders.price_category` (tipe SP) vs `sp_order_items.price_category` (kategori harga per baris) vs `sp_orders.sp_category` (kategori produk reguler/loyang/trolly). **Dan `expired_date` BUKAN soal validitas harga** — artinya tenggat wajib kirim/risiko pinalti, lihat `05_WORKFLOW_MAP.md §Logistics`.
17. **[BARU, 21 Agu 2026] `dispatched_at IS NOT NULL` dan `status IN ('in_transit','delivered')` menjawab DUA pertanyaan BERBEDA — jangan "diseragamkan".** `delivery_notes.dispatched_at` ditulis di **satu tempat saja** (`dispatch_delivery`, `schema_snapshot.sql:573`) dan **tidak pernah di-reset oleh siapa pun** — diverifikasi doc-keeper via grep seluruh snapshot (**7 hit**: 1 tulis `:573` · 5 baca di RPC dashboard `:823`/`:825`/`:832`/`:943`/`:945` · 1 definisi kolom `:5025`) + grep `src/` (nol penulis FE). Jadi: **`dispatched_at IS NOT NULL` = "PERNAH berangkat"** (fakta historis, satu arah) vs **`status IN ('in_transit','delivered')` = "SEDANG/SUDAH terkirim"** (kondisi sekarang, bisa mundur). Keduanya sudah hidup berdampingan **dengan benar**: `sp_recompute_status` memakai versi status (`v_has_dispatch`, `:2661`) karena ia menjawab *"SP ini SEKARANG di tahap apa"*; `get_storbit_dashboard_stats` (`:823`, `:832`) dan guard baru `generate_picking_from_sp` memakai `dispatched_at` karena keduanya menanyakan sejarah. **Titik yang membedakan keduanya = `cancel_delivery`** (`:173-216`): fungsi itu membalik `shipped_qty` + mengembalikan stok (`inbound`) tapi **TIDAK me-reserve ulang picking-nya** dan **tidak mengosongkan `dispatched_at`**. Konsekuensi konkret kalau guard picking memakai versi status: SP yang SJ-nya dibatalkan setelah berangkat akan **terkunci permanen** padahal stoknya sudah bebas dan outstanding-nya sudah kembali penuh — generate picking baru justru tindakan yang BENAR di situ. ⚠️ **Catatan penyeimbang, jangan disalin buta:** pemakaian `dispatched_at` di RPC dashboard **WAJIB dipasangkan** filter `dn.status <> 'cancelled'` (`:822`, `:831`) karena pertanyaannya *"telat atau tidak"*; guard picking **sengaja TIDAK memakai filter itu** karena pertanyaannya *"stoknya sudah pernah dilepas atau belum"* — dan pelepasan itu tetap nyata walau SJ-nya kemudian dibatalkan. Filter yang sama, dua konteks, dua jawaban benar yang berbeda.
18. **[BARU, 24 Agu 2026] `roles`/`departments`/`positions`/`branches` sudah GLOBAL (`company_id = NULL`) — memfilter `.eq('company_id', …)` pada keempat tabel ini SELALU mengembalikan NOL BARIS, dan gagalnya SENYAP.** Dua migrasi 21 Agu 2026 melakukannya: `20260821000003_globalize_roles.sql` (14 kode role — survivor `company_id = NULL` `:87`, duplikat per-company **soft-deleted** `:84`, `user_roles.role_id` **di-repoint ke survivor** `:49`) dan `20260821000002_globalize_org_structure.sql` (`departments` `:59`/`:86`, `positions` `:111`/`:119`, `branches` `:133`). **Kenapa senyap:** PostgREST mengembalikan array kosong, bukan error — kode pemanggil yang berpola *"kalau roleIds kosong, `return`"* berhenti tanpa toast, tanpa `console.error`, tanpa apa pun di Network tab yang tampak salah. ⚠️ **Dan tak bisa diandalkan tes pakai super_admin:** `roles_read` punya `OR is_super_admin()` top-level sehingga super_admin **bisa membaca baris soft-deleted** — id-nya terisi, query lanjut ke langkah berikutnya, lalu **berhenti di sana** karena `user_roles` sudah di-repoint. Dua jalur berbeda, satu gejala yang sama; jangan salah simpulkan sbg masalah RLS/hak akses. **Pola yang BENAR untuk resolusi role → user (3 langkah):** (1) `roles`: filter **`.in('code', …)` + `.is('deleted_at', null)` SAJA** — jangan sentuh `company_id`; (2) `user_roles`: **DI SINILAH** company-scoping ditegakkan (`.eq('company_id', …)` + `.eq('is_active', true)` + `.is('revoked_at', null)`) — kolomnya masih `NOT NULL` dan **tak pernah** di-null-kan migrasi mana pun; (3) `profiles`: `.eq('active', true)`. Mencabut filter di langkah (1) **tidak** membuka kebocoran lintas entitas justru karena langkah (2) tetap ada. Contoh hidup: `src/modules/crm/salesRoster.js:33-40`. ⚠️ **Untuk daftar master yang dibaca LANGSUNG sebagai opsi dropdown** (mis. `useUserAccess.js` — Role/Departemen/Posisi/Cabang di halaman User Access) **tak ada langkah (2)**, jadi mencabut filternya = memutuskan daftar itu memang Group-wide. Sejalan dgn tujuan globalisasi, tapi itu **keputusan produk**, bukan sekadar ganti baris. ⚠️ **Jebakan data pre-existing:** kode `supervisor` **tidak punya baris sama sekali** di tabel `roles` (0 baris, sebelum maupun sesudah migrasi) — daftar yang menyebutnya (`fetchAssignees`, `notifyManagers`) memang tak pernah bisa me-resolve kode itu; **bukan** akibat globalisasi, jangan didiagnosis ulang dari nol. Titik yang sudah ditambal & yang masih terbuka: `08_TECH_DEBT.md` **TD-207** (3 file CRM, RESOLVED) / **TD-209** (7 titik lain, OPEN — termasuk dropdown Role di User Access). Kerabat sumbu berbeda: **TD-208** (`profile.company_id` vs `activeCompanyId`).
